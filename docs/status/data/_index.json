{
  "file": "README.md",
  "title": "KlainMainLang — Implementation Status",
  "segments": [
    {
      "kind": "block",
      "text": "> Per-area implementation status for the TypeScript → native compiler. For what the project is and how to build/run it, start at the root [README](../../README.md); this page only tracks **how much of each surface is implemented, and how faithfully** — coverage percentages, caveats, and the gaps that matter."
    },
    {
      "kind": "block",
      "text": "This file is the scannable index: per-area completion % plus the caveats/blockers that matter most. Each linked page carries the full feature-by-feature table (and, where relevant, its own Known Limitations) for that area — trust the linked page over this summary if they ever drift apart. All detail pages follow one layout — see [Status page format](#status-page-format)."
    },
    {
      "kind": "block",
      "text": "## Contents"
    },
    {
      "kind": "block",
      "text": "- [TypeScript Core Language](#typescript-core-language) — core JavaScript/TypeScript language & standard library (works the same in any JS host)\n- [Web Platform APIs](#web-platform-apis) — WHATWG/browser-standard APIs (also implemented by Node.js, but not part of the JS *language* itself)\n- [Node.js APIs](#nodejs-apis) — `fs`, `process`, and a real `http.createServer(…).listen()` server — Node-specific runtime globals with no browser equivalent\n- [Desktop (`klain:` bespoke)](#desktop-klain-bespoke) — the `klain:webview` desktop-window module, a capability with no Node/Web equivalent\n- [Terminal UI (`klain:` bespoke)](#terminal-ui-klain-bespoke) — the `klain:tui` native TUI framework (vendored Yoga flexbox + ANSI diff painter)\n- [Cross-Cutting](#cross-cutting) — concerns spanning every feature area (memory management)\n- [What Is NOT Implemented](#what-is-not-implemented) — core language gaps, by priority/complexity\n- [Fidelity Gaps in Shipped Features](#fidelity-gaps-in-shipped-features) — features marked ✅/100% that still have real, non-cosmetic differences from actual JS/TS behavior\n- [Platform Support & Windows Notes](#platform-support--windows-notes) — faithful per-platform behaviour and the known Windows-specific gaps, in one place\n- [Design Documents (TDDs)](#design-documents-tdds)\n- [Roadmap & priorities](#roadmap--priorities) — pointer to the prioritized backlog ([docs/BACKLOG.md](../BACKLOG.md))\n- [Status page format](#status-page-format) — the shared layout every detail page follows"
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## TypeScript Core Language"
    },
    {
      "kind": "sectionTotal"
    },
    {
      "kind": "block",
      "text": "**Strict Coverage** counts a feature only when its row's Caveats cell is empty (zero known caveats/bugs of any severity), over the same denominator as Coverage ([ADR-00205](../adr/ADR-00205.md)). Both figures derive directly from each page's table per the [Status page format](#status-page-format)."
    },
    {
      "kind": "categoryTable",
      "columns": [
        "Category",
        "Coverage",
        "Strict",
        "Page",
        "Caveats"
      ],
      "rows": [
        {
          "category": "Control flow statements",
          "pageFile": "LANGUAGE-CONSTRUCTS.md",
          "caveats": "• A `for` loop's non-declaration init clause (`for (i = 0, j = 10; ...)`) is a clean rejection (the update clause does take comma-separated expressions — [ADR-00156](../adr/ADR-00156.md))<br>• No ASI restriction on a line break before a postfix `++`/`--`<br>• `finally` does not run on an early `return`/`break`/`continue` inside an `async`/generator body (it does everywhere else — [ADR-00191](../adr/ADR-00191.md))"
        },
        {
          "category": "Operators",
          "pageFile": "LANGUAGE-CONSTRUCTS.md",
          "caveats": "• `++`/`--` work on identifier, member, and index targets; the one edge is a postfix update on a side-effecting receiver used as a value, which evaluates the receiver twice ([ADR-00376](../adr/ADR-00376.md))<br>• `typeof` of a static-method reference outside the covered Promise/Math/JSON tables (`Object.keys`, `console.log`, …) answers from inference, not `\"function\"` ([ADR-00282](../adr/ADR-00282.md))<br>• `&&`/`\\|\\|` yield a bool by default; `-compat=js` makes them value-preserving for same-typed operands ([ADR-00186](../adr/ADR-00186.md)/[ADR-00220](../adr/ADR-00220.md))"
        },
        {
          "category": "Variable declarations",
          "pageFile": "LANGUAGE-CONSTRUCTS.md",
          "caveats": "Definite-assignment is enforced for a typed `let` read before assignment (`var` is exempt — hoisted and zero/`undefined`-initialized, no TDZ, [ADR-00454](../adr/ADR-00454.md)) — including `if`/`else`, `do/while`, and `switch`-with-`default` control flow — but the analysis is sound-not-complete: a binding assigned only inside a maybe-skipped `for`/`while` body, or in a `try` that may throw first, still escapes (reading its deterministic zero/`undefined` default — [ADR-00215](../adr/ADR-00215.md) — not uninitialized memory); correlated conditions (`if (c) {x=1} if (!c) {x=2}`) are conservatively *rejected*, matching TypeScript. See [TDD-00071](../tdd/TDD-00071.md)/[ADR-00210](../adr/ADR-00210.md)–[ADR-00215](../adr/ADR-00215.md)"
        },
        {
          "category": "Functions & closures",
          "pageFile": "LANGUAGE-CONSTRUCTS.md",
          "caveats": "• The `arguments` object is synthesized from same-typed parameters (`.length`/indexing/`for…of` — [ADR-00387](../adr/ADR-00387.md)); mixed-type params, a rest/destructured param, and arrow functions are clean rejections — use a `...rest` parameter there; class method bodies get the same synthesis ([ADR-00464](../adr/ADR-00464.md)) (a leading `this` parameter is parsed and erased — [ADR-00373](../adr/ADR-00373.md))<br>• Function overload signatures are parsed and **erased** — call sites type-check against the implementation's parameter list only ([ADR-00446](../adr/ADR-00446.md))<br>• Nested function declarations capture enclosing locals (by reference, [TDD-00129](../tdd/TDD-00129.md) Stage 1) but only for use at or after their declaration point, and can't yet be declared one block deeper than the enclosing body — see [TDD-00057](../tdd/TDD-00057.md)<br>• Tagged template literals have no `.raw` property on the `strings` array (real `String.raw` stays separately out of scope, see [ADR-00028](../adr/ADR-00028.md)) — see [TDD-00059](../tdd/TDD-00059.md)/[ADR-00152](../adr/ADR-00152.md)<br>• A top-level named function can't reference a sibling top-level binding whose value is a connection handle (`Worker`/`WebSocket`/`EventSource`/channels/`XMLHttpRequest`), a `Promise`, or an inferred-type-argument/nested generic instance (scalars/strings/arrays/`TypedArray`s/objects/`Map`/`Set`/class instances incl. `new Box<number>()`, the value/event handles — `Blob`/`Date`/`URL`/`RegExp`/`Headers`/`Request`/`AbortController`/`Event`/… — and streams + `EventEmitter` — plus `http.createServer` handles ([ADR-00426](../adr/ADR-00426.md)) — are promoted to module globals and work — [ADR-00342](../adr/ADR-00342.md)); a default parameter can't reference an earlier parameter ([ADR-00166](../adr/ADR-00166.md))<br>• A named function expression whose name shadows a top-level function of the same name is a clean compile error — rename one ([TDD-00060](../tdd/TDD-00060.md)/[ADR-00178](../adr/ADR-00178.md))<br>• A free `function*` generator may be top-level **or nested** (a nested one captures enclosing state by reference; an array capture is a clean rejection); a generator *expression* works as a top-level `const G = function* ...` binding with the element type inferred from yields, other positions stay clean rejections ([TDD-00096](../tdd/TDD-00096.md)/[ADR-00293](../adr/ADR-00293.md))"
        },
        {
          "category": "Type primitives",
          "pageFile": "TYPE-SYSTEM.md",
          "caveats": "• `number` is a JS-faithful IEEE-754 double (`0.1 + 0.2` → `0.30000000000000004`, `10 / 3` → `3.333…`, `1 / 0` → `Infinity`); the `int8..int64`/`uint8..uint64` types are the integer escape hatch (exact past 2^53 — an `int64`/`uint64` literal is parsed straight to a 64-bit integer; a plain `number` past 2^53 rounds exactly as JS does). Bitwise ops and the count/index builtins (`.length`/`indexOf`/`push`/…) also return a `number`, so `(7 & 6) / (7 & 5)` and `arr.length / other.length` are float divides (TDD-00123 Stages 1–3 / [ADR-00377](../adr/ADR-00377.md), [ADR-00379](../adr/ADR-00379.md))<br>• No `bigint` `.toLocaleString`/`asIntN` ([TDD-00074](../tdd/TDD-00074.md))"
        },
        {
          "category": "Async / Promise",
          "pageFile": "LANGUAGE-CONSTRUCTS.md"
        },
        {
          "category": "String methods",
          "pageFile": "STRING-METHODS.md",
          "caveats": "• No `.normalize()`<br>• `.codePointAt()` answers `NaN` where real JS gives `undefined`"
        },
        {
          "category": "RegExp",
          "pageFile": "REGEXP.md",
          "caveats": "• `u`/`y`/`d` flags out of scope for V1<br>• Several other deliberate scope narrowings (no implicit string→RegExp coercion, etc.) — see [REGEXP.md](REGEXP.md)'s own Caveats<br>• `new RegExp` doesn't validate its flags string ([ADR-00166](../adr/ADR-00166.md))"
        },
        {
          "category": "Array methods",
          "pageFile": "ARRAY-METHODS.md",
          "caveats": "• A length-mutating method (`.push`/`.pop`/`.shift`/`.unshift`/`.splice`) inside a callee still mutates a copy when the array crossed as an **object field**/**array element** (`obj.items`, `grid[i]`) or a HOF-callback element — only a plain **variable** argument has full JS reference semantics ([TDD-00127](../tdd/TDD-00127.md)/[ADR-00517](../adr/ADR-00517.md))<br>• `.flat(depth?)`/`.flatMap(fn)`: `depth` must be a compile-time constant (or `Infinity`), since this compiler's array element types are static ([TDD-00029](../tdd/TDD-00029.md)/[ADR-00107](../adr/ADR-00107.md))<br>• `Array.from` iterates arrays, Sets, Maps (entries), strings, and class iterators — generators and `thisArg` are not supported ([ADR-00482](../adr/ADR-00482.md)/[ADR-00491](../adr/ADR-00491.md))<br>• A nested-array element (`T[][]`) still zero-fills an element-absence miss (`.pop()`/`.at()`/…) instead of reading `undefined` ([ADR-00246](../adr/ADR-00246.md))"
        },
        {
          "category": "Number / Math",
          "pageFile": "NUMBER-MATH.md",
          "caveats": "• `Number.prototype.toString(radix?)` renders fractional digits, but a non-power-of-two repeating fraction is capped at 1100 digits and its trailing digits can differ from V8 (base 10 / power-of-two bases are exact)"
        },
        {
          "category": "Object & collections",
          "pageFile": "OBJECT-COLLECTIONS.md",
          "caveats": "• `Object.create`/`getPrototypeOf`/`setPrototypeOf`/`__proto__` work on **dynamic (`any`-typed) objects only** — statically-typed structs have no prototype link ([TDD-00155](../tdd/TDD-00155.md) Stage 3)<br>• `WeakMap`/`WeakSet`/`WeakRef` are strong-backed under `-mm=manual`; real weak semantics only under `-mm=gc` ([TDD-00112](../tdd/TDD-00112.md))<br>• Object literal method shorthand (`{ foo() {...} }`) has no `this` binding (no nominal type to give it a shape, no dynamic call-site binding) — a clean compile-time rejection, not silently wrong. See [ADR-00169](../adr/ADR-00169.md)"
        },
        {
          "category": "JSON",
          "pageFile": "JSON.md",
          "caveats": "• A runtime `space` argument and a non-null `replacer` on `JSON.stringify` are clean rejections ([ADR-00222](../adr/ADR-00222.md)) — a literal `space` pretty-prints both statically-typed and dynamic values ([ADR-00639](../adr/ADR-00639.md))<br>• A statically-typed value boxed into `any` (e.g. a class instance) can't be stringified through the dynamic walker (no runtime shape) — the typed `JSON.stringify` paths cover it directly"
        },
        {
          "category": "console",
          "pageFile": "CONSOLE.md",
          "caveats": "• No `console.table()`<br>• `console.trace` prints no stack<br>• `console.time()`/`.timeEnd()` are a single global timer slot, not per-label<br>• A printed string truncates *on display* at an embedded null byte (`printf \"%s\"`); the stored string is intact — [TDD-00120](../tdd/TDD-00120.md)"
        },
        {
          "category": "Global functions & constants",
          "pageFile": "GLOBAL-FUNCTIONS.md",
          "caveats": "• `eval` is an opt-in embedded-engine path ([TDD-00046](../tdd/TDD-00046.md)), not started<br>• `parseInt` lacks hex auto-detect with an omitted radix<br>• `NaN`/`Infinity` shadowing needs `-compat=js`, not unconditional"
        },
        {
          "category": "Type system features",
          "pageFile": "TYPE-SYSTEM.md",
          "caveats": "• Type assertions (`as T`/`as const`/`satisfies`) and the `readonly T[]` array/tuple modifier parse and run but are **erased** — they don't re-type the expression or enforce immutability ([ADR-00371](../adr/ADR-00371.md)/[ADR-00373](../adr/ADR-00373.md))<br>• `typeof` type queries, ambient `declare` declarations, and string index signatures (`{ [k: string]: T }`) are supported ([ADR-00389](../adr/ADR-00389.md)/[ADR-00388](../adr/ADR-00388.md)/[ADR-00390](../adr/ADR-00390.md)); **template literal types** parse but erase to `string` ([ADR-00561](../adr/ADR-00561.md))<br>• Generics support object/interface/class type arguments ([TDD-00069](../tdd/TDD-00069.md)) and `<T extends X>` constraints ([TDD-00113](../tdd/TDD-00113.md)); a *function*'s type arguments are inference-only (no explicit `identity<number>(5)`), and Map/Set/Promise/closure type arguments are a clean rejection ([TDD-00010](../tdd/TDD-00010.md), [TDD-00037](../tdd/TDD-00037.md))<br>• Union types (beyond `T \\| null`) allow scalar members, object members (one object member, or ≥2 as a discriminated union with a first-position string-literal tag), and a `ReadableStream` member; usable as an **object field** (checked + boxed at construction, [TDD-00119](../tdd/TDD-00119.md)) but not yet as an **array element**; flow narrowing (`typeof`/truthiness/`==null`/`tag===lit`, if-else + early-return) works for a union local or field ([TDD-00043](../tdd/TDD-00043.md)/[TDD-00114](../tdd/TDD-00114.md)/[TDD-00115](../tdd/TDD-00115.md)/[TDD-00116](../tdd/TDD-00116.md))<br>• Tuple types are fixed-shape structs ([TDD-00066](../tdd/TDD-00066.md)/[ADR-00201](../adr/ADR-00201.md))<br>• Intersection types are object-members-only, with a field conflict rejected rather than the `-compat=js` `never`-field ([TDD-00078](../tdd/TDD-00078.md)/[ADR-00225](../adr/ADR-00225.md))<br>• Mapped & utility types are evaluated to concrete object types; `Partial`/`Required`/`Readonly` are structural no-ops, no `as` remapping ([TDD-00079](../tdd/TDD-00079.md)/[ADR-00228](../adr/ADR-00228.md)–[ADR-00230](../adr/ADR-00230.md))<br>• Conditional types + `infer` evaluate at compile time (monomorphized check type); `infer` is scoped to Array/Promise/bare forms, no function-signature inference ([ADR-00231](../adr/ADR-00231.md))"
        },
        {
          "category": "Classes / OOP",
          "pageFile": "LANGUAGE-CONSTRUCTS.md",
          "caveats": "• **Parameter properties** (`constructor(public x: number)`/`private`/`readonly`) declare + assign their field ([ADR-00447](../adr/ADR-00447.md); `readonly` is not enforced as immutability), and the **`readonly` *field* modifier** parses too — both recorded, not enforced as immutability ([ADR-00480](../adr/ADR-00480.md))<br>• `class X extends Error` works one level deep ([ADR-00630](../adr/ADR-00630.md)); extending an Error *subclass* is a clean rejection, and `.stack` doesn't exist<br>• Static field initializers (`static x = 5`) work ([ADR-00375](../adr/ADR-00375.md)); clean rejections: static/async generator methods, a dynamic computed member key (`[Symbol.asyncIterator]`/`[Symbol.iterator]` are accepted, desugared to the iteration methods — [TDD-00089](../tdd/TDD-00089.md)/[ADR-00278](../adr/ADR-00278.md)), and a class expression used as a runtime value<br>• A field initializer can currently see constructor params (real JS's separate initializer scope forbids this — [ADR-00180](../adr/ADR-00180.md))<br>• `#x` private names have no early-error check for `#m`/`static #m` name collision or the `#constructor`-is-banned rule ([ADR-00155](../adr/ADR-00155.md))"
        },
        {
          "category": "JSDoc tags",
          "pageFile": "JSDOC.md",
          "caveats": "• The typing tags (`@type`/`@param`/`@returns`/`@typedef`/`@callback`/`@template`/`@satisfies`) type an untyped binding/function/named type, and the class + visibility + documentation tags (`@implements`/`@private`/`@readonly`/`@override`/`@deprecated`/…/`@enum`) are accepted and erased, matching TS's runtime behavior and this compiler's TS-modifier stance ([TDD-00125](../tdd/TDD-00125.md)). Only `@import` (comment-form import) and `@constructor` (function-as-constructor) are unsupported. The `int8..uint64`/`float32` width keywords and `@erased` are this project's own extensions, tracked separately on the page and excluded from the parity figure"
        },
        {
          "category": "JSDoc type expressions",
          "pageFile": "JSDOC.md",
          "caveats": "• `?T` nullable is recognized as a leading marker only (a `?` buried mid-expression is left to the parser); `!T` and `{...T}` varargs are stripped rather than modeled; a `=` optional marker doesn't yet make the parameter structurally optional<br>• A nested `function(...)` type inside another type isn't rewritten to the arrow form, and an `import(\"./m\").T` qualifier drops to the bare name — resolving only when that module is part of the build ([TDD-00125](../tdd/TDD-00125.md) Stage 4)"
        },
        {
          "category": "Modules",
          "pageFile": "MODULES.md",
          "caveats": "• Whole-program compile only<br>• `klmpm` is Stage 1 only — a `klain_modules/<name>/` package must be hand-built, no package-manager tool yet ([TDD-00054](../tdd/TDD-00054.md))<br>• No namespace re-exports (`export * as ns from`); no npm/`node_modules` interop ([TDD-00053](../tdd/TDD-00053.md)); dynamic `import()` works opt-in via `-dynamic-import=lazy` — real laziness through a `dlopen`'d `.so`/`.dylib` island per target ([ADR-00515](../adr/ADR-00515.md)/[TDD-00056](../tdd/TDD-00056.md)); the eager compile-time-merge backend ([TDD-00055](../tdd/TDD-00055.md)) and a dynamic-or-lazy `require()` stay unbuilt<br>• Static CommonJS `require('<literal>')` works, desugared to an import ([ADR-00369](../adr/ADR-00369.md))<br>• Node-style built-ins (`fs`/`path`/…) require a default, namespace, named, or `require` import, not ambient; the `node:` prefix resolves too ([TDD-00049](../tdd/TDD-00049.md)/[ADR-00419](../adr/ADR-00419.md))"
        },
        {
          "category": "**Language-feature index**",
          "pageText": "[TYPESCRIPT-FEATURES.md](TYPESCRIPT-FEATURES.md)",
          "coverageOverride": "— index",
          "strictOverride": "—",
          "caveats": "Completeness map of every TypeScript language feature (implemented / partial / not started / out of scope) — informational, not counted"
        }
      ]
    },
    {
      "kind": "block",
      "text": "## Web Platform APIs"
    },
    {
      "kind": "block",
      "text": "WHATWG/W3C-standard APIs — the kind a browser **and** Node.js both implement. Not part of the JS *language* itself, but not Node-specific either. Filtered to those that make sense outside a browser context; pure browser-only APIs (DOM, Canvas, WebGL, CSS, etc.) are out of scope (the **Gamepad API** is being reinterpreted natively over OS controller input — [TDD-00170](../tdd/TDD-00170.md)) — see [NOTIFICATIONS-MISC.md](NOTIFICATIONS-MISC.md)."
    },
    {
      "kind": "sectionTotal"
    },
    {
      "kind": "categoryTable",
      "columns": [
        "Category",
        "Coverage",
        "Strict",
        "Page",
        "Caveats"
      ],
      "rows": [
        {
          "category": "Timers",
          "pageFile": "TIMERS.md",
          "caveats": "• A timer callback must be a zero-argument `void` function (a closure or a bare top-level-function reference — [ADR-00200](../adr/ADR-00200.md))<br>• `setImmediate` and a same-tick `setTimeout(fn, 0)` are indistinguishable — `__kml_timer_drain` is one flat fire-time-ordered queue with no check-vs-timers phases"
        },
        {
          "category": "Encoding / Text",
          "pageFile": "ENCODING-TEXT.md",
          "caveats": "UTF-8 only — see [TDD-00034](../tdd/TDD-00034.md) for non-UTF-8 scope"
        },
        {
          "category": "URL",
          "pageFile": "URL.md",
          "caveats": "• `URLSearchParams` keeps only one value per key (known limitation)<br>• `URLPattern` is object-init only with a literals/`*`/`:name`/`:name?` grammar subset; `.exec()` returns a merged `Map` of named groups, not the spec's `URLPatternResult`"
        },
        {
          "category": "Binary data & Typed Arrays",
          "pageFile": "BINARY-DATA-TYPED-ARRAYS.md",
          "caveats": "• TypedArray construction is restricted to a variable declaration's initializer (Node `Buffer`'s call-syntax statics are exempt); no `.buffer`<br>• BigInt64Array/BigUint64Array support an explicit method allow-list — everything else is a compile-time rejection<br>• Buffer has no `utf16le`, and encodings must be string literals<br>• No `Atomics.waitAsync`, no `getFloat16`"
        },
        {
          "category": "Web Crypto",
          "pageFile": "WEB-CRYPTO.md",
          "caveats": "• `importKey`/`generateKey`/`deriveBits`/`deriveKey`/`sign`/`verify` reject any algorithm outside the supported set (AES-GCM/CBC, RSA-OAEP/RSA-PSS, ECDSA, HMAC, PBKDF2/HKDF) as not implemented<br>• Key-format, curve, and backend-specific restrictions (`jwk` as a `Map`, CommonCrypto label/salt limits, `publicExponent` fixed to 65537) live in the page's per-row Caveats"
        },
        {
          "category": "Performance & Timing (incl. Date)",
          "pageFile": "PERFORMANCE-TIMING.md",
          "caveats": "`Date` is UTC-only, never local time"
        },
        {
          "category": "Networking (fetch, WebSocket, SSE)",
          "pageFile": "NETWORKING.md",
          "caveats": "• `WebSocket` (server and client, [TDD-00039](../tdd/TDD-00039.md)) has no binary send; both `ws://` and `wss://` work in each direction — the server speaks `wss://` when attached to an `https.createServer` ([TDD-00158](../tdd/TDD-00158.md))<br>• `XMLHttpRequest` ([TDD-00040](../tdd/TDD-00040.md)) is legacy-synchronous-mode only<br>• `.text()`/`.json()` truncate at an embedded null byte — use `.arrayBuffer()` for binary bodies ([ADR-00094](../adr/ADR-00094.md))<br>• `Response.text()`/`.json()`/`.arrayBuffer()` buffer the whole body eagerly at the accessor call rather than lazily off the fetch reactor ([TDD-00186](../tdd/TDD-00186.md))"
        },
        {
          "category": "Streams",
          "pageFile": "STREAMS.md",
          "caveats": "• No BYOB readers / byte controllers; `ReadableStream.from()` takes arrays only; `desiredSize` is `0` (not `null`) once errored<br>• Node stream classes carry string chunks; `extends Transform` has no `_flush` override; `finished` returns no cleanup fn<br>• `Blob.stream()` delivers the whole blob as one `Uint8Array` chunk, not incrementally — full per-row lists on the page"
        },
        {
          "category": "Events & Cancellation",
          "pageFile": "EVENTS-CANCELLATION.md",
          "caveats": "• Single-target dispatch — no capture/bubble/propagation (`stopPropagation` is a no-op)<br>• `removeEventListener` matches by closure identity (the listener must be a named binding); no `class X extends EventTarget`<br>• `Event`/`CustomEvent` expose only `type`/`defaultPrevented`/`detail`; the fuller WHATWG properties and `new Event` init options are absent/ignored<br>• `AbortSignal` `timeout` latches only when an await loop checks it and isn't wired into `setTimeout`; a custom `abort(reason)` still throws `AbortError`, and `DOMException` carries no numeric `.code`"
        },
        {
          "category": "Workers / Concurrency",
          "pageFile": "CONCURRENCY-WORKERS.md",
          "caveats": "• One listener per event, arrow-function-literal handlers only; one message type per direction/channel<br>• `-mm=manual` leaks per message; `close()` leaks pipe fds<br>• Shared memory (`SharedArrayBuffer`/`Atomics`) is tracked on [BINARY-DATA-TYPED-ARRAYS.md](BINARY-DATA-TYPED-ARRAYS.md)"
        },
        {
          "category": "**JS built-in object index**",
          "pageText": "[JAVASCRIPT-BUILTINS.md](JAVASCRIPT-BUILTINS.md)",
          "coverageOverride": "— index",
          "strictOverride": "—",
          "caveats": "Completeness map of every JavaScript standard built-in object (implemented / partial / not started / out of scope) — informational, not counted"
        }
      ]
    },
    {
      "kind": "block",
      "text": "## Node.js APIs"
    },
    {
      "kind": "block",
      "text": "Node.js-specific runtime globals — not part of any Web/browser standard, but essential for the CLI-application and microservice use cases this project actually targets. Most (`fs.*`, `path.*`, `os.*`, `querystring.*`, `assert`, `http.createServer`, `cluster.*`, and this project's own `Memory.free`) require a default, namespace, or named import (`import fs from 'fs'` or `import { readFileSync } from 'fs'`) — see [MODULES.md](MODULES.md)'s import-gated-bindings row and [TDD-00049](../tdd/TDD-00049.md)/[ADR-00141](../adr/ADR-00141.md)/[ADR-00142](../adr/ADR-00142.md). `process`/`console` stay ambient, like `Math`/`JSON`, matching real Node/JS."
    },
    {
      "kind": "sectionTotal"
    },
    {
      "kind": "categoryTable",
      "columns": [
        "Category",
        "Coverage",
        "Strict",
        "Page",
        "Caveats"
      ],
      "rows": [
        {
          "category": "File System (fs)",
          "pageFile": "FILE-SYSTEM.md",
          "caveats": "• The legacy callback form (`fs.readFile(path, cb)`) and binary-data async writes still run inline on the loop thread, and the thread pool is POSIX-only so far ([TDD-00185](../tdd/TDD-00185.md))<br>• `fs.createReadStream` ignores consumer backpressure (the worker reads ahead); `createWriteStream` still writes inline on the loop thread; both deliver **string** chunks ([TDD-00186](../tdd/TDD-00186.md)/[TDD-00108](../tdd/TDD-00108.md))<br>• `readFileSync`/`readFile` still text-only by design — use `readFileSyncBytes`/binary-aware `writeFileSync` for binary data ([ADR-00094](../adr/ADR-00094.md))"
        },
        {
          "category": "Process / CLI I/O",
          "pageFile": "PROCESS-CLI.md",
          "caveats": "• `process.env` writes work but there's no `delete` (no dynamic-delete operator)<br>• `process.on(...)` covers `'SIGINT'`/`'SIGTERM'`/`'exit'`/`'uncaughtException'` but not `'unhandledRejection'`; an `'uncaughtException'` handler runs then still exits (can't resume)<br>• `process.version`/`versions` report the pinned Node baseline + `klain`; real linked-lib versions (`openssl`/`zlib`) and a `--node-compat` floor are follow-ons<br>• `process.memoryUsage()`'s `external`/`arrayBuffers` are always 0 (no off-heap accounting), and `heapTotal`/`heapUsed` measure this compiler's own heap, not a V8 heap ([ADR-00791](../adr/ADR-00791.md))<br>• `child_process.fork()` is self-fork only — forking a different module stays out"
        },
        {
          "category": "HTTP Server",
          "pageFile": "HTTP-SERVER.md",
          "caveats": "• `createServer`'s `res` is a V1 subset (`writeHead`/`setHeader`/`write`/`end`) and `.listen` blocks until close; one server instance per program, `http.close()` doesn't allow a re-listen ([TDD-00131](../tdd/TDD-00131.md) Stage 1)<br>• The `createServer`/`listen` options object is mostly rejected; keep-alive only on a buffered `res.end` (chunked/streaming responses force `Connection: close`)"
        },
        {
          "category": "`path`",
          "pageFile": "PATH.md",
          "caveats": "Host-flavoured — a bare `path` is `path.win32` on Windows and `path.posix` elsewhere, with `path.posix`/`path.win32` naming one explicitly on any host ([TDD-00178](../tdd/TDD-00178.md)); the posix flavour's `join`/`normalize` drops a trailing slash where Node keeps it"
        },
        {
          "category": "`os`",
          "pageFile": "OS.md",
          "caveats": ""
        },
        {
          "category": "`events` (`EventEmitter`)",
          "pageFile": "EVENT-EMITTER.md",
          "caveats": "• Multi-argument events via a tuple payload (`emit(e, a, b)` — [ADR-00392](../adr/ADR-00392.md)); no open-ended untyped `...args` beyond a declared tuple<br>• `instanceof EventEmitter` is a compile-time constant, not a runtime tag"
        },
        {
          "category": "SQLite (`node:sqlite`)",
          "pageFile": "SQLITE.md",
          "caveats": "• Result rows need an explicit shape (`.all<T>()`/`.get<T>()`); untyped `SELECT *` awaits a dynamic-object mode<br>• `db.aggregate()`, error `.code`, and a lazy iterator are later stages; sessions/changesets, extension loading, and async `backup()` are rejected with a clear message (unavailable in the system libsqlite3 / out of scope)"
        },
        {
          "category": "Other core modules (`querystring`, `assert`, `test`, `zlib`, `net`, `util`, `dns`, `dgram`, `cluster`, `tls`, `http` client, `https`, `stream/web`, `vm`, `http2`)",
          "pageFile": "NODE-CORE-MODULES.md",
          "caveats": "`querystring`/`assert`/`zlib`/`net`/`util`/`dns`/`dgram`/`cluster`/`tls` done (`tls` = client + server); `https` = client (`get`/`request` over libcurl) + server (`https.createServer`, HTTPS/1.1, [ADR-00505](../adr/ADR-00505.md)); `stream/web` re-exports the WHATWG classes; `diagnostics_channel` pub/sub (string messages); `http2` ships all four stages — `createServer` (h2c) + `createSecureServer` (h2 over TLS, `allowHTTP1` 1.1 fallback), compat + `.on('stream')` APIs, client sessions, constants/settings ([TDD-00139](../tdd/TDD-00139.md)/[TDD-00111](../tdd/TDD-00111.md)); `vm` not started"
        },
        {
          "category": "**Built-in module index**",
          "pageText": "[NODE-MODULES.md](NODE-MODULES.md)",
          "coverageOverride": "— index",
          "strictOverride": "—",
          "caveats": "Completeness map of every Node built-in module (importable / global-only / not started / out of scope) — informational, not counted"
        }
      ]
    },
    {
      "kind": "block",
      "text": "## Desktop (`klain:` bespoke)"
    },
    {
      "kind": "block",
      "text": "Capabilities with no Node/Web equivalent, under the [`klain:` namespace](../tdd/TDD-00131.md)."
    },
    {
      "kind": "sectionTotal"
    },
    {
      "kind": "categoryTable",
      "columns": [
        "Category",
        "Coverage",
        "Strict",
        "Page",
        "Caveats"
      ],
      "rows": [
        {
          "category": "Webview (desktop windows)",
          "pageFile": "DESKTOP-WEBVIEW.md",
          "strictOverride": "0/20, 0%",
          "caveats": "• One window per process (V1); a second `new Webview` is a clean rejection; multi-window deferred (engine run-loop/app-delegate rework)<br>• Raw `w.bind` (string-JSON-in/out, sync + `async Promise<string>`) plus **typed** bind — `bindings: {…}` / `w.bindTyped` auto-decode declared param types + JSON-encode the return, incl. async (`Promise<T>`) + nested-tuple params ([ADR-00441](../adr/ADR-00441.md)/[ADR-00442](../adr/ADR-00442.md)); `--emit-window-dts` writes the page-side `Window` typing; rejection carries a fixed message<br>• Timers/microtasks/async bind run on the GUI thread via a page-tick pump ([ADR-00439](../adr/ADR-00439.md)); servers/fd-loops still need a Worker<br>• `new Webview({ serve: './dist' })` / `klain:assets` `embedDir` embed a built SPA into the binary and serve it from an in-binary server — single-file apps, no external `dist/` ([ADR-00443](../adr/ADR-00443.md)); `klainmain -package` wraps it in a `.app`/`.desktop` ([ADR-00440](../adr/ADR-00440.md))<br>• Verified on macOS (M4) and Windows 11 (Edge WebView2, [ADR-00725](../adr/ADR-00725.md); `-package` → GUI-subsystem exe, [ADR-00726](../adr/ADR-00726.md)); Linux compile-tier only, not yet run"
        }
      ]
    },
    {
      "kind": "block",
      "text": "## Terminal UI (`klain:` bespoke)"
    },
    {
      "kind": "block",
      "text": "A native terminal-UI framework with no Node/Web equivalent, under the [`klain:` namespace](../tdd/TDD-00131.md) — flexbox layout ([Yoga](https://github.com/facebook/yoga), vendored) plus a double-buffered ANSI diff painter."
    },
    {
      "kind": "sectionTotal"
    },
    {
      "kind": "categoryTable",
      "columns": [
        "Category",
        "Coverage",
        "Strict",
        "Page",
        "Caveats"
      ],
      "rows": [
        {
          "category": "TUI framework (`klain:tui`)",
          "pageFile": "TERMINAL-UI.md",
          "caveats": "• Flexbox is a pragmatic Yoga-style subset (no percentage units / `position:absolute` / `aspectRatio` yet)<br>• Text is width-aware via a standard `wcwidth` table, but ZWJ grapheme clusters (flag / family / skin-tone emoji sequences) still paint as their separate wide glyphs<br>• Immediate-mode: the tree is re-laid-out every frame (the cell diff keeps output minimal); a retained node tree is deferred<br>• The `state → view → update` loop is userland TS over `klain:tty` + `SIGWINCH` — no built-in app-runner / callback-driven loop until the FFI trampoline ([TDD-00150](../tdd/TDD-00150.md) Stage 2)<br>• Verified on macOS (M4) and, under a ConPTY pseudo-console in the suite, Windows 11 ([ADR-00727](../adr/ADR-00727.md)); Linux compile-tier, not yet run"
        }
      ]
    },
    {
      "kind": "block",
      "text": "## Cross-Cutting"
    },
    {
      "kind": "block",
      "text": "Concerns that span every feature area rather than living in one of them."
    },
    {
      "kind": "categoryTable",
      "columns": [
        "Area",
        "Status",
        "Strict",
        "Page",
        "Caveats"
      ],
      "rows": [
        {
          "category": "Memory management",
          "pageFile": "MEMORY-MANAGEMENT.md",
          "coverageOverride": "3/3 modes (`manual`, `gc`, `auto`)",
          "caveats": "• `manual` (default) never frees on its own<br>• `auto`'s escape analysis is deliberately conservative — a value it can't prove block-local still leaks (the safe fallback, [TDD-00173](../tdd/TDD-00173.md))<br>• `-optimize-memory` (stack allocation + value-returned tuples, [TDD-00134](../tdd/TDD-00134.md)) is off by default while the analysis matures; flipping both to the shipped default is gated on [TDD-00174](../tdd/TDD-00174.md)"
        },
        {
          "category": "Compatibility mode (`-compat`)",
          "pageText": "—",
          "coverageOverride": "✅ 2 modes (`strict`, `js`)",
          "strictOverride": "—",
          "caveats": "• The whole-program strict-vs-JS axis ([TDD-00075](../tdd/TDD-00075.md)/[ADR-00217](../adr/ADR-00217.md)): `strict` (default) is the compiler's opinionated, safer-than-JS semantics; `-compat=js` opts into JS-faithful behavior. It's a third category for divergences — deliberate design choices, not bugs or permanent narrowings<br>• Current inhabitant — ambient-global shadowing (absorbs the former `-globals` flag, [TDD-00050](../tdd/TDD-00050.md)/[ADR-00143](../adr/ADR-00143.md)): `strict` rejects a declaration colliding with a built-in name (`Math`/`fetch`/…) as a compile error; `js` allows real-JS/browser shadowing, plain-identifier globals only (`new`-form `Map`/`Date`/`RegExp` stay reserved either way)<br>• Inhabitant — `bigint`↔float comparison: `strict` rejects it (a likely bug); `-compat=js` does JS's exact real-number comparison (exact even past 2^53)<br>• Inhabitant — vanilla-JS classes ([TDD-00022](../tdd/TDD-00022.md)/[ADR-00624](../adr/ADR-00624.md)): under `-compat=js` a class with no declared fields collects them from its constructor's `this.x =` assignments, and unannotated parameters — constructors ([ADR-00624](../adr/ADR-00624.md)) and top-level functions ([ADR-00625](../adr/ADR-00625.md)) — take their types from call sites (first-inferrable-site-wins; disagreement on a user function's parameter is a hard error; identifier arguments resolve through top-level bindings). `strict` keeps the explicit-declaration requirement<br>• Inhabitant — definite-assignment ([TDD-00022](../tdd/TDD-00022.md) sub-problem 5): the TS-only \"used before being assigned\" early error is suppressed under `-compat=js` (plain JS reads `undefined`; the zero-initialized slot makes the suppressed read deterministic). TDZ and redeclaration stay on in both modes — they are real JS errors<br>• Inhabitant — real `any` operators + implicit-`any` ([TDD-00076](../tdd/TDD-00076.md)/[ADR-00628](../adr/ADR-00628.md)): `strict` rejects arithmetic on a dynamic value and keeps the unannotated-parameter number default; `js` runtime-dispatches operators on the NaN-boxed word ([TDD-00156](../tdd/TDD-00156.md)) with JS coercion, and an unannotated parameter falls annotation → call-site inference → boxed `any` — `function add(a, b) { return a + b }` runs as real JS"
        }
      ]
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## What Is NOT Implemented"
    },
    {
      "kind": "block",
      "text": "This section is core-*language* gaps only. Not-yet-built library/runtime APIs (Web Platform + Node modules) live in the prioritized backlog ([docs/BACKLOG.md](../BACKLOG.md)), not here."
    },
    {
      "kind": "block",
      "text": "### Medium complexity"
    },
    {
      "kind": "block",
      "text": "| Feature | Notes |\n|---|---|\n| Spread into a fixed-arity function, a fixed parameter slot, or an uncommon variadic builtin | `f(...arr)` where `f` has no rest parameter, `f(...arr)` where the spread would land on a fixed slot of a rest function, and `Math.hypot(...a)`/`String.fromCharCode(...a)`. The first two need a per-slot unpack against this compiler's static arity; the third needs bespoke per-builtin array handling. A clean compile error today, not a miscompile. ([TDD-00106](../tdd/TDD-00106.md)) |"
    },
    {
      "kind": "block",
      "text": "### High complexity"
    },
    {
      "kind": "block",
      "text": "| Feature | Notes |\n|---|---|\n| Decorator class *replacement* + static-field decorators | The two remaining decorator pieces ([TDD-00161](../tdd/TDD-00161.md)): a class decorator that returns a replacement class is refused at runtime — a documented divergence under the static model, whose home is a future `-compat=js`/first-class-constructor world — and static-field decorators are not run |\n| Dynamic `import()` — runtime-variable specifier | A genuinely *runtime-computed* specifier (`import(expr)`) is out of scope — whole-program AOT can't resolve it; a bounded template-literal form is a possible follow-on ([TDD-00056](../tdd/TDD-00056.md)). Literal-specifier `import()` needs the opt-in `-dynamic-import=lazy` flag |\n| Dynamic property add/delete on *statically-typed* objects | A statically-*annotated* struct stays fixed-shape by design (the two-tier decision, [TDD-00155](../tdd/TDD-00155.md)) — runtime add/delete, prototype chains, and `Proxy` traps require an `any`-typed value or, under `-compat=js`, an untyped literal. See [OBJECT-COLLECTIONS.md](OBJECT-COLLECTIONS.md) |"
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## Fidelity Gaps in Shipped Features"
    },
    {
      "kind": "block",
      "text": "Every row below is marked ✅ (or 100%) on its own page — the feature genuinely works for its core, documented cases — but each hides a real, non-cosmetic difference from how actual JavaScript/TypeScript behaves, beyond a one-line documented scope narrowing. Not reflected in the coverage percentages above. None of these are scheduled — pick up opportunistically alongside the open bugs or the backlog below."
    },
    {
      "kind": "block",
      "text": "| Feature | Gap | Where documented |\n|---|---|---|\n| `EventEmitter` (100%) | `.emit(event, data)` takes one payload type per instance (multi-argument via a declared tuple payload, [ADR-00392](../adr/ADR-00392.md)), not real Node's open-ended variadic `...args`; `instanceof EventEmitter` is a compile-time constant, never a runtime tag (never a registered class). | [EVENT-EMITTER.md](EVENT-EMITTER.md)'s Known Limitations |\n| Array methods (100%) | `.sort()`'s custom comparator (a separate C-ABI `qsort()` trampoline, not a direct closure call), `.indexOf()`/`.includes()` (compare a bare register directly, no callback at all), and `Object.groupBy()` (buckets store every element as a raw `i64`, a different scheme than a plain array's backing buffer) still reject a nested-array element — every *other* element-consuming method (`.map`/`.filter`/`.forEach`/`.reduce`/`.find`/`.findIndex`/`.findLast`/`.findLastIndex`/`.some`/`.every`, and `.join()`/array→string coercion) supports one, see [ADR-00152](../adr/ADR-00152.md) | [ARRAY-METHODS.md](ARRAY-METHODS.md)'s own caveats paragraph |\n| RegExp (100%) | `.exec()`/`.match()` turn an unmatched optional capture group into `\"\"` instead of a per-element `null`, and lack `index`/`input`/`groups`; `.matchAll()` is an eager `string[][]`, not a lazy iterator; no implicit string→RegExp coercion anywhere; `.replace()`/`.replaceAll()` template support is `$1`-`$9`/`$&`/`$$` only (no `` $` ``/`$'`) with a fixed-arity `(match, offset, string)` callback (no variadic captured groups); `.split()` doesn't replicate real JS's zero-length-match splitting or splice captured groups into the result | [REGEXP.md](REGEXP.md)'s own caveats paragraph |\n| `.codePointAt()` / `.localeCompare()` (✅) | Byte-sequence stand-ins, not real Unicode/locale behavior — `.codePointAt()` is `.charCodeAt()` under another name (no surrogate-pair decoding), `.localeCompare()` is a length-aware byte compare (binary-safe, but still byte-order, not collation). Correct only for ASCII/Latin-1 text | [STRING-METHODS.md](STRING-METHODS.md) |\n| `setImmediate` (✅) | Indistinguishable from a same-tick `setTimeout(fn, 0)` — real Node guarantees `setImmediate` fires first when scheduled from an I/O callback (distinct check/timers event-loop phases); this compiler's timer queue is a single flat fire-time-ordered list with no phase concept | [TIMERS.md](TIMERS.md) |\n| `XMLHttpRequest` / `WebSocket` (100% as part of Networking) | `XMLHttpRequest` only implements the spec's legacy synchronous mode (no default-async, callback-interleaved mode); `WebSocket` has no binary `.send()`. Both `ws://` and `wss://` work in each direction — the server speaks `wss://` when the klain:ws `WebSocketServer` (or a raw `'upgrade'` handler) is attached to an `https.createServer` ([TDD-00158](../tdd/TDD-00158.md)) | [NETWORKING.md](NETWORKING.md) |\n| Under-assigned (non-optional) class fields; array/tuple-typed optional fields; nested-array destructured elements (all ✅/100%) | Still zero-fill instead of reading `undefined` ([TDD-00187](../tdd/TDD-00187.md) residue): a non-optional class field left unassigned by the constructor (no definite-assignment check), an omitted array/tuple-typed optional field (empty array — its slot has no absent state), and a nested-array destructured element | [ADR-00779](../adr/ADR-00779.md), [ADR-00157](../adr/ADR-00157.md) |"
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## Platform Support & Windows Notes"
    },
    {
      "kind": "block",
      "text": "The compiler targets the host it runs on (no cross-compilation): Linux (x86-64) and macOS (Apple Silicon) are the primary platforms, and Windows (x86-64) is supported through the mingw-w64 UCRT toolchain ([TDD-00177](../tdd/TDD-00177.md)). This section gathers the platform-keyed behaviour that would otherwise be scattered through per-feature caveats — separating the differences that are **faithful to Node** (correct, not gaps) from the **known Windows gaps** that remain. The gaps are acknowledged here rather than carried as feature caveats: none are scheduled, and they are picked up opportunistically as low-priority follow-ups to the port."
    },
    {
      "kind": "block",
      "text": "### Faithful platform differences (correct — Node behaves the same)\n\nThese are not gaps: the compiler matches Node's own per-platform behaviour. Documented here for reference, not as caveats.\n\n| Area | Behaviour |\n|---|---|\n| `process.platform` | `win32` / `linux` / `darwin`, resolved at compile time to the build host |\n| `os.EOL` | `\\r\\n` on Windows, `\\n` on POSIX ([TDD-00177](../tdd/TDD-00177.md)) |\n| `path` | a bare `path` is `path.win32` on Windows and `path.posix` elsewhere, with `path.posix`/`path.win32` explicit on any host ([TDD-00178](../tdd/TDD-00178.md)) |\n| `process.getuid`/`geteuid`/`getgid`/`getegid` | present on POSIX; rejected at compile time on Windows, where Node leaves them undefined |\n| Signals | `SIGINT` (Ctrl+C) and `SIGBREAK` (Ctrl+Break) fire on Windows; `SIGTERM` listeners are accepted but never fire (nothing delivers it there) and `process.kill` terminates unconditionally ([ADR-00728](../adr/ADR-00728.md)) |\n| Child exit code | the full 32-bit value on Windows, the 8-bit POSIX wait-status field on Linux/macOS — as Node reports on each ([ADR-00759](../adr/ADR-00759.md)) |\n| `fs.chmod` | toggles the read-only attribute on Windows (no POSIX mode bits) |\n| `os.homedir()`/`os.tmpdir()` | read `USERPROFILE`/`TEMP` on Windows ([ADR-00739](../adr/ADR-00739.md)) |\n| `fs.symlink` | needs Developer Mode or an elevated token on Windows (as it does for any program) |"
    },
    {
      "kind": "block",
      "text": "### Known Windows gaps (acknowledged, not yet tackled)\n\nDivergences or missing pieces specific to the Windows platform layer. Kept here as a backlog rather than as caveats on the individual features — they may be tackled in the future.\n\n| Gap | Detail | Tracked in |\n|---|---|---|\n| Event-loop reactor / CRT fd table | blocking `process.stdin`/pipe reads, a ~10 ms idle poll slice, a 384-socket `select()` cap, `dup2` socket aliasing, synchronous `CreatePipe` child stdio | scoped in [TDD-00182](../tdd/TDD-00182.md) (owned handle table) + [TDD-00183](../tdd/TDD-00183.md) (IOCP) — not started |\n| `cluster` round-robin | workers share one listening socket instead of the primary's round-robin; `http.close()` from a worker doesn't reach siblings | [TDD-00177](../tdd/TDD-00177.md) |\n| Named pipes / `EPIPE` on write | `net.connect({ path })` has no `\\\\.\\pipe\\…` (Windows named-pipe) mapping; the WSA→errno table is complete, so a peer reset on a plaintext socket surfaces `ECONNRESET`/`ECONNABORTED` faithfully — the realistic broken-connection case ([ADR-00742](../adr/ADR-00742.md)). Two write-error nuances stay deferred (low-value): `WSAESHUTDOWN`→`EPIPE` (libuv parity) is not observably reachable — the JS `Writable` layer intercepts write-after-`end()` before the syscall; and on long-lived streaming / pub-sub connections (gRPC over HTTP/2, `wss`) a TLS write goes through OpenSSL's own BIO, whose surfaced error code is unverified against Node. A persistent-connection probe (client `destroy()`s, server keeps writing) would settle the latter | [TDD-00180](../tdd/TDD-00180.md) §5 |\n| `net` socket `setEncoding`/`ref`/`unref`/`pause`/`resume`/`setTimeout` | accepted but no-ops (cross-platform, most visible here) | [NODE-CORE-MODULES.md](NODE-CORE-MODULES.md) |\n| Non-ASCII console input (full reliability) | the console input code page is set to UTF-8 so typed non-ASCII arrives as UTF-8 via the CRT read ([ADR-00747](../adr/ADR-00747.md)); Node's fully-reliable wide `ReadConsoleW` path is not implemented | [TDD-00180](../tdd/TDD-00180.md) §2 |\n| `process.env` | a UCRT startup snapshot — a live `SetEnvironmentVariableW` is invisible, and there is no key enumeration on any host | [TDD-00180](../tdd/TDD-00180.md) §6 |"
    },
    {
      "kind": "block",
      "text": "### Out of scope on Windows by design\n\n`--static` (a compiled `.exe` links the optional libraries' `ucrt64/bin` DLLs; a fully static libcurl/OpenSSL build is a deferred Stage-5 evaluation, [TDD-00177](../tdd/TDD-00177.md) open question 5), `-crypto=commoncrypto` (macOS-only, ships with that OS), and the ASan/UBSan sanitizers (no mingw runtime in LLVM's Windows build). `-mm=gc` (Boehm) works on Windows, with `GC_set_handle_fork` skipped since there is no `fork()` there ([ADR-00719](../adr/ADR-00719.md))."
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## Design Documents (TDDs)"
    },
    {
      "kind": "block",
      "text": "Anything big enough to need a design pass before implementation gets scoped out in a Technical Design Document under `docs/tdd/` first. The individual `docs/tdd/TDD-*.md` files are the source of truth for status; [`docs/tdd/README.md`](../tdd/README.md)'s full index (number, title, status, implementing ADRs) and the backlog below are both generated from them."
    },
    {
      "kind": "block",
      "text": "Listed below is **every not-yet-done TDD** (Not Started, In Progress, or Partially Implemented). Which rows appear and their Status are derived from the TDD files, so a completed or superseded one drops out automatically; each row's title and notes are this page's own. Several also have a live pointer elsewhere in this file (noted in the row)."
    },
    {
      "kind": "tddBacklog",
      "editorial": {
        "188": {
          "title": "Runtime call-frame stack for `console.trace()` / `Error.stack`",
          "notes": "BACKLOG §3: the runtime-readable stack trace. Node's `console.trace()` and `Error.stack` materialize a *string of `.ts` frames* the running program can read in an ordinary `-O2` build — distinct from [TDD-00073](../tdd/TDD-00073.md)'s DWARF-for-debuggers (`-g`/`-O0`, `lldb`/`gdb` only) and from a native `backtrace()` (mangled `@fn__kml_modN` symbols, `-O2` inlining drops frames). The mechanism is a **per-fiber shadow call stack**: a static `FrameDesc{name,file,defLine}` per emitted function, pushed/popped around each function body, walked to format `at <fn> (<file>:<line>)`. Two hard parts: the `setjmp`/`longjmp` unwind (a `longjmp` to `catch` skips the epilogue pops, so `emitTry` snapshots and restores the shadow depth) and per-fiber isolation (each ucontext async fiber owns its own stack, joining the scheduler's context-switch swap set). Recommended behind `--stack-traces` (off by default) since always-on push/pop taxes the CLI/microservice hot path; `Error.stack` gains a new `errorObjType` field captured at construction. `console.trace` today prints its `Trace:` message line but no frames"
        },
        "187": {
          "title": "`undefined` for a concrete scalar — retiring the zero-fill caveat family",
          "notes": "Remaining: the `{value, done}` iterator/stream-read record \u2014 `ReadableStream.read()` after close needs a discriminated `{done: false, value: T} | {done: true, value: undefined}` record design (plus `desiredSize` `0`-vs-`null`), not a plain field flip. Deliberate non-goal: a generator's `.next().value` stays bare `T` \u2014 tsc types it `any` there, so the current typing is already stricter than TS ([ADR-00781](../adr/ADR-00781.md))"
        },
        "186": {
          "title": "Real async streaming I/O (pool-backed file streams, fetch body Promises)",
          "notes": "Stage 4 of [TDD-00185](../tdd/TDD-00185.md), spun out. Remaining: consumer backpressure on the pooled read streams (the worker reads ahead of the consumer), a `STREAM_ERROR` completion kind for mid-read failures, and lazy fetch-body buffering \u2014 `Response.text()`/`.json()`/`.arrayBuffer()` buffer the whole body eagerly at the accessor call instead of off the fetch reactor, so intervening work doesn't overlap the download"
        },
        "185": {
          "title": "Blocking-work thread pool for real async I/O",
          "notes": "A libuv-style shared thread pool (default 4, `UV_THREADPOOL_SIZE`) runs blocking fs work off-thread and settles the Promise on the loop thread via a completion queue \u2014 the same wakeup primitive [TDD-00182](../tdd/TDD-00182.md)/[TDD-00183](../tdd/TDD-00183.md) want. Remaining: the legacy callback form (`fs.readFile(path, cb)`) and binary-data async writes stay inline on the loop thread, and the pool is POSIX-only \u2014 Windows waits on the reactor work"
        },
        "183": {
          "title": "Windows IOCP reactor (completion-model I/O)",
          "notes": "**Option B** from [TDD-00180](../tdd/TDD-00180.md): the native completion-model reactor (overlapped `WSARecv`/`AcceptEx`, one `GetQueuedCompletionStatusEx`) — Windows' faithful long-term I/O model. A **committed follow-on to [TDD-00182](../tdd/TDD-00182.md)** (Option A), sequenced after it and built on its owned handle table; **lower priority than A but not deferred**. Waits on A so A's table/wakeup are shaped for B's reuse rather than discarded"
        },
        "182": {
          "title": "Windows owned handle table + event-loop reactor",
          "notes": "Elaborates [TDD-00180](../tdd/TDD-00180.md)'s recommended **Option A** into an implementable design: replace the CRT fd table with an owned, kind-tagged handle table and wait once on a unified wait object the [ADR-00757](../adr/ADR-00757.md) wakeup channel signals — killing the blocking stdin/pipe reads, the 10 ms poll spin, the 384-socket cap, and `dup2` aliasing, while keeping the `select()` contract so the IR-level net/http/stdin callers are untouched. IOCP (Option B) stays the parked upgrade path. Staged 1–6; not to be started from the Windows box"
        },
        "181": {
          "title": "fs.watch — native file-change watching across the event loop",
          "notes": "The faithful, event-driven `fs.watch`: a native OS change source folded into the `select()` loop via the standard hook trio. **Complete** — all four stages shipped: the `FSWatcher` object + event-loop integration, the Linux `inotify` backend ([ADR-00756](../adr/ADR-00756.md), a readable fd, no thread), the Windows `ReadDirectoryChangesW` backend ([ADR-00757](../adr/ADR-00757.md), a watcher thread + a loopback wakeup socket — the loop wakeup channel the §1 audit found missing), and the macOS `kqueue`/`EVFILT_VNODE` backend ([ADR-00758](../adr/ADR-00758.md)), each verified on its platform; the coverage row, reference entry, and example are shipped"
        },
        "180": {
          "title": "Windows faithfulness audit — where the platform layer is still CRT/POSIX-shaped",
          "notes": "Subsystem-by-subsystem audit of the shipped Win32 layer against libuv/Node Windows behaviour: the mingw toolchain decision held; the structural debt is the CRT fd table (blocking stdin/pipe reads, 10 ms poll slices, socket cap — recommends an owned handle table before IOCP), plus ranked incremental gaps in console encoding, `child_process` shell/quoting, fs sharing/`\\\\?\\` paths, errno tables, and Winsock option translation, security-flavoured rows first"
        },
        "177": {
          "title": "Windows support — mingw-w64 toolchain, Win32-native platform layer",
          "notes": "Stages 0–5 shipped ([ADR-00718](../adr/ADR-00718.md), [ADR-00719](../adr/ADR-00719.md)): the full E2E suite, the examples, and TLS/HTTP2/wss servers run on Windows 11 x86-64 with the documented skips (signals, POSIX-only tools, sanitizers, symlinks without Developer Mode). `path` defaulting to `path.win32` shipped separately ([TDD-00178](../tdd/TDD-00178.md)). `klain:webview` (Edge WebView2) and `-package` (GUI-subsystem exe) followed ([ADR-00725](../adr/ADR-00725.md), [ADR-00726](../adr/ADR-00726.md)). Open: cluster round-robin scheduling (workers share one listening socket instead), an IOCP reactor, `--static` on Windows, CI"
        },
        "175": {
          "title": "Deep reclamation under `-mm=auto` (container elements, object graphs)",
          "notes": "Staged: type-directed deep free for transitively-owned bindings (**Stage 1 shipped** — typed `JSON.parse`/`res.json()` trees, [ADR-00716](../adr/ADR-00716.md)) → fresh-producer inference (`@fresh`) → scoped refcounting for mid-block garbage (the rbtree shape); the main [TDD-00174](../tdd/TDD-00174.md) flip blocker surfaced by the first benchmark campaign"
        },
        "168": {
          "title": "`async_hooks` — `AsyncLocalStorage`",
          "notes": "Context propagation over the existing fiber model — the store lives in the coroutine task struct, so cross-`await` propagation is nearly free; low-level `createHook`/async-id lifecycle is a documented out-of-scope faithful impossibility"
        },
        "3": {
          "title": "Alternative fetch backend (Go helper vs libcurl)",
          "notes": "Low priority"
        },
        "11": {
          "title": "IndexedDB-compatible storage API",
          "notes": "Real IDB shape over a pluggable backend — deliberately deprioritized ([docs/BACKLOG.md](../BACKLOG.md) §6)"
        },
        "22": {
          "title": "Best-effort vanilla-JS compatibility (opt-in)",
          "notes": "In progress on the `-compat=js` axis ([ADR-00624](../adr/ADR-00624.md)–[ADR-00628](../adr/ADR-00628.md)): constructor-inferred class fields, call-site-inferred params, suppressed definite-assignment, pre-ES6 prototype classes ([TDD-00155](../tdd/TDD-00155.md) Stage 4), and now the implicit-`any` operator half ([TDD-00076](../tdd/TDD-00076.md) — `function add(a,b){return a+b}` runs as real JS on the NaN-boxed value). Remaining: dynamic property add on *static* literals (D1 Stage 6), `ToPrimitive` on object operands, loose-`==` coercion"
        },
        "33": {
          "title": "Direct hardware / framebuffer access",
          "notes": "Same FFI-adjacent gap as 00032; Linux-only by nature"
        },
        "34": {
          "title": "`TextDecoder` non-UTF-8 encodings",
          "notes": "See the Encoding / Text row"
        },
        "36": {
          "title": "Freestanding microcontroller target (Pi Pico)",
          "notes": "Deliberately low priority; minimal-core scope only"
        },
        "45": {
          "title": "Raspberry Pi (aarch64 SBC) target — boot image + GPIO",
          "notes": "Hosted-Linux target, distinct from 00036's freestanding Pico; low priority"
        },
        "46": {
          "title": "Opt-in embedded JS engine for `eval`/`vm` (quickjs-ng)",
          "notes": "Gates dynamic `eval`/`vm`; the static-literal subset already runs via the `eval` fast path"
        },
        "48": {
          "title": "WebAssembly target (`wasm32-unknown-wasi`)",
          "notes": "Reference/feasibility; `-mm=gc` and the fiber loop have no wasm equivalent"
        },
        "53": {
          "title": "npm / `node_modules` interop (opt-in)",
          "notes": "See the Modules row"
        },
        "54": {
          "title": "klmpm — a native package manager",
          "notes": "Stage 1 only (hand-built `klain_modules/` packages) — see the Modules row"
        },
        "55": {
          "title": "Dynamic `import(...)` and `import.meta`",
          "notes": "`import.meta.url` exists; dynamic `import()` works opt-in via `-dynamic-import=lazy` (real laziness via `.so` islands) — see the Modules row"
        },
        "56": {
          "title": "Dynamic `import(...)` V2 — runtime-loaded shared libraries",
          "notes": "Genuine laziness at odds with whole-program AOT — a `.so`/`.dylib` island per target"
        },
        "65": {
          "title": "Generalized destructuring (for-of, nested patterns, object rest)",
          "notes": "Declaration/for-of/nested-param forms done; object rest and assignment-form nested patterns deferred"
        },
        "68": {
          "title": "Object-model evolution — static reach vs. dynamic/prototype model",
          "notes": "Direction decided: near-term static shipped; a deferred native dynamic/prototype model is where prototype semantics + vanilla-JS compat land — designed in [TDD-00155](../tdd/TDD-00155.md)"
        },
        "72": {
          "title": "Enriched diagnostics + strict spec-error-matching mode",
          "notes": "A caret-snippet renderer (default) plus JS-error-kind/wording matching behind a strict mode"
        },
        "73": {
          "title": "Runtime debug symbols (DWARF via LLVM debug metadata)",
          "notes": "`DISubprogram`/`!dbg` under a `--debug` `-O0` build; positions already on every AST node"
        },
        "75": {
          "title": "`-compat` — whole-program semantic-fidelity axis",
          "notes": "`strict`/`js` modes ship; more inhabitants pending — see [Cross-Cutting](#cross-cutting)"
        },
        "76": {
          "title": "Real `any` semantics as a `-compat` inhabitant",
          "notes": "Implicit-`any` on unannotated params + runtime operator dispatch on a boxed `any`; successor to superseded [TDD-00005](../tdd/TDD-00005.md)"
        },
        "82": {
          "title": "External conformance suites V3 — WPT + Node core (supersedes [00008](../tdd/TDD-00008.md))",
          "notes": "Widens the benchmark from Test262 to the DOM-free WPT slice and Node buckets; ongoing"
        },
        "105": {
          "title": "Node `cluster` module",
          "notes": "Core cluster works; some IPC edges (`.close` propagation, etc.) pending"
        },
        "121": {
          "title": "Conformance V4 — in-scope Test262 + Node-core + TS accept/reject oracles",
          "notes": "Three oracle tracks beside the Test262 runner; drives the leverage histograms"
        },
        "124": {
          "title": "Self-hosting — rewriting the compiler in its own language",
          "notes": "Feasibility/reference doc; gated on the language reaching the needed surface"
        },
        "126": {
          "title": "Opt-in JSDoc/inline-TS type-enforcement flag",
          "notes": "Promote the currently-erased modifier tags (`@readonly`/`@private`/…) to enforced front-end checks"
        },
        "128": {
          "title": "`@pure` — compile-time function-purity enforcement",
          "notes": "A front-end purity checker (zero runtime cost), sibling to 00126"
        },
        "129": {
          "title": "Closure capture for nested function declarations",
          "notes": "Stage 1 (capture) done; full pre-declaration hoisting + capturing generators pending — see the Functions row"
        },
        "131": {
          "title": "Node.js API fidelity + the `klain:` namespace",
          "notes": "Ongoing per-module: real `http.createServer`, stream classes, `process`/`net` stages shipped; more modules to go"
        },
        "132": {
          "title": "Node streams as real classes (`class X extends Readable/Writable`)",
          "notes": "Readable/Writable/Duplex/Transform + PassThrough done (options form too); residual `_flush` override and some `super(options)` edges deferred — see the Streams row"
        },
        "143": {
          "title": "`klain:sync` — Go-fidelity goroutine runtime (preemptive M:N, channels, select)",
          "notes": "The concurrency differentiator, done for real: GMP scheduler, a safepoint compiler pass + preemption, preemptible stacks (converges with [00135](../tdd/TDD-00135.md)). `async`/Promises/`Worker` unchanged; `klain:` opt-in"
        },
        "144": {
          "title": "Selectable webview backend — CEF / QtWebEngine (cross-platform, GTK-free)",
          "notes": "A `-webview=<backend>` flag opting the [00142](../tdd/TDD-00142.md) desktop feature onto a Chromium engine — GTK-free on Linux, uniform across OSes (ties to Windows, [00020](../tdd/TDD-00020.md)). System engine stays default; Chromium opt-in (heavy). Low priority"
        },
        "134": {
          "title": "Escape analysis + value-layout optimizations (`-optimize-memory`)",
          "notes": "Stack-`alloca` non-escaping allocations, flat value-type arrays, register-returned small tuples. **Stage 1 shipped** ([ADR-00702](../adr/ADR-00702.md)): `-optimize-memory` stack-allocates non-escaping object literals via the TDD-00173 escape checker (entry-block alloca + at-site re-zeroing; composes with `-mm=auto` and FinalizationRegistry). Closure headers/envs stack-allocate too ([ADR-00704](../adr/ADR-00704.md) — capture cells stay heap), as do tuple-annotated literals and instances of classes passing a conservative whole-class `this`-audit ([ADR-00705](../adr/ADR-00705.md)) — **Stage 1 complete** across every fixed-size allocation-site kind. **Stage 2 shipped** ([ADR-00706](../adr/ADR-00706.md)): `/** @value */` flat value-type arrays — inline AoS element layout, value-copy writes, interior-pointer-view reads; flag-independent per-binding opt-in. **Stage 3 shipped** ([ADR-00707](../adr/ADR-00707.md)): value-returned small tuples — an eligible ≤2-scalar-field tuple return is a register-passable aggregate, making `const [v, err] = f()` allocation-free. **All stages complete**; default-on is gated by [TDD-00174](../tdd/TDD-00174.md)"
        },
        "135": {
          "title": "Precise/moving GC backend (Immix)",
          "notes": "A third memory mode — precise compacting collector; gated on precise stack maps"
        },
        "142": {
          "title": "`klain:webview` — desktop apps via the system webview",
          "notes": "Remaining: multi-window \u2014 a second `new Webview` is a clean rejection until the engine run-loop/app-delegate rework lands \u2014 see the Desktop row"
        },
        "145": {
          "title": "Status tracking as JSON source-of-truth (Markdown generated)",
          "notes": "Phases 1–5 shipped — `docs/status/data/*.json` is canonical, these pages are generated, numbers derive, CI guards the sync, the importer stays as on-demand `export`/`roundtrip` tooling ([ADR-00444](../adr/ADR-00444.md), [ADR-00445](../adr/ADR-00445.md)); remaining: the website reference projection deriving badges/differences from the status source"
        },
        "146": {
          "title": "Sailfish OS target — cross-compiled CLI + Gecko webview backend",
          "notes": "Sailfish is glibc/aarch64 Linux: CLI needs only a `--target`/`--sysroot` mode + RPM packaging; UI rides `sailfish-components-webview` (Gecko) as a `-webview=sailfish` backend under [00144](../tdd/TDD-00144.md)'s flag"
        },
        "147": {
          "title": "Android target — NDK backend port + WebView apps via a thin Java shim",
          "notes": "bionic port (fibers without ucontext, vendored curl/pcre2/gc), JNI library entry, then WebView apps with a Gradle-free `-package apk`; shares the cross-target flag with [00146](../tdd/TDD-00146.md)"
        },
        "32": {
          "title": "Native library bindings / GUI",
          "notes": "Placeholder, not a committed design — general FFI is the real prerequisite; the GUI route is met instead by the system webview ([TDD-00142](../tdd/TDD-00142.md))"
        },
        "150": {
          "title": "TUI framework roadmap — native klain:tui → FFI-unlocked opentui/GUI → Vue/React compat",
          "notes": "Staged on [00031](../tdd/TDD-00031.md)'s shipped primitives, ordered by ease×value under native>Vue>else: Stage 1 native `klain:tui` (vendors Yoga, owns the diff painter + `state→view` loop, no FFI); Stage 2 general C FFI (the [00032](../tdd/TDD-00032.md) foundation — unlocks opentui's native backend, GTK/Qt/SDL/imgui GUI, ncurses, DB clients); Stage 3 opentui; Stage 4 vue-termui; Stage 5 ink/React (lowest). Supports all options, none rejected"
        },
        "151": {
          "title": "`node:sqlite` — a real synchronous SQLite API",
          "notes": "The one database surface that is also a built-in Node API, so implemented faithfully under its real specifier (synchronous like `fs.readFileSync`; libsqlite3 links like libcurl). Remaining: the dynamic-row mode for untyped `SELECT *` (rows currently need an explicit `.all<T>()` shape), `db.aggregate()`, error `.code`, and a lazy `iterate()`; sessions/changesets, extension loading, and async `backup()` are rejected as out of scope. Not the KV-for-IndexedDB use [TDD-00011](../tdd/TDD-00011.md) set aside"
        },
        "155": {
          "title": "The native dynamic object model (D1) — property bag, prototype chain, descriptors",
          "notes": "The design for [00068](../tdd/TDD-00068.md)'s deferred Axis-2 endpoint — the single largest caveat cluster in these pages (~30% of all caveats trace to the static object model). Two-tier: typed objects stay fixed-shape structs; `any`/`JSON.parse`/prototype code gets a per-instance bag (any-box values, proto pointer, descriptors). Staged: core bag → untyped `JSON.parse` → prototype chain → vanilla-JS prototype classes → descriptors → static bridge → `Proxy`/`Reflect`. Stage 1 (the core bag behind `any`: member/bracket get/set, `delete`/`in`/keys/`for...in`, spread, identity) shipped — [ADR-00621](../adr/ADR-00621.md); Stage 2 (untyped `JSON.parse` → dynamic trees, tag-11 dynamic arrays, `JSON.stringify` of dynamic values) shipped — [ADR-00622](../adr/ADR-00622.md); Stage 3 (the prototype chain: `Object.create`/`get`/`setPrototypeOf`, `__proto__`, chain-walking get/`in`, cycle rejection) shipped — [ADR-00623](../adr/ADR-00623.md); Stage 4 (vanilla-JS prototype classes under `-compat=js`: function constructors with boxed `this`, dynamic-ABI prototype methods (tag 12), chain-walking method dispatch, `F.prototype`, `Base.call(this, ...)`, string-concat with dynamic operands) shipped — [ADR-00627](../adr/ADR-00627.md); Stage 5 (property descriptors: `defineProperty`/`getOwnPropertyDescriptor`/`getOwnPropertyNames`, literal + defined accessors with receiver `this`, enumerability, real freeze/seal + probes, strict TypeErrors; js-mode untyped literals become dynamic objects) shipped — [ADR-00629](../adr/ADR-00629.md); Stages 6+7 (`class X extends Error`, `Proxy` get/set/has/deleteProperty traps, the `Reflect` surface; the widening analysis superseded by js-mode's bare-literal dynamic default) shipped — [ADR-00630](../adr/ADR-00630.md). **All seven stages implemented.** Remaining follow-up: the slot-storage collapse"
        },
        "161": {
          "title": "Decorators (`@decorator` on classes, members, parameters)",
          "notes": "Remaining: class-decorator *replacement* (refused at runtime under the static vtable model — a documented divergence whose home is a future `-compat=js`/first-class-constructor world; a replacement decorator reading typed `this.field` hits the same boundary) and static-field decorators. Both dialects (experimental + `-decorators=standard`), every member placement, factory decorators, and `-emit-decorator-metadata` otherwise run ([ADR-00641](../adr/ADR-00641.md)\u2013[ADR-00649](../adr/ADR-00649.md))"
        },
        "162": {
          "title": "Implicit `any` for untyped bindings + clean rejection of incompatible reassignment",
          "notes": "Closes the largest invalid-IR sub-family (untyped `var x` reassigned across types stores a scalar raw into a ptr slot). Wiring over the existing D1 dynamic box (an annotated `let x: any` cross-type reassignment already works): route an untyped-uninitialized binding to implicit `any` in the strict lane too (already the case under `-compat=js`), and cleanly reject an incompatible reassignment to an inferred-typed binding instead of emitting a bad store. Operator-on-`any` and compat=js binding-widening are follow-on stages"
        },
        "163": {
          "title": "`FinalizationRegistry` — mode-dependent finalization + leak diagnostics",
          "notes": "The `WeakRef` companion, reimagined per memory mode rather than ported: `-mm=gc` gives real Boehm-finalizer semantics; `-mm=manual` gives *deterministic* finalization at the `Memory.free()` point plus an `atexit` survivor-flush (deterministic destructors the GC world can't offer); `-finalizers=report` turns the registry into a labeled **leak detector** for the leak-by-default model (registered-but-never-freed reported at exit by held value). One API, honest per-mode timing (the spec's best-effort clause permits all of it), staying useful under `-mm=auto` too (compiler-inserted frees are the same death signal — they route through the shared `freeSymbol` chokepoint, [TDD-00173](../tdd/TDD-00173.md); unprovable registrations flush at `atexit` since `auto` has no collector). Turns a half-missing competitor feature into a memory-management + debugging tool. **Stages 1–4 shipped** ([ADR-00701](../adr/ADR-00701.md)): manual-mode deterministic firing at `Memory.free` + exit survivor-flush, real Boehm-finalizer firing under `-mm=gc` (with the TLS-root and lazy-invocation gotchas solved), `-finalizers=report` leak lines, tokens/`unregister`, `-compat=js` dynamic targets. **Stage 5 shipped** ([ADR-00703](../adr/ADR-00703.md)): register's target position is a free-notified retention, so under `-mm=auto` (and explicit `@free`/`@owned` in manual mode) a registered target's cleanup fires deterministically at scope exit. All five stages implemented"
        },
        "164": {
          "title": "General C FFI — AOT `node:ffi` (`dlopen` + static signatures + callback trampolines)",
          "notes": "The split-out concrete design for [00150](../tdd/TDD-00150.md) Stage 2 / the [00032](../tdd/TDD-00032.md) placeholder foundation — the pivot that unlocks opentui's native backend, the GTK/Qt/SDL/imgui GUI crossover, ncurses, and DB clients. Now anchored to Node's real first-party `node:ffi` (v26.1.0, experimental): implement the actual Node API (`dlopen`/`DynamicLibrary`/plain-object signatures/`registerCallback`/raw-memory helpers) rather than a bespoke dialect, so portable FFI code compiles unchanged. The fit is strong — the type-name enum ≈ the JSDoc int-width set, 64-bit↔`bigint` already modelled, `dlopen`/`dlsym` are the `ensure*()` libc-primitive shape, and a *static* signature is easier to lower AOT than Node's runtime-parsed one. Staged A (load + typed calls) → B (raw memory) → C (closure→C-function-pointer trampoline — the one genuinely new codegen piece, the core gap 00032/00150 named). Not started"
        },
        "165": {
          "title": "Importable specifiers for the Web-global-backed Node modules (`url`/`timers`/`perf_hooks`/`buffer`/`events`)",
          "notes": "Remaining (Stage 4): new feature surfaces on the now-importable Web-global-backed modules, ranked by leverage \u2014 `PerformanceObserver` extensions ([TDD-00166](../tdd/TDD-00166.md)) and `timers/promises`. The import wiring itself (same-name/`node:`/default/aliased specifiers) is complete"
        },
        "166": {
          "title": "`PerformanceObserver` (perf_hooks)",
          "notes": "Observe `mark`/`measure` performance entries. **V1 shipped** ([ADR-00673](../adr/ADR-00673.md)): `new PerformanceObserver(cb)` + `observe({entryTypes})`/`disconnect()`, a global observer registry `performance.mark`/`measure` walk, and `getEntries()` yielding `PerformanceEntry` objects — **synchronous** dispatch. Follow-ups: async batched delivery (the load-bearing fidelity gap), `buffered`/`takeRecords`/single-`type` form, `getEntriesBy*`"
        },
        "167": {
          "title": "`events` async helpers (`once`/`on`)",
          "notes": "`events.once` shipped ([ADR-00675](../adr/ADR-00675.md)) — desugars to `new Promise((res) => ee.once(name, (…a) => res([…a])))`, unblocked by the `Promise<T[]>` value fix ([ADR-00674](../adr/ADR-00674.md)). `events.on` (async iterator over a buffered event queue) shipped too ([ADR-00677](../adr/ADR-00677.md)) — eager listener registration, unbounded buffer, consumer parks when the queue drains"
        },
        "169": {
          "title": "Faithful async rejection values",
          "notes": "Preserve the original rejected/thrown value (a NaN-boxed `any`) instead of coercing to a stringified Error — fixes `Promise.allSettled` `.reason` fidelity and `throw <non-Error>`; touches the exception + combinator paths"
        },
        "170": {
          "title": "Gamepad API (native controller input)",
          "notes": "The W3C `navigator.getGamepads()` shape backed by **SDL2** (`SDL_GameController`) — one cross-platform library gives the standard mapping DB, hotplug, and rumble, so it's a mapping exercise rather than per-OS backends. Reinterprets a formerly out-of-scope browser API, like URL/fetch/EventTarget"
        },
        "171": {
          "title": "Geolocation API (native reinterpretation)",
          "notes": "The W3C `navigator.geolocation` shape backed by tiered native sources — V1 is IP-based geolocation over the existing `fetch` stack (zero new dep, matches a headless server), with `enableHighAccuracy` selecting OS services (Linux GeoClue / macOS CoreLocation) and serial-NMEA GPS as opt-in follow-ons; `KML_GEOLOCATION` override for pinning/CI"
        },
        "172": {
          "title": "File API family (fs-backed data + UI-abstracted picker)",
          "notes": "`File`/`FileReader` as a thin fs-backed reinterpretation (no UI, near-term), plus `showOpenFilePicker`/`FileSystemFileHandle` behind a pluggable UI-backend seam — `klain:tui` picker widget, `klain:webview` native dialog, explicit-path degradation headless. Ties into the interactive-native-app direction"
        },
        "174": {
          "title": "Maturity gate for flipping `-mm=auto` (+ `-optimize-memory`) to default",
          "notes": "Two flips: dev-default first (tests/examples/per-delivery checks compile in auto+optimize so every deliverable soaks the escape analysis by construction), shipped CLI default gated on quantified exit criteria — mode-differential lanes over the examples/E2E/conformance corpora, ASan-armed, an escape-shaped fuzz lane, klainload soak, free census"
        },
        "176": {
          "title": "General `as T` on dynamic values — projection, rejection, or erasure?",
          "notes": "The design question [ADR-00715](../adr/ADR-00715.md)'s narrow carve-out defers: whether strict-mode `any as T` should emit a runtime dynamic→concrete projection, become a clean rejection, or stay erased with per-shape carve-outs; the scalar unbox subset is the likely first slice"
        }
      }
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## Roadmap & priorities\n\nRemaining work and its prioritization live in one place now: [docs/BACKLOG.md](../BACKLOG.md) — the hand-maintained, leverage-ranked backlog reconciled against this status data and the TDD index. This page tracks *what is implemented and how faithfully*; the backlog tracks *what to do next and in what order*. (The old inline Roadmap section duplicated it and drifted.)"
    },
    {
      "kind": "block",
      "text": "---"
    },
    {
      "kind": "block",
      "text": "## Status page format"
    },
    {
      "kind": "block",
      "text": "Every detail page linked from this index follows one layout, so the tables stay scannable and this index can deep-link into them:"
    },
    {
      "kind": "block",
      "text": "- **Status is binary** — each feature row is ✅ or ❌, nothing else. An implemented feature with a real behavioral divergence still counts ✅, but that divergence lives in the **Caveats** column, and any non-empty Caveats cell excludes the row from Strict Coverage.\n- **Two note columns, both bulleted** — **Caveats** (behavioral divergences from real JS/TS) and **Notes** (implementation/representation detail only, no behavioral gap). Neither ever appears in the Status field, and caveats are not also summarized in a block at the top of the page.\n- **One table per category** — when a page hosts more than one of this index's categories (e.g. [TYPE-SYSTEM.md](TYPE-SYSTEM.md)'s *Type primitives* vs *Type system features*), it splits into a `##`-headed table per category. Headings are the **plain category name only** (`## Control flow statements`) — never with numbers in them, keeping the page's own GitHub anchors clean (`#control-flow-statements`) for in-page navigation. A single-category page uses one table and no category heading.\n- **Numbers derive from the tables, and appear once** — Coverage = ✅ rows / total rows; Strict = (✅ rows with an empty Caveats cell) / total rows. They live only in the page's top `**Coverage**`/`**Strict Coverage**` line (which lists each category's figure when a page has several), never repeated in a section heading.\n- **This index links to the page, not a section anchor** — each category row's Page cell points at the whole page file (`PAGE.md`), never a `#section` fragment, so the link resolves in every Markdown viewer (some IDEs can't navigate a cross-file header anchor and error on the click); it keeps its own one-line Coverage/Strict figure matching the page, which is the source of truth if the two ever drift."
    },
    {
      "kind": "block",
      "text": "Every detail page linked from this index follows this format.\n\n**These pages are generated.** The source of truth is `docs/status/data/*.json` (one file per detail page plus `_index.json` for this index); `make status` regenerates every page and this index from it, and CI fails if a page is edited directly (each page also opens with a do-not-edit comment banner naming its JSON source). Every Coverage/Strict fraction, percentage, and section total is derived from the row tables at generation time, never typed by hand — an exact integer percent renders bare (`50%`), a non-exact one with a leading tilde (`~93%`)."
    },
    {
      "kind": "block",
      "text": "*Last updated: 2026-08-28*"
    }
  ]
}
