Node modules written in TypeScript (TDD-00231), compiled with the program
that imports them. `stream.ts` is `stream`; `stream_promises.ts` is
`stream/promises` (a `/` in the specifier is a `_` in the file name).
