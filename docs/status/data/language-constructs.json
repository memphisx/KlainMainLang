{
  "id": "language-constructs",
  "file": "LANGUAGE-CONSTRUCTS.md",
  "title": "Language Constructs",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index."
    },
    {
      "kind": "coverage",
      "style": "multiCategory"
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format)."
    },
    {
      "kind": "table",
      "table": {
        "heading": "Control flow statements",
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`if` / `else if` / `else`",
            "status": "implemented"
          },
          {
            "feature": "`while` loop",
            "status": "implemented"
          },
          {
            "feature": "`do…while` loop",
            "status": "implemented"
          },
          {
            "feature": "`for (init; cond; update)`",
            "status": "implemented"
          },
          {
            "feature": "`for…of` over arrays, strings, `Map`, `Set`, and a class implementing `next(): T \\| null`",
            "status": "implemented",
            "caveats": [
              "A bare `for (const v of map)` iterates values (use `.keys()` for keys — [ADR-00011](../adr/ADR-00011.md)); the two-name pattern `for (const [k, v] of map)` decomposes entries ([ADR-00481](../adr/ADR-00481.md))",
              "A string iterates per byte, not per Unicode code point — correct for ASCII/Latin-1, the same byte-string narrowing the rest of the string layer carries ([ADR-00535](../adr/ADR-00535.md))"
            ],
            "notes": [
              "The loop variable may be a destructuring pattern (`for (const [a, b] of pairs)` over an array-of-arrays, `for (const { x, y } of points)`), including nested patterns (`for (const [[a, b], c] of …)`) — see [ADR-00192](../adr/ADR-00192.md)/[ADR-00193](../adr/ADR-00193.md)/[TDD-00065](../tdd/TDD-00065.md) Stages 1–2",
              "Iterates arrays, a string (its characters, one per byte — [ADR-00535](../adr/ADR-00535.md)), `Map` (values), `Set` (elements), generators, a class implementing `next(): T \\| null` ([TDD-00009](../tdd/TDD-00009.md) Stage 1a, a compile-time structural check), **and the spec's real protocol**: a class or object literal with a `[Symbol.iterator]()` returning an iterator whose `next()` yields `{value, done}` — dispatched ahead of the structural shape when both exist ([ADR-00278](../adr/ADR-00278.md)/[ADR-00279](../adr/ADR-00279.md))",
              "A `next(): T \\| null` with a non-pointer scalar `T` returns a presence-flagged aggregate ([TDD-00064](../tdd/TDD-00064.md)/[ADR-00199](../adr/ADR-00199.md)), so a legitimately-yielded `0`/`false` does not end iteration; a pointer `T` keeps its null-pointer sentinel — see [ADR-00063](../adr/ADR-00063.md)"
            ]
          },
          {
            "feature": "`for…in` over object keys",
            "status": "implemented"
          },
          {
            "feature": "`switch` / `case` / `default` / `break`",
            "status": "implemented",
            "notes": [
              "Numeric, string, and boolean discriminants"
            ]
          },
          {
            "feature": "`break` / `continue` in loops, including labeled (`outer: for (...) { break outer; }`)",
            "status": "implemented",
            "notes": [
              "See [ADR-00010](../adr/ADR-00010.md)",
              "A `break`/`continue` (labeled or not) inside a nested closure/function expression can only target a loop declared within that closure's own body, not one in an enclosing function — a clean compile-time rejection (`break`/`continue` outside of a loop, or an undefined label), not a silent misparse, matching JS's own no-cross-function-boundary label rule ([ADR-00168](../adr/ADR-00168.md))"
            ]
          },
          {
            "feature": "`return`",
            "status": "implemented",
            "notes": [
              "Typed; `void` implicit return handled"
            ]
          },
          {
            "feature": "`throw new Error(msg)`",
            "status": "implemented",
            "notes": [
              "Via `setjmp` / `longjmp`",
              "A built-in error constructor is also a first-class *value* (`assert.throws(TypeError, fn)`, `x === RangeError`): a boxed funcref with exact identity, `typeof` `\"function\"`, native-function stringification ([ADR-00289](../adr/ADR-00289.md))"
            ]
          },
          {
            "feature": "`try` / `catch` / `finally`",
            "status": "implemented",
            "caveats": [
              "A catch pattern can only destructure the caught value's fixed `{kind, message, name}` shape (every thrown value, including a thrown non-Error primitive, is force-shaped into that one runtime layout), not an arbitrary custom object's own fields, and there's no array-pattern form ([ADR-00170](../adr/ADR-00170.md))"
            ],
            "notes": [
              "Single catch variable, or a destructured object pattern (`catch ({ message, name })`)",
              "`finally` runs on an early `return`/`break`/`continue` (labeled or not), not only on fall-through — the return value is captured before it, a `return` in `finally` overrides, and nested finallys run innermost-first, matching JS ([ADR-00191](../adr/ADR-00191.md)). This holds inside an `async` function and a generator too, including a `finally` that itself `await`s ([ADR-00612](../adr/ADR-00612.md))"
            ]
          },
          {
            "feature": "Optional catch binding (`try {} catch {}`, no bound param) — ES2019",
            "status": "implemented",
            "notes": [
              "The `(e)` clause is optional — `emit_exceptions.go` already handled an empty `Param` correctly, so this was a parser-only change. See [ADR-00086](../adr/ADR-00086.md)"
            ]
          },
          {
            "feature": "`debugger` statement",
            "status": "implemented",
            "notes": [
              "Parsed and erased to a no-op — an AOT-compiled native binary has no attached inspector to break into, matching plain JS with no debugger attached. `debugger` is a reserved word, never a real identifier. See [ADR-00373](../adr/ADR-00373.md)"
            ]
          }
        ]
      }
    },
    {
      "kind": "table",
      "table": {
        "heading": "Operators",
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "Numeric literals (`42`, `3.14`, `0xFF`, `0b101`, `0o77`)",
            "status": "implemented"
          },
          {
            "feature": "Numeric separators (`1_000_000`)",
            "status": "implemented",
            "notes": [
              "Stripped at lex time — every downstream consumer (parser, codegen) sees a clean literal, never the `_`",
              "A leading/trailing/doubled separator is a clean lexer error, as is a separator inside a legacy-octal/non-octal-decimal literal (`0_0`, `08_0` — a SyntaxError even in non-strict mode, [ADR-00197](../adr/ADR-00197.md)). See [ADR-00085](../adr/ADR-00085.md)"
            ]
          },
          {
            "feature": "String literals (single/double quote)",
            "status": "implemented",
            "caveats": [
              "An embedded NUL (`\\x00`, `\\0`) truncates the string, since strings are stored as NUL-terminated C strings",
              "No strict-mode `SyntaxError` for a legacy octal escape (accepted unconditionally)"
            ],
            "notes": [
              "The full ES escape grammar is decoded: `\\b \\f \\v`, `\\xHH`, `\\uHHHH`, `\\u{H…}`, Annex B legacy octal (`\\0`–`\\377`) and `\\8`/`\\9`, line continuation, and the NonEscapeCharacter rule (`\\A` → `A`) — see [ADR-00194](../adr/ADR-00194.md); templates share the same decoding"
            ]
          },
          {
            "feature": "Boolean literals (`true` / `false`)",
            "status": "implemented"
          },
          {
            "feature": "`null` literal",
            "status": "implemented",
            "notes": [
              "`T \\| null` union type supported"
            ]
          },
          {
            "feature": "`undefined` literal",
            "status": "implemented"
          },
          {
            "feature": "Template literals `` `Hello ${name}` ``",
            "status": "implemented",
            "notes": [
              "Arbitrary interpolation depth"
            ]
          },
          {
            "feature": "Arithmetic operators `+ - * / %`",
            "status": "implemented"
          },
          {
            "feature": "`**` (exponentiation, ES2016)",
            "status": "implemented",
            "notes": [
              "`number` operands are doubles, so `2 ** -1` is `0.5` and `2 ** 0.5` is a real root — JS-faithful ([ADR-00377](../adr/ADR-00377.md)); explicit *integer*-typed operands use exact i64 exponentiation-by-squaring and stay i64 (a negative exponent then yields 0, the integer analogue) — the escape-hatch behavior",
              "`2 ** 10`, right-associative (`2 ** 3 ** 2` === 512), binds tighter than `* / %`, with `**=` — see [ADR-00187](../adr/ADR-00187.md)",
              "`-2 ** 2` is a `SyntaxError` (parenthesize as `(-2) ** 2` or `-(2 ** 2)`), matching JS"
            ]
          },
          {
            "feature": "Comparison operators `== === != !== < > <= >=`",
            "status": "implemented",
            "notes": [
              "String comparison via `strcmp`",
              "NaN compares per JS: `NaN !== NaN` (and `NaN != x`) is `true` while `===`/`<`/`>`/`<=`/`>=` against NaN are all `false` — `!=`/`!==` use the unordered `fcmp une` predicate ([ADR-00188](../adr/ADR-00188.md))"
            ]
          },
          {
            "feature": "Logical operators `&& \\|\\| !`",
            "status": "implemented",
            "caveats": [
              "Under `-compat=js`, `&&`/`\\|\\|` are value-preserving only for **same-typed** operands; differently-typed operands (`0 \\|\\| \"x\"`) stay bool, since the value-preserving result would be a union this compiler can't represent ([ADR-00220](../adr/ADR-00220.md))"
            ],
            "notes": [
              "Result type is a bool under `-compat=strict` (default); `-compat=js` makes `&&`/`\\|\\|` value-preserving (`x \\|\\| \"default\"` yields the string) for same-typed operands — a `-compat`-governed behavior ([ADR-00186](../adr/ADR-00186.md)/[ADR-00220](../adr/ADR-00220.md)/[TDD-00075](../tdd/TDD-00075.md))",
              "Genuinely short-circuiting in both modes — `&&` only evaluates its right operand when the left is truthy, `\\|\\|` only when the left is falsy, so `x && x.field` / `arr.length > 0 && arr[0]` don't run the right side down the guarded branch ([ADR-00186](../adr/ADR-00186.md))"
            ]
          },
          {
            "feature": "Bitwise operators `& \\| ^ ~ << >> >>>`",
            "status": "implemented"
          },
          {
            "feature": "Assignment operators `+= -= *= /= %= &= \\|= ^= <<= >>= >>>=`",
            "status": "implemented"
          },
          {
            "feature": "Logical assignment operators `&&= \\|\\|= ??=` (ES2021)",
            "status": "implemented",
            "notes": [
              "Genuinely short-circuiting (like the plain `&&`/`\\|\\|` operators — [ADR-00186](../adr/ADR-00186.md)) — the right side is only evaluated down the branch the operator's own rule requires",
              "Works against scalar variables, array elements, object fields, static class fields, and a **computed dynamic-object key** (`dynObj[key] ??= v`, through the map get/set — [ADR-00600](../adr/ADR-00600.md)) — including a nullable-scalar (`number \\| null`) field, which routes through the presence-flagged `{ i1, T }` path so `obj.n ??= v` fires exactly when the field is absent ([TDD-00064](../tdd/TDD-00064.md)/[ADR-00199](../adr/ADR-00199.md)/[ADR-00246](../adr/ADR-00246.md)). See [ADR-00087](../adr/ADR-00087.md)"
            ]
          },
          {
            "feature": "Increment / decrement `++ --` (prefix & postfix)",
            "status": "implemented",
            "notes": [
              "Works on an identifier, a member (`obj.x++`/`this.x++`/`C.staticField++`/`makeObj().x++`), and an index (`arr[i]++`/`arr[side()]++`/`makeArr()[i]++`) target; a consumed **postfix** update hoists any side-effecting component — member receiver, array-producing object, and index expression — so each runs exactly once, matching JS ([ADR-00606](../adr/ADR-00606.md))",
              "A member/index target desugars to the equivalent `+= 1`/`-= 1` compound assignment, reusing every member/index/static-field assignment path; prefix yields the new value, postfix the old ([ADR-00376](../adr/ADR-00376.md))"
            ]
          },
          {
            "feature": "Ternary `cond ? a : b`",
            "status": "implemented",
            "notes": [
              "A `cond ? scalar : null` (or `cond ? null : scalar`) branch mix is a `T | null` value — the null survives into `??`/concat/`=== null`/an unannotated binding, not collapsed to a payload zero ([ADR-00538](../adr/ADR-00538.md))"
            ]
          },
          {
            "feature": "Comma / sequence operator `(a, b, c)`",
            "status": "implemented",
            "caveats": [
              "A sequence whose *first* operand is a lone identifier (`(a, b)`) is instead parsed as an arrow-function parameter list — a known ambiguity; write a non-identifier first operand ([ADR-00179](../adr/ADR-00179.md))"
            ],
            "notes": [
              "Evaluates each operand left to right, yields the last (its type is the sequence's type)",
              "Also usable in a `for` header",
              "Works at statement level too (`a(), b();` — [ADR-00476](../adr/ADR-00476.md))"
            ]
          },
          {
            "feature": "Nullish coalescing `??`",
            "status": "implemented",
            "notes": [
              "Works on a pointer `T \\| null` (string/object/…) and, since [TDD-00064](../tdd/TDD-00064.md)/[ADR-00199](../adr/ADR-00199.md), a non-pointer scalar `T \\| null` (`number \\| null`, `boolean \\| null`, …) too — a real presence bit, so `let x: number \\| null = null; x ?? 42` yields `42` while a present `0` yields `0`"
            ]
          },
          {
            "feature": "Optional chaining `?.`",
            "status": "implemented",
            "notes": [
              "Null-guards ptr fields; returns null on null receiver",
              "An array receiver (`arr?.length`, `arr?.map(...)`) uses plain access — an array is a value aggregate, never a null pointer ([ADR-00539](../adr/ADR-00539.md))"
            ]
          },
          {
            "feature": "`typeof` operator",
            "status": "implemented",
            "caveats": [
              "`typeof <namespace>.<method>` answers `\"function\"` for the common built-in namespaces — `Promise`/`Math`/`JSON`, `console` (any member), and the static methods of `Object`/`Number`/`String`/`Array`/`Date`/`Symbol`/`Reflect`/`Boolean` ([ADR-00282](../adr/ADR-00282.md)/[ADR-00596](../adr/ADR-00596.md)); a non-method static (`Number.MAX_VALUE`) still answers through normal inference, and a method on a namespace outside this allow-list falls back to inference",
              "`typeof value.method` is `\"function\"` for a class instance method and the common built-in string/array methods ([ADR-00607](../adr/ADR-00607.md)); a string/array method name outside that curated set falls back to inference (a wrong `\"number\"`)"
            ],
            "notes": [
              "Compile-time constant; resolved from inferred type. Heap-backed built-in values (a Promise, `Map`/`Set`, a generator instance, `Date`, `null`, …) are `\"object\"`; classes, implemented built-in constructors (`Promise`, `Date`, the error family, …) and implemented global functions (`fetch`, `setTimeout`, …) are `\"function\"`; `Math`/`JSON`/`console` are `\"object\"`; an undeclared name or runtime-absent static (`Promise.try`) is `\"undefined\"`, as in JS ([ADR-00282](../adr/ADR-00282.md))",
              "`typeof value.method` answers `\"function\"` for a class instance method or a common built-in string/array method, while a field, `length`, or an accessor property answers through normal inference (a class getter reads as its return type) ([ADR-00607](../adr/ADR-00607.md))"
            ]
          },
          {
            "feature": "`in` operator (`key in obj`)",
            "status": "implemented",
            "notes": [
              "Reduces to the same compile-time `FieldIndex` lookup `Object.hasOwn()`/`.hasOwnProperty()` already use (object shapes are fully structural/static — no dynamic property add/delete) — a string-literal key against an object is a compile-time-constant `true`/`false`, not a runtime scan",
              "On a bare `any` value it *is* a runtime membership scan of the D1 dynamic object's property bag, and the key may be runtime-computed ([TDD-00155](../tdd/TDD-00155.md) Stage 1); `delete anyVal.key` likewise deletes for real",
              "Parsed as a contextual keyword (not a reserved token) at the same precedence as `instanceof`, matching how `for...in` already recognizes \"in\". See [ADR-00091](../adr/ADR-00091.md)"
            ]
          },
          {
            "feature": "Spread in array literals `[...a, ...b]`",
            "status": "implemented",
            "notes": [
              "Elisions (`[1,,3]`) parse too — a hole reads as `undefined`/zero and counts toward the length ([ADR-00467](../adr/ADR-00467.md))"
            ]
          }
        ]
      }
    },
    {
      "kind": "table",
      "table": {
        "heading": "Variable declarations",
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`const` / `let` / `var` declarations",
            "status": "implemented",
            "caveats": [
              "Definite-assignment analysis is sound-not-complete: a typed `let` assigned only in a maybe-skipped loop or across correlated conditions still falls through to the `undefined`/zero default rather than raising `used before being assigned` ([TDD-00071](../tdd/TDD-00071.md) Stage 2/[ADR-00213](../adr/ADR-00213.md))",
              "A top-level `const`/`let` still isn't readable from a named `function` only in the invariant's genuine edges — a `bigint`, a *scalar*-returning un-annotated call (`emitVarDecl`'s `i64` default), a `new Set([…])` typed from its initializer array, or an init depending on a runtime-local (a `{ …rest }` spread of a destructuring target); an arrow/closure still captures any of them ([TDD-00093](../tdd/TDD-00093.md))"
            ],
            "notes": [
              "A top-level `const`/`let`/`var` is a **module global** — readable and writable from any named `function`, not only a capturing arrow — whenever its type is determinable before `main()` runs: a scalar/string/array/object/`Map`/`Set`, an **un-annotated call to a named function** (`const cfg = loadConfig()`), or an initializer referencing an **earlier global** (`const b = a`). A mutation through it (a reassignment, `arr.push`, `obj.field =`, `map.set`) is seen everywhere, including by a capturing closure ([ADR-00273](../adr/ADR-00273.md)/[TDD-00093](../tdd/TDD-00093.md))",
              "`var` is function-scoped (leaks its enclosing block, and re-declarable with the same name); `let`/`const` are block-scoped; `const` rebinding is rejected",
              "Block-scoped `let`/`const` redeclaration is a compile error; the top-level duplicate check is kind-aware (`var x; var x` allowed, `let x; let x` / `let x; var x` rejected) and the cross-block `let x; { var x }` intersection is caught too — see [TDD-00070](../tdd/TDD-00070.md)/[ADR-00210](../adr/ADR-00210.md)/[ADR-00211](../adr/ADR-00211.md)",
              "A `let`/`const` read before its declaration is a distinct `cannot access 'x' before initialization` TDZ error (including the block-shadowing form) — [TDD-00071](../tdd/TDD-00071.md) Stage 1/[ADR-00212](../adr/ADR-00212.md)",
              "A typed `let` read before assignment is a `used before being assigned` error for the common cases; `var` is exempt — hoisted and zero/`undefined`-initialized, no TDZ ([TDD-00071](../tdd/TDD-00071.md) Stage 2/[ADR-00213](../adr/ADR-00213.md)/[ADR-00454](../adr/ADR-00454.md))"
            ]
          },
          {
            "feature": "Array destructuring `const [a, b] = arr`",
            "status": "implemented",
            "caveats": [
              "*Assignment* to already-declared targets (`[a, b] = expr`, not a fresh declaration) now reaches declaration-form parity for nesting ([ADR-00595](../adr/ADR-00595.md)): nested array/object patterns (`[[a, b], c] = e`), a trailing `...rest`, and a per-element `= default` (out-of-bounds is the presence signal) all work; a compound operator (`[a, b] += …`) stays rejected, and a member target (`[obj.x, arr[i]] = e`) isn't wired yet"
            ],
            "notes": [
              "Holes are supported (`const [, b] = arr` skips index 0)",
              "The source may be a string — `const [a, b] = \"xy\"` binds its characters (`a`=`\"x\"`, `b`=`\"y\"`), one per byte, in both the declaration and assignment forms ([ADR-00536](../adr/ADR-00536.md))",
              "Default values (`[a = 1]`) fire exactly when that position is past the source array's actual length — see [ADR-00157](../adr/ADR-00157.md)/[ADR-00158](../adr/ADR-00158.md)",
              "A rest element (`[a, ...rest]`) works in the declaration form, a destructured parameter, and the assignment form alike — a real, independent copy of every remaining position, empty (not an error) when the source is shorter than the pattern — see [ADR-00161](../adr/ADR-00161.md)",
              "Usable as a for-of loop variable (`for (const [a, b] of pairs)`, over an array-of-arrays), reusing the same unpack core — see [ADR-00192](../adr/ADR-00192.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 1",
              "Nested patterns (`[[a, b], c]`, `[{ x }, { y }]`) work in a declaration, for-of loop variable, and destructured parameter — see [ADR-00193](../adr/ADR-00193.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 2"
            ]
          },
          {
            "feature": "Object destructuring `const { x, y } = obj`",
            "status": "implemented",
            "caveats": [
              "Default values (`{ x = 1 }`) are supported only for a field typed as a nullable *reference* (`T \\| null` where T is string/array/object/class) — the only field shape with a real \"not provided\" signal; a default on a nullable scalar (`number \\| null`, whose `null` is a fake in-band 0 sentinel — [ADR-00158](../adr/ADR-00158.md)) or any non-nullable field is a clean compile-time rejection",
              "*Assignment* to already-declared targets now supports nested patterns (`({ p, q: { r, s } } = obj)`, `({ arr: [x, y] } = obj)`) and object-property defaults, shorthand `{ x = default }` and keyed `{ k: x = default }`, on a nullable-reference field, at declaration-form parity ([ADR-00595](../adr/ADR-00595.md)/[ADR-00597](../adr/ADR-00597.md)); a `= default` on a nested position stays rejected — matching the declaration form",
              "`{ ...rest }` over an `any`/union/generic source (Stage 3c) and in the *assignment* form (`({ a, ...rest } = e)`) stay clean rejections",
              "A computed key (`{ [k]: v }`) is supported only for a **constant** string/number literal key (`{ [\"a\"]: v }`, `{ [0]: v }` — resolves to that field); a runtime-valued key on a fixed object is a clean rejection ([ADR-00609](../adr/ADR-00609.md))"
            ],
            "notes": [
              "Renaming is supported (`{ x: y }`)",
              "*Assignment* to already-declared targets (`({ x, y } = expr)`, parens required at statement level, matching real JS) — see [ADR-00160](../adr/ADR-00160.md)",
              "Nested patterns (`{ inner: { v }, tag }`, `{ coords: [x, y] }`) work in a declaration, for-of loop variable, and destructured parameter — see [ADR-00193](../adr/ADR-00193.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 2",
              "String- or numeric-literal keys (`{ \"k\": v }`, `{ 0: v }`) reuse the object-literal key grammar — a non-identifier key has no shorthand, so it must bind through an explicit `: name` ([ADR-00203](../adr/ADR-00203.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 3a)",
              "A rest element (`{ a, ...rest }`) works in the declaration, for-of, and destructured-parameter forms ([ADR-00204](../adr/ADR-00204.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 3b): `rest` binds a fresh residual object of every visible source field not named earlier — a plain object (a class instance's rest strips hidden metadata, matching JS's own-enumerable copy), shallow-copied (a pointer-valued field is shared), usable like any object; requires a statically-known source shape",
              "Usable as a for-of loop variable (`for (const { x, y } of points)`) — see [ADR-00192](../adr/ADR-00192.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 1"
            ]
          }
        ]
      }
    },
    {
      "kind": "table",
      "table": {
        "heading": "Functions & closures",
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "Tagged template literals (`` tag`Hello ${x}` ``)",
            "status": "implemented",
            "caveats": [
              "A **user** tag's `strings` argument has no `.raw` property (this compiler's arrays carry no extra properties) — the built-in `String.raw` tag itself is implemented and interleaves the raw, un-escaped quasis ([ADR-00562](../adr/ADR-00562.md))"
            ],
            "notes": [
              "Desugars to a plain call (`tag([\"a\",\"b\"], x)`) — interpolated values reach the tag function as real typed values, not implicitly stringified the way a plain template literal's own interpolation is",
              "Any callable — a named function, a class method, or an arrow function/closure (including with a `...rest` parameter) — works as a tag. See [TDD-00059](../tdd/TDD-00059.md)/[ADR-00151](../adr/ADR-00151.md)/[ADR-00152](../adr/ADR-00152.md)"
            ]
          },
          {
            "feature": "Function declarations (top-level)",
            "status": "implemented",
            "caveats": [
              "Cannot reference a sibling top-level binding whose value is a connection handle (`Worker`/`WebSocket`/`EventSource`, `BroadcastChannel`/`MessageChannel`, `XMLHttpRequest` — construction opens a thread/socket), a `Promise`, or a generic class instance with **inferred** rather than explicit type arguments (`new Box(5)`, or a nested `new Box<Box<number>>()`) — such a binding stays a `main()` local outside a named function's fresh scope, failing with `undefined variable`. Scalars, strings, arrays, `TypedArray`s, objects, `Map`/`Set`, class instances (including an explicit `new Box<number>()`), the value/event handles (`Blob`, `Date`, `Error`, `URL`, `URLSearchParams`, `URLPattern`, `RegExp`, `Headers`, `ArrayBuffer`, `DataView`, `TextEncoder`/`TextDecoder`, `Request`, `AbortController`, `Event`/`CustomEvent`, `EventTarget`), and the streams (`ReadableStream`/`WritableStream`/`TransformStream`/`CompressionStream`, the Node streams) + `EventEmitter`, and `http.createServer` handles ([ADR-00426](../adr/ADR-00426.md)) are promoted to module globals and are readable ([TDD-00093](../tdd/TDD-00093.md)/[ADR-00342](../adr/ADR-00342.md)); arrow functions/closures capture everything regardless ([TDD-00057](../tdd/TDD-00057.md))",
              "A genuinely circular pair of mutually-recursive unannotated functions can't converge on a return type and keeps the scalar-default fallback ([TDD-00058](../tdd/TDD-00058.md))",
              "The `arguments` object is synthesized from the declared parameters when they all share one type ([ADR-00387](../adr/ADR-00387.md)) — `.length`, indexing, and `for…of` work; mixed-type parameters, a rest/destructured parameter, and an arrow function (which has no own `arguments` in JS) are clean rejections, and it reflects the declared parameters rather than growing with extra untyped arguments (there is no variadic call beyond an explicit `...rest`); class method bodies get the same synthesis ([ADR-00464](../adr/ADR-00464.md))"
            ],
            "notes": [
              "Named, typed params, typed return",
              "A leading `this` parameter (`function f(this: T, …)`) — TS's explicit-`this` typing form — is parsed and erased, exactly as real TS erases it at emit; the remaining parameters bind by their real positions ([ADR-00373](../adr/ADR-00373.md))"
            ]
          },
          {
            "feature": "Arrow functions / lambdas",
            "status": "implemented",
            "notes": [
              "Full closures; captures via heap-allocated env struct",
              "An arrow inside a class method captures the enclosing lexical `this` like any other variable (nested arrows included; a `function(){}` body keeps its own dynamic `this` and stays a clean rejection) ([ADR-00460](../adr/ADR-00460.md))"
            ]
          },
          {
            "feature": "Function expressions (`var f = function(x) {...}`, callback arguments, IIFE bodies)",
            "status": "implemented",
            "caveats": [
              "A generic function used by value is out of scope (no single monomorphized symbol to point at) ([ADR-00200](../adr/ADR-00200.md))"
            ],
            "notes": [
              "Anonymous and named. A named function expression (`var f = function fact(n) {...}`) binds its own name inside its own body — for self-reference/recursion — without leaking to the enclosing scope, via a synthetic self-capture whose env cell holds the closure's own header ([ADR-00178](../adr/ADR-00178.md)). This holds even when the name shadows a *top-level function* of the same name: the resolver binds the expression's name in its own scope before rewriting the body, so the self-reference stays the expression while outer calls still reach the top-level function ([ADR-00601](../adr/ADR-00601.md))",
              "`async function(...) {...}` expressions are supported",
              "An IIFE runs inline — `(function(){…})()`, `(() => …)()`, and a named recursive IIFE `(function fact(n){…})(5)` alike — via the general function-value call path",
              "Reuses arrow functions' closure machinery — same captures, same heap-allocated env struct. See [TDD-00060](../tdd/TDD-00060.md)/[ADR-00168](../adr/ADR-00168.md)"
            ]
          },
          {
            "feature": "Default parameter values",
            "status": "implemented",
            "caveats": [
              "A default expression may reference an earlier *scalar/string* parameter — `function f(a: number, b: number = a * 2)`, including chaining (`c = b + 1`) — for a free function, an instance method, a static method, and a constructor (including a parameter-property ctor `constructor(public x = 1, public y = x)`) ([ADR-00598](../adr/ADR-00598.md)/[ADR-00599](../adr/ADR-00599.md)); the earlier param's passed value (or its own default) is bound only while the default is evaluated, so an argument still sees the caller's scope. An earlier param of **any** supported kind is referenceable — scalar/string, *array* (`b = a.length`, `a[i]` — [ADR-00610](../adr/ADR-00610.md)), and *nullable-scalar* (`b = (a ?? 0) + 1` — [ADR-00611](../adr/ADR-00611.md)) — across free functions, instance/static methods, and constructors; a rest-parameter constructor is unsupported"
            ]
          },
          {
            "feature": "Optional parameters (`param?`)",
            "status": "implemented",
            "caveats": [
              "An omitted `?` argument gets the parameter type's zero value — the same `undefined` stand-in default parameter values/destructuring defaults use, not real JS's `undefined` ([ADR-00164](../adr/ADR-00164.md))"
            ]
          },
          {
            "feature": "Rest parameters (`...args: number[]`)",
            "status": "implemented",
            "notes": [
              "Works on named function declarations, class methods, arrow functions/closures, and **function type annotations** (`(...xs: number[]) => R`, `(head: number, ...tail: number[]) => R`, and the same as a `type` alias) alike. See [ADR-00151](../adr/ADR-00151.md)/[ADR-00152](../adr/ADR-00152.md)/[ADR-00385](../adr/ADR-00385.md)"
            ]
          },
          {
            "feature": "Destructured function parameters (`function f({ x, y }: T) {}` / `function f([a, b]: T[]) {}`)",
            "status": "implemented",
            "caveats": [
              "Requires an explicit type annotation on the pattern (no sensible unannotated default the way a scalar param has); no combination with `...` or a whole-parameter default value",
              "A `= default` on a nested position is a clean rejection; per-element defaults follow the same rules as the statement-level destructuring rows"
            ],
            "notes": [
              "Each individual pattern element/field may have its own default (`function f([a = 1]: number[])`, `function f({ x = \"d\" }: T)`)",
              "Nested patterns (`function f([[a, b]]: number[][])`, `function f({ p: { x } }: T)`) work — see [ADR-00193](../adr/ADR-00193.md)/[TDD-00065](../tdd/TDD-00065.md) Stage 2",
              "Works on named function declarations, class methods/constructors, and arrow functions alike — including an array-destructured param on an arrow function/closure, since array-typed closure parameters are supported ([ADR-00152](../adr/ADR-00152.md)). See [ADR-00109](../adr/ADR-00109.md)/[ADR-00158](../adr/ADR-00158.md)"
            ]
          },
          {
            "feature": "Nested function declarations (`function outer() { function inner() {...}; return inner(); }`)",
            "status": "implemented",
            "caveats": [
              "A declaration inside a lexical block (an `if`/`for`/`while`/`do-while`/bare block, a `try`/`catch`/`finally` block, or a `switch` case) is supported and block-scoped ([TDD-00152](../tdd/TDD-00152.md)/[ADR-00602](../adr/ADR-00602.md)); the residual is the specialized `for…of`/`for…in` iterator variants (generator/stream/`Symbol.iterator` paths) that iterate their body directly (no miscompile — capturing such a loop variable is a clean rejection)",
              "A block-nested declaration capturing a **C-style `for` loop variable** is a clean rejection — the loop variable is a single per-iteration cell; copy it to a `const` inside the loop body and capture that ([ADR-00602](../adr/ADR-00602.md))",
              "Captures enclosing locals/parameters (by reference, emitted as a closure value — [TDD-00129](../tdd/TDD-00129.md) Stage 1/[ADR-00386](../adr/ADR-00386.md)), but only for use **at or after** the declaration point — a capturing nested function called before its declaration is a clean error (full pre-declaration hoisting is Stage 2); a non-capturing one keeps full hoisting/forward-reference. Capturing an array variable, and a capturing nested *generator*, stay clean rejections",
              "Generic (`<T>`) nested declarations and same-scope duplicate names are clean compile errors ([TDD-00057](../tdd/TDD-00057.md)/[ADR-00149](../adr/ADR-00149.md))"
            ],
            "notes": [
              "Hoisted, forward-referenceable, self-recursive, visible to its own further-nested descendants"
            ]
          },
          {
            "feature": "`Function.prototype.call` / `.apply` / `.bind`",
            "status": "implemented",
            "caveats": [
              "`thisArg` is evaluated then **ignored** — a closure has no rebindable `this`, so method-borrowing (calling a method value against a different receiver) is unsupported; only the arguments are forwarded",
              "Only on a first-class function **value** (a closure, function parameter, arrow/function expression, or named function) — call/apply/bind on a built-in (`net.connect.apply(…)`) isn't supported, since built-ins aren't function values",
              "`.apply` takes either a literal array (`f.apply(null, [a, b])`) or a runtime array spread into a **rest** parameter (`f.apply(null, arr)` where `f` is `(...xs) => …`); a runtime array into a fixed-arity function is the usual spread-into-fixed-arity rejection",
              "`.bind` is V1-scoped to functions whose parameters are plain scalar/string/pointer types with no rest slot — an array/nullable-scalar/rest parameter is a clean compile error"
            ],
            "notes": [
              "`f.call(thisArg, a, b)` / `f.apply(thisArg, [a, b])` lower to a direct `f(a, b)`; `f.apply(thisArg, arr)` spreads into the rest slot; `f.bind(thisArg, …bound)` builds a new partially-applied closure (a trampoline capturing `f` + bound args). All type-checked against `f`'s parameters exactly as a normal call ([TDD-00137](../tdd/TDD-00137.md))"
            ]
          },
          {
            "feature": "Function overload signatures (`function f(x: number): number;` + impl)",
            "status": "implemented",
            "caveats": [
              "Signatures are parsed and **erased** — call sites type-check against the implementation's parameter list only, with no per-signature arity/type narrowing (an ill-formed group — a signature with no implementation, interrupted, or name-mismatched — is still a clean rejection)"
            ],
            "notes": [
              "Top-level `function` groups, class methods, and constructor overloads ([ADR-00446](../adr/ADR-00446.md)); only the implementation is compiled"
            ]
          },
          {
            "feature": "Generator functions (`function* f(): T { yield x; }`, `.next(value)`, `for...of`)",
            "status": "implemented",
            "caveats": [
              "A free `function*` may be top-level **or nested** (a nested `function*` captures enclosing state by reference — an enclosing `let` mutated after the generator is created is seen by a later `.next()` — reusing the closure-boxing on the instance's `__env`; an *array* capture stays a clean rejection); a generator *expression* is supported only as a top-level `const/let/var G = function* ...` binding (rewritten to a named declaration, [TDD-00096](../tdd/TDD-00096.md)/[ADR-00293](../adr/ADR-00293.md)) — an argument/nested/IIFE use is a clean rejection. Instance generator methods — sync and `async *m()` — are supported separately (rows below), but `static`/`abstract` generator methods stay clean rejections ([TDD-00094](../tdd/TDD-00094.md))",
              "The return-type annotation is optional — the element type is inferred from the body's yields (numeric join, `yield*` delegation, return fallback; only a genuinely non-joinable mix still requires the annotation — [ADR-00293](../adr/ADR-00293.md)); still requires a plain non-destructured parameter list and a non-array element/parameter type (a tuple/object element type works)",
              "Calling `.next()` again after completion returns `{value: <T's zero value>, done: true}` — this compiler's usual stand-in for real JS's `undefined` ([TDD-00061](../tdd/TDD-00061.md)/[ADR-00173](../adr/ADR-00173.md))"
            ],
            "notes": [
              "Real suspend/resume via this compiler's own `ucontext.h`-based fiber primitive (the same one `http.listen`/`await fetch(...)` already use), not a synchronous stand-in — see [ADR-00171](../adr/ADR-00171.md)/[ADR-00172](../adr/ADR-00172.md)",
              "The full iterator protocol: `.throw(e)` injects at the suspension point (a body `try/catch` handles it, else it propagates to the caller and finishes the generator), `.return(v)` completes the generator running enclosing `finally` blocks, and `yield*` delegates to an inner generator — forwarding sent values / `.throw()` / `.return()` and evaluating to the inner's return value ([ADR-00259](../adr/ADR-00259.md)/[TDD-00086](../tdd/TDD-00086.md)). A `return` inside a `try/finally` runs the `finally`",
              "Leaving a `for...of` over a generator early closes the iterator — a `break`, a `return` from the loop body, and an outer labeled `break`/`continue` all drive the suspended generator's `.return()`, so an enclosing `finally` in the generator body runs (interleaved innermost-first with any body/consumer `finally`), matching JS ([ADR-00613](../adr/ADR-00613.md)/[ADR-00614](../adr/ADR-00614.md)); a `throw` propagating out of the body is the one path that doesn't yet close it (exceptions unwind through a separate mechanism)"
            ]
          }
        ]
      }
    },
    {
      "kind": "table",
      "table": {
        "heading": "Async / Promise",
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`async` functions",
            "status": "implemented",
            "notes": [
              "**Every** async fn returns a real task-shaped `Promise<T>`: a fn containing an `await` runs on a ucontext fiber (so it can yield at every await); a no-`await` one settles inline. No `libcurl` linked unless `fetch` is used ([TDD-00084](../tdd/TDD-00084.md) Part A / [ADR-00249](../adr/ADR-00249.md)/[ADR-00268](../adr/ADR-00268.md))",
              "Nullable-scalar, destructured, **and rest** parameters in a genuinely-suspending fn are all marshalled through the coroutine task's args bundle — a rest param's trailing args pack into an array that binds like any other array param ([TDD-00084](../tdd/TDD-00084.md) Part C / [ADR-00256](../adr/ADR-00256.md))",
              "Named `async function` declarations, `async (...) => ...` arrows, and async function expressions all supported"
            ]
          },
          {
            "feature": "`await` expressions",
            "status": "implemented",
            "notes": [
              "A Promise is a **reusable value** — `await p` twice returns the same result, and a member is readable after a combinator; `await` reads the slot without freeing it, including a raw-fetch `Promise<Response>` (its pending struct is never freed and `__kml_await_fetch` short-circuits an already-done handle) ([ADR-00270](../adr/ADR-00270.md)/[TDD-00090](../tdd/TDD-00090.md))",
              "Every `await` yields a microtask tick — of an already-settled promise **and of a non-promise value** (`await 1` defers the continuation exactly as JS does; identity in value only) — and the resume is a microtask ordered in FIFO with `.then`/`queueMicrotask` (matches JS: `f(); log(\"c\")` where `f` awaits prints `a c b`) ([ADR-00268](../adr/ADR-00268.md)/[TDD-00088](../tdd/TDD-00088.md), [ADR-00281](../adr/ADR-00281.md)); inside an *async generator body* an await parks the step itself ([ADR-00283](../adr/ADR-00283.md))",
              "Registers a resume reaction on the promise and parks; on `fetch()`'s `Promise<Response>` it really waits ([ADR-00050](../adr/ADR-00050.md)); an explicitly `: Promise<T>`-annotated variable, a plain function returning `new Promise`, and an `async` fn returning a promise all `await` correctly ([ADR-00265](../adr/ADR-00265.md))"
            ]
          },
          {
            "feature": "`Promise.all` / `.race` / `.allSettled`",
            "status": "implemented",
            "notes": [
              "A member Promise is a **reusable value**: the combinator reads each slot without freeing it, so any member — a value/task promise **or** a raw-fetch `Promise<Response>` — can be awaited again or read after the combinator ([ADR-00270](../adr/ADR-00270.md)/[TDD-00090](../tdd/TDD-00090.md))",
              "`.race`/`.any` over `setTimeout`-based task promises return the shortest-delay member — `__kml_timer_fire_next` fires strictly earliest-due and the waiter list wakes the awaiter event-driven ([ADR-00266](../adr/ADR-00266.md))",
              "Real concurrency over `Array<Promise<Response>>` (fetch) **and** over `Array<Promise<T>>` from may-suspend async fns ([TDD-00083](../tdd/TDD-00083.md) Stage 2); only a genuinely-already-resolved array collapses to a synchronous collect",
              "A rejecting task member rejects `.all`/`.race` (re-thrown at the `await`) and reports `rejected` in `.allSettled` (fiber-safe exceptions, [ADR-00244](../adr/ADR-00244.md))",
              "`.race`/`.any` over task promises wait via an event-driven waiter list (a settling member wakes the awaiter), not a busy poll ([ADR-00266](../adr/ADR-00266.md))"
            ]
          },
          {
            "feature": "`Promise.any`",
            "status": "implemented",
            "notes": [
              "Over task promises it skips rejected members and resolves to the first *fulfilled* one; when every member rejects it throws a real `AggregateError` carrying each reason on `.errors` ([ADR-00248](../adr/ADR-00248.md))",
              "Over `Array<Promise<Response>>` (raw fetches) it settles to the first transport-successful response and throws an `AggregateError` only when every fetch fails at the transport level — an HTTP 404/500 is a fulfilled fetch, per WHATWG ([ADR-00251](../adr/ADR-00251.md)/[TDD-00084](../tdd/TDD-00084.md) Part C)"
            ]
          },
          {
            "feature": "`Promise.resolve` / `Promise.reject`",
            "status": "implemented",
            "notes": [
              "Both build a settled *task* promise, so `await` / `.then` / `.catch` / `.finally` and `Promise.all`/`.any`/`.race` arrays all work. `Promise.resolve(v)` fulfils with `v` (an already-promise arg flattens through, no-arg is `Promise<void>`); `Promise.reject(e)` rejects with the error (a bare primitive wrapped like a thrown value) — no fiber/libcurl linked ([ADR-00262](../adr/ADR-00262.md))",
              "`Promise.reject(e)` is typed `Promise<never>`, so a rejected value assigns to any binding — `const s: string = await Promise.reject(...)` type-checks (the value is dead, `await` re-throws) ([ADR-00264](../adr/ADR-00264.md))"
            ]
          },
          {
            "feature": "`new Promise((resolve, reject) => …)` (executor constructor)",
            "status": "implemented",
            "notes": [
              "**Thenable adoption**: `resolve(anotherPromise)` settles the outer promise with the inner's value (or reason) when the inner settles — reaction-based, so it's a microtask tick like `.then`, not synchronous ([ADR-00271](../adr/ADR-00271.md)/[TDD-00091](../tdd/TDD-00091.md)). Only a typed `Promise` argument adopts; a custom duck-typed `{ then }` object isn't adopted (this compiler has no `PromiseLike<T>` type — it's a clean type mismatch, not a silent miss)",
              "Synchronous resolve/reject, conditional settle, and **first-settle-wins** (a later resolve/reject is ignored); `.then`/`.catch`/`await` all work ([ADR-00263](../adr/ADR-00263.md)/[TDD-00087](../tdd/TDD-00087.md))",
              "The executor may be an arrow, a `function(…) {…}` expression, a closure-typed variable, or a bare top-level-function reference ([ADR-00267](../adr/ADR-00267.md))",
              "**Deferred** resolution from a `setTimeout` callback (`new Promise((res) => setTimeout(() => res(v), ms))`) — awaiting drives the microtask queue and fires due timers until it settles",
              "Works returned across a function boundary too — a plain function or an `async` fn returning `new Promise(...)` (the `delay()` helper pattern), via async-return flattening ([ADR-00265](../adr/ADR-00265.md)); `T` from `new Promise<T>`, default `number`"
            ]
          },
          {
            "feature": "`Promise.prototype.then` / `.catch` / `.finally` + `queueMicrotask`",
            "status": "implemented",
            "notes": [
              "`.then`/`.catch`/`.finally` directly on a raw fetch `Promise<Response>` never blocks the synchronous script: the transport wait is deferred to a queued microtask that settles a bridge promise at event-loop drain ([ADR-00280](../adr/ADR-00280.md), replacing [ADR-00258](../adr/ADR-00258.md)'s synchronous drive); the fetch promise stays a reusable value (`p.then(f)` + `await p` in either order share one transfer)",
              "A missing/`undefined`/`null` callback slot is a pass-through, as in JS — `p.then()` chains the source value, `.then(undefined, onR)` is the `.catch` shape ([ADR-00281](../adr/ADR-00281.md))",
              "The callback runs as a microtask when the promise settles and its result **chains** into the returned promise — `p.then(f).then(g)` passes `f`'s result to `g`, `.catch` recovers, `.finally` passes through ([ADR-00248](../adr/ADR-00248.md)); a `.catch` error param is the error object with or without annotation ([ADR-00255](../adr/ADR-00255.md))",
              "`await`ing a chain result (`await p.then(f)`) waits for the chain's microtask ([ADR-00262](../adr/ADR-00262.md)); works on any async fn's result and directly on a raw fetch `Promise<Response>` (4xx/5xx fulfils, transport failure rejects — [ADR-00258](../adr/ADR-00258.md))",
              "`queueMicrotask` is a FIFO drained after the synchronous script and before timers"
            ]
          },
          {
            "feature": "Async generators (`async function*`, `async *m()`)",
            "status": "implemented",
            "notes": [
              "**V8/spec step timing** ([ADR-00283](../adr/ADR-00283.md), node-diff verified): `.next()` starts the body *synchronously* up to the first `await`/`yield`; an `await` inside the body **parks the step** (its `Promise<{value,done}>` stays pending, the consumer's script continues, the settle re-enters the fiber as a microtask — so body side effects interleave with consumer-side microtasks exactly as node's do, and `await <plain value>` in a body defers one tick); `.next`/`.throw`/`.return` during an in-flight step **queue** (AsyncGeneratorEnqueue) and are serviced in order when it settles — `Promise.all`-style double-`.next()` and a `.return()` whose `finally` awaits both work",
              "Top-level **or nested** `async function*`, and a class `async *m()` method (a nested one captures enclosing state by reference on the instance's `__env`; an array capture is a clean rejection) ([ADR-00274](../adr/ADR-00274.md))",
              "`.next()` returns a real `Promise<{value,done}>` — settled when the body reached a `yield`/completion synchronously, genuinely *pending* while the body is parked at an `await` ([ADR-00275](../adr/ADR-00275.md)'s settle-through-`__kml_promise_settle` rule, [ADR-00283](../adr/ADR-00283.md)'s step model)",
              "`yield*` delegates to another generator **or** a user `Symbol.asyncIterator` async iterable inside an async generator (awaiting + re-yielding each element); a `.throw(e)`/`.return(v)` on the outer at an async-iterable `yield*` **delegates into the inner iterator's own `.throw`/`.return`** when it has them (the inner may catch a throw and keep yielding, or run its own cleanup), else runs the outer's own propagate/complete path ([ADR-00274](../adr/ADR-00274.md)/[ADR-00275](../adr/ADR-00275.md)/[ADR-00276](../adr/ADR-00276.md))<br> • Fuses the sync-generator fiber with the coroutine-task scheduler park ([ADR-00253](../adr/ADR-00253.md)/[ADR-00254](../adr/ADR-00254.md)): the body both `yield`s to its consumer and `await`s (a `fetch` busy-spins, a task promise drives the scheduler), a throw rejects the outstanding `.next()`, and a method binds `this` from the receiver",
              "The full protocol: `.throw(e)` / `.return(v)` ([ADR-00260](../adr/ADR-00260.md)) and `yield*` over an inner async generator — each inner step's promise awaited, forwarding sent values / `.throw()` / `.return()` ([ADR-00261](../adr/ADR-00261.md)/[TDD-00086](../tdd/TDD-00086.md)) — the same isolated-swap machinery the sync generators use, settling the returned promise (fulfilled if a body `try/catch` handles the throw or the return runs its finallys, rejected on an uncaught throw) ([ADR-00260](../adr/ADR-00260.md)/[TDD-00086](../tdd/TDD-00086.md))"
            ]
          },
          {
            "feature": "`for await...of` (async iteration)",
            "status": "implemented",
            "notes": [
              "Consumes an async-generator instance, a user class implementing the async-iteration protocol by hand — a `[Symbol.asyncIterator]()` returning an iterator whose `async next(): Promise<{value,done}>` is awaited each turn (`return this` self-iterators and separate iterator objects both work) ([ADR-00269](../adr/ADR-00269.md)/[TDD-00089](../tdd/TDD-00089.md)) — an **object literal** carrying `[Symbol.asyncIterator]` (arrow-valued or method shorthand, returning an async/sync generator or a class iterator; captures via the closure, since object-literal methods have no `this`) ([ADR-00279](../adr/ADR-00279.md)), **or any sync iterable the sync `for...of` takes**, awaiting each element: an array (a promise element awaits to its value — including a raw fetch `Promise<Response>` element, built to its `Response` — a rejected element propagates and stops the loop, a plain value awaits as identity), a `Map` (values) / `Set` (elements), a sync generator, and a `[Symbol.iterator]()` class or object literal ([ADR-00272](../adr/ADR-00272.md)/[TDD-00092](../tdd/TDD-00092.md), [ADR-00277](../adr/ADR-00277.md)/[ADR-00278](../adr/ADR-00278.md))",
              "Awaits each `.next()` promise (re-throwing a rejected step) before reading `.done`/`.value` ([ADR-00253](../adr/ADR-00253.md)/[TDD-00085](../tdd/TDD-00085.md)); the loop is a suspension point in itself — an enclosing async fn parks at the first element even when nothing else in it awaits, so the top-level script continues first, as in JS ([ADR-00281](../adr/ADR-00281.md))",
              "A destructuring loop variable (`for await (const { x, y } of …)`, `const [a, b]` over a tuple element) unpacks each element in the body, reusing the sync for-of destructuring core ([ADR-00257](../adr/ADR-00257.md))"
            ]
          }
        ]
      }
    },
    {
      "kind": "table",
      "table": {
        "heading": "Classes / OOP",
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "Enums (numeric)",
            "status": "implemented",
            "notes": [
              "Auto-increment, explicit values",
              "Bracket access (`E[\"B\"]`) and the numeric reverse mapping (`E[1]` → `\"B\"`, unmatched → `\"undefined\"`) work; a string enum's reverse lookup is a clean rejection ([ADR-00480](../adr/ADR-00480.md))"
            ]
          },
          {
            "feature": "Enums (string)",
            "status": "implemented",
            "notes": [
              "Members are interned string constants; the enum name (and its `[]` array form) used as a type annotation resolves to the enum's `ptr` backing type, so a variable/param/field/array explicitly typed with the enum, comparison, and `switch` all work ([ADR-00247](../adr/ADR-00247.md))"
            ]
          },
          {
            "feature": "Namespaces (`namespace X {}` / `module X {}`, function merging)",
            "status": "implemented",
            "caveats": [
              "Top-level declarations only (a namespace inside a function body is not supported); no `declare namespace`, no cross-module `export namespace`; the same namespace member declared in two files is a link-time duplicate-symbol error, not file-private ([TDD-00095](../tdd/TDD-00095.md)/[TDD-00148](../tdd/TDD-00148.md))",
              "Type members (class/interface/type/enum) desugar to *bare-name* top-level declarations — two namespaces declaring the same class name collide, ([ADR-00450](../adr/ADR-00450.md)); outside `X.Enum.Member` / `X.Class.static` chains resolve through the qualifier strip ([ADR-00480](../adr/ADR-00480.md))",
              "A top-level namespace `const` initializer can't reference a sibling member (it evaluates outside the namespace context); sibling references *inside member function bodies* work, with consts subject to the [ADR-00342](../adr/ADR-00342.md) promotion type limits"
            ],
            "notes": [
              "Merges with a same-name top-level `function X(...)` — TS's callable-object duality idiom (`assert(x)` + `assert.sameValue(...)`); members desugar to flat top-level declarations",
              "V2 ([TDD-00148](../tdd/TDD-00148.md)/[ADR-00450](../adr/ADR-00450.md)): the `module X {}` spelling, `export namespace/module` tolerated, non-exported members (visible to siblings, outside access cleanly rejected), class/interface/type/enum members, and bare sibling references between member functions",
              "V3 ([ADR-00453](../adr/ADR-00453.md)): nested namespaces and dotted declarations (`module A.B {}`), flattened to dotted names; outside `A.B.member` chains and TS-order relative references (`B.f()` inside `A` finds `A.B`) resolve",
              "Import-equals aliases (`import X = Y.Z`, `export import X = N` in a namespace body) resolve as namespace aliases; an alias to a *value* member and `import x = require('…')` stay clean rejections ([ADR-00456](../adr/ADR-00456.md))",
              "`export declare …` ambient members are parsed and erased ([ADR-00462](../adr/ADR-00462.md))",
              "Executable body statements run at initialization (flattened in order); top-level `export declare …` is ambient-erased ([ADR-00468](../adr/ADR-00468.md))",
              "Qualified type references (`ns.Type` in annotations) resolve to the final segment's bare name, like qualified `new`/`extends` ([ADR-00470](../adr/ADR-00470.md))"
            ]
          },
          {
            "feature": "Interfaces (structural)",
            "status": "implemented",
            "notes": [
              "Heap-allocated objects"
            ]
          },
          {
            "feature": "Type aliases",
            "status": "implemented"
          },
          {
            "feature": "Object literals `{ key: value }`",
            "status": "implemented",
            "caveats": [
              "No duplicate-`__proto__`-key detection (Annex B legacy `SyntaxError` rule)"
            ],
            "notes": [
              "A key may be `IDENT`, or a string/numeric literal (`{\"foo\": 1}`, `{0: \"a\"}`) — in an object literal *and* in an `interface`/object-type annotation (`interface P { \"first-name\": string; 0: T }`) ([ADR-00154](../adr/ADR-00154.md)/[ADR-00608](../adr/ADR-00608.md))",
              "Constant-key bracket access reads and writes the matching field on a fixed object or class instance — `o[\"a\"]`, `o[0]`, `o[\"a\"] = v`, `o[\"a\"] += v` — the same field `o.a` reaches; a non-constant key on a fixed object stays a clean rejection (use a `{ [k: string]: T }` dynamic object for runtime keys) ([ADR-00608](../adr/ADR-00608.md))"
            ]
          },
          {
            "feature": "Getters / setters (`get x() {}` / `set x(v) {}`) on classes and object literals",
            "status": "implemented",
            "caveats": [
              "An accessor-bearing **object literal** can't be assigned to a *differently-shaped* structural object type (`const p: { x: number } = objWithGetter`) — its accessors are methods, not fields; a clean rejection, used directly it works ([ADR-00603](../adr/ADR-00603.md))",
              "`console.log` of an accessor object prints only its data fields, not `x: [Getter]` (an inspect-fidelity gap)",
              "Static accessors, and logical assignment (`&&=`/`\\|\\|=`/`??=`) on an accessor, remain out of scope for V1 ([TDD-00030](../tdd/TDD-00030.md)/[ADR-00110](../adr/ADR-00110.md))"
            ],
            "notes": [
              "`get`/`set` are contextual (not reserved), parsed the same way `in` (ADR-00091) already is — a field/method/variable literally named `get`/`set` keeps working everywhere outside this one position",
              "No new storage: an accessor is registered as an ordinary method under an internal dispatch key, reusing inheritance/override/vtable dispatch/visibility checking unchanged",
              "`obj.x`/`obj.x = v`/compound assignment (`+=` etc.) all route through the accessor, including from inside the class's own other methods (`this.x`) and through optional chaining (`obj?.x`)",
              "An **object literal** with a getter/setter is lowered to a synthetic anonymous class, so the accessor body gets a real `this` reading/writing the object's own fields (a data initializer may reference an enclosing local; an unannotated getter's return type is inferred from the field it reads) ([TDD-00153](../tdd/TDD-00153.md)/[ADR-00603](../adr/ADR-00603.md))"
            ]
          },
          {
            "feature": "`new Error(msg)`",
            "status": "implemented"
          },
          {
            "feature": "Built-in `Error` subtypes (`new TypeError(msg)`, `RangeError`, `SyntaxError`, `EvalError`, `URIError`, `ReferenceError`, `DOMException`) and `instanceof` against them",
            "status": "implemented",
            "caveats": [
              "`class X extends Error` works one level deep ([ADR-00630](../adr/ADR-00630.md)): construction, `super(msg)`, `.message`/`.name` (default `\"Error\"` unless assigned), extra fields/methods, throw/catch with `instanceof` discrimination against sibling subclasses, `Error.prototype.toString`; extending an Error *subclass* is a clean rejection, and `.stack`/`Error.captureStackTrace` don't exist",
              "`DOMException` carries no legacy numeric `.code`"
            ],
            "notes": [
              "[TDD-00013](../tdd/TDD-00013.md) Option A — a small fixed kind enum (a hidden runtime tag on the same shared `Error` object shape, not a real class hierarchy), plus a real `.name` field",
              "`instanceof Error` matches any kind; `instanceof TypeError` etc. matches only that one. See [ADR-00082](../adr/ADR-00082.md)",
              "`new DOMException(message?, name?)` takes its `.name` from the 2nd argument (default `\"Error\"`), unlike the fixed-name kinds; per the modern WebIDL spec it is `instanceof Error` too, and fetch cancellation throws one (`AbortError`/`TimeoutError`) — [ADR-00240](../adr/ADR-00240.md)"
            ]
          },
          {
            "feature": "`new Array<T>(n?)`",
            "status": "implemented",
            "notes": [
              "Zero-arg `new Array<T>()` is an empty array ([ADR-00463](../adr/ADR-00463.md))"
            ]
          },
          {
            "feature": "`new Map<K,V>()`",
            "status": "implemented"
          },
          {
            "feature": "`new Set<T>()`",
            "status": "implemented"
          },
          {
            "feature": "`class` (fields, constructor, methods, `this`, `new ClassName(args)`)",
            "status": "implemented",
            "caveats": [
              "Instance-field initializers (`x = expr`) work, and a class with fields needs **no** explicit constructor — a bare declared field (`x: number`) reads as its calloc'd deterministic-zero value (0/false/null, the [ADR-00157](../adr/ADR-00157.md) convention), any initializers that exist run in a synthesized constructor ([ADR-00374](../adr/ADR-00374.md)); the one remaining rejection is a **derived** class adding fields when its base has a rest-parameter constructor (write an explicit `super(...)`). static field initializers (`static x = 5`) work — lowered to assignments run in declaration order in the class's static-init, ahead of any `static {}` block, with an unannotated one typed by inference ([ADR-00375](../adr/ADR-00375.md)). An instance-field initializer lowered into the constructor can currently see the constructor's own parameters (which real JS's separate initializer scope forbids), and an unannotated initializer of an expression shape the compiler doesn't recognize falls back to `i64` ([TDD-00063](../tdd/TDD-00063.md)/[ADR-00180](../adr/ADR-00180.md))",
              "A computed/dynamic member name (identifier, call, or interpolation) is a clean rejection; the recognized well-known-symbol keys are `[Symbol.asyncIterator]` and `[Symbol.iterator]`, desugared to the async-/sync-iteration methods ([TDD-00089](../tdd/TDD-00089.md), [ADR-00278](../adr/ADR-00278.md))",
              "A class expression used as a runtime value (argument, return, nested binding) or a named self-reference (`class D {...}` whose body references `D`) is a clean rejection; `extends <expression>` is out of scope",
              "Class early-error gaps (deferred, no valid program miscompiles): a field initializer containing `arguments` or a `super()` call, a `super()` call in a method parameter default, field-definition ASI on the same line (`field = 1 method(){}`), and the `#constructor` private-name ban all compile instead of raising the spec's `SyntaxError`",
              "A bare field (`x;`) defaults to `number` (the unannotated-parameter convention; JSDoc `@type` overrides) — TS infers implicit `any`, so a non-numeric use is a shifted typed error ([ADR-00474](../adr/ADR-00474.md))"
            ],
            "notes": [
              "[TDD-00009](../tdd/TDD-00009.md) Stage 1 — instances reuse the same heap-object/GEP machinery interfaces already use; methods compile to plain static calls (`this` as an implicit first arg), no closure indirection. See [ADR-00063](../adr/ADR-00063.md)",
              "The **`readonly` field modifier** is enforced as a compile-time immutability check: a write is allowed only in the declaring class's constructor (and field initializers), and rejected in a method, from outside (`obj.x = v`), in a nested closure, or in a subclass constructor writing an inherited readonly field — TS's rule ([TDD-00154](../tdd/TDD-00154.md)/[ADR-00604](../adr/ADR-00604.md)). Deep immutability (`readonly xs: number[]` then `xs.push(...)`) stays unenforced, matching TS's own shallow check",
              "Qualified constructors and bases parse: `new stream.Readable(...)` and `class X extends events.EventEmitter` dispatch on the final name exactly like the bare form (the qualifier is not validated against the module — [ADR-00408](../adr/ADR-00408.md))"
            ]
          },
          {
            "feature": "Constructor parameter properties (`constructor(public x: number, readonly y: T)`)",
            "status": "implemented",
            "notes": [
              "Desugars at parse time to a declared field + `this.x = x` constructor prologue ([ADR-00447](../adr/ADR-00447.md)); `private`/`protected` get the same enforcement as declared fields; outside a constructor the modifiers are a clean parse error",
              "`readonly` is enforced as a compile-time immutability check, the same as a `readonly` declared field — a write after construction is rejected ([TDD-00154](../tdd/TDD-00154.md)/[ADR-00604](../adr/ADR-00604.md))"
            ]
          },
          {
            "feature": "`class` `static` members/`static {}` blocks, `private`/`protected` visibility, `abstract` classes/methods, `implements`",
            "status": "implemented",
            "notes": [
              "[TDD-00009](../tdd/TDD-00009.md) Stage 4 — `static` members are inherited through `extends` like real JS/TS (a non-redeclared static field shares its base's storage, not a per-subclass copy)",
              "`private`/`protected` are compile-time-only (zero runtime check, matching real TypeScript's own erasure) and correctly reject `super.privateMethod()`",
              "`abstract` reuses Stage 3's override-detection machinery almost entirely for free; `implements` is a compile-time-only structural self-check against a new interface-method-signature grammar, not a mechanism for polymorphic dispatch through an interface-typed reference. See [ADR-00084](../adr/ADR-00084.md)"
            ]
          },
          {
            "feature": "Real JS/TS `#x` runtime-private field syntax",
            "status": "implemented",
            "caveats": [
              "No class-field-initializer syntax (`#x = 1;` — a pre-existing gap for every field, not `#`-specific)",
              "No early-error check for a `#m`/`static #m` name collision in the same class, or for the `#constructor`-is-always-banned rule ([ADR-00155](../adr/ADR-00155.md))"
            ],
            "notes": [
              "Fields, methods, static fields/methods, and get/set accessors — exact-class-only access (same rule `private` already enforces), plus real reflection-hiding (`Object.keys`/`.values`/`.entries`/`JSON.stringify`/spread never see a `#`-named field, unlike `private`'s type-level-only erasure). See [TDD-00021](../tdd/TDD-00021.md)/[ADR-00155](../adr/ADR-00155.md)"
            ]
          },
          {
            "feature": "`class` inheritance (`extends`/`super`)",
            "status": "implemented",
            "notes": [
              "[TDD-00009](../tdd/TDD-00009.md) Stage 3 — single inheritance, base-first field layout, `super(args)`/`super.method(args)`, an implicit pass-through constructor when a derived class adds no fields of its own",
              "Dispatch is static (a plain direct call, identical to Stage 1/2) except for a method provably overridden somewhere in the whole program, which goes through a per-tree vtable instead — decided once at compile time via whole-program override analysis, not per call site. See [ADR-00083](../adr/ADR-00083.md)",
              "`extends` also accepts one built-in generic, `EventEmitter<T>` ([TDD-00023](../tdd/TDD-00023.md)/[ADR-00089](../adr/ADR-00089.md)) — a synthetic root never itself registered as a real class"
            ]
          },
          {
            "feature": "`instanceof` (against user-defined classes)",
            "status": "implemented",
            "caveats": [
              "`x instanceof Object` on a value of an *undecidable* static type (`any`, a union, or a nullable `T | null`) is a clean rejection — its runtime primitive-vs-object identity isn't statically known ([ADR-00605](../adr/ADR-00605.md))"
            ],
            "notes": [
              "[TDD-00009](../tdd/TDD-00009.md) Stages 2-3 — every instance carries a hidden runtime type tag. A class-typed variable whose static class is `T` itself or an ancestor of `T` folds to a compile-time constant; one whose static class is an *ancestor* of the right-hand class (e.g. `const s: Shape = new Circle(...); s instanceof Circle`) needs a real runtime tag read. See [ADR-00067](../adr/ADR-00067.md)/[ADR-00083](../adr/ADR-00083.md)",
              "`instanceof` against five built-in types (`Array`, `Map`, `Set`, `Date`, `RegExp`) is a compile-time constant off the already-known static type ([ADR-00162](../adr/ADR-00162.md)); `instanceof` against `Error`/`TypeError`/etc. is a separate, sibling mechanism keyed off a different hidden tag (see the `Error` subtypes row above and [TDD-00013](../tdd/TDD-00013.md))",
              "`x instanceof Object` is a compile-time constant computed by *negating* the closed set of primitive types (number/string/boolean/bigint/symbol/null/undefined) — true for every non-primitive (class instance, object, array, function, `Date`, `Map`/`Set`, `RegExp`, …), false for a primitive ([ADR-00605](../adr/ADR-00605.md))"
            ]
          },
          {
            "feature": "Experimental decorators — class, property, parameter & method `@decorator`",
            "status": "implemented",
            "caveats": [
              "A class decorator that *returns a replacement constructor* is refused at runtime (a loud throw, not a silent drop) — the static-class model has no constructor-replacement routing yet ([TDD-00161](../tdd/TDD-00161.md) Stage 4b); observe-only class decorators (the registration pattern) run faithfully. A decorator on a generic class is a clean compile-time rejection",
              "Accessor (get/set), static, and generator method decorators, and method decorators on a generic class, are a clean rejection; a method whose parameter/return types can't be marshalled through the decorator ABI (array/nullable/rest parameters) is also rejected",
              "Factory-call decorators (`@dec(...)`) work across placements (the factory runs, its returned function decorates) when the returned decorator is a named function or a typed closure; a returned **`any`-typed closure that captures** the factory's arguments hits the separate dynamic-prototype-method capture limitation — annotate the factory's return type to route it through the typed-closure path",
              "The standard (TC39) `(value, context)` dialect (`-decorators=standard`, default is experimental) covers class (observe), method, and instance-field decorators (the field initializer + `context.addInitializer` callbacks run per-instance in the constructor), with `context = { kind, name, static, private, addInitializer, metadata }`; getter/setter and `accessor x` auto-field decorators run (a decorated `accessor` desugars to a backing field + generated get/set, and the decorator follows the TC39 `{ get, set, init }` protocol — a returned get/set replaces the accessor, a returned init transforms the backing field's initial value). Static-field decorators are still a clean rejection; TC39 has no parameter decorators. A returned *replacement* that reads typed `this.field` hits the general replacement-`this` limitation below",
              "A decorator *replacement* function (method, getter, or setter) runs as a dynamic function whose `this` is a boxed receiver, so it cannot do typed `this.field` access on a statically-typed instance — an observe-only decorator (returns the original) is unaffected, and a replacement that doesn't touch typed `this` fields works",
              "A dynamic function (any closure boxed into an `any`/decorator position) now binds its **declared parameter types** (so a typed body like a `(initial: number)` field initializer works) and **captures enclosing locals** (by shared heap cell, into the tag-12 record's env — so factory decorators, wrapping method decorators, and capturing `addInitializer` callbacks all work) ([ADR-00647](../adr/ADR-00647.md))",
              "`emitDecoratorMetadata` design types are name-carrying descriptor objects (`{ name: \"Number\" }`, the class name for a class type) — correct for `.name` inspection, but not the real runtime constructors (no first-class class-constructor value), so identity checks fail; `Reflect.metadata` used manually as a decorator factory is rejected; a metadata target must be a real dynamic object (a boxed static class instance is rejected at runtime)"
            ],
            "notes": [
              "[TDD-00161](../tdd/TDD-00161.md) Stages 1-5 / [ADR-00641](../adr/ADR-00641.md)–[ADR-00645](../adr/ADR-00645.md) — the **experimental** dialect (default) runs property (`(target, key)`) and parameter (`(target, key, index)`; `key` is `undefined` for a constructor parameter) decorators at the class's source position, method decorators `(target, key, descriptor)` with a live property descriptor whose `value` is the method as a callable, and class decorators `(constructor)` last (after member decorators). A method decorator that mutates `descriptor.value` or returns a new descriptor re-routes that method's calls through a per-method runtime slot; an observe-only one leaves the original static dispatch. Multiple decorators on one element apply bottom-up; return values are threaded per the experimental `__decorate` semantics. The **standard** (TC39) dialect (`-decorators=standard`) runs class, method, instance-field, getter/setter, and `accessor` auto-field decorators `(value, context)` — every member placement; method/accessor replacement via a per-member slot, field initializers + `context.addInitializer` callbacks run per-instance in a constructor tail; dynamic functions bind declared parameter types and capture enclosing locals ([ADR-00647](../adr/ADR-00647.md)–[ADR-00649](../adr/ADR-00649.md))",
              "With `-emit-decorator-metadata`, a decorated member gets `design:type`/`design:paramtypes`/`design:returntype` metadata (defined before user decorators run) readable through `Reflect.getMetadata`/`getOwnMetadata`/`hasMetadata`; `Reflect.defineMetadata` writes user metadata directly"
            ]
          }
        ]
      }
    },
    {
      "kind": "block",
      "text": "## Known limitations"
    },
    {
      "kind": "block",
      "text": "- **Strict-mode early errors**: there's no general strict-mode parser context, so several Test262 early-error cases around a literal `\"use strict\"` directive stay unrejected — notably `eval`/`arguments` as an *assignment* target in strict code, and bindings inside a getter/setter accessor body. A narrower safe subset is caught: duplicate parameter names, a non-simple parameter list, and `eval`/`arguments` as a parameter or `let`/`const`/`var` (or for-of/for-in loop-variable) binding name under `\"use strict\"` (or an always-strict class method). Block-scope `let`/`const`-vs-`var` redeclaration *is* tracked — a strict-independent early error, including the cross-nested-block `let x; { var x }` intersection (see [TDD-00070](../tdd/TDD-00070.md)/[ADR-00210](../adr/ADR-00210.md)/[ADR-00211](../adr/ADR-00211.md))."
    }
  ]
}
