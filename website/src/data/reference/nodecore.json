{
  "surface": "nodecore",
  "title": "Other Node core modules",
  "kind": "node",
  "statusPage": "NODE-CORE-MODULES.md",
  "coversRows": "all",
  "coverage": {
    "loose": "16/17",
    "loosePct": 94,
    "strict": "2/17",
    "strictPct": 12
  },
  "lede": "The smaller Node built-ins, grouped here rather than each given a full page. net, tls, http and https are Node's own layering ported to TypeScript — a net.Socket is a Duplex over a native TCP handle, a TLSSocket a Socket whose handle carries a non-blocking OpenSSL session, and http/https build on them — all on one event loop with dgram and dns. Messages across process and channel boundaries (cluster, diagnostics_channel) are strings, and vm stays out of scope behind the same blocker as bare eval().",
  "entries": [
    {
      "id": "nodecore.querystring",
      "name": "querystring (.parse, .stringify, .escape, .unescape)",
      "statusRow": "querystring (.parse, .stringify)",
      "signature": "querystring.parse(str: string, sep?: string, eq?: string, options?: ParseOptions): ParsedUrlQuery; querystring.stringify(obj?: ParsedUrlQueryInput, sep?: string, eq?: string, options?: StringifyOptions): string",
      "badge": "exact",
      "description": "Node's legacy \"a=b&c=d\" parse/stringify, ported from Node's own lib/querystring.js. parse returns a null-prototype object; a repeated key collects its values into an array; a leading '?' is plain text (unlike new URLSearchParams(str)). stringify takes an object whose array values repeat the key. Custom separators, maxKeys and custom encode/decode functions are honoured; escape, unescape, unescapeBuffer, encode and decode are exported too.",
      "params": [
        {
          "name": "str / obj",
          "type": "string | ParsedUrlQueryInput",
          "desc": "A raw query tail to parse, or an object to serialize."
        },
        {
          "name": "sep, eq",
          "type": "string",
          "desc": "The pair and key/value separators ('&' and '=' by default)."
        }
      ],
      "returns": {
        "type": "ParsedUrlQuery | string",
        "desc": "The parsed object, or the serialized query string."
      },
      "exampleKey": "querystring/querystring",
      "snippet": "const parsed = querystring.parse(\"topic=compilers%20and%20types&tag=a&tag=b\")\nconsole.log(parsed.topic)  // compilers and types\nconsole.log(parsed.tag)    // [ 'a', 'b' ]",
      "seeAlso": [
        "nodecore.httpclient"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/querystring.html"
      }
    },
    {
      "id": "nodecore.assert",
      "name": "assert (.ok, bare assert(cond, msg?), .equal/.strictEqual, .notEqual/.notStrictEqual, .deepEqual/.deepStrictEqual (+not*), .fail, .throws, .match/.doesNotMatch)",
      "statusRow": "assert (.ok, bare assert(cond, msg?), .equal/.strictEqual, .notEqual/.notStrictEqual, .deepEqual/.deepStrictEqual (+not*), .fail, .throws, .match/.doesNotMatch)",
      "signature": "assert(cond: boolean, msg?: string): void; assert.strictEqual(a, b); assert.deepStrictEqual(a, b); assert.throws(fn)",
      "badge": "caveats",
      "description": "Node's real assertion library — distinct from console.assert, which logs and continues. A failure throws a catchable AssertionError (kind-tagged as a base Error, since this compiler's fixed error-kind enum has no dedicated slot), the shape scripts and tests actually use.",
      "returns": {
        "type": "void",
        "desc": "Nothing, or throws AssertionError."
      },
      "exampleKey": "assert/assert",
      "snippet": "assert.strictEqual(\"a\" + \"b\", \"ab\")\nassert.throws(() => { throw new Error(\"boom\") })",
      "differences": [
        "`equal`/`strictEqual` and `deepEqual`/`deepStrictEqual` (and their `not*` counterparts) are aliases of each other — this compiler's `==` has no implicit coercion to distinguish them",
        "`deepStrictEqual` covers scalars, strings, arrays, and objects (recursively, incl. nested); Map/Set/class instances, nullable-scalar members, and TypedArrays are a clean compile error (V1)",
        "`.match`/`.doesNotMatch`'s regexp argument must be statically a RegExp",
        "`.throws` only checks that *something* was thrown, not the thrown value's type/message"
      ],
      "seeAlso": [
        "nodecore.test"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/assert.html"
      }
    },
    {
      "id": "nodecore.test",
      "name": "test / node:test (the **runner**: test/it, describe/suite, TestContext t.test/t.after/t.skip/t.todo/t.diagnostic, hooks before/after/beforeEach/afterEach — plus the native helpers mustCall/mustCallAtLeast/mustNotCall/skip/expectsError + env probes)",
      "statusRow": "test / node:test (the **runner**: test/it, describe/suite, TestContext t.test/t.after/t.skip/t.todo/t.diagnostic, hooks before/after/beforeEach/afterEach — plus the native helpers mustCall/mustCallAtLeast/mustNotCall/skip/expectsError + env probes)",
      "signature": "import { test, describe, mustCall } from 'test'; test(name, fn); mustCall(fn, n=1)",
      "badge": "caveats",
      "description": "The node:test runner plus the native helpers. mustCall is a first-class builtin (not Node's internal ../common): it wraps fn so its invocation count is verified at process exit via the existing exit sink — an unmet or exceeded count prints a diagnostic and exits non-zero, giving examples and tests real async assertions instead of console.log.",
      "returns": {
        "type": "void | (...args) => void",
        "desc": "Registration, or a counting-trampoline wrapper."
      },
      "differences": [
        "Runner: **run-at-registration** (top-level code after a `test()` runs after it here, before it in Node); `beforeEach`/`afterEach` are single slots (last wins) and also fire for subtests; no `mock.*`, `t.plan`, `run()`, concurrency, or snapshots",
        "`mustCall` supports callbacks of **0–3 simple (scalar/pointer) parameters** — a rest param, >3 args, or an array/nullable-scalar param is a clean compile error (the counting trampoline is signature-parametric)",
        "`mustNotCall()` returns a `() => void`; using it where a non-zero-arity callback is expected is a type mismatch",
        "`mustSucceed` is count-only (the \"err is falsy\" check is deferred); `expectWarning` runs the thunk but doesn't capture warnings"
      ],
      "seeAlso": [
        "nodecore.assert"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/test.html"
      }
    },
    {
      "id": "nodecore.util",
      "name": "util (.inspect, .format)",
      "statusRow": "util (.inspect, .format)",
      "signature": "util.format(fmt: string, ...args): string; util.inspect(value): string",
      "badge": "caveats",
      "description": ".inspect reuses the same value formatter as console.log (strings single-quoted, ClassName { … }, arrays, 10n). .format handles %s/%d/%i/%f/%j/%o/%O/%%, appends excess args space-separated, and leaves unrecognized specifiers literal — matching Node.",
      "params": [
        {
          "name": "fmt",
          "type": "string",
          "desc": "A string-literal format for .format."
        },
        {
          "name": "value",
          "type": "any",
          "desc": "The value to render for .inspect."
        }
      ],
      "returns": {
        "type": "string",
        "desc": "The formatted/inspected string."
      },
      "exampleKey": "util/format_inspect",
      "snippet": "console.log(util.format(\"%s = %d\", \"count\", 42))\nconsole.log(util.inspect({ name: \"kml\", tags: [\"ts\", \"native\"] }))",
      "differences": [
        "No `.promisify` — the callback builtins (`zlib.gzip`, `child_process.exec`) are name-dispatched calls, not first-class function values to wrap; importing `promisify` fails cleanly",
        "`.format` requires a string-**literal** format (compile-time walk); a runtime format string is a compile error",
        "`.inspect`'s options object is accepted but ignored (no `depth`/`colors`)"
      ],
      "seeAlso": [],
      "spec": {
        "mdn": "https://nodejs.org/api/util.html"
      }
    },
    {
      "id": "nodecore.net",
      "name": "net (TCP server: net.createServer, server.listen/address()/on('connection'|'listening')/close; TCP client: net.connect/createConnection (positional or { port, host }); connection socket .on('data'|'end')/.write/.end/.address()/.destroy()/.setNoDelay()/.setKeepAlive(); net.isIP/isIPv4/isIPv6)",
      "statusRow": "net (TCP server: net.createServer, server.listen/address()/on('connection'|'listening')/close; TCP client: net.connect/createConnection (positional or { port, host }); connection socket .on('data'|'end')/.write/.end/.address()/.destroy()/.setNoDelay()/.setKeepAlive(); net.isIP/isIPv4/isIPv6)",
      "signature": "net.createServer(cb).listen(port, cb?); net.connect(port, host?, cb?)",
      "badge": "caveats",
      "description": "Node's lib/net.js ported to TypeScript over native TCP handles on the event loop. A Socket is a Duplex — back-pressure and 'drain', pause/resume, setEncoding, setTimeout, ref/unref; Server listens by port/host/backlog/options or a Unix-socket path, dual-stack :: by default. Errors are built as Node builds them (connect ECONNREFUSED, getaddrinfo ENOTFOUND, with .code/.errno/.syscall/.address/.port).",
      "returns": {
        "type": "net.Server | net.Socket",
        "desc": "A server or connection handle."
      },
      "exampleKey": "net/echo_server",
      "snippet": "const server = net.createServer((socket) => {\n  socket.on('data', (chunk: Uint8Array) => { socket.write(chunk); });\n});\nserver.listen(9099);",
      "differences": [
        "`connect`/`createConnection` take a port, an options object or `{ path }`; the connect is non-blocking and a failure arrives as an async `'error'` then `'close'`",
        "`listen`'s `'listening'` and `'error'` fire on the next tick, as in Node; `maxConnections` and `getConnections` are supported; `isIP`/`isIPv4`/`isIPv6`",
        "BlockList, SocketAddress and happy-eyeballs address selection (`autoSelectFamily`) are not ported",
        "Windows keeps the earlier code-generated `net` (its TCP handles are POSIX sockets)"
      ],
      "seeAlso": [
        "nodecore.tls",
        "nodecore.dgram"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/net.html"
      }
    },
    {
      "id": "nodecore.dgram",
      "name": "dgram (UDP sockets: createSocket('udp4'), bind, address(), setBroadcast(), on('message'), send, close)",
      "statusRow": "dgram (UDP sockets: createSocket('udp4'), bind, address(), setBroadcast(), on('message'), send, close)",
      "signature": "dgram.createSocket('udp4'); socket.bind(port); socket.setBroadcast(flag); socket.send(msg, port, host)",
      "badge": "caveats",
      "description": "UDP sockets. Each socket's fd folds into the same select() event loop as net/child_process. 'message' fires with a Buffer and an rinfo { address, port }; send(msg, port, host) resolves host (name or IP) via the dns helper. .address() reports the real bound { address, family, port } via getsockname; .setBroadcast(flag) is a real setsockopt(SO_BROADCAST). msg/send payloads take a string/Buffer/ArrayBuffer/DataView.",
      "returns": {
        "type": "dgram.Socket",
        "desc": "A UDP socket handle."
      },
      "exampleKey": "dgram/echo",
      "snippet": "const server = dgram.createSocket('udp4');\nserver.on('message', (msg: Uint8Array, rinfo) => {\n  server.send('reply:' + dec.decode(msg), rinfo.port, rinfo.address);\n});\nserver.bind(9199);",
      "differences": [
        "`udp4` only — no `udp6`",
        "No `'listening'`/`'error'` events, options object, or multicast",
        "Message listeners must be arrow/function-expression literals",
        "`socket.send(msg, port, host)` positional form only — no `(msg, offset, length, port, address, cb)` form; no `.setTTL()`/membership methods"
      ],
      "seeAlso": [
        "nodecore.net",
        "nodecore.dns"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/dgram.html"
      }
    },
    {
      "id": "nodecore.tls",
      "name": "tls (TLS-wrapped sockets)",
      "statusRow": "tls (TLS-wrapped sockets)",
      "signature": "tls.connect(port, host[, {rejectUnauthorized}][, onSecureConnect]); tls.createServer({ cert, key }, connectionListener)",
      "badge": "caveats",
      "description": "Node's _tls_wrap shape in TypeScript: a TLSSocket is a net.Socket whose handle carries a non-blocking OpenSSL session, so the handshake runs on the event loop. tls.connect supports servername, rejectUnauthorized, ca/cert/key and ALPN, firing 'secureConnect' with authorized/authorizationError and Node's verify error codes and messages; tls.createServer emits 'secureConnection' and 'tlsClientError'; createSecureContext, getProtocol and getCipher.",
      "returns": {
        "type": "tls.TLSSocket | tls.Server",
        "desc": "A net-shaped TLS handle."
      },
      "exampleKey": "tls/tls_client",
      "snippet": "const sock = tls.connect(443, \"example.com\", () => {\n  sock.write(\"GET / HTTP/1.1\\r\\nHost: example.com\\r\\nConnection: close\\r\\n\\r\\n\");\n});\nsock.on(\"data\", (chunk: string) => { response += chunk; });",
      "differences": [
        "OpenSSL-only (libssl) — a `-crypto=commoncrypto` build using `tls` is a clean compile error (Mac's CommonCrypto has no TLS API)",
        "Session resumption, OCSP, renegotiation, PSK and `getPeerCertificate()`'s certificate object are not ported",
        "Windows keeps the earlier code-generated, blocking `tls`"
      ],
      "seeAlso": [
        "nodecore.net",
        "nodecore.https"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/tls.html"
      }
    },
    {
      "id": "nodecore.dns",
      "name": "dns (dns.lookup, dns.resolve4/resolve, dns.promises.lookup)",
      "statusRow": "dns (dns.lookup, dns.resolve4/resolve, dns.promises.lookup)",
      "signature": "dns.lookup(host, cb); dns.resolve4(host, cb); await dns.promises.lookup(host)",
      "badge": "caveats",
      "description": "getaddrinfo(3)-backed name resolution. lookup returns one dotted-quad, resolve4 all A records as a string[], promises.lookup a Promise<{address, family}>.",
      "params": [
        {
          "name": "host",
          "type": "string",
          "desc": "The hostname to resolve."
        }
      ],
      "returns": {
        "type": "void | Promise<{address, family}>",
        "desc": "Callback delivery, or a Promise."
      },
      "exampleKey": "dns/lookup",
      "snippet": "dns.lookup(\"localhost\", (err, address, family) => {\n  console.log(\"localhost ->\", address, \"family\", family);\n});",
      "differences": [
        "`dns.resolve4`/`resolve` use `getaddrinfo` (the OS resolver), not real DNS `A` queries — no `resolve6`/`resolveMx`/`resolveTxt`/`reverse`",
        "IPv4 (`AF_INET`) only; `family` is always 4",
        "The callback forms fire synchronously, not on a later event-loop tick",
        "Failure passes a generic `Error` (`getaddrinfo ENOTFOUND`), not a `code`-tagged Node error"
      ],
      "seeAlso": [
        "nodecore.net"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/dns.html"
      }
    },
    {
      "id": "nodecore.zlib",
      "name": "zlib (gzip/gunzip, deflate/inflate, deflateRaw/inflateRaw, unzip, each *Sync + (err, result) callback)",
      "statusRow": "zlib (gzip/gunzip, deflate/inflate, deflateRaw/inflateRaw, unzip, each *Sync + (err, result) callback)",
      "signature": "zlib.gzipSync(input): Buffer; zlib.gunzipSync(buf): Buffer; zlib.gzip(input, (err, out) => …)",
      "badge": "caveats",
      "description": "One-shot compression over the same libz backend as CompressionStream, driven by a shared @__kml_zlib_oneshot helper. Input may be a Buffer/TypedArray, ArrayBuffer, DataView, or string (UTF-8); output is always a Buffer.",
      "params": [
        {
          "name": "input",
          "type": "string | Buffer | ArrayBuffer | DataView",
          "desc": "The data to compress or decompress."
        }
      ],
      "returns": {
        "type": "Buffer",
        "desc": "The compressed/decompressed bytes."
      },
      "exampleKey": "zlib/zlib",
      "snippet": "const gz = zlib.gzipSync(text)\nconsole.log(\"gunzip:\", dec.decode(zlib.gunzipSync(gz)) === text)\nconst best = zlib.deflateSync(text, { level: 9 })",
      "differences": [
        "The callback form fires synchronously, not deferred to the next event-loop tick",
        "No Brotli (`brotliCompress*`) — it would pull in `-lbrotli`, a new system dependency this binding deliberately avoids",
        "No class/stream forms (`createGzip()`, `zlib.Gzip`, …) — the WHATWG `CompressionStream`/`DecompressionStream` cover the streaming case",
        "`{ level }` is the only supported option and must be a compile-time constant"
      ],
      "seeAlso": [
        "nodecore.streamweb"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/zlib.html"
      }
    },
    {
      "id": "nodecore.vm",
      "name": "vm (vm.Script, sandboxed eval-like execution)",
      "statusRow": "vm (vm.Script, sandboxed eval-like execution)",
      "signature": "// not supported",
      "badge": "missing",
      "description": "Not implemented. vm hits the same fundamental blocker as bare eval() — there is no embedded engine to run source assembled at runtime. An opt-in embedded-engine path is scoped but not started.",
      "seeAlso": [],
      "spec": {
        "mdn": "https://nodejs.org/api/vm.html"
      }
    },
    {
      "id": "nodecore.diagnostics_channel",
      "name": "diagnostics_channel (channel/subscribe/unsubscribe/hasSubscribers, Channel .publish/.hasSubscribers/.name)",
      "statusRow": "diagnostics_channel (channel/subscribe/unsubscribe/hasSubscribers, Channel .publish/.hasSubscribers/.name)",
      "signature": "const ch = diagnostics_channel.channel(name); ch.subscribe(fn); ch.publish(msg)",
      "badge": "caveats",
      "description": "Name-keyed pub/sub: channel/subscribe/unsubscribe/hasSubscribers, with a Channel handle exposing .publish/.hasSubscribers/.name. Subscribers may be 0/1/2-arity, and unsubscribe is by pointer identity.",
      "returns": {
        "type": "Channel | boolean | void",
        "desc": "A channel handle, subscription result, or nothing."
      },
      "differences": [
        "Messages are **strings** (serialize with `JSON.stringify`) — a typed subset can't hand arbitrary object shapes to unknown subscribers",
        "Subscriber params must be string-typed (an untyped pre-wrapped subscriber is a clean rejection, never silent reinterpretation)",
        "`tracingChannel` and the store bindings (`bindStore`/`runStores`) are clean rejections; a Channel handle isn't module-global-promotable (capture it in a closure, not a named top-level function)"
      ],
      "seeAlso": [],
      "spec": {
        "mdn": "https://nodejs.org/api/diagnostics_channel.html"
      }
    },
    {
      "id": "nodecore.httpclient",
      "name": "http client (http.get/http.request)",
      "statusRow": "http client (http.get/http.request)",
      "signature": "http.get(url, res => …); const req = http.request(opts); req.write(body); req.end()",
      "badge": "caveats",
      "description": "Node's _http_agent/_http_client in TypeScript over net. http.get and http.request return a ClientRequest (an OutgoingMessage) and the response is an IncomingMessage — a Readable.",
      "returns": {
        "type": "ClientRequest",
        "desc": "A request handle; the response arrives via callback."
      },
      "differences": [
        "ClientRequest: chunked or `Content-Length` bodies, `'response'`/`'socket'`/`'information'`/`'upgrade'`/`'error'`, `abort`/`destroy`/`setTimeout`",
        "`http.Agent` pools keep-alive sockets; `http.globalAgent` keeps them alive and unref'd, as Node 19+",
        "`http.request(new URL(…))` is not accepted — pass a string or an options object"
      ],
      "seeAlso": [
        "nodecore.https"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/http.html"
      }
    },
    {
      "id": "nodecore.https",
      "name": "https (client: https.get/https.request)",
      "statusRow": "https (client: https.get/https.request)",
      "signature": "https.get(url, res => …); const req = https.request(opts); req.end()",
      "badge": "caveats",
      "description": "https.createServer is a tls.Server running the http module's connection handling; https.request/get/Agent/globalAgent connect with tls.connect. Same request and response objects as http.",
      "returns": {
        "type": "ClientRequest",
        "desc": "A request handle; the response arrives via callback."
      },
      "differences": [
        "Same client limit as `http` for a `URL` argument"
      ],
      "seeAlso": [
        "nodecore.httpclient",
        "nodecore.tls"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/https.html"
      }
    },
    {
      "id": "nodecore.streamweb",
      "name": "stream/web (re-exports: ReadableStream/WritableStream/TransformStream/CompressionStream/DecompressionStream)",
      "statusRow": "stream/web (re-exports: ReadableStream/WritableStream/TransformStream/CompressionStream/DecompressionStream)",
      "signature": "import { ReadableStream, TransformStream } from 'stream/web'",
      "badge": "exact",
      "description": "The names bind as identity to the ambient WHATWG constructors that already exist — importing them from stream/web is interchangeable with using the globals. The queuing-strategy classes are not exported.",
      "returns": {
        "type": "typeof ReadableStream",
        "desc": "The same ambient constructors as the globals."
      },
      "seeAlso": [
        "nodecore.zlib"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/webstreams.html"
      }
    },
    {
      "id": "nodecore.cluster",
      "name": "cluster (fork, worker messaging, isPrimary/isWorker/worker, Worker.id/.process)",
      "statusRow": "cluster (fork, worker messaging, isPrimary/isWorker/worker, Worker.id/.process)",
      "signature": "cluster.fork(); cluster.on('exit', (worker, code) => {}); cluster.workers[id]; cluster.setupPrimary({ args, silent }); cluster.disconnect(cb?)",
      "badge": "caveats",
      "description": "Unifies with http.listen({ workers: N })'s worker-id global — the primary stays in the event loop while workers live (via their IPC handles), then blocks in waitpid as the backstop. Workers carry the fork IPC channel with Node's json serialization mode: strings AND objects/arrays/numbers/booleans cross as themselves, both directions. Cluster-level events mirror Node's — cluster.on('fork'/'online'/'message'/'exit'/'disconnect'/'listening'), each passing the Worker first ('listening' adds { address, port, addressType }), in either registration order. cluster.workers is live (a reaped worker is dropped before its 'exit' listeners run, like Node) with cluster.workers[id] lookup; several listeners per event coexist in registration order; cluster.disconnect(cb) flushes in-flight messages; setupPrimary honors args (worker argv tail) and silent (worker stdio piped to worker.process.stdout/stderr), read back as cluster.settings.",
      "returns": {
        "type": "cluster.Worker",
        "desc": "A worker handle."
      },
      "differences": [
        "`setupPrimary`'s `exec`/`execArgv` are a compile-time rejection — workers re-exec this compiled program itself; there is no separate script file for a worker to run (a genuine impossibility of native compilation); `args` and `silent` work on every platform, Windows included",
        "`'online'` fires from a queued microtask (the worker is exec'd by then), not a wire handshake; `cluster.workers` iterates and indexes by id but is not a real id-keyed *object* (no `Object.keys` over it)",
        "Workers each bind the port via `SO_REUSEPORT`; on Linux the kernel load-balances accepts across them, on Darwin the distribution is uneven (often one worker) — correctness holds, balancing differs",
        "Can't be combined with Worker threads (same restriction as `http.listen({workers})`)"
      ],
      "seeAlso": [
        "nodecore.net"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/cluster.html"
      }
    },
    {
      "id": "nodecore.http2",
      "name": "http2 **module** (http2.createServer/connect, Http2Session/Http2Stream)",
      "statusRow": "http2 **module** (http2.createServer/connect, Http2Session/Http2Stream)",
      "signature": "http2.createServer().on('stream', (stream, headers) => …); http2.connect(url).request({ ':path' })",
      "badge": "caveats",
      "description": "The explicit http2 module, over the same nghttp2 h2c driver that http.listen already rides transparently. Server-side serves h2c (and still answers 1.1, a superset of Node); the client opens an h2c session and submits body-less requests.",
      "returns": {
        "type": "Http2Session | Http2Stream",
        "desc": "A session or stream handle."
      },
      "exampleKey": "http2/client_session",
      "snippet": "const server = http2.createServer();\nserver.on('stream', (stream, headers) => {\n  stream.respond({ ':status': 200, 'content-type': 'text/plain' });\n  stream.end(\"hello \" + headers[':path'].slice(1));\n});",
      "differences": [
        "**Server-side Stages 1–2**: `http2.createServer` on the http server handle, serving **h2c** (it also still answers 1.1, a superset of Node); compat `.on('request', (req, res))` **and** the core streams API — `.on('stream', (stream, headers))` with pseudo-headers on the map, `stream.respond({ ':status', … })` incl. response headers, `.end`/`.write`, request body via `.on('data'|'end')` (one synchronous chunk)",
        "`stream.close()`/`setEncoding`/`resume`/`pause` are accepted no-ops; responses are written when the handler returns (no mid-stream RST/flow control)",
        "**Client (Stage 3)**: `http2.connect(\"http://…\")` (h2c only — a TLS authority throws) → session `.request({ ':path', ':method', headers… })` → stream `.on('response'|'data'|'end')`; requests are **body-less** (END_STREAM at submit — `req.write`/`end(body)` are clean rejections); response `':status'` arrives as a *string* in the headers map (Node types it as a number); session/stream `'error'`/`'close'` listeners are accepted no-ops",
        "**Stage 4**: `http2.constants` (compile-time namespace — header/method names, RFC 7540 error codes; unknown members reject cleanly) and `getDefaultSettings`/`getPackedSettings`/`getUnpackedSettings` (nghttp2 wire format; the invalid-value RangeError taxonomy is not modeled)",
        "`createSecureServer` (needs a TLS server mode) and request bodies remain **clean rejections**",
        "Sessions, streams, server/client push, settings frames, and the whole explicit-API surface are unimplemented — staged over the existing nghttp2 h2c driver; client-mode nghttp2 is the main new work"
      ],
      "seeAlso": [
        "nodecore.httpclient",
        "nodecore.tls"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/http2.html"
      }
    },
    {
      "id": "nodecore.async_hooks",
      "name": "async_hooks (AsyncLocalStorage, AsyncResource)",
      "statusRow": "async_hooks (AsyncLocalStorage, AsyncResource)",
      "signature": "const als = new AsyncLocalStorage<T>(); als.run(store, () => als.getStore()); AsyncLocalStorage.bind(fn); new AsyncResource(name).runInAsyncScope(fn)",
      "badge": "caveats",
      "description": "AsyncLocalStorage carries a typed store through an async call tree: run/getStore/exit/enterWith/disable, with real propagation across await (the store rides the coroutine task struct, so it survives an await/park/resume and a spawned task inherits it) and across a setTimeout/setInterval scheduled inside run(). Static AsyncLocalStorage.bind(fn) and AsyncResource.runInAsyncScope capture the current context and replay it later.",
      "differences": [
        "AsyncLocalStorage.snapshot() is a clean rejection — its runner is fully generic (parameter/return types vary per call site), needing generic first-class closures this compiler lacks",
        "runInAsyncScope accepts thisArg but doesn't bind it (no dynamic this on closures); a run() callback that throws doesn't restore the frame yet",
        ".then/.catch reactions and EventEmitter listeners don't propagate context yet (only await, task-spawn, and timers do)",
        "The low-level createHook/executionAsyncId/async-id lifecycle is out of scope — a compiled AOT binary has no V8 promise-hooks or GC-tracked async-ids"
      ],
      "seeAlso": [
        "nodecore.diagnostics_channel"
      ],
      "spec": {
        "mdn": "https://nodejs.org/api/async_context.html"
      }
    }
  ]
}
