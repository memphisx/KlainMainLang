{
  "id": "node-core-modules",
  "file": "NODE-CORE-MODULES.md",
  "title": "Other Node.js Core Modules",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index. Smaller or lower-priority Node built-in modules, grouped here rather than given a full page each. Split out to its own page (rather than [PROCESS-CLI.md](PROCESS-CLI.md)) once it's substantial enough to need one."
    },
    {
      "kind": "coverage",
      "style": "standard"
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format)."
    },
    {
      "kind": "table",
      "table": {
        "featureColumn": "Module",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`querystring`",
            "status": "implemented",
            "caveats": [
              "Repeated keys collapse to the last value instead of an array — `.parse('a=1&a=3')` yields `{a:'3'}`, not Node's `{a:['1','3']}`"
            ],
            "notes": [
              "Surface: `.parse`, `.stringify`",
              "A `Map<string,string>` in, out — the same shape `req.query`/`URLSearchParams` already use",
              "`.parse` doesn't strip a leading `?` (matching real Node; distinct from `new URLSearchParams(str)`, which does)"
            ]
          },
          {
            "feature": "`assert`",
            "status": "implemented",
            "caveats": [
              "`equal`/`strictEqual` and `deepEqual`/`deepStrictEqual` (and their `not*` counterparts) are aliases of each other — this compiler's `==` has no implicit coercion to distinguish them",
              "`deepStrictEqual`: Map/Set/class instances, nullable-scalar members, and TypedArrays are a clean compile error (V1)",
              "`.match`/`.doesNotMatch`'s regexp argument must be statically a RegExp ([ADR-00428](../adr/ADR-00428.md))",
              "`.throws` only checks that *something* was thrown, not the thrown value's type/message",
              "No `assert.rejects`/`doesNotReject` (needs await-a-rejection machinery) and no `AssertionError` class ([ADR-00499](../adr/ADR-00499.md))"
            ],
            "notes": [
              "Surface: `.ok`, bare `assert(cond, msg?)`, `.equal`/`.strictEqual`, `.notEqual`/`.notStrictEqual`, `.deepEqual`/`.deepStrictEqual` (+`not*`), `.fail`, `.throws`, `.doesNotThrow`, `.ifError`, `.match`/`.doesNotMatch`",
              "`deepStrictEqual` covers scalars, strings, arrays, and objects (recursively, incl. nested)"
            ]
          },
          {
            "feature": "`test` / `node:test`",
            "status": "implemented",
            "caveats": [
              "**Run-at-registration**: top-level code after a `test()` runs after it here, before it in Node. See [TDD-00140](../tdd/TDD-00140.md), [ADR-00419](../adr/ADR-00419.md)",
              "`beforeEach`/`afterEach` are single slots (last wins) and also fire for subtests",
              "No `mock.*`, `t.plan`, `run()`, concurrency, or snapshots",
              "`mustCall` rejects rest-parameter callbacks (a rest tail is not a fixed ABI slot list) ([ADR-00496](../adr/ADR-00496.md))",
              "`mustNotCall()` returns a `() => void`; using it where a non-zero-arity callback is expected is a type mismatch",
              "`mustSucceed` is count-only (the \"err is falsy\" check is deferred); `expectWarning` runs the thunk but doesn't capture warnings"
            ],
            "notes": [
              "Surface: the **runner**: `test`/`it`, `describe`/`suite`, TestContext `t.test`/`t.after`/`t.skip`/`t.todo`/`t.diagnostic`, hooks `before`/`after`/`beforeEach`/`afterEach` — plus the native helpers `mustCall`/`mustCallAtLeast`/`mustNotCall`/`skip`/`expectsError` + env probes",
              "`mustCall` wraps every fixed callback signature — arrays, nullable scalars, any arity ([ADR-00496](../adr/ADR-00496.md))",
              "`import { mustCall } from 'test'` — a first-class builtin (not Node's internal `../common`). `mustCall(fn, n=1)` — plus Node's function-less `mustCall()`/`mustCall(n)` forms, which count a synthesized noop ([ADR-00423](../adr/ADR-00423.md)) — wraps `fn` so its invocation count is verified at process exit (via the existing exit sink); an unmet/exceeded count prints a diagnostic and exits non-zero, giving examples/tests real async assertions instead of `console.log`. See [TDD-00122](../tdd/TDD-00122.md)/[ADR-00370](../adr/ADR-00370.md)",
              "Distinct from `console.assert` (see [CONSOLE.md](CONSOLE.md)) — throws a catchable `AssertionError` (kind-tagged as a base `Error`, since this compiler's fixed error-kind enum has no dedicated slot) instead of logging and continuing"
            ]
          },
          {
            "feature": "`util`",
            "status": "implemented",
            "caveats": [
              "No `.promisify` — the callback builtins (`zlib.gzip`, `child_process.exec`) are name-dispatched calls, not first-class function values to wrap; importing `promisify` fails cleanly ([ADR-00325](../adr/ADR-00325.md))",
              "`.format` requires a string-**literal** format (compile-time walk); a runtime format string is a compile error",
              "`.inspect`'s options object is accepted but ignored (no `depth`/`colors`)"
            ],
            "notes": [
              "Surface: `.inspect`, `.format`",
              "`.inspect` reuses the same value formatter as `console.log` (strings single-quoted, `ClassName { … }`, arrays, `10n`)",
              "`.format`: `%s`/`%d`/`%i`/`%f`/`%j`/`%o`/`%O`/`%%`, excess args appended space-separated, unrecognized specifiers left literal — matching Node"
            ]
          },
          {
            "feature": "`net`",
            "status": "implemented",
            "caveats": [
              "`net.connect` uses a **blocking** `connect()` — a slow/unreachable host blocks the event loop until the OS connect timeout ([ADR-00328](../adr/ADR-00328.md))",
              "A connect failure **throws** a catchable `Error` rather than emitting an async `'error'` event",
              "Server binds IPv4 `0.0.0.0` only — no `host`/`path`/options object on `listen`",
              "IPv4-native bind: an accepted socket reports `127.0.0.1` where Node's dual-stack default reports `::ffff:127.0.0.1`, and the server reports `IPv4`/`0.0.0.0` not `IPv6`/`::`",
              "IPC `{ path }` Unix-domain socket: Linux path-layout unverified (Mac-verified only) ([ADR-00588](../adr/ADR-00588.md))",
              "No `'error'`/`'close'`/`'connect'` events on server or socket yet",
              "`setEncoding`/`ref`/`unref`/`pause`/`resume`/`setTimeout` are accepted but no-ops",
              "No back-pressure: `.write` never returns `false` and there's no `'drain'` event",
              "Listeners must be arrow/function-expression literals",
              "`net.Server`/`net.Socket` are not exposed as constructible classes",
              "Socket `'close'` fires with no `hadError` argument, and `'error'` listeners stay a rejection — the runtime's failure paths throw instead of emitting ([ADR-00501](../adr/ADR-00501.md))"
            ],
            "notes": [
              "Surface: TCP server: `net.createServer`, `server.listen`/`address()`/`on('connection'\\|'listening')`/`close`; TCP client: `net.connect`/`createConnection` (positional or `{ port, host }`); connection socket `.on('data'\\|'end'\\|'close'\\|'connect'/'ready')`/`.write`/`.end`/`.address()`/`.destroy()`/`.setNoDelay()`/`.setKeepAlive()`; `net.isIP`/`isIPv4`/`isIPv6`",
              "Accept loop and per-connection read pipes fold into the same `select()` event loop as `child_process` ([ADR-00324](../adr/ADR-00324.md)); a client socket reuses the same machinery once connected. A plausible base for a future `WebSocket` (see [NETWORKING.md](NETWORKING.md))",
              "`address()` reads the real bound address+port via `getsockname`+`inet_ntop`, making the `listen(0)` ephemeral-port idiom (`listen(0, () => net.connect(server.address().port, …))`) usable; `net.isIP` parses via `inet_pton` (0/4/6) ([ADR-00397](../adr/ADR-00397.md)/[ADR-00399](../adr/ADR-00399.md))",
              "`'data'` delivers a Buffer; `.write`/`.end` take a string/Buffer/ArrayBuffer/DataView",
              "`.address()` reports the real bound `{ family: \"IPv4\", address, port }` via `getsockname` — `0.0.0.0` for a server, the real local IP for a connected socket",
              "`net.connect` accepts every positional arity (`(port)`, `(port, cb)`, `(port, host[, cb])`) and `{ port, host }` (host defaults to `localhost`); `on('listening')` fires when the server binds, registered before or after `listen()` ([ADR-00413](../adr/ADR-00413.md))",
              "`socket.setNoDelay`/`setKeepAlive` are real `setsockopt` calls — `setKeepAlive(enable, initialDelay)` honours the idle-time argument (`TCP_KEEPIDLE`, or `SIO_KEEPALIVE_VALS` on Windows) ([ADR-00760](../adr/ADR-00760.md))",
              "See [ADR-00358](../adr/ADR-00358.md)",
              "`isIP`/`isIPv4`/`isIPv6` match Node's strict parser, not the platform `inet_pton`: leading-zero v4 octets and >4-digit v6 groups rejected, `%zone` IDs accepted ([ADR-00457](../adr/ADR-00457.md))"
            ]
          },
          {
            "feature": "`dgram`",
            "status": "implemented",
            "caveats": [
              "`udp4` only — no `udp6`",
              "No `'listening'`/`'error'` events, options object, or multicast",
              "Message listeners must be arrow/function-expression literals",
              "`socket.send(msg, port, host)` positional form only — no `(msg, offset, length, port, address, cb)` form; no `.setTTL()`/membership methods"
            ],
            "notes": [
              "Surface: UDP sockets: `createSocket('udp4')`, `bind`, `address()`, `setBroadcast()`, `on('message')`, `send`, `close`",
              "Each socket's fd folds into the same `select()` event loop as `net`/`child_process` ([ADR-00327](../adr/ADR-00327.md))",
              "`.address()` reports the real bound `{ address, family, port }` via `getsockname` (the `bind(0)` ephemeral-port idiom); `.setBroadcast(flag)` is a real `setsockopt(SO_BROADCAST)` — required before sending to a broadcast address ([ADR-00581](../adr/ADR-00581.md))",
              "`'message'` fires with a Buffer and an rinfo `{ address, port }`; `send(msg, port, host)` resolves `host` (name or IP) via the `dns` helper",
              "`msg`/`send` payloads take a string/Buffer/ArrayBuffer/DataView"
            ]
          },
          {
            "feature": "`tls`",
            "status": "implemented",
            "caveats": [
              "**Blocking** TCP connect + TLS handshake on both sides (a slow peer stalls the loop), the same simplification `net.connect` makes",
              "OpenSSL-only (libssl) — a `-crypto=commoncrypto` build using `tls` is a clean compile error (Mac's CommonCrypto has no TLS API)",
              "Client options `{ rejectUnauthorized }` only — no client-cert / ALPN / `servername` override (SNI = the host)",
              "Server `{ cert, key }` are PEM **strings** only — one cert per server, no chain/passphrase, no `requestCert`"
            ],
            "notes": [
              "Surface: TLS-wrapped sockets",
              "Options may be an object literal or a bound object value ([ADR-00413](../adr/ADR-00413.md))",
              "**Client** `tls.connect(port, host[, {rejectUnauthorized}][, onSecureConnect])` (cert verification + SNI on by default) and **server** `tls.createServer({ cert, key }, connectionListener)` (`.listen`/`.close`, per-connection `SSL_accept`) — both return **net-shaped** handles (an `SSL*` in the socket), so `.on('data')`/`.write()`/`.end()`/`.on('connection')` reuse the net machinery, routing through `SSL_read`/`SSL_write`. A handshake/verify failure throws a catchable `Error` ([TDD-00109](../tdd/TDD-00109.md)/[TDD-00110](../tdd/TDD-00110.md)/[ADR-00344](../adr/ADR-00344.md)/[ADR-00345](../adr/ADR-00345.md))"
            ]
          },
          {
            "feature": "`dns`",
            "status": "implemented",
            "caveats": [
              "`dns.resolve4`/`resolve` use `getaddrinfo` (the OS resolver), not real DNS `A` queries — no `resolve6`/`resolveMx`/`resolveTxt`/`reverse`",
              "IPv4 (`AF_INET`) only; `family` is always 4",
              "The callback forms fire synchronously, not on a later event-loop tick",
              "Failure passes a generic `Error` (`getaddrinfo ENOTFOUND`), not a `code`-tagged Node error"
            ],
            "notes": [
              "Surface: `dns.lookup`, `dns.resolve4`/`resolve`, `dns.promises.lookup`",
              "`getaddrinfo(3)`-backed ([ADR-00326](../adr/ADR-00326.md), [ADR-00329](../adr/ADR-00329.md)); `lookup` returns one dotted-quad, `resolve4` all A records as a `string[]`, `promises.lookup` a `Promise<{address, family}>`",
              "Verified on Mac (arm64) with the Darwin `struct addrinfo` `ai_addr`/`ai_next` offsets probed on the machine — the same layout the WebSocket-client resolver still gates out on Darwin"
            ]
          },
          {
            "feature": "`zlib`",
            "status": "implemented",
            "caveats": [
              "The callback form fires synchronously, not deferred to the next event-loop tick",
              "No Brotli (`brotliCompress*`) — it would pull in `-lbrotli`, a new system dependency this binding deliberately avoids",
              "No class/stream forms (`createGzip()`, `zlib.Gzip`, …) — the WHATWG `CompressionStream`/`DecompressionStream` cover the streaming case ([STREAMS.md](STREAMS.md))",
              "`{ level }` is the only supported option and must be a compile-time constant"
            ],
            "notes": [
              "Surface: `gzip`/`gunzip`, `deflate`/`inflate`, `deflateRaw`/`inflateRaw`, `unzip`, each `*Sync` + `(err, result)` callback",
              "One-shot calls over the same libz backend as `CompressionStream` ([ADR-00302](../adr/ADR-00302.md)), driven by a shared `@__kml_zlib_oneshot` helper ([ADR-00321](../adr/ADR-00321.md))",
              "Input may be a Buffer/TypedArray, ArrayBuffer, DataView, or string (UTF-8); output is always a Buffer"
            ]
          },
          {
            "feature": "`vm`",
            "status": "missing",
            "notes": [
              "Surface: `vm.Script`, sandboxed `eval`-like execution",
              "Same fundamental blocker as the already-tracked bare `eval()` — see [GLOBAL-FUNCTIONS.md](GLOBAL-FUNCTIONS.md). Opt-in embedded-engine path scoped in [TDD-00046](../tdd/TDD-00046.md), not started"
            ]
          },
          {
            "feature": "`diagnostics_channel`",
            "status": "implemented",
            "caveats": [
              "Messages are **strings** (serialize with `JSON.stringify`) — a typed subset can't hand arbitrary object shapes to unknown subscribers",
              "Subscriber params must be string-typed (an untyped pre-wrapped subscriber is a clean rejection, never silent reinterpretation)",
              "`tracingChannel` and the store bindings (`bindStore`/`runStores`) are clean rejections; a Channel handle isn't module-global-promotable (capture it in a closure, not a named top-level function) ([ADR-00420](../adr/ADR-00420.md))"
            ],
            "notes": [
              "Surface: `channel`/`subscribe`/`unsubscribe`/`hasSubscribers`, Channel `.publish`/`.hasSubscribers`/`.name`",
              "Name-keyed pub/sub; 0/1/2-arity subscribers, pointer-identity unsubscribe"
            ]
          },
          {
            "feature": "`http` client",
            "status": "implemented",
            "caveats": [
              "`.abort()`/`.destroy()` cancel only a not-yet-fired request — an in-flight transfer isn't torn down",
              "`.on('error')` is accepted but never fired — transport failures throw instead",
              "`http.Agent`/`https.Agent` are inert — pool options are validated but configure nothing (one connection per request), `.destroy()` is a no-op, `.sockets`/`.requests` reject cleanly ([ADR-00432](../adr/ADR-00432.md))",
              "`headers` is literal-form-only; request-body chunks are strings only (`req.write()`/`req.end(body)`)",
              "`agent` is accepted and ignored — one connection per request (Node's `agent: false`), no pooling"
            ],
            "notes": [
              "Surface: `http.get`/`http.request`",
              "Request bodies: `req.write(chunk)` stages a string chunk (appending across calls), `req.end([body])` appends a final chunk before firing, pairing with a `method: \"POST\"` option; response surface is `res.statusCode`/`res.on('data'/'end')`/`res.resume()`/`setEncoding`. Both calls return a ClientRequest handle (`request()` sends nothing until `.end()`). Options objects are literal or variable-bound, supporting `{ port, path, host/hostname, method, headers, agent }`. See [ADR-00575](../adr/ADR-00575.md), [ADR-00430](../adr/ADR-00430.md), [ADR-00429](../adr/ADR-00429.md), [ADR-00500](../adr/ADR-00500.md), [TDD-00138](../tdd/TDD-00138.md)",
              "libcurl-backed with async event-loop delivery, contextually-typed callbacks incl. `test` wrappers ([ADR-00404](../adr/ADR-00404.md)–[ADR-00406](../adr/ADR-00406.md), [ADR-00412](../adr/ADR-00412.md))"
            ]
          },
          {
            "feature": "`https`",
            "status": "implemented",
            "caveats": [
              "`https.createServer` is HTTPS/1.1 only — an http/1.1-only ALPN forces even an h2-capable client to 1.1",
              "Inherits `http.createServer`'s V1 `res` subset and one-request-per-connection (`Connection: close`) posture",
              "Same V1 client surface and caveats as `http.get`/`request` ([TDD-00138](../tdd/TDD-00138.md))"
            ],
            "notes": [
              "Surface: `https.get`/`https.request` client + `https.createServer`",
              "`https.createServer({ cert, key }[, handler])` uses the same `(req, res)` core as `http.createServer`, with the connection fiber's socket I/O routed through per-fd `SSL_read`/`SSL_write` shims; streaming response bodies over TLS are supported. See [TDD-00111](../tdd/TDD-00111.md), [ADR-00505](../adr/ADR-00505.md)",
              "`get`/`request` ride the libcurl backend, so TLS handshake + certificate verification come from the URL scheme — no https-specific machinery ([ADR-00410](../adr/ADR-00410.md)); verified against a live TLS host (Mac)"
            ]
          },
          {
            "feature": "`stream/web`",
            "status": "implemented",
            "notes": [
              "Surface: re-exports: `ReadableStream`/`WritableStream`/`TransformStream`/`CompressionStream`/`DecompressionStream`",
              "The names bind as identity to the ambient WHATWG constructors that already exist — importing them is interchangeable with the globals ([ADR-00410](../adr/ADR-00410.md)); the queuing-strategy classes are not exported"
            ]
          },
          {
            "feature": "`cluster`",
            "status": "implemented",
            "caveats": [
              "`setupPrimary`'s `exec`/`execArgv` are a compile-time rejection — workers re-exec this compiled program itself, so there is no separate script file for a worker to run (a genuine impossibility of native compilation, [ADR-01015](../adr/ADR-01015.md)); `silent` is POSIX-only until the Windows spawn lane lands ([TDD-00177](../tdd/TDD-00177.md))",
              "`'online'` fires from a queued microtask (the worker is exec'd by then), not a wire handshake; `cluster.workers` iterates and indexes by id but is not a real id-keyed *object* (no `Object.keys` over it)",
              "On Darwin, `SO_REUSEPORT` accept distribution is uneven (often one worker) — correctness holds, balancing differs from Linux's kernel load-balancing",
              "Can't be combined with Worker threads (same restriction as `http.listen({workers})`)"
            ],
            "notes": [
              "Surface: `fork`, worker messaging (strings AND objects/arrays/numbers/booleans — Node's json serialization mode, both directions, [ADR-01015](../adr/ADR-01015.md)), `isPrimary`/`isWorker`/`workerId`, `Worker.id`/`.process`, cluster-level `cluster.on('fork'/'online'/'message'/'exit'/'disconnect'/'listening')` (listeners get the Worker first, either registration order; 'listening' passes Node's `{ address, port, addressType }`), `cluster.workers` (live — a reaped worker is dropped BEFORE its `'exit'` listeners run, like Node — with `cluster.workers[id]` lookup), `cluster.disconnect(cb?)` (flushes in-flight messages), `setupPrimary({ args, silent })` + `cluster.settings` ([ADR-01012](../adr/ADR-01012.md)/[ADR-01015](../adr/ADR-01015.md))",
              "Several listeners per event coexist in registration order — `worker.on(...)` lists plus the cluster-level relays ([ADR-01015](../adr/ADR-01015.md))",
              "Workers each bind the port via `SO_REUSEPORT`",
              "Unifies with `http.listen({ workers: N })`'s worker-id global — the primary stays in the event loop while workers live (their IPC handles), then blocks in `waitpid` as the backstop",
              "Workers carry the TDD-00141 fork IPC channel ([ADR-00427](../adr/ADR-00427.md)): `worker.send`/`.on('message'/'online'/'exit'/'close'/'error')`/`.disconnect()`/`.kill()`/`.destroy()`/`.process.pid`; in-worker `process.send`/`process.on('message')`"
            ]
          },
          {
            "feature": "`http2` **module**",
            "status": "implemented",
            "caveats": [
              "`http2.createServer`/`createSecureServer` `(req, res)` handlers don't receive the request body — `req.on('data')`/`req.stream()` isn't wired to the h2 body (the `klain:http` `http.listen` h2 path delivers it as one buffered `req.body` chunk; TDD-00218)",
              "`stream.close()`/`setEncoding`/`resume`/`pause` are accepted no-ops; responses are written when the handler returns (no mid-stream RST/flow control)",
              "Client `http2.connect` is h2c only — a TLS authority throws",
              "Client requests are body-less (END_STREAM at submit) — `req.write`/`end(body)` are clean rejections",
              "Client response `':status'` arrives as a *string* in the headers map (Node types it as a number); session/stream `'error'`/`'close'` listeners are accepted no-ops",
              "`getDefaultSettings`/`getPackedSettings`/`getUnpackedSettings`: the invalid-value `RangeError` taxonomy is not modeled",
              "`createSecureServer` h2 connections are served one-at-a-time (the TLS drive is blocking in the accept path)",
              "Server/client push streams are unsupported ([TDD-00139](../tdd/TDD-00139.md))"
            ],
            "notes": [
              "Surface: `http2.createServer`/`createSecureServer`/`connect`, `Http2Session`/`Http2Stream`",
              "Server-side Stages 1–2: `http2.createServer` on the http server handle serves **h2c** (also answers 1.1, a superset of Node); compat `.on('request', (req, res))` and the core streams API — `.on('stream', (stream, headers))` with pseudo-headers on the map, `stream.respond({ ':status', … })` incl. response headers, `.end`/`.write`. See [TDD-00139](../tdd/TDD-00139.md), [ADR-00414](../adr/ADR-00414.md), [ADR-00415](../adr/ADR-00415.md)",
              "Client (Stage 3): `http2.connect(\"http://…\")` → session `.request({ ':path', ':method', headers… })` → stream `.on('response'\\|'data'\\|'end')`",
              "Stage 4: `http2.constants` compile-time namespace (header/method names, RFC 7540 error codes; unknown members reject cleanly); `getDefaultSettings`/`getPackedSettings`/`getUnpackedSettings` produce nghttp2 wire format",
              "`createSecureServer({ cert, key }[, handler])` serves **h2 over TLS**: each accepted fd is TLS-handshaken, ALPN selects `h2`, driving the same nghttp2 session as h2c over `SSL_read`/`SSL_write` shims; compat and `.on('stream')` APIs inherited unchanged. A client that doesn't negotiate `h2` is dropped after the handshake (Node's `allowHTTP1:false` default) unless `allowHTTP1: true` serves it over the HTTPS/1.1 fiber path. See [TDD-00111](../tdd/TDD-00111.md) Stage 3b, [ADR-00504](../adr/ADR-00504.md), [ADR-00505](../adr/ADR-00505.md)",
              "Transparent h2, no `http2` import needed: `fetch` negotiates h2 over TLS (ALPN); `http.listen` serves **h2c** (cleartext prior-knowledge) alongside 1.1, riding nghttp2 (`http2src/http2.c`, linked when `http.listen` is used), the h2 preface detected per-connection and dispatched through the shared 1.1 handler. h2-over-TLS is served explicitly via `http2.createSecureServer`; async handlers under h2 remain unsupported ([TDD-00111](../tdd/TDD-00111.md) Stage 3b, [ADR-00357](../adr/ADR-00357.md))"
            ]
          },
          {
            "feature": "`async_hooks`",
            "status": "implemented",
            "notes": [
              "Surface: `AsyncLocalStorage`, `AsyncResource`",
              "`AsyncLocalStorage<T>`: `run`/`getStore`/`exit`/`enterWith`/`disable` with real context propagation **across `await`** — the store lives in the coroutine task struct, so a value set with `run(...)` survives an await/park/resume and a spawned task inherits it ([ADR-00678](../adr/ADR-00678.md)); a `setTimeout`/`setInterval` scheduled inside `run(...)` carries it to its fire ([ADR-00679](../adr/ADR-00679.md))",
              "Static `AsyncLocalStorage.bind(fn)` (a wrapper of `fn`'s own signature that reinstalls the captured context) and `AsyncResource` (`runInAsyncScope`, capturing at construction) capture-and-replay the context ([ADR-00680](../adr/ADR-00680.md))",
              "`getStore()` on an unset/disabled store is a real `undefined` typed `T | undefined`, distinguishable from a genuine `0`/`\"\"`/null store ([ADR-00782](../adr/ADR-00782.md))"
            ],
            "caveats": [
              "`AsyncLocalStorage.snapshot()` is a clean rejection — its runner is fully generic (parameter/return types vary per call site), needing generic first-class closures this compiler doesn't have",
              "`AsyncResource.runInAsyncScope(fn, thisArg, …)` accepts `thisArg` but does not bind it (closures carry no dynamic `this`); a `run(...)` callback that throws does not restore the frame (exception-safe restore is a follow-up)",
              "`.then`/`.catch` reaction callbacks and EventEmitter listeners do not yet propagate context (only `await`, task-spawn, and timers do)",
              "The low-level `createHook`/`executionAsyncId`/`triggerAsyncId`/async-id lifecycle is out of scope — a compiled AOT binary has no V8 promise-hooks or GC-tracked async-ids (a faithful impossibility), so those stay clean rejections"
            ]
          }
        ]
      }
    }
  ]
}
