# TDD-NNNNN: <Short title>

- **Status**: Not Started | In Progress | Partially Implemented | Implemented | Superseded
- **Date**: YYYY-MM-DD

<!-- Status is a PURE enum — exactly one of the words above, nothing appended.
No "(see ADR-NNNNN)", no "Superseded by TDD-NNNNN". Any such detail (the
implementing ADRs, what superseded this) goes in prose below or, in the TDD
index table, in its Related ADRs / Notes columns — never in Status. -->


## Context

What problem or need does this address? Why is it being scoped now rather than later?

## Design

The approach(es) considered, in enough detail that implementation could start directly from this document. Lay out tradeoffs if there are multiple viable options; note a recommended direction if one exists, and why.

## Prerequisites

What's already built and reusable vs. what's still missing, so the work can be picked off incrementally.

| Dependency | Status | Notes |
|---|---|---|
| | | |

## Open questions

Anything genuinely undecided that implementation will need to resolve, rather than something this document already settled.
