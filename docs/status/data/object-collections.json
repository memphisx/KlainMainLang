{
  "id": "object-collections",
  "file": "OBJECT-COLLECTIONS.md",
  "title": "Object / Collections",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index."
    },
    {
      "kind": "coverage",
      "style": "standard"
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format)."
    },
    {
      "kind": "table",
      "table": {
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "Object literals `{ a: 1 }`",
            "status": "implemented"
          },
          {
            "feature": "Field access `obj.field`",
            "status": "implemented"
          },
          {
            "feature": "Object destructuring",
            "status": "implemented"
          },
          {
            "feature": "`Object.keys(obj)`",
            "status": "implemented",
            "notes": [
              "On a bare `any` value: runtime key enumeration of the D1 dynamic object's bag, `[]` for a boxed primitive, TypeError for null/undefined — matching JS ([TDD-00155](../tdd/TDD-00155.md) Stage 1)",
              "Keys enumerate in ES own-property order (array-index keys ascending numeric first, then insertion order) across `Object.keys`/`values`/`entries`, `for...in`, spread and `JSON.stringify`, for static and dynamic objects alike ([ADR-00996](../adr/ADR-00996.md))",
              "An omitted optional field (`x?: T`) is not a key — filtered at runtime by its presence, as `JSON.stringify` already did ([ADR-01063](../adr/ADR-01063.md))"
            ],
            "caveats": [
              "`Object.values`/`entries`/`for...in` still list an omitted optional field (as `undefined`); only `keys` filters"
            ]
          },
          {
            "feature": "`Object.values(obj)`",
            "status": "implemented",
            "caveats": [
              "A heterogeneous object's values are stringified ([ADR-00492](../adr/ADR-00492.md)); a homogeneous shape returns a real typed `V[]`",
              "On a *static* array (`Object.values([7, 8])`) it is a clean rejection; a bare `any` holding an array answers its elements"
            ],
            "notes": [
              "On a bare `any` value: the D1 dynamic object's enumerable own values (or an array's elements) as a dynamic array, TypeError for null/undefined, `[]` for a boxed primitive ([ADR-01077](../adr/ADR-01077.md))"
            ]
          },
          {
            "feature": "`Object.entries(obj)`",
            "status": "implemented",
            "caveats": [
              "A heterogeneous object's values are stringified — its value union is representable only as `any`, whose operators aren't dispatched yet ([ADR-00492](../adr/ADR-00492.md)); a homogeneous shape returns real typed `[string, V]` tuples",
              "On a *static* array (`Object.entries([7, 8])`) it is a clean rejection; a bare `any` holding an array answers `[\"0\", v0], …`"
            ],
            "notes": [
              "Returns a real `[string, string][]` tuple array; destructure with `for (const [k, v] of Object.entries(obj))`",
              "On a bare `any` value: a dynamic array of `[key, value]` pairs — the dynamic object's enumerable own entries in ES order, or an array's index/element pairs — TypeError for null/undefined, `[]` for a boxed primitive ([ADR-01077](../adr/ADR-01077.md))"
            ]
          },
          {
            "feature": "`Object.groupBy(arr, fn)`",
            "status": "implemented"
          },
          {
            "feature": "`Object.assign(target, ...src)`",
            "status": "implemented",
            "caveats": [
              "Every field a source contributes must already exist on `target`'s struct type — a source field `target`'s type doesn't have is a clean compile error (fixed-shape heap structs), not grafted on as in real JS ([ADR-00054](../adr/ADR-00054.md))"
            ],
            "notes": [
              "Mutates and returns `target`"
            ]
          },
          {
            "feature": "`Object.create()` / `getPrototypeOf` / `setPrototypeOf` / `__proto__` (dynamic objects)",
            "status": "implemented",
            "caveats": [
              "Prototype machinery exists on **dynamic (`any`-typed) objects only** — statically-typed structs have no prototype link, and a boxed primitive's `getPrototypeOf` answers `null` (no primitive prototype objects)",
              "`Object.create`'s property-descriptors second argument is rejected until descriptors land",
              "Inherited properties don't appear in `for...in` (own-only enumeration; real JS walks the chain's enumerables)"
            ],
            "notes": [
              "The D1 prototype chain ([TDD-00155](../tdd/TDD-00155.md) Stage 3, [ADR-00623](../adr/ADR-00623.md)): property reads and the `in` operator walk the chain (own shadows inherited; `delete` re-exposes the proto's value), writes/`Object.keys`/`JSON.stringify` stay own-table — matching JS. `Object.create(proto|null)`, `get/setPrototypeOf`, the `__proto__` accessor (JS setter semantics: non-object values silently ignored), and the `{ __proto__: base }` literal form all work; a prototype cycle throws V8's `Cyclic __proto__ value`",
              "A null prototype is tracked distinctly from an ordinary object's (unmodeled) `Object.prototype` link: such objects render `[Object: null prototype] { … }`, `Object.getPrototypeOf({})` is the shared `Object.prototype` object (not `null`), and `{ __proto__: null }` builds a null-prototype object ([ADR-01094](../adr/ADR-01094.md))"
            ]
          },
          {
            "feature": "`Object.defineProperty` / `getOwnPropertyDescriptor` / `getOwnPropertyNames` / accessors (dynamic objects)",
            "status": "implemented",
            "caveats": [
              "Dynamic (`any`-typed / js-mode-literal) objects only — statically-typed structs have no descriptor table",
              "`Object.defineProperties` and the descriptor-aware `Object.assign` form are follow-ons"
            ],
            "notes": [
              "Real property descriptors ([TDD-00155](../tdd/TDD-00155.md) Stage 5, [ADR-00629](../adr/ADR-00629.md)): spec create-defaults (absent attributes are false), non-configurable redefinition rules, data and accessor descriptor forms. Object-literal `get`/`set` compile to dynamic-ABI functions invoked with the original receiver (inherited getters through the prototype chain included); `Object.keys`/`for...in`/`JSON.stringify` honor enumerability (stringify and spread invoke getters); rejected writes/deletes throw module-strict JS TypeErrors"
            ]
          },
          {
            "feature": "`new Proxy(target, handler)`",
            "status": "implemented",
            "caveats": [
              "Traps implemented: `get`/`set`/`has`/`deleteProperty` — dispatched in the dynamic-object runtime entry points, forwarding to the target when absent ([ADR-00630](../adr/ADR-00630.md)); other traps (`ownKeys`, `getOwnPropertyDescriptor`, `apply`, `construct`, …) are not consulted (those operations forward to the target)",
              "The target must be a dynamic object (an untyped/`any` literal); statically-typed structs can't be proxied"
            ],
            "notes": [
              "`JSON.stringify` of a proxy serializes its target (spec behavior for data properties)"
            ]
          },
          {
            "feature": "`Reflect.get/set/has/deleteProperty/ownKeys/getPrototypeOf/setPrototypeOf/isExtensible/preventExtensions/defineProperty` + `defineMetadata`/`getMetadata`/`hasMetadata`",
            "status": "implemented",
            "caveats": [
              "`Reflect.construct` is missing; `Reflect.apply` takes a function value (a code-generated builtin such as `Math.max` is not one)",
              "The metadata API (`defineMetadata`/`getMetadata`/`getOwnMetadata`/`hasMetadata`/`hasOwnMetadata`, TDD-00161 Stage 3) stores on a dynamic-object target and does not walk the prototype chain (so `get` == `getOwn`); `Reflect.metadata` as a decorator factory is rejected",
              "Boolean-returning forms (`set`/`deleteProperty`/`setPrototypeOf`) return the success flag rather than throwing, per spec",
              "Every form requires a dynamic (`any`-typed, or `-compat=js`) target: a statically-typed object operand is a clean compile-time rejection (strict mode does not widen a typed struct to a bag, [ADR-00630](../adr/ADR-00630.md)), including `get`/`has`"
            ]
          },
          {
            "feature": "`Object.freeze(obj)`",
            "status": "implemented",
            "notes": [
              "Real runtime enforcement, not a no-op: tracks `obj`'s heap pointer in a global frozen-object set, checked at every field-write site, so a blocked write throws a catchable Error even through a different alias/function parameter ([ADR-00055](../adr/ADR-00055.md))",
              "A **dynamic** object freezes via its descriptor attributes instead ([ADR-00629](../adr/ADR-00629.md)): writes/adds/deletes throw, `Object.isFrozen`/`isSealed`/`isExtensible` answer truthfully; `Object.seal` and `Object.preventExtensions` enforce their own tiers the same way"
            ]
          },
          {
            "feature": "`Object.seal(obj)`",
            "status": "implemented",
            "notes": [
              "A static object's shape is fixed at compile time, so seal enforces nothing further on it; it records the object's integrity level so `Object.isSealed`/`isFrozen`/`isExtensible` answer as in JS — as do `Object.preventExtensions` and `Object.freeze` ([ADR-01094](../adr/ADR-01094.md))"
            ]
          },
          {
            "feature": "`Object.hasOwn()` / `.hasOwnProperty()`",
            "status": "implemented",
            "caveats": [
              "The key must be a string literal — a runtime-computed key is a clean compile error (no runtime field-name table to check it against) ([ADR-00065](../adr/ADR-00065.md))"
            ],
            "notes": [
              "Object shapes are fully structural/static, so this is a compile-time `FieldIndex` lookup, not a runtime scan — except an optional field (`x?: T`), whose answer is its runtime presence (`\"age\" in { name }` is `false`, ADR-01063)"
            ]
          },
          {
            "feature": "`Object.fromEntries()`",
            "status": "implemented",
            "caveats": [
              "Keys must be strings (a `[string, V][]` array) — real JS stringifies any key and accepts symbol keys ([ADR-00348](../adr/ADR-00348.md))"
            ],
            "notes": [
              "Returns a *dynamic object* (a `Map<string,V>`-backed value, [TDD-00012](../tdd/TDD-00012.md)); read via `obj.field`/`obj[key]`. Seeds via the same `[K,V][]` walker `new Map(entries)` uses ([ADR-00347](../adr/ADR-00347.md))"
            ]
          },
          {
            "feature": "Object spread `{ ...obj, key: val }`",
            "status": "implemented",
            "notes": [
              "Spreading a bare `any` (`{ ...process.env, X: '1' }`, a `JSON.parse` result) builds the literal as a D1 dynamic object, since its key set exists only at run time; the result is `any` ([ADR-01077](../adr/ADR-01077.md))"
            ]
          },
          {
            "feature": "Computed property keys `{ [expr]: value }`",
            "status": "implemented",
            "caveats": [
              "`V` is inferred from the first property only",
              "`...spread` combined with a computed key isn't supported yet",
              "The declared-type form (`{ [key: string]: T }`) isn't supported yet"
            ],
            "notes": [
              "A *dynamic object* — storage-wise a real `Map<string,V>` reusing `new Map<K,V>()`'s runtime, with `.field`/`[expr]` sugar layered on top ([TDD-00012](../tdd/TDD-00012.md) / [ADR-00066](../adr/ADR-00066.md))",
              "Exception: a well-known-symbol key (`[Symbol.asyncIterator]`/`[Symbol.iterator]`, colon or method-shorthand form) desugars to a reserved *static* key, so the literal stays a static struct and the member drives `for...of`/`for await...of` iteration ([ADR-00279](../adr/ADR-00279.md))"
            ]
          },
          {
            "feature": "Shorthand property `{ x }`",
            "status": "implemented"
          },
          {
            "feature": "Method shorthand `{ foo() {...} }`",
            "status": "implemented",
            "caveats": [
              "`this` in a method-shorthand body is bound only when the literal's contextual type gives the method a `this: T` parameter (`{ read() { this.push(x) } }` against `read: (this: S, n: number) => void`, [ADR-01158](../adr/ADR-01158.md)); otherwise it is a clean compile-time rejection, where TypeScript types it as the object literal",
              "No generator method shorthand (`*g() {}`): rejected like a generator expression used as a value ([ADR-00169](../adr/ADR-00169.md))"
            ],
            "notes": [
              "Desugars to `{ foo: function() {...} }` — a plain anonymous function value, reusing the same closure machinery ([TDD-00060](../tdd/TDD-00060.md))"
            ]
          },
          {
            "feature": "`Map.set/get/has/delete/keys/values`",
            "status": "implemented",
            "notes": [
              "`.get()` (`Map` and `WeakMap`) returns `V \\| undefined` — a missing key reads as a real `undefined` (as in Node), distinguishable from a stored `0`/`false`/`\"\"` via the presence-flagged nullable-scalar representation (scalar `V`) or the null-pointer miss typed `undefined` (pointer `V`); narrow / `?? d` / `!` before use ([TDD-00064](../tdd/TDD-00064.md)/[ADR-00833](../adr/ADR-00833.md)). (`URLSearchParams.get` is spec'd `string \\| null` and keeps `null`.)",
              "Keys of one kind pick a specialized runtime (string keys content-hashed, numeric keys raw-i64); a bare `new Map()` whose keys are **heterogeneous** widens to an any-keyed runtime with NaN-boxed keys/values and JS **SameValueZero** equality — string/number/NaN/boolean/object/array/`null`/`undefined` keys coexist, `NaN` matches `NaN`, and objects key by reference. Its values are `any` (boxed) ([TDD-00211](../tdd/TDD-00211.md)/[ADR-00932](../adr/ADR-00932.md))",
              "An explicitly-typed **object/array-keyed** map (`Map<{…},V>`, `Map<T[],V>`) also rides the any-keyed runtime: keys box for reference identity (same reference round-trips through get/has/delete, a distinct reference of equal content is a different key), and `keys()`/`entries()`/`forEach` surface the live references ([ADR-00948](../adr/ADR-00948.md))"
            ]
          },
          {
            "feature": "`Map.size`",
            "status": "implemented"
          },
          {
            "feature": "`Map.entries()`",
            "status": "implemented",
            "notes": [
              "Returns a real `[K, V][]` tuple array ([TDD-00066](../tdd/TDD-00066.md)/[ADR-00201](../adr/ADR-00201.md)); iterate with `for (const [k, v] of m.entries())`; replaced the earlier object-shaped stand-in ([ADR-00053](../adr/ADR-00053.md))"
            ]
          },
          {
            "feature": "`Map.forEach()`",
            "status": "implemented",
            "notes": [
              "Calls `fn(value, key, map)`, matching real JS's full callback signature — the 3rd `map` argument is the map being iterated ([ADR-00573](../adr/ADR-00573.md)); a callback declaring fewer parameters just receives the leading ones ([ADR-00053](../adr/ADR-00053.md))"
            ]
          },
          {
            "feature": "`Map.clear()`",
            "status": "implemented",
            "notes": [
              "Resets size to 0 in place — doesn't free/reallocate the backing arrays (\"leak by design\" memory model); immediately reusable afterward ([ADR-00053](../adr/ADR-00053.md))"
            ]
          },
          {
            "feature": "`new Map(entries)`",
            "status": "implemented",
            "caveats": [
              "Accepts only a `[K, V][]` array of 2-tuples, narrowed from the spec's `Iterable<[K, V]>` — the only iterable/pair concept a general expression has here ([ADR-00347](../adr/ADR-00347.md))"
            ],
            "notes": [
              "K/V are inferred from the entries when no `<K, V>` is given; duplicate keys resolve last-wins; the entries source may be a bare literal or an already-declared `[K, V][]` variable ([TDD-00066](../tdd/TDD-00066.md))"
            ]
          },
          {
            "feature": "`new Set(iterable)`",
            "status": "implemented",
            "caveats": [
              "Accepts only an array expression, narrowed from the spec's `Iterable<T>` — the only iterable concept a general expression has here ([ADR-00159](../adr/ADR-00159.md))"
            ]
          },
          {
            "feature": "`Set.add/has/delete/values`",
            "status": "implemented",
            "notes": [
              "An **object/array-typed** Set (`Set<{…}>`, `Set<T[]>`) rides the any-keyed runtime with reference identity — the same reference is one element, a distinct reference of equal content is another; iteration (`values`/`for…of`/`forEach`) surfaces the live references ([ADR-00948](../adr/ADR-00948.md))"
            ]
          },
          {
            "feature": "`Set.size`",
            "status": "implemented"
          },
          {
            "feature": "`Set.forEach()`",
            "status": "implemented",
            "notes": [
              "Calls `fn(value, value, set)` — real JS's `Set.prototype.forEach` passes the value twice (Map/Set callback-shape parity) then the set itself; each argument is supplied per the callback's declared arity ([ADR-00053](../adr/ADR-00053.md)/[ADR-00573](../adr/ADR-00573.md))"
            ]
          },
          {
            "feature": "`Set.clear()`",
            "status": "implemented",
            "notes": [
              "Same in-place reset as `Map.clear()` ([ADR-00053](../adr/ADR-00053.md))"
            ]
          },
          {
            "feature": "`WeakMap` / `WeakSet` / `WeakRef`",
            "status": "implemented",
            "caveats": [
              "Object-identity keys only (a primitive key is a clean compile error); non-iterable (no `size`/iteration — matches spec)",
              "Under `-mm=manual` (default) a weak reference is strong: nothing is ever collected, so `.deref()` never nulls and keys persist (\"leak by design\"). Real weak semantics require `-mm=gc` ([TDD-00112](../tdd/TDD-00112.md)/[ADR-00349](../adr/ADR-00349.md))"
            ],
            "notes": [
              "`-mm=gc`: genuinely weak via Boehm disappearing links (the referent word is `GC_malloc_atomic`/unscanned so it isn't traced); a collected referent's `.deref()`/`WeakMap.has` observably drops. `gc()` (Node `--expose-gc` idiom) forces a full collection under gc, a no-op under manual",
              "A `new WeakMap()` initial-entries argument is out of V1 scope"
            ]
          }
        ]
      }
    },
    {
      "kind": "block",
      "text": "## Known limitations"
    },
    {
      "kind": "block",
      "text": "- Objects are fixed-shape heap structs — no dynamic property add/delete (`Object.freeze`/`.seal` don't need to enforce this; it's already structurally impossible)."
    }
  ]
}
