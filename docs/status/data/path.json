{
  "id": "path",
  "file": "PATH.md",
  "title": "path",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index. Node's `path` module — portable filesystem path manipulation, in both of Node's flavours: a bare `path` is the host's flavour (`path.win32` on Windows, `path.posix` elsewhere), and `path.posix` / `path.win32` name one explicitly on any host ([TDD-00178](../tdd/TDD-00178.md)). Import-gated (`import path from 'path'`, `import { join, win32 } from 'path'`) — see [TDD-00049](../tdd/TDD-00049.md)/[ADR-00141](../adr/ADR-00141.md)/[ADR-00142](../adr/ADR-00142.md)."
    },
    {
      "kind": "coverage",
      "style": "standard"
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format)."
    },
    {
      "kind": "table",
      "table": {
        "featureColumn": "API",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`path.join(...segments)`",
            "status": "implemented",
            "notes": [
              "posix: joins with `/`, then normalizes (collapses repeated slashes, drops `.` segments, resolves `..` against segments already seen in the same call)",
              "win32: Node's `join` — mixed `/`/`\\` accepted and normalised to `\\`, UNC roots (`\\\\server\\share`) preserved, drive roots (`C:\\`) never escaped by `..`, trailing separator kept, reserved device names (`CON`, `COM1:`) left un-normalised"
            ],
            "caveats": [
              "posix only: drops a trailing slash on the result — `path.join('foo', 'bar/')` is `'foo/bar'`, where Node preserves `'foo/bar/'` (the win32 flavour keeps it)."
            ]
          },
          {
            "feature": "`path.resolve(...segments)`",
            "status": "implemented",
            "notes": [
              "posix: absolute-always — starts from `process.cwd()`, walks segments left to right, any segment starting with `/` resets the accumulator (discarding everything before it, including cwd) — equivalent to real Node's right-to-left \"stop at the last absolute segment\" algorithm",
              "win32: Node's right-to-left walk with device tracking — the last device seen wins, a segment rooted on another drive discards earlier ones, a drive-relative segment (`C:x`) is resolved against that drive's own current directory (`process.env['=C:']`, falling back to `cwd`), a rooted-but-driveless segment (`\\abs`) takes `cwd`'s drive"
            ],
            "caveats": [
              "`path.win32.resolve()` on a non-Windows host resolves against a POSIX `cwd` (`/home/me` → `\\home\\me\\foo`), exactly as Node does there; only meaningful on Windows."
            ]
          },
          {
            "feature": "`path.dirname(p)`",
            "status": "implemented",
            "notes": [
              "Directory portion; trailing separators trimmed first",
              "win32: a drive root (`C:\\`) or UNC root (`\\\\server\\share`) is its own dirname; the input's own separators are kept (dirname slices, never rewrites)"
            ]
          },
          {
            "feature": "`path.basename(p, ext?)`",
            "status": "implemented",
            "notes": [
              "Final path segment, optionally with a trailing `ext` stripped — not stripped when doing so would consume the whole segment, unless the *entire* `path` argument equals `ext` (then returns `\"\"`), matching real Node's own asymmetric rule here",
              "win32: a leading `X:` device is skipped before scanning; both separators end a segment",
              "See [ADR-00368](../adr/ADR-00368.md)"
            ]
          },
          {
            "feature": "`path.extname(p)`",
            "status": "implemented",
            "notes": [
              "Extension of the basename, including the leading `.`; `\"\"` for a dotfile whose only `.` is its first character",
              "win32: a leading `X:` device is skipped before scanning"
            ]
          },
          {
            "feature": "`path.parse(p)` / `path.format(obj)`",
            "status": "implemented",
            "notes": [
              "`parse` returns `{root, dir, base, ext, name}`; `format` is the inverse (`base` wins over `name`+`ext`, `dir` falls back to `root`)",
              "win32: `root` is `C:\\`, `C:` (drive-relative), `\\` or a UNC share root with its trailing separator (`\\\\server\\share\\`); `format` joins with `\\`"
            ]
          },
          {
            "feature": "`path.isAbsolute(p)`",
            "status": "implemented",
            "notes": [
              "posix: `p[0] === '/'`",
              "win32: a leading `/` or `\\` (including UNC), or `X:` followed by a separator; drive-relative `C:x` is not absolute"
            ]
          },
          {
            "feature": "`path.sep` / `path.delimiter`",
            "status": "implemented",
            "notes": [
              "Compile-time constants for the host's flavour: `/` and `:` on Linux/macOS, `\\` and `;` on Windows; `path.posix.sep` / `path.win32.sep` (and `delimiter`) name a flavour explicitly"
            ]
          },
          {
            "feature": "`path.posix` / `path.win32`",
            "status": "implemented",
            "notes": [
              "Both flavour objects exist on every host, as in Node: `path.posix.join(...)`, `path.win32.parse(...)`, `import { win32 } from 'path'`. The win32 algorithms are a C port of Node's `lib/path.js` (`codegen/llvm/pathsrc/path_win32.c`), compiled in only when a program uses them; the posix algorithms are the emitted-IR runtime ([TDD-00178](../tdd/TDD-00178.md), [ADR-00722](../adr/ADR-00722.md))"
            ],
            "caveats": [
              "The flavour objects are dispatch targets, not values: `const p = path.win32` and then `p.join(...)` is not supported — call through `path.win32.join(...)` or a named import (`import { win32 } from 'path'`).",
              "`matchesGlob` is missing from both flavours (it needs Node's glob matcher)."
            ]
          },
          {
            "feature": "`path.normalize(p)` / `path.relative(from, to)` / `path.toNamespacedPath(p)`",
            "status": "implemented",
            "notes": [
              "Both flavours, from Node's own algorithms ([ADR-00723](../adr/ADR-00723.md)): `normalize` resolves `.`/`..` and repeated separators and **keeps a trailing separator** (`a//b/` → `a/b/`, `C:/a//b/` → `C:\\a\\b\\`); `relative` resolves both ends against `process.cwd()` first and walks up with `..` (win32 compares case-insensitively and returns `to` outright across drives or UNC shares); `toNamespacedPath` is the identity for posix and, for win32, the resolved `\\\\?\\C:\\…` / `\\\\?\\UNC\\…` long-path form"
            ]
          }
        ]
      }
    }
  ]
}
