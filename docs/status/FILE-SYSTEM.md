<!-- GENERATED FILE — do not edit. Source of truth: docs/status/data/file-system.json; edit the JSON, then run `make status`. -->

# File System (fs)

> Part of the [Implementation Status](README.md) index. Node-`fs`-shaped synchronous file I/O for reading/writing config, data, and logs — not `File`/`FileReader`/`FileSystemFileHandle` (those model browser sandbox/permission concepts that don't exist for a native CLI/microservice program with direct filesystem access).

**Coverage**: 19/20 (95%) · **Strict Coverage**: 2/20 (10%).

Format: [Status page format](README.md#status-page-format).

| Feature | Status | Caveats | Notes |
|---|---|---|---|
| `fs.readFileSync(path[, 'utf8'])` | ✅ | • Text-only by design — a file with embedded null bytes reads back shorter than its real size (`.length` is `strlen`-based); use `fs.readFileSyncBytes(path)` for a binary file<br>• With no encoding the result is still a `string`, not a `Buffer` (the text-only default above); a non-`'utf8'` encoding is a clean rejection ([ADR-00785](../adr/ADR-00785.md)) | • Reads the whole file into a string<br>• The optional encoding argument accepts `'utf8'`/`'utf-8'` (a bare string or `{ encoding: 'utf8' }`) — a faithful no-op since strings are UTF-8, so the everyday `readFileSync(path, 'utf8')` idiom compiles ([ADR-00785](../adr/ADR-00785.md))<br>• Throws a catchable `Error` (built from `strerror(errno)`) if the file can't be opened<br>• Pseudo-files under `/proc` and `/sys` (which report `st_size` 0 yet stream real bytes) read correctly — the read grows to EOF rather than trusting the stat size ([ADR-00811](../adr/ADR-00811.md))<br>• See [ADR-00023](../adr/ADR-00023.md) |
| `fs.readFileSyncBytes(path)` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)). | • Binary-safe sibling of `readFileSync`: returns a `Uint8Array` with the file's exact byte count (no `strlen` involved), so a file with an embedded null byte reads back whole<br>• No `Buffer` class — this compiler has none; plain `ArrayBuffer`/TypedArrays instead<br>• See [ADR-00094](../adr/ADR-00094.md) |
| `fs.writeFileSync(path, data[, opts])` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)).<br>• The options argument supports only `{ encoding: 'utf8', flag }` — `mode` and non-`'utf8'` encodings are clean rejections ([ADR-00785](../adr/ADR-00785.md)). | • Creates or truncates the file with `data`. Throws on failure<br>• `data` may be a string (the original, `strlen`-based path, unchanged) or an `ArrayBuffer`/TypedArray (binary-safe, writes the real byte count via `fwrite` directly — [ADR-00094](../adr/ADR-00094.md)), dispatched on `data`'s inferred type<br>• Optional third argument: `'utf8'` (or `{ encoding: 'utf8', flag }`); `{ flag: 'a' }` appends instead of truncating, `{ flag: 'w' }` (default) truncates ([ADR-00785](../adr/ADR-00785.md)) |
| `fs.appendFileSync(path, data[, opts])` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)).<br>• The options argument supports only `{ encoding: 'utf8', flag }` — `mode` and non-`'utf8'` encodings are clean rejections ([ADR-00785](../adr/ADR-00785.md)). | • Like `writeFileSync`, but appends instead of truncating (creates the file if it doesn't exist). Throws on failure<br>• Same string/`ArrayBuffer`/TypedArray dispatch as `writeFileSync`<br>• Optional third argument `'utf8'` / `{ encoding: 'utf8', flag }` accepted (the flag is a no-op — it always appends) ([ADR-00785](../adr/ADR-00785.md)) |
| `fs.existsSync(path)` | ✅ | | • Plain existence check via POSIX `access()`<br>• Deliberately does **not** throw for a missing path — matches real Node's own `existsSync`, one of the few `fs` functions that reports "doesn't exist" as `false` rather than an error |
| `fs.statSync(path)` / `fs.lstatSync(path)` | ✅ | • The full numeric Stats surface is exposed — `dev`/`mode`/`nlink`/`uid`/`gid`/`rdev`/`blksize`/`ino`/`size`/`blocks`/`atimeMs`/`mtimeMs`/`ctimeMs`/`birthtimeMs` plus `isFile()`/`isDirectory()`/`isSymbolicLink()` ([ADR-00565](../adr/ADR-00565.md)) — but not the Date-valued `atime`/`mtime`/`ctime`/`birthtime` accessors; times are integer milliseconds only ([ADR-00495](../adr/ADR-00495.md)/[ADR-00497](../adr/ADR-00497.md))<br>• `birthtimeMs` is `0` on Linux — glibc's `struct stat` carries no creation time (it needs `statx`); Darwin reports the real `st_birthtimespec` | |
| `fs.rmSync` / `realpathSync` / `mkdtempSync` / `symlinkSync` / `linkSync` / `readlinkSync` / `chmodSync` / `truncateSync` / `accessSync` | ✅ | • `rmSync` options must be a `{recursive, force}` boolean-literal object; no `maxRetries`/`retryDelay` (a Windows-centric retry knob — the retryable errors don't arise on the POSIX removal path)<br>• `readlinkSync` reads targets of any length (a growing buffer — [ADR-00788](../adr/ADR-00788.md)); `accessSync`'s mode defaults to existence-only (F_OK) and `fs.constants.F_OK`/`R_OK`/`W_OK`/`X_OK` name the others ([ADR-00795](../adr/ADR-00795.md)) | • `linkSync(existing, newPath)` makes a hard link — a second name for one inode, so `nlink` rises and the content outlives unlinking the original; on Windows it is `CreateHardLinkW` (unprivileged, unlike `symlinkSync` which needs Developer Mode) ([ADR-00751](../adr/ADR-00751.md)) |
| `fs.openSync` / `closeSync` / `readSync` / `writeSync` / `fstatSync` | ✅ | • `openSync` flags must be a string literal (`r r+ w w+ a a+ wx ax`) or a raw numeric mask — `fs.constants` has the access/copyFile constants but not the host-specific `O_*` open flags ([ADR-00795](../adr/ADR-00795.md))<br>• `writeSync` writes string data only (no Buffer/offset/length variant); `readSync`'s buffer must be a `Uint8Array`/`Buffer` ([ADR-00498](../adr/ADR-00498.md)) | • On Windows, files open through `CreateFileW` with `FILE_SHARE_DELETE`, so a file this process holds open can still be unlinked or renamed (matching POSIX/Node); `a`/`a+` use a kernel append (`FILE_APPEND_DATA`), not a CRT seek-then-write ([ADR-00743](../adr/ADR-00743.md))<br>• On Windows, a path at/over `MAX_PATH` gets the `\\?\` extended-length prefix (after `GetFullPathNameW` normalization), so fs ops on paths longer than 260 chars work instead of failing path-too-long — across every fs call, not just `openSync` ([ADR-00748](../adr/ADR-00748.md)) |
| `fs.fsyncSync(fd)` / `fdatasyncSync(fd)` / `ftruncateSync(fd[, len])` | ✅ | • `fdatasyncSync` is an alias for `fsyncSync` (the data-only optimization isn't observable in the file's contents)<br>• A bad fd throws a coded `Error` (`e.code === 'EBADF'`); the message is `fd operation failed '': <reason>`, not Node's `EBADF: …, fsync` form; `err.syscall` (`'fsync'`/`'ftruncate'`/`'fstat'` — [ADR-00768](../adr/ADR-00768.md)) and the numeric `err.errno` ([ADR-00770](../adr/ADR-00770.md)) match Node; only `err.path` is absent (fd ops carry no path in Node either) | • `fsyncSync`/`fdatasyncSync` flush an open fd to disk; `ftruncateSync` resizes it (shrinks or zero-fill-grows), `len` defaulting to 0 as in Node<br>• On POSIX these are the `fsync`/`ftruncate` syscalls; on Windows they are Win32 shims (`FlushFileBuffers` / `SetFilePointerEx`+`SetEndOfFile`), so the same code works on every host ([ADR-00750](../adr/ADR-00750.md)) |
| `fs.utimesSync(path, …)` / `fs.futimesSync(fd, …)` | ✅ | • `atime`/`mtime` accept a number or `Date` only — a numeric string (which Node also coerces) is not supported | • Set the access/modification times, by path (`utimesSync`) or open fd (`futimesSync` — [ADR-00788](../adr/ADR-00788.md)). `atime`/`mtime` are a number of seconds (Node's number form) or a `Date` (its epoch); sub-second precision is carried as microseconds<br>• On POSIX these are `utimes(2)`/`futimes(2)`; on Windows a `SetFileTime` shim (Unix time → the 1601-epoch `FILETIME`, keyed on the path or `_get_osfhandle(fd)`), so the same code works on every host ([ADR-00755](../adr/ADR-00755.md), [ADR-00788](../adr/ADR-00788.md)) |
| `fs.unlinkSync(path)` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)). | • Deletes a file. Throws on failure<br>• A directory throws as Node does per host (`EISDIR` on Linux, `EPERM` on macOS/Windows) instead of being silently removed ([ADR-00736](../adr/ADR-00736.md)) |
| `fs.mkdirSync(path)` | ✅ | • Options support only the literal `{ recursive: true }` (creates every missing prefix, idempotent — [ADR-00487](../adr/ADR-00487.md)); the plain form still throws on an existing path or missing parent | • Creates a directory via POSIX `mkdir()`, mode `0777` reduced by the process umask as usual<br>• See [ADR-00027](../adr/ADR-00027.md) |
| `fs.rmdirSync(path[, { recursive: true }])` | ✅ | | • Removes an *empty* directory via POSIX `rmdir()` — deliberately directory-only (fails on a plain file, unlike `remove()`/`unlinkSync`)<br>• `{ recursive: true }` removes the whole tree by routing to `rmSync`'s recursive removal ([ADR-00578](../adr/ADR-00578.md)); Node deprecated the option on `rmdirSync` in favor of `rmSync` but still honors it, and a missing path still throws (no `force`)<br>• See [ADR-00027](../adr/ADR-00027.md) |
| `fs.readdirSync(path[, { withFileTypes, recursive }])` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)).<br>• `Dirent` exposes `.name`/`.parentPath` and all seven kind predicates ([ADR-00787](../adr/ADR-00787.md)); the `.path` alias for `.parentPath` is absent, and a filesystem returning `DT_UNKNOWN` (rare) reports all kinds `false` rather than falling back to `lstat`<br>• `{ recursive: true, withFileTypes: true }` together is a clean rejection (recursive Dirents need `parentPath`); a `DT_UNKNOWN` filesystem is not descended; `encoding: 'buffer'` remains unsupported; recursive paths join with `'/'` (Node uses the platform separator). | • Lists a directory's entries (excluding `.`/`..`) as a `string[]`, in whatever order the OS's own `readdir()` returns them — no ordering guarantee, matching real Node<br>• Built from `struct dirent`'s `d_name` field at a `runtime.GOOS`-conditional byte offset, independently verified by a compiled `offsetof` probe on both Darwin and (via Docker, see [ADR-00051](../adr/ADR-00051.md)) x86-64 Linux<br>• `{ withFileTypes: true }` returns `Dirent[]` — each with `.name`, `.parentPath` (the directory it was read from), and the full kind-predicate set `.isFile()`/`.isDirectory()`/`.isSymbolicLink()`/`.isFIFO()`/`.isCharacterDevice()`/`.isBlockDevice()`/`.isSocket()`, all classified from the entry's `d_type` (from the `FindFirstFile` attributes on Windows, whose `dirent` has none) with no per-file `stat` ([ADR-00752](../adr/ADR-00752.md), [ADR-00787](../adr/ADR-00787.md))<br>• `{ recursive: true }` walks the whole subtree, returning every nested entry as a `'/'`-joined path relative to the start directory (a genuine recursive descent; only the top-level open throws) ([ADR-00786](../adr/ADR-00786.md))<br>• See [ADR-00027](../adr/ADR-00027.md) |
| `fs.renameSync(oldPath, newPath)` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)). | • Renames/moves a file via POSIX `rename()`. Throws on failure<br>• See [ADR-00027](../adr/ADR-00027.md) |
| `fs.copyFileSync(src, dest[, mode])` | ✅ | • Only the error message text differs from Node's `ENOENT: …, <syscall> '<path>'` format — `err.code`/`syscall`/`path`/`errno` all match Node ([ADR-00768](../adr/ADR-00768.md)).<br>• `COPYFILE_FICLONE`/`FICLONE_FORCE` mode bits are accepted but a best-effort no-op (no reflink); `fs.constants.COPYFILE_EXCL`/`COPYFILE_FICLONE`/`COPYFILE_FICLONE_FORCE` name all three ([ADR-00795](../adr/ADR-00795.md)) | • Binary-safe: composes the raw `__kml_fs_read_file_raw`/`__kml_fs_write_file_bytes` helpers ([ADR-00094](../adr/ADR-00094.md)), threading the source's exact byte count, so a file with an embedded null byte copies back whole — no new C-level I/O code<br>• The optional `mode` bitmask is honoured for `COPYFILE_EXCL` (bit `1`): the copy fails with `EEXIST` if `dest` already exists (an atomic `O_EXCL` guard), instead of the default overwrite ([ADR-00788](../adr/ADR-00788.md))<br>• See [ADR-00027](../adr/ADR-00027.md)/[ADR-00525](../adr/ADR-00525.md) |
| Async variants — callback (`fs.readFile(path, cb)`) + Promise (`fs.promises.readFile` / `import from 'fs/promises'`) | ✅ | • Both the callback form (`fs.readFile(path, cb)`, firing from a GC-safe settle reaction) and a binary `writeFile`/`appendFile` (an `ArrayBuffer`/`TypedArray` body, copied raw at submit) run on the pool now, off the loop thread ([ADR-00820](../adr/ADR-00820.md)). The pool is POSIX-first — Windows keeps the inline (delivery-only async) path pending the reactor work ([TDD-00185](../tdd/TDD-00185.md))<br>• `readFile` delivers a string (text) — inherits `readFileSync`'s text-only caveat; no async binary (`Buffer`) form<br>• No `options`/`encoding` argument; no *async* `stat` (the sync `fs.statSync`/`lstatSync` exist — see the row above — but have no callback/Promise form yet) | • Covers the sync set: `readFile`/`writeFile`/`appendFile`/`unlink`/`mkdir`/`rmdir`/`rename`/`copyFile`/`readdir`. Callback gets `(err[, data])`; the Promise rejects with the same `Error` the sync form throws<br>• Reuses each `*Sync` runtime helper verbatim, catching its throw via the compiler's own `setjmp` catch primitive — no parallel non-throwing I/O code |
| `fs.createReadStream` / `fs.createWriteStream` | ✅ | • Chunks are **strings**, not `Buffer`s — text-first, like `readFileSync` (an embedded NUL truncates a chunk); binary (`Uint8Array`) streaming is a follow-on<br>• Consumer backpressure is honored ([ADR-00821](../adr/ADR-00821.md)): the pool worker reads one chunk per demand (credit-gated on the readable's high-water mark), at most ~one chunk outstanding; a mid-read failure surfaces as a stream `'error'`. On Windows the pre-pool eager read-to-EOF fill is kept (the pool runtime is POSIX-only) pending the reactor work ([TDD-00186](../tdd/TDD-00186.md))<br>• Options: `createReadStream` takes `{ highWaterMark }`, `createWriteStream` `{ flags: "w"\|"a" }` — both compile-time literals; `start`/`end`/`fd`/`encoding` are clean errors | • `createReadStream` returns a Node `Readable` (consumable via `for await` or `.on('data')`/`.on('end')`/`.pipe()`); `createWriteStream` a Node `Writable` (`.write`/`.end`, `'finish'`/`'close'`/`'drain'`). A read→write `.pipe()` round-trips. A failed open throws a catchable `Error` (like `readFileSync`) ([TDD-00108](../tdd/TDD-00108.md)/[ADR-00343](../adr/ADR-00343.md))<br>• Also wired `for await` over any Node `Readable` (was WHATWG-only) |
| `fs.watch(path, listener)` → `FSWatcher` | ✅ | • `filename` is faithful on Linux (inotify names the changed entry) but is the **watched path** on macOS — kqueue/`EVFILT_VNODE` watches an fd, not a directory's entries, so it cannot name a changed child (Node uses FSEvents there); a documented divergence, the file-watch case (the common one) is faithful ([ADR-00758](../adr/ADR-00758.md))<br>• One listener slot per event (`.on('change'|'rename', …)`, arrow-literal only) — the `Worker`/`ChildProcess` posture, not the full `EventEmitter`<br>• `recursive` is honoured natively where the OS supports it (macOS/Windows); on Linux (inotify) it watches only the named path — not the subtree<br>• `encoding` accepts only `'utf8'`; no coalescing/ordering guarantees (as in Node, event streams are platform-dependent) | • The faithful, event-driven watch — a native OS change source folded into the `select()` loop via the standard hook trio (keepalive/fdset/dispatch), no `stat`-poll. Linux `inotify` and macOS `kqueue`/`EVFILT_VNODE` are readable fds (no thread); Windows `ReadDirectoryChangesW` runs on a watcher thread that signals a loopback wakeup socket<br>• `.close()` removes the wait fd and drops the keepalive so a watch-only program can exit; `{ persistent }` maps to the keepalive<br>• See [TDD-00181](../tdd/TDD-00181.md) |
| `File` / `FileReader` / `FileSystemFileHandle` (browser-flavored File API) | ❌ | | **Differentiator (scoped — [TDD-00172](../tdd/TDD-00172.md)).** Splits in two: the **data layer** (`File extends Blob`, `FileReader.readAsText`/`ArrayBuffer`/`DataURL`) is a thin fs-backed reinterpretation with no UI — path-backed `File`s, reads firing `onload` on the event loop; the **picker layer** (`showOpenFilePicker` → `FileSystemFileHandle`) resolves a path through a pluggable UI backend — a `klain:tui` file-picker widget, a `klain:webview` native dialog, or an explicit-path degradation when headless. Serves interactive native apps (TUI file managers/editors); the data layer is the near-term, standalone-useful piece. |
