{
  "id": "url",
  "file": "URL.md",
  "title": "URL",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index."
    },
    {
      "kind": "coverage",
      "style": "standard"
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format)."
    },
    {
      "kind": "table",
      "table": {
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`URL`",
            "status": "implemented",
            "notes": [
              "Parses via libcurl's URL API (`curl_url`/`curl_url_set`/`curl_url_get`) rather than a hand-rolled parser — the same \"reuse the already-linked C library\" approach `fetch` established",
              "`new URL(url, base)` resolves a relative `url` against `base` ([ADR-00579](../adr/ADR-00579.md)) — curl's own relative resolution: an absolute `url` overwrites `base`, and an invalid `base` throws (\"Invalid URL\"), matching Node",
              "Component **setters** (`url.hash = …`, `url.protocol = …`, etc.) re-parse the URL and re-derive every field so the components never desync ([ADR-00572](../adr/ADR-00572.md)): marker normalization matches Node (`#`/`?` on hash/search, a trailing `:` on protocol are tolerated); an invalid `href` assignment throws, the other setters silently ignore an invalid value (Node's own behavior); `searchParams` re-populates from the new query",
              "Setters cover `href`/`protocol`/`host`/`hostname`/`port`/`pathname`/`search`/`hash`/`username`/`password` ([ADR-00577](../adr/ADR-00577.md)); `host` splits on the first `:` into hostname + port, and a value with no port keeps the existing port (Node's own behavior)",
              "Fields: `href`, `protocol`, `host`, `hostname`, `port`, `pathname`, `search`, `hash`, `origin`, `username`, `password`, `searchParams` — all plain reads via the ordinary object field-access path, no dispatched methods",
              "WHATWG normalization ([ADR-00874](../adr/ADR-00874.md)/[TDD-00203](../tdd/TDD-00203.md)): a default port is stripped (`new URL('http://example.com:80/x').port` is `''`) and the host is lowercased (`new URL('HTTP://ExAmple.COM/x').hostname` is `'example.com'`)",
              "The WHATWG **static** methods `URL.parse(input[, base])` → `URL \\| null` and `URL.canParse(input[, base])` → `boolean` are implemented (non-throwing, unlike `new URL()`) — distinct from the legacy lowercase `url.parse()` module function below",
              "`JSON.stringify(url)` honors `URL.prototype.toJSON()` (returns `href`), so it serializes the `href` string, not the component struct",
              "A part absent from the URL (no port/query/fragment) reads as `\"\"`, matching real `URL`'s own default",
              "Throws a catchable Error (\"Invalid URL\") on a malformed URL",
              "`URL`/`URLSearchParams` are ambient globals — no import needed, and identical in browsers and Node (Node also re-exports them from `node:url`); the legacy `url.*` helpers below are Node-only and require `import … from 'node:url'`",
              "See [ADR-00076](../adr/ADR-00076.md)"
            ],
            "caveats": [
              "libcurl-backed, so a non-special scheme is rejected where WHATWG accepts it (`new URL('foo:bar')` throws; Node: valid with `pathname` `'bar'`) — faithful non-special-scheme parsing needs a hand-written WHATWG parser rather than libcurl ([TDD-00203](../tdd/TDD-00203.md)). Default-port stripping and host-case normalization are now done (see notes)."
            ]
          },
          {
            "feature": "`URLSearchParams`",
            "status": "implemented",
            "caveats": [
              "`url.searchParams` is a **snapshot**, not live: mutating it (`url.searchParams.append(...)`) does not write back into `url.search`/`url.href`. Re-serialize and assign `url.search = params.toString()` to apply changes ([TDD-00203](../tdd/TDD-00203.md)).",
              "Spreading a `URLSearchParams` directly (`[...params]`) yields an empty array — the same pre-existing limitation as `[...map]`; use `[...params.entries()]` (which works). Not URLSearchParams-specific."
            ],
            "notes": [
              "Backed by an ordered name/value **pair list** (the WHATWG \"list of tuples\", [ADR-00873](../adr/ADR-00873.md)/[TDD-00203](../tdd/TDD-00203.md)) — replaces the former `Map<string,string>`, so duplicate keys and cross-key insertion order round-trip faithfully (`?a=1&b=2&a=3` → `getAll('a')` is `['1','3']`)",
              "Full method surface: `append`/`set` (replace-first-drop-rest)/`get` (first match, `null` if absent)/`getAll`/`has`/`delete`/`sort` (stable, by key)/`size`, plus `keys()`/`values()`/`entries()`/`forEach()`, `for…of` iteration (default iterator = entries), and `console.log` rendering — `has`/`delete` accept the WHATWG optional 2nd `value` argument",
              "`toString()` serializes application/x-www-form-urlencoded (a space is `+`, not `%20`); a non-string name/value is stringified (`append('n', 1)` → `\"1\"`); `params + ''` / `` `${params}` `` serialize via `toString`",
              "`new URLSearchParams(init)`: a query string (leading `?` tolerated), an array of `[name, value]` pairs, a record object, or another `URLSearchParams` (copied)",
              "See [ADR-00076](../adr/ADR-00076.md)"
            ]
          },
          {
            "feature": "`URLPattern`",
            "status": "implemented",
            "caveats": [
              "Object-literal init only, over `protocol`/`hostname`/`port`/`pathname`/`search`/`hash`/`username`/`password` ([ADR-00585](../adr/ADR-00585.md)) — no constructor-string form, no `baseURL`",
              "Pattern grammar is literals, `\\` escapes, `*`, `:name`, and `:name?` — `{}` groups, `+` modifiers, and inline `(regex)` groups throw a `TypeError` at construction",
              "`.exec()` returns a merged `Map<string, string> \\| null` of every named group across components (an unset optional group is absent), not the spec's per-component `URLPatternResult` object — needs the deferred dynamic object model ([TDD-00068](../tdd/TDD-00068.md))",
              "`*` wildcards aren't exposed as numbered groups; no `hasRegExpGroups`"
            ],
            "notes": [
              "Each component pattern compiles once at construction to an anchored PCRE2 regex (embedded C runtime); input URLs parse via libcurl's URL API, so `.test`/`.exec` link `-lpcre2-8` + `-lcurl` — both already conditional. An unparseable input is `false`/`null`, never a throw; an omitted init component defaults to `*` and reads back as such from the component fields ([TDD-00100](../tdd/TDD-00100.md)/[ADR-00311](../adr/ADR-00311.md))"
            ]
          },
          {
            "feature": "`url.parse(urlString)` (legacy)",
            "status": "implemented",
            "caveats": [
              "Absolute URLs only — a malformed/relative input throws `Invalid URL` (like `new URL()`), rather than Node's never-throw lenient object ([ADR-00669](../adr/ADR-00669.md)/[TDD-00165](../tdd/TDD-00165.md) Stage 4)",
              "An absent component is `\"\"`, not Node's `null` (same representation as the WHATWG `URL` object above)",
              "`slashes` and the `parseQueryString`-object form of `query` are not produced yet; `url.format`/`fileURLToPath` and the other legacy helpers are separate follow-ons"
            ],
            "notes": [
              "The legacy `Url` shape (`protocol`/`auth`/`host`/`port`/`hostname`/`hash`/`search`/`query`/`pathname`/`path`/`href`), distinct from the WHATWG `URL`; importable named (`import { parse } from 'url'`), via namespace (`import * as url from 'url'`), and via default (`import url from 'node:url'`)"
            ]
          },
          {
            "feature": "`url.format(urlObject)` (legacy)",
            "status": "implemented",
            "caveats": [
              "The `options` argument (`auth`/`fragment`/`search`/`unicode` toggles) is not supported; components are taken verbatim, no re-encoding ([ADR-00670](../adr/ADR-00670.md))",
              "Reconstructs `protocol // [auth@] host pathname (search|?query) hash`; a WHATWG `URL` argument serializes to its `href`"
            ],
            "notes": [
              "The inverse of `url.parse` — round-trips a legacy `Url` object back to a string, and also accepts a plain object with the legacy fields; importable named, via namespace, or via default"
            ]
          },
          {
            "feature": "`url.fileURLToPath(url)` (POSIX)",
            "status": "implemented",
            "caveats": [
              "POSIX only; the `options` (`windows`) argument is unsupported, and a non-`localhost` host is ignored rather than rejected ([ADR-00671](../adr/ADR-00671.md))"
            ],
            "notes": [
              "Converts a `file:` URL (string or WHATWG `URL`) to the percent-decoded filesystem path; throws on a non-`file:` scheme"
            ]
          },
          {
            "feature": "`url.pathToFileURL(path)` (POSIX)",
            "status": "implemented",
            "caveats": [
              "POSIX only; the `options` argument is unsupported ([ADR-00671](../adr/ADR-00671.md))"
            ],
            "notes": [
              "Resolves the path to absolute, percent-encodes it, and returns a WHATWG `URL` with the `file:` scheme"
            ]
          },
          {
            "feature": "`url.resolve(from, to)` (legacy)",
            "status": "implemented",
            "caveats": [
              "Absolute bases only — a scheme-less/malformed base throws `Invalid URL`, the same leniency gap as `url.parse` ([ADR-00672](../adr/ADR-00672.md))"
            ],
            "notes": [
              "Resolves `to` against the base `from` (the `new URL(to, from)` resolution), returning the resulting URL string"
            ]
          },
          {
            "feature": "`url.urlToHttpOptions(url)`",
            "status": "implemented",
            "caveats": [
              "Expects a WHATWG `URL`; a legacy `Url`/plain object isn't accepted ([ADR-00672](../adr/ADR-00672.md))"
            ],
            "notes": [
              "Remaps a `URL` into the `http.request` option bag (`protocol`/`hostname`/`hash`/`search`/`pathname`/`path`/`href`/`port`/`auth`)"
            ]
          },
          {
            "feature": "`url.domainToASCII(domain)` / `url.domainToUnicode(domain)`",
            "status": "implemented",
            "caveats": [
              "IDN conversion of a **non-ASCII** domain requires the libcurl build to include an IDN backend (libidn2) — present on typical Linux, **absent on the Mac build** where a non-ASCII domain returns `\"\"` (Node's own failure contract). ASCII domains pass through everywhere ([ADR-00672](../adr/ADR-00672.md))"
            ],
            "notes": [
              "Punycode/IDN conversion via libcurl (`CURLU_PUNYCODE`/`CURLU_PUNY2IDN`); returns `\"\"` on failure, matching Node"
            ]
          }
        ]
      }
    }
  ]
}
