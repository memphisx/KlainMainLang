{
  "id": "file-system",
  "file": "FILE-SYSTEM.md",
  "title": "File System (fs)",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index. Node-`fs`-shaped synchronous file I/O for reading/writing config, data, and logs — not `File`/`FileReader`/`FileSystemFileHandle` (those model browser sandbox/permission concepts that don't exist for a native CLI/microservice program with direct filesystem access)."
    },
    {
      "kind": "coverage",
      "style": "standard"
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format)."
    },
    {
      "kind": "table",
      "table": {
        "featureColumn": "Feature",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": true,
        "rows": [
          {
            "feature": "`fs.readFileSync(path[, 'utf8'])`",
            "status": "implemented",
            "caveats": [
              "Text-only by design — a file with embedded null bytes reads back shorter than its real size (`.length` is `strlen`-based); use `fs.readFileSyncBytes(path)` for a binary file",
              "With no encoding the result is still a `string`, not a `Buffer` (the text-only default above); a non-`'utf8'` encoding is a clean rejection ([ADR-00785](../adr/ADR-00785.md))"
            ],
            "notes": [
              "Reads the whole file into a string",
              "The optional encoding argument accepts `'utf8'`/`'utf-8'` (a bare string or `{ encoding: 'utf8' }`) — a faithful no-op since strings are UTF-8, so the everyday `readFileSync(path, 'utf8')` idiom compiles ([ADR-00785](../adr/ADR-00785.md))",
              "Throws a catchable `Error` (built from `strerror(errno)`) if the file can't be opened",
              "See [ADR-00023](../adr/ADR-00023.md)"
            ]
          },
          {
            "feature": "`fs.readFileSyncBytes(path)`",
            "status": "implemented",
            "notes": [
              "Binary-safe sibling of `readFileSync`: returns a `Uint8Array` with the file's exact byte count (no `strlen` involved), so a file with an embedded null byte reads back whole",
              "No `Buffer` class — this compiler has none; plain `ArrayBuffer`/TypedArrays instead",
              "See [ADR-00094](../adr/ADR-00094.md)"
            ],
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format."
            ]
          },
          {
            "feature": "`fs.writeFileSync(path, data[, opts])`",
            "status": "implemented",
            "notes": [
              "Creates or truncates the file with `data`. Throws on failure",
              "`data` may be a string (the original, `strlen`-based path, unchanged) or an `ArrayBuffer`/TypedArray (binary-safe, writes the real byte count via `fwrite` directly — [ADR-00094](../adr/ADR-00094.md)), dispatched on `data`'s inferred type",
              "Optional third argument: `'utf8'` (or `{ encoding: 'utf8', flag }`); `{ flag: 'a' }` appends instead of truncating, `{ flag: 'w' }` (default) truncates ([ADR-00785](../adr/ADR-00785.md))"
            ],
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format.",
              "The options argument supports only `{ encoding: 'utf8', flag }` — `mode` and non-`'utf8'` encodings are clean rejections ([ADR-00785](../adr/ADR-00785.md))."
            ]
          },
          {
            "feature": "`fs.appendFileSync(path, data[, opts])`",
            "status": "implemented",
            "notes": [
              "Like `writeFileSync`, but appends instead of truncating (creates the file if it doesn't exist). Throws on failure",
              "Same string/`ArrayBuffer`/TypedArray dispatch as `writeFileSync`",
              "Optional third argument `'utf8'` / `{ encoding: 'utf8', flag }` accepted (the flag is a no-op — it always appends) ([ADR-00785](../adr/ADR-00785.md))"
            ],
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format.",
              "The options argument supports only `{ encoding: 'utf8', flag }` — `mode` and non-`'utf8'` encodings are clean rejections ([ADR-00785](../adr/ADR-00785.md))."
            ]
          },
          {
            "feature": "`fs.existsSync(path)`",
            "status": "implemented",
            "notes": [
              "Plain existence check via POSIX `access()`",
              "Deliberately does **not** throw for a missing path — matches real Node's own `existsSync`, one of the few `fs` functions that reports \"doesn't exist\" as `false` rather than an error"
            ]
          },
          {
            "feature": "`fs.statSync(path)` / `fs.lstatSync(path)`",
            "status": "implemented",
            "caveats": [
              "The full numeric Stats surface is exposed — `dev`/`mode`/`nlink`/`uid`/`gid`/`rdev`/`blksize`/`ino`/`size`/`blocks`/`atimeMs`/`mtimeMs`/`ctimeMs`/`birthtimeMs` plus `isFile()`/`isDirectory()`/`isSymbolicLink()` ([ADR-00565](../adr/ADR-00565.md)) — but not the Date-valued `atime`/`mtime`/`ctime`/`birthtime` accessors; times are integer milliseconds only ([ADR-00495](../adr/ADR-00495.md)/[ADR-00497](../adr/ADR-00497.md))",
              "`birthtimeMs` is `0` on Linux — glibc's `struct stat` carries no creation time (it needs `statx`); Darwin reports the real `st_birthtimespec`"
            ]
          },
          {
            "feature": "`fs.rmSync` / `realpathSync` / `mkdtempSync` / `symlinkSync` / `linkSync` / `readlinkSync` / `chmodSync` / `truncateSync` / `accessSync`",
            "status": "implemented",
            "notes": [
              "`linkSync(existing, newPath)` makes a hard link — a second name for one inode, so `nlink` rises and the content outlives unlinking the original; on Windows it is `CreateHardLinkW` (unprivileged, unlike `symlinkSync` which needs Developer Mode) ([ADR-00751](../adr/ADR-00751.md))"
            ],
            "caveats": [
              "`rmSync` options must be a `{recursive, force}` boolean-literal object; no `maxRetries`/`retryDelay` (a Windows-centric retry knob — the retryable errors don't arise on the POSIX removal path)",
              "`readlinkSync` reads targets of any length (a growing buffer — [ADR-00788](../adr/ADR-00788.md)); `accessSync`'s mode defaults to existence-only (F_OK) and `fs.constants.F_OK`/`R_OK`/`W_OK`/`X_OK` name the others ([ADR-00795](../adr/ADR-00795.md))"
            ]
          },
          {
            "feature": "`fs.openSync` / `closeSync` / `readSync` / `writeSync` / `fstatSync`",
            "status": "implemented",
            "notes": [
              "On Windows, files open through `CreateFileW` with `FILE_SHARE_DELETE`, so a file this process holds open can still be unlinked or renamed (matching POSIX/Node); `a`/`a+` use a kernel append (`FILE_APPEND_DATA`), not a CRT seek-then-write ([ADR-00743](../adr/ADR-00743.md))",
              "On Windows, a path at/over `MAX_PATH` gets the `\\\\?\\` extended-length prefix (after `GetFullPathNameW` normalization), so fs ops on paths longer than 260 chars work instead of failing path-too-long — across every fs call, not just `openSync` ([ADR-00748](../adr/ADR-00748.md))"
            ],
            "caveats": [
              "`openSync` flags must be a string literal (`r r+ w w+ a a+ wx ax`) or a raw numeric mask — `fs.constants` has the access/copyFile constants but not the host-specific `O_*` open flags ([ADR-00795](../adr/ADR-00795.md))",
              "`writeSync` writes string data only (no Buffer/offset/length variant); `readSync`'s buffer must be a `Uint8Array`/`Buffer` ([ADR-00498](../adr/ADR-00498.md))"
            ]
          },
          {
            "feature": "`fs.fsyncSync(fd)` / `fdatasyncSync(fd)` / `ftruncateSync(fd[, len])`",
            "status": "implemented",
            "notes": [
              "`fsyncSync`/`fdatasyncSync` flush an open fd to disk; `ftruncateSync` resizes it (shrinks or zero-fill-grows), `len` defaulting to 0 as in Node",
              "On POSIX these are the `fsync`/`ftruncate` syscalls; on Windows they are Win32 shims (`FlushFileBuffers` / `SetFilePointerEx`+`SetEndOfFile`), so the same code works on every host ([ADR-00750](../adr/ADR-00750.md))"
            ],
            "caveats": [
              "`fdatasyncSync` is an alias for `fsyncSync` (the data-only optimization isn't observable in the file's contents)",
              "A bad fd throws a coded `Error` (`e.code === 'EBADF'`); the message is `fd operation failed '': <reason>`, not Node's `EBADF: …, fsync` form; `err.syscall` (`'fsync'`/`'ftruncate'`/`'fstat'` — [ADR-00768](../adr/ADR-00768.md)) and the numeric `err.errno` ([ADR-00770](../adr/ADR-00770.md)) match Node; only `err.path` is absent (fd ops carry no path in Node either)"
            ]
          },
          {
            "feature": "`fs.utimesSync(path, …)` / `fs.futimesSync(fd, …)`",
            "status": "implemented",
            "notes": [
              "Set the access/modification times, by path (`utimesSync`) or open fd (`futimesSync` — [ADR-00788](../adr/ADR-00788.md)). `atime`/`mtime` are a number of seconds (Node's number form) or a `Date` (its epoch); sub-second precision is carried as microseconds",
              "On POSIX these are `utimes(2)`/`futimes(2)`; on Windows a `SetFileTime` shim (Unix time → the 1601-epoch `FILETIME`, keyed on the path or `_get_osfhandle(fd)`), so the same code works on every host ([ADR-00755](../adr/ADR-00755.md), [ADR-00788](../adr/ADR-00788.md))"
            ],
            "caveats": [
              "`atime`/`mtime` accept a number or `Date` only — a numeric string (which Node also coerces) is not supported"
            ]
          },
          {
            "feature": "`fs.unlinkSync(path)`",
            "status": "implemented",
            "notes": [
              "Deletes a file. Throws on failure",
              "A directory throws as Node does per host (`EISDIR` on Linux, `EPERM` on macOS/Windows) instead of being silently removed ([ADR-00736](../adr/ADR-00736.md))"
            ],
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format."
            ]
          },
          {
            "feature": "`fs.mkdirSync(path)`",
            "status": "implemented",
            "caveats": [
              "Options support only the literal `{ recursive: true }` (creates every missing prefix, idempotent — [ADR-00487](../adr/ADR-00487.md)); the plain form still throws on an existing path or missing parent"
            ],
            "notes": [
              "Creates a directory via POSIX `mkdir()`, mode `0777` reduced by the process umask as usual",
              "See [ADR-00027](../adr/ADR-00027.md)"
            ]
          },
          {
            "feature": "`fs.rmdirSync(path[, { recursive: true }])`",
            "status": "implemented",
            "notes": [
              "Removes an *empty* directory via POSIX `rmdir()` — deliberately directory-only (fails on a plain file, unlike `remove()`/`unlinkSync`)",
              "`{ recursive: true }` removes the whole tree by routing to `rmSync`'s recursive removal ([ADR-00578](../adr/ADR-00578.md)); Node deprecated the option on `rmdirSync` in favor of `rmSync` but still honors it, and a missing path still throws (no `force`)",
              "See [ADR-00027](../adr/ADR-00027.md)"
            ]
          },
          {
            "feature": "`fs.readdirSync(path[, { withFileTypes, recursive }])`",
            "status": "implemented",
            "notes": [
              "Lists a directory's entries (excluding `.`/`..`) as a `string[]`, in whatever order the OS's own `readdir()` returns them — no ordering guarantee, matching real Node",
              "Built from `struct dirent`'s `d_name` field at a `runtime.GOOS`-conditional byte offset, independently verified by a compiled `offsetof` probe on both Darwin and (via Docker, see [ADR-00051](../adr/ADR-00051.md)) x86-64 Linux",
              "`{ withFileTypes: true }` returns `Dirent[]` — each with `.name`, `.parentPath` (the directory it was read from), and the full kind-predicate set `.isFile()`/`.isDirectory()`/`.isSymbolicLink()`/`.isFIFO()`/`.isCharacterDevice()`/`.isBlockDevice()`/`.isSocket()`, all classified from the entry's `d_type` (from the `FindFirstFile` attributes on Windows, whose `dirent` has none) with no per-file `stat` ([ADR-00752](../adr/ADR-00752.md), [ADR-00787](../adr/ADR-00787.md))",
              "`{ recursive: true }` walks the whole subtree, returning every nested entry as a `'/'`-joined path relative to the start directory (a genuine recursive descent; only the top-level open throws) ([ADR-00786](../adr/ADR-00786.md))",
              "See [ADR-00027](../adr/ADR-00027.md)"
            ],
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format.",
              "`Dirent` exposes `.name`/`.parentPath` and all seven kind predicates ([ADR-00787](../adr/ADR-00787.md)); the `.path` alias for `.parentPath` is absent, and a filesystem returning `DT_UNKNOWN` (rare) reports all kinds `false` rather than falling back to `lstat`",
              "`{ recursive: true, withFileTypes: true }` together is a clean rejection (recursive Dirents need `parentPath`); a `DT_UNKNOWN` filesystem is not descended; `encoding: 'buffer'` remains unsupported; recursive paths join with `'/'` (Node uses the platform separator)."
            ]
          },
          {
            "feature": "`fs.renameSync(oldPath, newPath)`",
            "status": "implemented",
            "notes": [
              "Renames/moves a file via POSIX `rename()`. Throws on failure",
              "See [ADR-00027](../adr/ADR-00027.md)"
            ],
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format."
            ]
          },
          {
            "feature": "`fs.copyFileSync(src, dest[, mode])`",
            "status": "implemented",
            "caveats": [
              "`err.code` matches Node (`e.code === 'ENOENT'`/`'EEXIST'`/… — [ADR-00684](../adr/ADR-00684.md)); `err.syscall` and `err.path` also match Node ([ADR-00768](../adr/ADR-00768.md)); the numeric `err.errno` also matches Node ([ADR-00770](../adr/ADR-00770.md) — the negated Linux value on Windows, not Node's `UV_E*` number); only the message text differs from Node's `ENOENT: …, <syscall> '<path>'` format.",
              "`COPYFILE_FICLONE`/`FICLONE_FORCE` mode bits are accepted but a best-effort no-op (no reflink); `fs.constants.COPYFILE_EXCL`/`COPYFILE_FICLONE`/`COPYFILE_FICLONE_FORCE` name all three ([ADR-00795](../adr/ADR-00795.md))"
            ],
            "notes": [
              "Binary-safe: composes the raw `__kml_fs_read_file_raw`/`__kml_fs_write_file_bytes` helpers ([ADR-00094](../adr/ADR-00094.md)), threading the source's exact byte count, so a file with an embedded null byte copies back whole — no new C-level I/O code",
              "The optional `mode` bitmask is honoured for `COPYFILE_EXCL` (bit `1`): the copy fails with `EEXIST` if `dest` already exists (an atomic `O_EXCL` guard), instead of the default overwrite ([ADR-00788](../adr/ADR-00788.md))",
              "See [ADR-00027](../adr/ADR-00027.md)/[ADR-00525](../adr/ADR-00525.md)"
            ]
          },
          {
            "feature": "Async variants — callback (`fs.readFile(path, cb)`) + Promise (`fs.promises.readFile` / `import from 'fs/promises'`)",
            "status": "implemented",
            "caveats": [
              "The `fs.promises` ops are genuinely non-blocking — `readFile`/`writeFile`/`appendFile`/`unlink`/`mkdir`/`rmdir`/`rename`/`copyFile`/`readdir` run on a background thread pool (default 4, `UV_THREADPOOL_SIZE`) and return a pending Promise the loop settles on completion, so other connections/timers keep running during the I/O ([TDD-00185](../tdd/TDD-00185.md)/[ADR-00773](../adr/ADR-00773.md)/[ADR-00774](../adr/ADR-00774.md)). Two cases stay on the inline (delivery-only async, no concurrency gain — [TDD-00107](../tdd/TDD-00107.md)/[ADR-00340](../adr/ADR-00340.md)) path: the legacy **callback** form (`fs.readFile(path, cb)`), and a `writeFile`/`appendFile` whose data is an `ArrayBuffer`/`TypedArray` (its byte-length write). The pool is POSIX-first — Windows keeps the inline path pending the reactor work",
              "`readFile` delivers a string (text) — inherits `readFileSync`'s text-only caveat; no async binary (`Buffer`) form",
              "No `options`/`encoding` argument; no *async* `stat` (the sync `fs.statSync`/`lstatSync` exist — see the row above — but have no callback/Promise form yet)"
            ],
            "notes": [
              "Covers the sync set: `readFile`/`writeFile`/`appendFile`/`unlink`/`mkdir`/`rmdir`/`rename`/`copyFile`/`readdir`. Callback gets `(err[, data])`; the Promise rejects with the same `Error` the sync form throws",
              "Reuses each `*Sync` runtime helper verbatim, catching its throw via the compiler's own `setjmp` catch primitive — no parallel non-throwing I/O code"
            ]
          },
          {
            "feature": "`fs.createReadStream` / `fs.createWriteStream`",
            "status": "implemented",
            "caveats": [
              "Chunks are **strings**, not `Buffer`s — text-first, like `readFileSync` (an embedded NUL truncates a chunk); binary (`Uint8Array`) streaming is a follow-on",
              "`createReadStream` reads on the background thread pool ([TDD-00186](../tdd/TDD-00186.md)/[ADR-00775](../adr/ADR-00775.md)) — the file is opened synchronously (a missing file still throws) then read off the loop thread, each highWaterMark chunk enqueued into the `Readable` in order, so the read no longer freezes the reactor. Consumer backpressure is not yet honored (the worker reads ahead and chunks queue in the readable — the same memory profile the eager path had); true highWaterMark backpressure is a follow-on. On Windows the pre-pool eager read-to-EOF fill is kept (the pool runtime is POSIX-only) pending the reactor work",
              "Options: `createReadStream` takes `{ highWaterMark }`, `createWriteStream` `{ flags: \"w\"\\|\"a\" }` — both compile-time literals; `start`/`end`/`fd`/`encoding` are clean errors"
            ],
            "notes": [
              "`createReadStream` returns a Node `Readable` (consumable via `for await` or `.on('data')`/`.on('end')`/`.pipe()`); `createWriteStream` a Node `Writable` (`.write`/`.end`, `'finish'`/`'close'`/`'drain'`). A read→write `.pipe()` round-trips. A failed open throws a catchable `Error` (like `readFileSync`) ([TDD-00108](../tdd/TDD-00108.md)/[ADR-00343](../adr/ADR-00343.md))",
              "Also wired `for await` over any Node `Readable` (was WHATWG-only)"
            ]
          },
          {
            "feature": "`fs.watch(path, listener)` → `FSWatcher`",
            "status": "implemented",
            "caveats": [
              "`filename` is faithful on Linux (inotify names the changed entry) but is the **watched path** on macOS — kqueue/`EVFILT_VNODE` watches an fd, not a directory's entries, so it cannot name a changed child (Node uses FSEvents there); a documented divergence, the file-watch case (the common one) is faithful ([ADR-00758](../adr/ADR-00758.md))",
              "One listener slot per event (`.on('change'|'rename', …)`, arrow-literal only) — the `Worker`/`ChildProcess` posture, not the full `EventEmitter`",
              "`recursive` is honoured natively where the OS supports it (macOS/Windows); on Linux (inotify) it watches only the named path — not the subtree",
              "`encoding` accepts only `'utf8'`; no coalescing/ordering guarantees (as in Node, event streams are platform-dependent)"
            ],
            "notes": [
              "The faithful, event-driven watch — a native OS change source folded into the `select()` loop via the standard hook trio (keepalive/fdset/dispatch), no `stat`-poll. Linux `inotify` and macOS `kqueue`/`EVFILT_VNODE` are readable fds (no thread); Windows `ReadDirectoryChangesW` runs on a watcher thread that signals a loopback wakeup socket",
              "`.close()` removes the wait fd and drops the keepalive so a watch-only program can exit; `{ persistent }` maps to the keepalive",
              "All three backends verified on their platforms (Linux/Windows/macOS). See [TDD-00181](../tdd/TDD-00181.md) / [ADR-00756](../adr/ADR-00756.md)–[ADR-00758](../adr/ADR-00758.md)"
            ]
          },
          {
            "feature": "`File` / `FileReader` / `FileSystemFileHandle` (browser-flavored File API)",
            "status": "missing",
            "notes": [
              "**Differentiator (scoped — [TDD-00172](../tdd/TDD-00172.md)).** Splits in two: the **data layer** (`File extends Blob`, `FileReader.readAsText`/`ArrayBuffer`/`DataURL`) is a thin fs-backed reinterpretation with no UI — path-backed `File`s, reads firing `onload` on the event loop; the **picker layer** (`showOpenFilePicker` → `FileSystemFileHandle`) resolves a path through a pluggable UI backend — a `klain:tui` file-picker widget, a `klain:webview` native dialog, or an explicit-path degradation when headless. Serves interactive native apps (TUI file managers/editors); the data layer is the near-term, standalone-useful piece."
            ],
            "notesPlain": true
          }
        ]
      }
    }
  ]
}
