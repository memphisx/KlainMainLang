{
  "id": "javascript-builtins",
  "file": "JAVASCRIPT-BUILTINS.md",
  "title": "JavaScript built-in objects (completeness index)",
  "segments": [
    {
      "kind": "block",
      "text": "> Part of the [Implementation Status](README.md) index. A **completeness map of JavaScript's standard built-in objects** (the MDN \"standard built-in objects\" surface + the ubiquitous Web globals) — companion to the [Node modules](NODE-MODULES.md) and [TypeScript features](TYPESCRIPT-FEATURES.md) indexes. It says, at a glance, which built-ins are supported, which work with real limits, which are not started, and which are out of scope for a **whole-program AOT compiler with fixed-shape objects and no runtime JS engine**. This page is **informational**: its rows are *not* counted toward the coverage percentages — the detailed, counted pages (linked per row) carry the per-method detail and the real numbers."
    },
    {
      "kind": "block",
      "text": "Format: [Status page format](README.md#status-page-format). ✅ = the built-in works (see its detail page for caveats); ❌ = not available today. Note: a ✅ here is a *loose* ✅ — per the project's rule, any caveat excludes a row from Strict Coverage, so several built-ins in the first table also appear with their residual limits in the second."
    },
    {
      "kind": "table",
      "table": {
        "heading": "Implemented",
        "featureColumn": "Built-in",
        "hasCaveats": true,
        "hasNotes": true,
        "countsTowardParity": false,
        "rows": [
          {
            "feature": "`Object` (literals, `keys`/`values`/`entries`/`assign`/`freeze`/`seal`/`fromEntries`/`hasOwn`/`groupBy`/`create`/`defineProperty`/`getOwnPropertyDescriptor`/`get`·`setPrototypeOf`)",
            "status": "implemented",
            "notes": [
              "→ [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`Function` — `.call`/`.apply`/`.bind`, overloads, arrows, closures (first-class values)",
            "status": "implemented",
            "notes": [
              "→ [Language constructs](LANGUAGE-CONSTRUCTS.md)"
            ]
          },
          {
            "feature": "`Boolean` / `Boolean(x)`",
            "status": "implemented",
            "notes": [
              "→ [Global functions](GLOBAL-FUNCTIONS.md)"
            ]
          },
          {
            "feature": "`Number` (all statics/constants, `toFixed`/`toString`/`toPrecision`/`toExponential`)",
            "status": "implemented",
            "notes": [
              "→ [Number & Math](NUMBER-MATH.md)"
            ],
            "caveats": [
              "`toExponential()` requires an explicit fraction-digits argument — the zero-argument auto-precision form (`(1.5).toExponential()`, valid in JS) is a compile error."
            ]
          },
          {
            "feature": "`Math` (full surface incl. `cbrt`/`clz32`/`fround`/`imul`/`hypot`/`expm1`/`log1p`)",
            "status": "implemented",
            "notes": [
              "→ [Number & Math](NUMBER-MATH.md)"
            ],
            "caveats": [
              "`Math.max`/`Math.min` require at least two arguments — the zero-arg (JS: `∓Infinity`) and one-arg forms are compile errors."
            ]
          },
          {
            "feature": "`BigInt` + `asIntN`/`asUintN`, arithmetic, literals",
            "status": "implemented",
            "notes": [
              "→ [Type system](TYPE-SYSTEM.md)"
            ]
          },
          {
            "feature": "`String` (~30 methods incl. `raw`, `fromCharCode`/`fromCodePoint`)",
            "status": "implemented",
            "notes": [
              "→ [String methods](STRING-METHODS.md)"
            ]
          },
          {
            "feature": "`Array` (~40 methods incl. ES2023 `toSorted`/`toReversed`/`with`/`findLast`, `from`/`of`/`isArray`)",
            "status": "implemented",
            "notes": [
              "→ [Array methods](ARRAY-METHODS.md)"
            ]
          },
          {
            "feature": "`Map` / `Set` (`set`/`get`/`has`/`delete`/iteration/`size`, `new Map(entries)`)",
            "status": "implemented",
            "notes": [
              "→ [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`WeakMap` / `WeakSet` / `WeakRef` (real weak semantics under `-mm=gc`)",
            "status": "implemented",
            "notes": [
              "→ [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`JSON` (`stringify`/`parse`, dynamic + typed trees, `toJSON`)",
            "status": "implemented",
            "notes": [
              "→ [JSON](JSON.md)"
            ]
          },
          {
            "feature": "`RegExp` (literal + ctor, `exec`/`test`/`match`/`matchAll`/`replace`/`replaceAll`/`split`/`search`) — PCRE2-backed",
            "status": "implemented",
            "notes": [
              "→ [RegExp](REGEXP.md)"
            ]
          },
          {
            "feature": "`Error` + subtypes (`TypeError`/`RangeError`/`SyntaxError`/`EvalError`/`URIError`/`ReferenceError`/`AggregateError`/`DOMException`), `class X extends Error` (1 level)",
            "status": "implemented",
            "notes": [
              "→ [Language constructs](LANGUAGE-CONSTRUCTS.md)"
            ],
            "caveats": [
              "No error-options second argument — `new Error(m, { cause })` fails to parse, so `.cause` is unavailable; `.stack` is typed a number, not a string (`typeof err.stack` is `'number'`, Node: `'string'`); and an Error has no `.toString()` / can't be interpolated (`` `${err}` `` is a compile error)."
            ]
          },
          {
            "feature": "`Symbol` (`Symbol()`/`for`/`keyFor`, `.description`, `typeof`) — opaque unique values",
            "status": "implemented",
            "notes": [
              "→ [Type system](TYPE-SYSTEM.md)"
            ]
          },
          {
            "feature": "`Promise` (`all`/`race`/`allSettled`/`any`/`resolve`/`reject`, executor, `then`/`catch`/`finally`)",
            "status": "implemented",
            "notes": [
              "→ [Language constructs](LANGUAGE-CONSTRUCTS.md)"
            ],
            "caveats": [
              "`Promise.allSettled` reports a rejected element's `.reason` as a wrapped `Error` (stringified message), not the original rejected value — `(await Promise.allSettled([Promise.reject(42)]))[0].reason.message` is `'42'`, where Node gives `.reason === 42`; the always-present `value`/`reason` fields also aren't omitted (a faithful reject-value model is [TDD-00169](../tdd/TDD-00169.md))."
            ]
          },
          {
            "feature": "async functions / `await` / async generators / `for await…of`",
            "status": "implemented",
            "notes": [
              "→ [Language constructs](LANGUAGE-CONSTRUCTS.md)"
            ]
          },
          {
            "feature": "Generators (`function*`, `yield`/`yield*`, `.next(value)`)",
            "status": "implemented",
            "notes": [
              "→ [Language constructs](LANGUAGE-CONSTRUCTS.md)"
            ]
          },
          {
            "feature": "`globalThis`",
            "status": "implemented",
            "notes": [
              "→ [Global functions](GLOBAL-FUNCTIONS.md)"
            ],
            "caveats": [
              "`globalThis` exists only inside `typeof globalThis` — used as a value (property access, assignment, identity), it fails compilation with `undefined variable 'globalThis'`."
            ]
          },
          {
            "feature": "Global functions (`isNaN`/`isFinite`/`parseInt`/`parseFloat`/`encodeURI(Component)`/`decodeURI(Component)`/`atob`/`btoa`)",
            "status": "implemented",
            "notes": [
              "→ [Global functions](GLOBAL-FUNCTIONS.md)"
            ]
          },
          {
            "feature": "`structuredClone`",
            "status": "implemented",
            "notes": [
              "→ [Global functions](GLOBAL-FUNCTIONS.md)"
            ]
          },
          {
            "feature": "`queueMicrotask`",
            "status": "implemented",
            "notes": [
              "→ [Timers](TIMERS.md)"
            ]
          },
          {
            "feature": "`ArrayBuffer`/`SharedArrayBuffer`/`DataView`/`Atomics` + all 11 TypedArrays",
            "status": "implemented",
            "notes": [
              "→ [Binary data & typed arrays](BINARY-DATA-TYPED-ARRAYS.md)"
            ]
          },
          {
            "feature": "`TextEncoder` / `TextDecoder` (UTF-8)",
            "status": "implemented",
            "notes": [
              "→ [Encoding & text](ENCODING-TEXT.md)"
            ]
          },
          {
            "feature": "`URL` / `URLSearchParams` / `URLPattern`",
            "status": "implemented",
            "notes": [
              "→ [URL](URL.md)"
            ]
          },
          {
            "feature": "`crypto` (`getRandomValues`/`randomUUID`, `crypto.subtle.*`)",
            "status": "implemented",
            "notes": [
              "→ [Web Crypto](WEB-CRYPTO.md)"
            ]
          },
          {
            "feature": "`Event`/`CustomEvent`/`EventTarget`/`AbortController`/`AbortSignal`/`DOMException`",
            "status": "implemented",
            "notes": [
              "→ [Events & cancellation](EVENTS-CANCELLATION.md)"
            ]
          },
          {
            "feature": "`setTimeout`/`setInterval`/`setImmediate` (+`clear*`)",
            "status": "implemented",
            "notes": [
              "→ [Timers](TIMERS.md)"
            ]
          },
          {
            "feature": "`performance` (`now`/`mark`/`measure`) + `Date` (+`now`/`parse`, setters, arithmetic)",
            "status": "implemented",
            "notes": [
              "→ [Performance timing](PERFORMANCE-TIMING.md)"
            ],
            "caveats": [
              "`Date` is a plain i64 epoch with no NaN — an invalid date is a `-1` sentinel (`new Date('bad').getTime()` is `-1`, Node: `NaN`) and out-of-range fields wrap rather than invalidating (`Date.parse('2020-13-45')` is a valid timestamp, Node: `NaN`); no UTC accessors (`Date.UTC`/`getUTC*`/`getTimezoneOffset`) or `Date.prototype.toString` — each is a compile error."
            ]
          },
          {
            "feature": "`Reflect` (`get`/`set`/`has`/`deleteProperty`/`ownKeys`/`get`·`setPrototypeOf`/`isExtensible`/`preventExtensions`/`defineProperty`) — dynamic target",
            "status": "implemented",
            "notes": [
              "→ [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`Proxy` (`get`/`set`/`has`/`deleteProperty` traps) — dynamic target",
            "status": "implemented",
            "notes": [
              "→ [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          }
        ]
      }
    },
    {
      "kind": "block",
      "text": "## Partial — works, with real caveats\n\nEach compiles and runs for its core case but carries a limitation, mostly driven by the fixed-shape runtime, byte-sequence strings, or the absence of a locale/ICU layer. The linked detail page has the specifics."
    },
    {
      "kind": "table",
      "table": {
        "heading": "",
        "featureColumn": "Built-in",
        "hasCaveats": true,
        "hasNotes": false,
        "countsTowardParity": false,
        "rows": [
          {
            "feature": "`String`",
            "status": "implemented",
            "caveats": [
              "`.normalize()` **missing** (no Unicode tables); `.at()` OOB returns `\"\"` not `undefined`; `.codePointAt()` == `.charCodeAt()` (byte strings, correct only ASCII/Latin-1); `.matchAll()` eager not lazy; `.localeCompare()` is byte-order → [String methods](STRING-METHODS.md)"
            ]
          },
          {
            "feature": "`Array`",
            "status": "implemented",
            "caveats": [
              "length-mutating methods propagate to caller only for plain **variable** params (not object-field/array-element receivers); `a.length = n` truncation compile-errors; **`.reduceRight()` absent**; `.keys`/`values`/`entries` materialized not lazy; `.flat(depth)` needs a constant depth → [Array methods](ARRAY-METHODS.md)"
            ]
          },
          {
            "feature": "`Object` / dynamic model",
            "status": "implemented",
            "caveats": [
              "prototype machinery, descriptors, accessors exist on **`any`-typed / js-mode dynamic objects only**; static structs are fixed-shape (no dynamic add/delete, no prototype); `Object.assign` can't graft new fields; `hasOwn` needs string-literal keys → [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`Reflect`",
            "status": "implemented",
            "caveats": [
              "missing `apply`/`construct`; requires a dynamic target → [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`Proxy`",
            "status": "implemented",
            "caveats": [
              "only `get`/`set`/`has`/`deleteProperty` traps; dynamic target only → [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "`Number`",
            "status": "implemented",
            "caveats": [
              "`toString(radix)` non-power-of-two fractional trailing-digit divergence; `toPrecision` fixed/exp threshold differs → [Number & Math](NUMBER-MATH.md)"
            ]
          },
          {
            "feature": "`Function` `.call`/`.apply`/`.bind`",
            "status": "implemented",
            "caveats": [
              "forward args but **ignore `thisArg`** (no method-borrowing); `.bind` scalar-param only; first-class function values, not built-ins → [Language constructs](LANGUAGE-CONSTRUCTS.md)"
            ]
          },
          {
            "feature": "`Symbol`",
            "status": "implemented",
            "caveats": [
              "no well-known symbols as runtime values; only `[Symbol.iterator]`/`[Symbol.asyncIterator]` recognized syntactically → [Type system](TYPE-SYSTEM.md)"
            ]
          },
          {
            "feature": "`RegExp`",
            "status": "implemented",
            "caveats": [
              "`u`/`y`/`d` flags **missing** (accepted, not implemented); `exec` result lacks `index`/`input`/`groups`; unmatched groups become `\"\"` not `null` → [RegExp](REGEXP.md)"
            ]
          },
          {
            "feature": "`JSON`",
            "status": "implemented",
            "caveats": [
              "statically-typed heterogeneous-array `stringify` **missing** (use a tuple/`any`); function/array `replacer` rejected; `space` must be literal → [JSON](JSON.md)"
            ]
          },
          {
            "feature": "TypedArrays / `ArrayBuffer`",
            "status": "implemented",
            "caveats": [
              "no `.buffer` back-ref; `resize`/`grow` need `{maxByteLength}`; views don't length-track resize → [Binary data & typed arrays](BINARY-DATA-TYPED-ARRAYS.md)"
            ]
          },
          {
            "feature": "`TextDecoder`",
            "status": "implemented",
            "caveats": [
              "UTF-8 only; non-UTF-8 labels throw `RangeError` at construction → [Encoding & text](ENCODING-TEXT.md)"
            ]
          },
          {
            "feature": "`URLSearchParams` / `URLPattern`",
            "status": "implemented",
            "caveats": [
              "`URLSearchParams` keeps one value per key; `URLPattern` is object-init only with a reduced grammar and a merged-`Map` `.exec()` result → [URL](URL.md)"
            ]
          },
          {
            "feature": "`EventTarget` / `Event` / `AbortSignal`",
            "status": "implemented",
            "caveats": [
              "single-target dispatch (no capture/bubble/propagation); reduced Event property set; `AbortSignal` custom `abort(reason)` surfaces as `AbortError`; wired into `fetch` not `setTimeout` → [Events & cancellation](EVENTS-CANCELLATION.md)"
            ]
          },
          {
            "feature": "`crypto.subtle`",
            "status": "implemented",
            "caveats": [
              "literal-only algorithm dispatch; ops throw synchronously rather than rejecting; jwk as `Map<string,string>`; `CryptoKey.algorithm`/`.usages` unimplemented → [Web Crypto](WEB-CRYPTO.md)"
            ]
          },
          {
            "feature": "`Date`",
            "status": "implemented",
            "caveats": [
              "UTC-only everywhere (deliberate); `parse` returns a `-1` sentinel not `NaN`; setters need a named-variable receiver; no locale/Intl formatting → [Performance timing](PERFORMANCE-TIMING.md)"
            ]
          },
          {
            "feature": "`performance`",
            "status": "implemented",
            "caveats": [
              "`mark`/`measure` last-write-wins; `measure` returns a plain number; no `PerformanceObserver`/entries → [Performance timing](PERFORMANCE-TIMING.md)"
            ]
          },
          {
            "feature": "`eval`",
            "status": "implemented",
            "caveats": [
              "general/dynamic eval **missing**; only a compile-time-constant `eval(\"<expression>\")` static subset works → [Global functions](GLOBAL-FUNCTIONS.md)"
            ]
          },
          {
            "feature": "`globalThis`",
            "status": "implemented",
            "caveats": [
              "member access to known globals only; no bare-value use, computed access, or new-global assignment → [Global functions](GLOBAL-FUNCTIONS.md)",
              "`globalThis` exists only inside `typeof globalThis` — used as a value (property access, assignment, identity), it fails compilation with `undefined variable 'globalThis'`."
            ]
          }
        ]
      }
    },
    {
      "kind": "block",
      "text": "## Not started (in scope)\n\nStandard built-ins that fit the model and would add value, not yet built."
    },
    {
      "kind": "table",
      "table": {
        "heading": "",
        "featureColumn": "Built-in",
        "hasCaveats": false,
        "hasNotes": true,
        "countsTowardParity": false,
        "rows": [
          {
            "feature": "`String.prototype.normalize()`",
            "status": "missing",
            "notes": [
              "Deliberately deferred (needs NFC/NFD/NFKC/NFKD tables) → [String methods](STRING-METHODS.md)"
            ]
          },
          {
            "feature": "`Array.prototype.reduceRight()`",
            "status": "missing",
            "notes": [
              "Unimplemented → [Array methods](ARRAY-METHODS.md)"
            ]
          },
          {
            "feature": "`RegExp` `u`/`v`/`y`/`d` flag semantics",
            "status": "missing",
            "notes": [
              "Accepted but not implemented → [RegExp](REGEXP.md)"
            ]
          },
          {
            "feature": "`FinalizationRegistry`",
            "status": "missing",
            "notes": [
              "Not started — `WeakRef` ships; the registry is designed in [TDD-00163](../tdd/TDD-00163.md)"
            ]
          },
          {
            "feature": "`Iterator` / `AsyncIterator` helpers (`Iterator.prototype.map`/`filter`/`take`/`drop`/…)",
            "status": "missing",
            "notes": [
              "No general lazy-iterator protocol exists yet (materialized iteration across Array/Map/Set/`matchAll`)"
            ]
          },
          {
            "feature": "`Reflect.apply` / `Reflect.construct`",
            "status": "missing",
            "notes": [
              "Explicitly missing → [Object, Map & Set](OBJECT-COLLECTIONS.md)"
            ]
          },
          {
            "feature": "Dynamic `eval` (arbitrary strings)",
            "status": "missing",
            "notes": [
              "The opt-in embedded JS engine is not started (only the static-subset eval works) → [Global functions](GLOBAL-FUNCTIONS.md)"
            ]
          }
        ]
      }
    },
    {
      "kind": "block",
      "text": "## Out of scope (by the whole-program AOT / no-runtime / fixed-shape model)\n\nBuilt-ins that depend on a live JS engine, a locale/ICU layer, or a dynamic property/protocol model native ahead-of-time output has no equivalent for. Listed for completeness, not planned."
    },
    {
      "kind": "table",
      "table": {
        "heading": "",
        "featureColumn": "Built-in",
        "hasCaveats": false,
        "hasNotes": true,
        "countsTowardParity": false,
        "rows": [
          {
            "feature": "`Intl.*` (Collator/NumberFormat/DateTimeFormat/…)",
            "status": "missing",
            "notes": [
              "No locale/ICU infrastructure — a bare `Intl` reference is an undefined-variable compile error; the reason `localeCompare`/`bigint.toLocaleString`/`Date.toLocale*` are gaps"
            ]
          },
          {
            "feature": "`Temporal` (proposal)",
            "status": "missing",
            "notes": [
              "Not a shipping standard; absent"
            ]
          },
          {
            "feature": "Dynamic `eval` / dynamic `import()` of arbitrary code",
            "status": "missing",
            "notes": [
              "No runtime JS engine ([ADR-00022](../adr/ADR-00022.md)); only the static-subset eval works"
            ]
          },
          {
            "feature": "Full `Proxy` trap set (`apply`/`construct`/`ownKeys`/`getOwnPropertyDescriptor`/`defineProperty`/…)",
            "status": "missing",
            "notes": [
              "Structurally limited to the 4 dynamic-object traps; the rest have no interception point in the fixed-shape model"
            ]
          },
          {
            "feature": "Prototype machinery on statically-typed objects",
            "status": "missing",
            "notes": [
              "Objects are fixed-shape heap structs; dynamic add/delete + prototype links live only in the `any`/js-mode dynamic path by design"
            ]
          },
          {
            "feature": "Well-known symbols as runtime protocol dispatch (`Symbol.hasInstance`/`toPrimitive`/`toStringTag`/`species`/…)",
            "status": "missing",
            "notes": [
              "No runtime protocol-dispatch point; only `Symbol.iterator`/`asyncIterator` are honored, syntactically"
            ]
          }
        ]
      }
    }
  ]
}
