<!-- GENERATED FILE — do not edit. Source of truth: docs/status/data/type-system.json; edit the JSON, then run `make status`. -->

# Type System

> Part of the [Implementation Status](README.md) index.

**Coverage**: Type primitives 12/12 (100%) · Type system features 19/19 (100%).

**Strict Coverage**: Type primitives 6/12 (50%) · Type system features 3/19 (~16%). A row counts toward Strict only when its **Caveats** column is empty.

Format: [Status page format](README.md#status-page-format).

## Type primitives

| Feature | Status | Caveats | Notes |
|---|---|---|---|
| `number` → `double` | ✅ | • A plain `number` past 2^53 loses precision exactly as JS does (`9007199254740993` is `…992`) — inherent to the double model, not a defect (use `int64`/`uint64` for exact 64-bit integers)<br>• Mixing an explicit integer type with a bare literal promotes to double (`int32 / 2` is `3.5`); use two integer-typed operands for integer division | • `number` is a JS-faithful IEEE-754 double — `0.1 + 0.2` is `0.30000000000000004`, `10 / 3` is `3.3333333333333335`, `2 ** -1` is `0.5`, `1 / 0` is `Infinity` (TDD-00123 Stage 1 / [ADR-00377](../adr/ADR-00377.md))<br>• `/** @type {int8..int64, uint8..uint64, float32, float64} */` overrides per-binding — the integer types are the escape hatch for real integer semantics (integer `/`, exact >2^53, bit-width storage)<br>• Bitwise `& \| ^ ~ << >> >>>` use JS 32-bit semantics and their result is a `number` (`(7 & 6) / (7 & 5)` is `1.2`), as are the count/index builtins `.length`/`indexOf`/`lastIndexOf`/`findIndex`/`search`/`charCodeAt`/`push` (`arr.length / other.length` is a float divide) — TDD-00123 Stages 2–3 / [ADR-00379](../adr/ADR-00379.md) |
| `string` → `ptr` | ✅ | | |
| `boolean` → `i1` | ✅ | | |
| `void` | ✅ | | |
| `null` / `undefined` | ✅ | • `null` and `undefined` share one `ptr null` sentinel, so strict `===`/`!==` between them can't distinguish the two — `null === undefined` is `true` (JS: `false`), and for a `null`-valued binding `x === undefined` is also `true`; `typeof`, `String()`, and loose `==` are unaffected. | • Sentinel `ptr null` |
| JSDoc extended integers | ✅ | | • `@type {int8\|int16\|int32\|int64\|uint8…uint64}` |
| JSDoc extended floats | ✅ | | • `@type {float32\|float64}`<br>• Float-to-string is JS-faithful: the shortest decimal that round-trips, per ECMAScript `Number::toString` — `1.1 + 2.2` prints `3.3000000000000003`, integers-valued floats drop the point, and the fixed/exponential switch matches JS's thresholds ([TDD-00080](../tdd/TDD-00080.md)/[ADR-00233](../adr/ADR-00233.md))<br>• `console.log(-0)` prints `0` (this compiler's `console.log` uses `ToString`, and `String(-0)`/`${-0}` are `"0"` in JS too); Node's `console.log` shows `-0` only via `util.inspect`, a console-display quirk<br>• A `float32` prints against its widened `double` |
| `any` | ✅ | • Arithmetic on an `any` is a clean compile error under `-compat=strict` only — `-compat=js` runtime-dispatches it ([TDD-00076](../tdd/TDD-00076.md)/[ADR-00628](../adr/ADR-00628.md)); the js-mode gaps that remain are `ToPrimitive` on object operands (arithmetic on a reference → `NaN`), loose-`==` coercion beyond the strict set, and unary `+` parsing<br>• Nested positions `any[]`/`{ x: any }` are clean compile errors<br>• A *statically-typed* object value boxed into `any` still rejects member access (runtime TypeError) — lifting a static struct into the dynamic view is allocation-site widening, [TDD-00155](../tdd/TDD-00155.md) Stage 6<br>• A dynamic object renders as `[object Object]` through `console.log`/`String()` (no inspect-style `{ a: 1 }` form); a dynamic array renders as its JS join (`1,two`), not `[ 1, 'two' ]`<br>• `Object.defineProperties` (plural) and descriptor-form `Object.assign` are follow-ons<br>• Primitive members through `any` (`x.length` on a boxed string/array) read as `undefined` — no runtime primitive-method dispatch yet | • **Single-word NaN-boxed value** ([TDD-00156](../tdd/TDD-00156.md)/[ADR-00628](../adr/ADR-00628.md)): numbers are offset-encoded doubles (int/float merged — >2^53 rounds exactly as JS), undefined/null/true/false small immediates, heap references low-3-bit-kind-tagged raw pointers (Boehm-safe interior pointers). The logical tag model of [TDD-00062](../tdd/TDD-00062.md) decodes from it unchanged<br>• Under `-compat=js`: real operator semantics — number fast path, string-concat `+`, ToNumber coercion of numeric strings/booleans/null (`"6"*7===42`, `null+1===1`, `undefined+1→NaN`), string/numeric relational, ToBoolean truthiness (`if (x)`, `!x` — both modes), compound assignment incl. dynamic members, and **implicit-`any`**: an unannotated parameter no call site can type (or with disagreeing sites — genuinely polymorphic) boxes and dispatches; an untyped uninitialized `let` is a dynamic `undefined`<br>• Supports declare/assign/reassign/print/`typeof`/`===` + bare `any` param/return on every function shape<br>• Objects and arrays box by reference — `===` is reference identity, `typeof` is `"object"` ([ADR-00008](../adr/ADR-00008.md), [ADR-00176](../adr/ADR-00176.md), [ADR-00177](../adr/ADR-00177.md))<br>• An `any`-boxed array keeps its `(ptr, len)` pair via a heap header — it unboxes back into a real array; `===` identity and `typeof` are unchanged, and the element type is still not carried (toString stays `[object Array]`) ([ADR-00478](../adr/ADR-00478.md))<br>• An object literal bound to a bare `any` becomes a **D1 dynamic object** ([TDD-00155](../tdd/TDD-00155.md) Stage 1, [ADR-00621](../adr/ADR-00621.md)): a real runtime property bag with member/bracket get/set (runtime-computed keys included), dynamic add, `delete`, `in`, `Object.keys`, `for...in` (insertion order), shallow spread merge, nesting, reference `===`, and Node-matching TypeErrors (`Cannot read properties of null (reading 'x')`) on null/undefined access<br>• An array literal in the same position becomes a **dynamic array** ([TDD-00155](../tdd/TDD-00155.md) Stage 2, [ADR-00622](../adr/ADR-00622.md)) — heterogeneous elements included — with `.length`, index reads/writes (JS extension semantics: holes read `undefined`), `Object.keys`/`for...in` index strings, and join-based toString; untyped `JSON.parse` builds whole trees of these<br>• Dynamic objects carry real **property descriptors** ([TDD-00155](../tdd/TDD-00155.md) Stage 5, [ADR-00629](../adr/ADR-00629.md)): `Object.defineProperty` (spec create-defaults, non-configurable redefinition rules), `getOwnPropertyDescriptor`/`getOwnPropertyNames`, object-literal `get`/`set` accessors (invoked with the original receiver, inherited-through-the-chain included; JSON/spread call getters), enumerability honored by `Object.keys`/`for...in`/JSON, and real `freeze`/`seal`/`preventExtensions` + `isFrozen`/`isSealed`/`isExtensible` — rejected writes/deletes throw the module-strict JS TypeErrors. Under `-compat=js` an **untyped object literal is a dynamic object** (ad-hoc property add/delete works — TDD-00022's last break shape); strict keeps literals as native static structs<br>• Dynamic objects carry a real **prototype chain** ([TDD-00155](../tdd/TDD-00155.md) Stage 3, [ADR-00623](../adr/ADR-00623.md)): `Object.create(proto|null)`, `get/setPrototypeOf`, `__proto__` (accessor semantics), the `{ __proto__: base }` literal form; reads and `in` walk the chain, writes/keys/stringify stay own — matching JS<br>• Under `-compat=js`, **vanilla-JS prototype classes** work end to end ([TDD-00155](../tdd/TDD-00155.md) Stage 4, [ADR-00627](../adr/ADR-00627.md)): function constructors (`new F(...)` → a dynamic object linked to `F.prototype`), prototype methods with a real boxed `this` (tag-12 dynamic-ABI functions; missing args read `undefined`), chain-walking method dispatch with override/inheritance, `Base.call(this, ...)` chaining, and `"str" + anyVal` ToString concatenation; `typeof method` → `"function"`, a non-function call throws the JS TypeError |
| `unknown` | ✅ | • Same as `any` (see above) | • Same Staged V1 scope as `any` |
| `never` | ✅ | | • A `(): never` function that always throws works correctly |
| `symbol` | ✅ | • No dynamic property keys<br>• No well-known symbols as runtime values (`Symbol.iterator`, etc.); the exceptions are `[Symbol.asyncIterator]` and `[Symbol.iterator]` recognized purely syntactically as class/object-literal computed-member keys ([TDD-00089](../tdd/TDD-00089.md), [ADR-00278](../adr/ADR-00278.md)/[ADR-00279](../adr/ADR-00279.md)), not evaluable `Symbol` values | • V1 opaque unique values — `Symbol()`/`Symbol("desc")`, `===`/`!==`, `typeof`, `.description`, `.toString()`<br>• The fixed-shape object model has no dynamic-property-bag mechanism (blocks dynamic keys) and no runtime protocol-dispatch point (blocks the registry) ([TDD-00044](../tdd/TDD-00044.md))<br>• `Symbol.for(key)` interns per key (shared identity); `Symbol.keyFor(sym)` returns the key or `null` ([ADR-00488](../adr/ADR-00488.md)) |
| `bigint` | ✅ | • No `.toLocaleString()` (an `Intl`-shaped gap) | • Backend-agnostic `__kml_bigint_*` ABI over an opaque `ptr` handle, selectable via `-bigint=libtommath\|gmp` (default `libtommath`, public domain; `gmp` LGPL) — each an embedded C file compiled+linked only when a program uses bigint ([TDD-00074](../tdd/TDD-00074.md)/[ADR-00216](../adr/ADR-00216.md))<br>• `123n`/hex/bin/oct/`_`-separator literals, `+ - * / % ** & \| ^ ~ << >>`, same-type comparisons **and cross-type comparison with an integer `number`** (`10n < 5`, `10n == 10`, exact), unary `-`/`~`, truthiness (`0n` falsy), `typeof`, `++`/`--`, compound assignment, `.toString([radix])`, params/returns/fields/arrays, `BigInt(int\|string)`, and the console-`n`-suffix vs `String()`-bare print split all work<br>• Division/modulo by zero throws a catchable `Error` (the same path as the i64 operators)<br>• `BigInt.asIntN(bits, x)` / `BigInt.asUintN(bits, x)` clamp to the low `bits` bits (two's-complement signed / non-negative), implemented in both bigint backends ([ADR-00584](../adr/ADR-00584.md))<br>• `Number(bigint)` converts to the nearest `double` (Infinity when out of range), matching JS ([ADR-00586](../adr/ADR-00586.md))<br>• Mixing a bigint and a number in **arithmetic** is a clean compile error (matches JS's `TypeError`); comparing a bigint with a **float** is a `-compat`-governed behavior ([TDD-00075](../tdd/TDD-00075.md)/[ADR-00217](../adr/ADR-00217.md)): default `strict` rejects it (a likely bug), and `-compat=js` does JS's exact real-number comparison (exact even past 2^53) — a design choice, not a caveat<br>• `>>>`, unary `+`, and `JSON.stringify` on a bigint are clean compile errors (all TypeErrors in JS)<br>• libbf (MIT) backend deferred to a follow-on stage |

## Type system features

| Feature | Status | Caveats | Notes |
|---|---|---|---|
| `T \| null` (nullable) | ✅ | | • A single-branch `T \| null` uses the presence-flagged nullable-scalar slot (`{ i1, T }`, [TDD-00064](../tdd/TDD-00064.md)); a multi-branch `A \| B \| null` is handled by the union box instead (see the Union types row) — both are supported |
| Object types (interfaces / inline `{}`) | ✅ | • `interface C extends A, B` merges simple base names' fields/method signatures (own declarations win); a **generic or qualified base** (`extends Boxed<number>`, `extends ns.Base`) parses but does not merge — its members stay missing from the derived interface ([ADR-00452](../adr/ADR-00452.md))<br>• An inline `{}` type's bare call signature is only supported alone (it desugars to the function type); mixing it with fields/an index signature is a clean rejection ([ADR-00448](../adr/ADR-00448.md)); an interface's *lone* call/construct signature likewise registers as the function type, mixed members rejected ([ADR-00455](../adr/ADR-00455.md))<br>• Declaration merging: same-name `interface` + `interface` pairs **merge** (members union, first wins on a collision — [ADR-00479](../adr/ADR-00479.md)); a same-name `interface` + `class` pair coexists with the class winning as the binding, the interface's extra members ignored ([ADR-00466](../adr/ADR-00466.md)) | • A literal's fields are coerced against the declared type wherever one is known (variable annotation, function parameter/return/default value, array element type, nested field) — not just the literal's own self-inferred type ([ADR-00077](../adr/ADR-00077.md)/[TDD-00007](../tdd/TDD-00007.md))<br>• Method signatures `name(params): R` (and `name?(…)`) in an inline `{}` type desugar to function-typed properties; a lone call signature `{ (n: number): string }` desugars to the function type itself; `field?: T` is accepted-but-required, matching interfaces ([ADR-00448](../adr/ADR-00448.md))<br>• Constructor types `new (params) => R` parse as the equivalent function type (accept-and-erase, [ADR-00452](../adr/ADR-00452.md)) |
| Array types `T[]` | ✅ | | • `{ptr, i64}` aggregate |
| `Promise<T>` | ✅ | | |
| Function types `(a: T) => R` | ✅ | • Closure adapters convert at any↔concrete signature boundaries — scalars, strings, arrays (length-preserving header boxing), nullable scalars, rest tails, arity narrowing; an *absent* nullable boxed into `any` reads as `undefined`, and void→concrete returns stay a passthrough ([ADR-00477](../adr/ADR-00477.md)/[ADR-00478](../adr/ADR-00478.md)) | • Closure struct `{funcPtr, envPtr}`<br>• A *generic* function type (`<T>(x: T) => T`) parses with its type parameters erased to `any` — generic functions are monomorphized declarations, not first-class values ([ADR-00469](../adr/ADR-00469.md))<br>• Type predicates (`x is T`) resolve to `boolean` and assertion signatures (`asserts x [is T]`) to `void` — parsed, with the flow-narrowing itself not modeled ([ADR-00474](../adr/ADR-00474.md))<br>• Generic class methods (`map<U>(f)`) compile via `any`-erasure through these adapters ([ADR-00477](../adr/ADR-00477.md)) |
| `Map<K,V>` | ✅ | • Numeric keys are compared by raw double bit-pattern, not SameValueZero — `+0` and `-0` count as distinct keys (`new Set([0,-0]).size` is `2`, JS: `1`; `m.set(0,v); m.get(-0)` misses, JS returns `v`); a `NaN` key dedups only when bit patterns match. | • Separate helpers for `<string,number>`, `<string,string>`, etc. |
| `Set<T>` | ✅ | • Numeric keys are compared by raw double bit-pattern, not SameValueZero — `+0` and `-0` count as distinct keys (`new Set([0,-0]).size` is `2`, JS: `1`; `m.set(0,v); m.get(-0)` misses, JS returns `v`); a `NaN` key dedups only when bit patterns match. | |
| Union types beyond `T \| null` | ✅ | • Scalar members (`number`/`string`/`boolean` + `null`/`undefined`), object/interface/class members (one object member ([TDD-00115](../tdd/TDD-00115.md)), or ≥2 as a **discriminated union** sharing a first-position string-literal tag ([TDD-00116](../tdd/TDD-00116.md))), and a `ReadableStream` member ([TDD-00119](../tdd/TDD-00119.md)); array/`ArrayBuffer` members, number-literal tags, and a non-first-position tag aren't supported<br>• Usable at the top level of a var declaration/function param/return and as an **object field** (checked + boxed at construction, [TDD-00119](../tdd/TDD-00119.md)); as an **array element** it's still rejected (element-level checking not wired)<br>• Flow narrowing is for a union **local** via `typeof`/truthiness/`==null` (if/else branches + early return); a field/element, discriminated-union tag, `switch (typeof x)`, and `as` casts aren't narrowed; an un-narrowed object union has no field access ([TDD-00114](../tdd/TDD-00114.md)) | • Same runtime box as `any`/`unknown` with a checked member set on top ([TDD-00043](../tdd/TDD-00043.md)/[ADR-00136](../adr/ADR-00136.md)); a narrowed local unboxes to the concrete member ([ADR-00354](../adr/ADR-00354.md)), an object member to its typed pointer ([ADR-00355](../adr/ADR-00355.md))<br>• An uninitialized union binding reads as boxed `undefined` (JS `let x;` semantics) until assigned ([ADR-00475](../adr/ADR-00475.md)) |
| Intersection types | ✅ | • Object-type members only — a non-object member (scalar/function/array/tuple/union, all `never` in TS), or a generic-with-type-arguments member, is a clean compile error<br>• A field declared with conflicting **non-object** types across members is rejected under `-compat=strict` (default); the `-compat=js` TS-faithful `never`-field is not yet implemented, so a conflict is currently rejected in both modes (same-named **object** fields are recursively intersected instead — `{ x: A } & { x: B }` ⇒ `x: A & B`)<br>• Inherits object types' missing-field leniency: a partial object literal for an `A & B`-typed target is not rejected (a pre-existing object-literal behavior, not intersection-specific) | • Collapses at `resolveType` into one merged `ObjectType` (member fields concatenated, compatible duplicates deduped) — no runtime tag, unlike a union; reuses all existing object machinery unchanged ([TDD-00078](../tdd/TDD-00078.md)/[ADR-00225](../adr/ADR-00225.md))<br>• `&` binds tighter than `\|` (`A & B \| C` ≡ `(A & B) \| C`)<br>• Works as a named interface / inline `{...}` / `type` alias member, and in variable/param/return/field/array-element positions |
| Tuple types | ✅ | • No rest/optional/named elements<br>• Constant index only — no non-constant index; compound assignment to an element (`t[0] += x`) is a clean rejection<br>• No array methods (`.map`/`.filter`/…) on a tuple (`.length` works)<br>• Not supported nested in `any` or a union | • Fixed-arity, heterogeneous, positional value stored as a fixed-shape struct ([TDD-00066](../tdd/TDD-00066.md)/[ADR-00201](../adr/ADR-00201.md))<br>• Supports declaration + tuple literal, constant-index read **and assignment** (`t[0]`, `t[0] = x`), `.length` (compile-time constant), destructuring (`const [a, b] = t`, `for (const [a, b] of tuples)`, nested), tuple parameters/returns/fields, array-shaped rendering (JSON/`String()`/`console.log`)<br>• `Map`/`Array`/`Object.entries()` return real `[K, V]` tuples |
| Mapped & utility types | ✅ | • `Partial`/`Required`/`Readonly` are structural no-ops — the object model already zero-fills omitted fields and does no mutation checking; the visibly-effective utilities are `Pick`/`Omit`/`Record`<br>• Mapped value body handles `T[K]` (homomorphic), a bare `K`, a concrete type (uniform), or `T[K]` wrapped one level in `Array`/`Promise`/`Set`; a `T[K]` nested deeper isn't substituted<br>• No key remapping (`as`) or modifier-removal (`-?`/`-readonly`); `?`/`readonly` are accepted but near-no-ops<br>• String-literal types resolve to `string` — the literal value is not narrowed/enforced<br>• An unrecognized `Name<Args>` (or a malformed `Pick`/`Omit`) is not yet a clean rejection — [TDD-00079](../tdd/TDD-00079.md) Stage 0 | • `Partial`/`Required`/`Readonly`/`NonNullable`/`Pick`/`Omit`/`Record`, the general mapped form `{ [K in keyof T]: V }`, `keyof`, indexed access `T[K]`, and string-literal types — all evaluated at compile time to concrete object types via the monomorphization seam, no runtime representation ([TDD-00079](../tdd/TDD-00079.md)/[ADR-00228](../adr/ADR-00228.md)–[ADR-00230](../adr/ADR-00230.md))<br>• Numeric-literal types (`type Dir = -1 | 0 | 1`, `{ retries: 3 }`) parse and resolve to `number` — like string-literal types, the value itself is not narrowed/enforced ([ADR-00459](../adr/ADR-00459.md))<br>• `Record<string, V>` (open key) is the index-signature dict — bracket read/write, `Object.keys`, `JSON.stringify` all work; literal-union keys keep the fixed shape ([ADR-00485](../adr/ADR-00485.md)) |
| Conditional types (`T extends U ? X : Y`) | ✅ | • `infer` supported for `Array<infer E>`/`Promise<infer V>` and a bare `infer R`; function-signature inference (`(...) => infer R`) and deeper `infer` nesting are deferred<br>• `assignable` is structural width subtyping (objects), element-wise (arrays), IR-equality with numeric leniency (scalars) — not full variance | • Evaluated at compile time off the monomorphized (concrete) check type — generic type aliases (`type A<T> = …`), `infer` capture, and the first general structural assignability predicate ([TDD-00079](../tdd/TDD-00079.md)/[ADR-00231](../adr/ADR-00231.md))<br>• Nested conditionals are right-associative |
| Generics on user functions/interfaces/classes | ✅ | • No explicit call-site type arguments for a *function* (`identity<number>(5)`) — inference-only, blocked by the `a<b>(c)` grammar ambiguity (a generic class still takes an explicit `new Box<T>(...)` list)<br>• A generic function needs an inferable `T`/`T[]`-typed parameter for each of its type parameters<br>• Map/Set/Promise/closure/dynamic type arguments are a clean rejection (scalars, arrays, and object/interface/class types are supported)<br>• V2 `@erased` covers functions only (not interfaces/classes) and bare `T` positions only — `T[]` or `T` nested in an object field is a clean compile error<br>• Arithmetic on an erased `T` hits the same "operator on any/unknown" rejection as plain `any` | • V1 (default): monomorphization — one specialized implementation per distinct combination of concrete types actually used; an object/interface/class type argument mangles structurally (a class by name), so field access/destructuring/`T[]` on a `T`-bound object ride the per-instantiation body ([TDD-00069](../tdd/TDD-00069.md)/[ADR-00352](../adr/ADR-00352.md))<br>• `<T extends X>` constraints are parsed and enforced (structural width subtyping) on functions and classes ([TDD-00113](../tdd/TDD-00113.md)/[ADR-00353](../adr/ADR-00353.md))<br>• Covers `function identity<T>(x: T): T`, `interface Box<T>`, `class Box<T>`, and multiple type parameters `<K, V>` ([TDD-00037](../tdd/TDD-00037.md)/[ADR-00132](../adr/ADR-00132.md))<br>• A generic class needs an explicit type-argument list at each `new Box<T>(...)` site; can't use `extends`/`implements`/`abstract`/static members ([ADR-00103](../adr/ADR-00103.md)/[TDD-00010](../tdd/TDD-00010.md))<br>• V2: `/** @erased */` above a generic function compiles its body once (bare-`T` positions become `any`/`unknown`), no name mangling or instantiation blowup ([ADR-00121](../adr/ADR-00121.md))<br>• Explicit call-site type arguments (`id<string>(x)`, `make<number>()`) pick the instantiation directly, bypassing inference; comparison expressions keep parsing via backtracking ([ADR-00473](../adr/ADR-00473.md)) |
| Index signatures (`{ [k: string]: T }`) | ✅ | • String or number key, map-backed ([TDD-00130](../tdd/TDD-00130.md)/[ADR-00390](../adr/ADR-00390.md)/[ADR-00461](../adr/ADR-00461.md)) — `d[key]` read/write, an object-literal initializer, and `Object.keys/values/entries` + `for…of` all work<br>• A mixed named+index shape (`{ id: number; [k: string]: T }`) is a clean rejection; the class-body form (`class C { [n: number]: T }`) parses and is **dropped** — indexing a class instance keeps its use-site rejection ([ADR-00476](../adr/ADR-00476.md))<br>• `JSON.stringify` of an index-signature dict serializes by key iteration (escaped keys, recursive values — [ADR-00482](../adr/ADR-00482.md)); compact output only (the `space` argument isn't threaded through this path) | • A `number` key is supported too — keys stringify (JS object keys are strings), sharing the map backing; the class-body form (`class C { [n: number]: T }`) stays a parse rejection ([ADR-00461](../adr/ADR-00461.md)) |
| `typeof` type queries (`type T = typeof x`) | ✅ | • Resolves the referenced value's type (a variable's object/scalar/array type, or a function's type), walking a `typeof a.b` member path ([ADR-00389](../adr/ADR-00389.md)) — usable as a `type` alias, an inline annotation, or a parameter/return type. A function-local binding shadowing a top-level one of the same name isn't distinguished (the top-level wins), and an unresolvable query degrades to the `number` default rather than erroring | |
| Type assertions (`x as T`, `as const`, `satisfies T`, `<T>x`) | ✅ | • **Erased, not enforcing** — the assertion is accepted and dropped: the value is unchanged and keeps its *own* inferred type, so `as T` does not re-type the expression (a sound cross-representation reinterpret isn't modeled). This matches TS's runtime erasure but not its static narrowing/widening; a cast the code *relies on* for typing won't take effect ([ADR-00371](../adr/ADR-00371.md)). One carve-out: `as T` directly on `JSON.parse(...)`/`.json()` **does** supply the projection target under `-compat=strict`, exactly as a declaration annotation would ([ADR-00715](../adr/ADR-00715.md)); the general `any as T` question is [TDD-00176](../tdd/TDD-00176.md) | • Parsed as a left-associative postfix on any expression; `as const` and `satisfies T` are likewise identity; the old-style prefix `<T>expr` form parses in the unary position and is erased identically ([ADR-00451](../adr/ADR-00451.md)) |
| Template literal types (`` `a-${T}` ``) | ✅ | • **Erased to `string`** — the literal pattern is parsed (no-substitution, multi-substitution, and `[]`-suffixed forms) but resolves to `string`, not narrowed/enforced; the same simplification string-literal types use ([ADR-00561](../adr/ADR-00561.md)) | |
| `readonly T[]` array-type modifier | ✅ | • **Erased, not enforcing** — `readonly number[]` and `readonly [T, U]` parse (in variable/parameter/return/field positions), but the modifier is dropped and mutation is not prevented, the same stance as `Readonly<T>` and the mapped-type `readonly` modifier. The `ReadonlyArray<T>` alias form is not covered ([ADR-00373](../adr/ADR-00373.md)) | |
| Ambient declarations (`declare const`/`function`/`class`/`module`/`namespace`) | ✅ | • `declare class` and the brace-bodied `global`/`module`/`namespace` blocks are parsed and erased ([ADR-00388](../adr/ADR-00388.md)); `declare var`/`function` are real bindings with defined fallback behavior ([ADR-00471](../adr/ADR-00471.md)). Ambient declarations describe externally-provided values; under whole-program AOT they have no target to bind to, so a runtime *use* of a declared binding this compiler doesn't itself provide is an ordinary `undefined variable` error | • `declare var` is a real zero-initialized binding; `declare function` compiles to a throwing 'no implementation' stub (clear runtime error only if called); redeclared signatures collapse last-wins; `declare class/namespace/module/global` stay erased ([ADR-00471](../adr/ADR-00471.md))<br>• Identifier-named `declare namespace/module X` members are real ambient bindings (vars zero-init and assignable, functions throwing stubs); string-named module and `global` blocks stay erased ([ADR-00474](../adr/ADR-00474.md))<br>• `declare [const] enum` is a real enum ([ADR-00476](../adr/ADR-00476.md)) |
