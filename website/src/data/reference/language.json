{
  "surface": "language",
  "title": "Language constructs",
  "kind": "language",
  "statusPage": "LANGUAGE-CONSTRUCTS.md",
  "coversRows": "all",
  "coverageScope": "subset",
  "coverage": { "loose": "75/76", "strict": "48/76" },
  "lede": "The core TypeScript surface this compiler lowers to native code: control flow, operators, declarations, functions and closures, async/Promise, and classes. Most of it matches JS/TS exactly, but the recurring divergences are worth knowing up front — strings are NUL-terminated (an embedded null truncates), a nullable scalar's null is often an in-band presence bit rather than a real undefined, `-compat` governs whether `&&`/`||` are value-preserving, and a top-level `const`/`let` is only readable from a named function when its type is knowable before main() runs (an arrow closure captures anything regardless). The whole surface is statically typed and structurally shaped — there is no dynamic property add/delete and no runtime eval.",
  "entries": [
    {
      "id": "lang.if",
      "name": "if / else if / else",
      "statusRow": "if / else if / else",
      "signature": "if (cond) { … } else if (cond) { … } else { … }",
      "badge": "exact",
      "description": "Conditional branching with the usual else-if chain and optional braceless single-statement bodies.",
      "seeAlso": ["lang.ternary"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/if...else" }
    },
    {
      "id": "lang.while",
      "name": "while loop",
      "statusRow": "while loop",
      "signature": "while (cond) { … }",
      "badge": "exact",
      "description": "Pre-condition loop; the body runs zero or more times.",
      "seeAlso": ["lang.dowhile", "lang.for"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/while" }
    },
    {
      "id": "lang.dowhile",
      "name": "do…while loop",
      "statusRow": "do…while loop",
      "signature": "do { … } while (cond)",
      "badge": "exact",
      "description": "Post-condition loop; the body always runs at least once before the condition is checked. break/continue work inside it.",
      "snippet": "let i = 0\ndo {\n  console.log(i)\n  i++\n} while (i < 3)",
      "seeAlso": ["lang.while"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/do...while" }
    },
    {
      "id": "lang.for",
      "name": "for (init; cond; update)",
      "statusRow": "for (init; cond; update)",
      "signature": "for (let i = 0; i < n; i++) { … }",
      "badge": "exact",
      "description": "The C-style three-clause for loop.",
      "seeAlso": ["lang.forof", "lang.while"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for" }
    },
    {
      "id": "lang.forof",
      "name": "for…of over arrays, Map, Set, and a class implementing next(): T | null",
      "statusRow": "for…of over arrays, Map, Set, and a class implementing next(): T | null",
      "signature": "for (const x of iterable) { … }",
      "badge": "caveats",
      "description": "Iterates arrays, Map (values), Set (elements), generators, and both the structural next(): T | null shape and the spec's real [Symbol.iterator]() protocol. The loop variable may be a destructuring pattern, including nested. A scalar next() returns a presence-flagged aggregate so a yielded 0/false doesn't end iteration.",
      "differences": [
        "A `Map` iterates values, not `[key, value]` entries, so `for (const [k, v] of map)` does not decompose entries — use `.keys()` for keys."
      ],
      "seeAlso": ["lang.forawaitof", "lang.arraydestructure"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for...of" }
    },
    {
      "id": "lang.forin",
      "name": "for…in over object keys",
      "statusRow": "for…in over object keys",
      "signature": "for (const key in obj) { … }",
      "badge": "exact",
      "description": "Iterates the field names of an object as strings.",
      "snippet": "const point = { x: 10, y: 20, z: 30 }\nfor (const key in point) {\n  console.log(key)\n}",
      "seeAlso": ["lang.forof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for...in" }
    },
    {
      "id": "lang.switch",
      "name": "switch / case / default / break",
      "statusRow": "switch / case / default / break",
      "signature": "switch (x) { case a: …; break; default: … }",
      "badge": "exact",
      "description": "Multi-way branch with numeric, string, and boolean discriminants.",
      "seeAlso": ["lang.if"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/switch" }
    },
    {
      "id": "lang.breakcontinue",
      "name": "break / continue in loops, including labeled (outer: for (...) { break outer; })",
      "statusRow": "break / continue in loops, including labeled (outer: for (...) { break outer; })",
      "signature": "outer: for (...) { break outer; }",
      "badge": "exact",
      "description": "Loop break/continue, including a labeled target on an enclosing loop. A break/continue inside a nested closure can only target a loop declared within that closure's own body — a clean compile-time rejection otherwise, matching JS's no-cross-function-boundary label rule.",
      "seeAlso": ["lang.for", "lang.while"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/label" }
    },
    {
      "id": "lang.return",
      "name": "return",
      "statusRow": "return",
      "signature": "return expr;",
      "badge": "exact",
      "description": "Typed return; an implicit void return is handled.",
      "seeAlso": ["lang.functiondecl"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/return" }
    },
    {
      "id": "lang.throw",
      "name": "throw new Error(msg)",
      "statusRow": "throw new Error(msg)",
      "signature": "throw new Error(msg)",
      "badge": "exact",
      "description": "Throws via setjmp/longjmp. A built-in error constructor is also a first-class value (a boxed funcref with exact identity, typeof \"function\").",
      "seeAlso": ["lang.trycatch", "lang.newerror"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/throw" }
    },
    {
      "id": "lang.trycatch",
      "name": "try / catch / finally",
      "statusRow": "try / catch / finally",
      "signature": "try { … } catch (e) { … } finally { … }",
      "badge": "caveats",
      "description": "Exception handling with a single catch variable or a destructured object pattern. finally runs on early return/break/continue (labeled or not), a return in finally overrides, and nested finallys run innermost-first.",
      "snippet": "function readWithCleanup(): number {\n  try {\n    return 42\n  } finally {\n    console.log(\"close resource\")\n  }\n}",
      "differences": [
        "A catch pattern can only destructure the caught value's fixed `{kind, message, name}` shape (every thrown value, including a thrown non-Error primitive, is force-shaped into that one runtime layout), not an arbitrary custom object's own fields, and there's no array-pattern form.",
        "`finally` runs on an early `return`/`break`/`continue` everywhere except inside an `async`/generator function body, where an early exit runs through a separate coroutine return path so `finally` still only runs on fall-through."
      ],
      "seeAlso": ["lang.throw", "lang.optcatch"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/try...catch" }
    },
    {
      "id": "lang.optcatch",
      "name": "Optional catch binding (try {} catch {}, no bound param)",
      "statusRow": "Optional catch binding (try {} catch {}, no bound param) — ES2019",
      "signature": "try { … } catch { … }",
      "badge": "exact",
      "description": "The catch parameter clause is optional (ES2019).",
      "seeAlso": ["lang.trycatch"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/try...catch#catch_binding" }
    },
    {
      "id": "lang.debugger",
      "name": "debugger statement",
      "statusRow": "debugger statement",
      "signature": "debugger;",
      "badge": "exact",
      "description": "Parsed and erased to a no-op — an AOT-compiled native binary has no attached inspector to break into, matching plain JS with no debugger attached. debugger is a reserved word, never a real identifier.",
      "seeAlso": ["lang.return"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/debugger" }
    },
    {
      "id": "lang.numliterals",
      "name": "Numeric literals (42, 3.14, 0xFF, 0b101, 0o77)",
      "statusRow": "Numeric literals (42, 3.14, 0xFF, 0b101, 0o77)",
      "signature": "42, 3.14, 0xFF, 0b101, 0o77",
      "badge": "exact",
      "description": "Decimal, float, hex, binary, and octal numeric literals.",
      "seeAlso": ["lang.numseparators"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Lexical_grammar#numeric_literals" }
    },
    {
      "id": "lang.numseparators",
      "name": "Numeric separators (1_000_000)",
      "statusRow": "Numeric separators (1_000_000)",
      "signature": "1_000_000",
      "badge": "exact",
      "description": "Underscore digit separators, stripped at lex time so every downstream consumer sees a clean literal. A leading/trailing/doubled separator, or one inside a legacy-octal literal, is a clean lexer error.",
      "seeAlso": ["lang.numliterals"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Lexical_grammar#numeric_separators" }
    },
    {
      "id": "lang.strliterals",
      "name": "String literals (single/double quote)",
      "statusRow": "String literals (single/double quote)",
      "signature": "'a', \"b\"",
      "badge": "caveats",
      "description": "Single- and double-quoted strings; the full ES escape grammar is decoded (\\b \\f \\v, \\xHH, \\uHHHH, \\u{H…}, Annex B legacy octal, line continuation, and the NonEscapeCharacter rule). Templates share the same decoding.",
      "differences": [
        "An embedded NUL (`\\x00`, `\\0`) truncates the string, since strings are stored as NUL-terminated C strings.",
        "No strict-mode `SyntaxError` for a legacy octal escape (accepted unconditionally)."
      ],
      "seeAlso": ["lang.template"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Lexical_grammar#string_literals" }
    },
    {
      "id": "lang.boolliterals",
      "name": "Boolean literals (true / false)",
      "statusRow": "Boolean literals (true / false)",
      "signature": "true, false",
      "badge": "exact",
      "description": "Boolean literals.",
      "seeAlso": ["lang.logical"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Lexical_grammar#boolean_literal" }
    },
    {
      "id": "lang.null",
      "name": "null literal",
      "statusRow": "null literal",
      "signature": "null",
      "badge": "exact",
      "description": "The null literal; a T | null union type is supported.",
      "seeAlso": ["lang.nullish", "lang.undefined"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/null" }
    },
    {
      "id": "lang.undefined",
      "name": "undefined literal",
      "statusRow": "undefined literal",
      "signature": "undefined",
      "badge": "exact",
      "description": "The undefined literal.",
      "seeAlso": ["lang.null"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined" }
    },
    {
      "id": "lang.template",
      "name": "Template literals `Hello ${name}`",
      "statusRow": "Template literals  Hello ${name}",
      "signature": "`Hello ${name}`",
      "badge": "exact",
      "description": "Backtick template literals with arbitrary interpolation depth.",
      "snippet": "const msg = `Hello, ${name}!`",
      "seeAlso": ["lang.taggedtemplate", "lang.strliterals"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals" }
    },
    {
      "id": "lang.arithmetic",
      "name": "Arithmetic operators + - * / %",
      "statusRow": "Arithmetic operators + - * / %",
      "signature": "a + b - c * d / e % f",
      "badge": "exact",
      "description": "The five basic arithmetic operators.",
      "seeAlso": ["lang.exponent"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#arithmetic_operators" }
    },
    {
      "id": "lang.exponent",
      "name": "** (exponentiation)",
      "statusRow": "** (exponentiation, ES2016)",
      "signature": "2 ** 10",
      "badge": "exact",
      "description": "Right-associative exponentiation (ES2016), binding tighter than * / %, with **=. number operands are doubles so 2 ** -1 is 0.5 and 2 ** 0.5 is a real root; explicit integer-typed operands use exact i64 exponentiation-by-squaring and stay i64. -2 ** 2 is a SyntaxError, matching JS.",
      "seeAlso": ["lang.arithmetic"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Exponentiation" }
    },
    {
      "id": "lang.comparison",
      "name": "Comparison operators == === != !== < > <= >=",
      "statusRow": "Comparison operators == === != !== < > <= >=",
      "signature": "a === b, a < b",
      "badge": "exact",
      "description": "The comparison operators; string comparison via strcmp. NaN compares per JS: NaN !== NaN is true while ===/</>/<=/>= against NaN are all false.",
      "seeAlso": ["lang.arithmetic"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#relational_operators" }
    },
    {
      "id": "lang.logical",
      "name": "Logical operators && || !",
      "statusRow": "Logical operators && || !",
      "signature": "a && b, a || b, !a",
      "badge": "caveats",
      "description": "Genuinely short-circuiting logical operators. Result type is bool under -compat=strict (default); -compat=js makes &&/|| value-preserving for same-typed operands (x || \"default\" yields the string).",
      "differences": [
        "Under `-compat=js`, `&&`/`||` are value-preserving only for **same-typed** operands; differently-typed operands (`0 || \"x\"`) stay bool, since the value-preserving result would be a union this compiler can't represent."
      ],
      "seeAlso": ["lang.logicalassign", "lang.nullish"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#logical_operators" }
    },
    {
      "id": "lang.bitwise",
      "name": "Bitwise operators & | ^ ~ << >> >>>",
      "statusRow": "Bitwise operators & | ^ ~ << >> >>>",
      "signature": "a & b, a << b, a >>> b",
      "badge": "exact",
      "description": "The bitwise and shift operators, including the unsigned right shift >>>.",
      "seeAlso": ["lang.assignops"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators#bitwise_operators" }
    },
    {
      "id": "lang.assignops",
      "name": "Assignment operators += -= *= /= %= &= |= ^= <<= >>= >>>=",
      "statusRow": "Assignment operators += -= *= /= %= &= |= ^= <<= >>= >>>=",
      "signature": "x += 1, x <<= 2",
      "badge": "exact",
      "description": "The full set of compound assignment operators.",
      "seeAlso": ["lang.logicalassign"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Assignment" }
    },
    {
      "id": "lang.logicalassign",
      "name": "Logical assignment operators &&= ||= ??=",
      "statusRow": "Logical assignment operators &&= ||= ??= (ES2021)",
      "signature": "x ??= v, x ||= d",
      "badge": "caveats",
      "description": "Genuinely short-circuiting logical assignment (ES2021). Works against scalar variables, array elements, object fields, and static class fields, including a nullable-scalar field routed through the presence-flagged path so obj.n ??= v fires exactly when the field is absent.",
      "differences": [
        "Computed-key dynamic-object bracket assignment is a clean compile error, not built."
      ],
      "seeAlso": ["lang.logical", "lang.nullish"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Logical_AND_assignment" }
    },
    {
      "id": "lang.incdec",
      "name": "Increment / decrement ++ -- (prefix & postfix)",
      "statusRow": "Increment / decrement ++ -- (prefix & postfix)",
      "signature": "i++, --j, obj.x++, arr[i]++",
      "badge": "caveats",
      "description": "Prefix and postfix increment/decrement. A member/index target desugars to the equivalent += 1 / -= 1 compound assignment; prefix yields the new value, postfix the old.",
      "differences": [
        "The one edge is a **postfix** update on a **side-effecting receiver whose value is consumed** (`makeObj().x++` read into a variable), where the receiver is evaluated twice — a statement-position postfix and a simple receiver (identifier/`this`/static name) are unaffected."
      ],
      "seeAlso": ["lang.assignops"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Increment" }
    },
    {
      "id": "lang.ternary",
      "name": "Ternary cond ? a : b",
      "statusRow": "Ternary cond ? a : b",
      "signature": "cond ? a : b",
      "badge": "exact",
      "description": "The conditional (ternary) operator.",
      "seeAlso": ["lang.if"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Conditional_operator" }
    },
    {
      "id": "lang.comma",
      "name": "Comma / sequence operator (a, b, c)",
      "statusRow": "Comma / sequence operator (a, b, c)",
      "signature": "(a, b, c)",
      "badge": "caveats",
      "description": "Evaluates each operand left to right and yields the last (its type is the sequence's type). Also usable in a for header.",
      "differences": [
        "A sequence whose *first* operand is a lone identifier (`(a, b)`) is instead parsed as an arrow-function parameter list — a known ambiguity; write a non-identifier first operand."
      ],
      "seeAlso": ["lang.for"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Comma_operator" }
    },
    {
      "id": "lang.nullish",
      "name": "Nullish coalescing ??",
      "statusRow": "Nullish coalescing ??",
      "signature": "a ?? b",
      "badge": "exact",
      "description": "Works on a pointer T | null (string/object/…) and a non-pointer scalar T | null (number | null, boolean | null) via a real presence bit — so let x: number | null = null; x ?? 42 yields 42 while a present 0 yields 0.",
      "seeAlso": ["lang.optchain", "lang.null"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing" }
    },
    {
      "id": "lang.optchain",
      "name": "Optional chaining ?.",
      "statusRow": "Optional chaining ?.",
      "signature": "obj?.field",
      "badge": "exact",
      "description": "Null-guards pointer fields; returns null on a null receiver.",
      "seeAlso": ["lang.nullish"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining" }
    },
    {
      "id": "lang.typeof",
      "name": "typeof operator",
      "statusRow": "typeof operator",
      "signature": "typeof x",
      "badge": "caveats",
      "description": "Compile-time constant resolved from inferred type. Heap-backed built-in values (a Promise, Map/Set, a generator, Date, null) are \"object\"; classes, implemented built-in constructors, and implemented global functions are \"function\"; Math/JSON/console are \"object\"; an undeclared name or runtime-absent static is \"undefined\".",
      "differences": [
        "A static-method reference on a namespace outside the covered tables — `Object.keys`, `console.log`, `Number.isInteger`, `Date.now` — answers from inference (usually `\"number\"`), not JS's `\"function\"`; the covered tables are the `Promise` combinators, the `Math` function set, and `JSON.parse`/`stringify`."
      ],
      "seeAlso": ["lang.instanceof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof" }
    },
    {
      "id": "lang.in",
      "name": "in operator (key in obj)",
      "statusRow": "in operator (key in obj)",
      "signature": "key in obj",
      "badge": "exact",
      "description": "Reduces to the same compile-time FieldIndex lookup Object.hasOwn() uses — object shapes are fully structural/static, so a string-literal key against an object is a compile-time-constant true/false, not a runtime scan. Parsed as a contextual keyword at instanceof precedence.",
      "seeAlso": ["lang.instanceof", "lang.objliteral"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/in" }
    },
    {
      "id": "lang.spread",
      "name": "Spread in array literals [...a, ...b]",
      "statusRow": "Spread in array literals [...a, ...b]",
      "signature": "[...a, ...b]",
      "badge": "exact",
      "description": "Array-literal spread.",
      "seeAlso": ["lang.restparams"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Spread_syntax" }
    },
    {
      "id": "lang.vardecl",
      "name": "const / let / var declarations",
      "statusRow": "const / let / var declarations",
      "signature": "const x = 1; let y; var z;",
      "badge": "caveats",
      "description": "var is function-scoped and re-declarable; let/const are block-scoped and const rebinding is rejected. A top-level const/let/var is a module global — readable and writable from any named function whenever its type is determinable before main() runs. Block redeclaration, TDZ, and use-before-assignment are compile errors.",
      "differences": [
        "Definite-assignment analysis is sound-not-complete: a typed `var`/`let` assigned only in a maybe-skipped loop or across correlated conditions still falls through to the `undefined`/zero default rather than raising `used before being assigned`.",
        "A top-level `const`/`let` still isn't readable from a named `function` only in the invariant's genuine edges — a `bigint`, a *scalar*-returning un-annotated call (the `i64` default), a `new Set([…])` typed from its initializer array, or an init depending on a runtime-local (a `{ …rest }` spread of a destructuring target); an arrow/closure still captures any of them."
      ],
      "seeAlso": ["lang.functiondecl", "lang.arrow"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/const" }
    },
    {
      "id": "lang.arraydestructure",
      "name": "Array destructuring const [a, b] = arr",
      "statusRow": "Array destructuring const [a, b] = arr",
      "signature": "const [a, , b, ...rest] = arr",
      "badge": "caveats",
      "description": "Array destructuring with holes, defaults (fire when past the source length), and a rest element that makes a real independent copy. Usable as a for-of loop variable and a destructured parameter; nested patterns work in a declaration/for-of/parameter.",
      "differences": [
        "*Assignment* to already-declared targets (`[a, b] = expr`, not a fresh declaration) is narrower: every target must be a plain variable — no nested patterns, no default, no compound operator.",
        "Nested patterns work in a declaration, for-of loop variable, and destructured parameter, but not yet in the *assignment* form; a `= default` on a nested position is a clean rejection."
      ],
      "seeAlso": ["lang.objdestructure", "lang.destructparams"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment#array_destructuring" }
    },
    {
      "id": "lang.objdestructure",
      "name": "Object destructuring const { x, y } = obj",
      "statusRow": "Object destructuring const { x, y } = obj",
      "signature": "const { x, y: z, ...rest } = obj",
      "badge": "caveats",
      "description": "Object destructuring with renaming, string/numeric-literal keys, nested patterns, and a rest element that binds a fresh residual object. Usable as a for-of loop variable and destructured parameter. Assignment to already-declared targets requires parens.",
      "differences": [
        "Default values (`{ x = 1 }`) are supported only for a field typed as a nullable *reference* (`T | null` where T is string/array/object/class) — the only field shape with a real \"not provided\" signal; a default on a nullable scalar or any non-nullable field is a clean compile-time rejection.",
        "*Assignment* to already-declared targets is the same narrower scope as the array form; nested patterns don't work in the assignment form and there's no `= default` on a nested position.",
        "`{ ...rest }` over an `any`/union/generic source and in the *assignment* form (`({ a, ...rest } = e)`) stay clean rejections.",
        "No computed key (`{ [key]: val }`).",
        "The object-*type* annotation grammar still rejects a string-literal-named field, so a genuinely non-identifier field name (`\"first-name\"`) is reachable only via object-literal type inference."
      ],
      "seeAlso": ["lang.arraydestructure", "lang.objliteral"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment#object_destructuring" }
    },
    {
      "id": "lang.taggedtemplate",
      "name": "Tagged template literals",
      "statusRow": "Tagged template literals ( tagHello ${x} )",
      "signature": "tag`Hello ${x}`",
      "badge": "caveats",
      "description": "Desugars to a plain call (tag([\"a\",\"b\"], x)) — interpolated values reach the tag as real typed values, not implicitly stringified. Any callable works as a tag, including an arrow/closure with a ...rest parameter.",
      "differences": [
        "No `.raw` property on the `strings` array (real `String.raw` stays separately out of scope)."
      ],
      "seeAlso": ["lang.template"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Template_literals#tagged_templates" }
    },
    {
      "id": "lang.functiondecl",
      "name": "Function declarations (top-level)",
      "statusRow": "Function declarations (top-level)",
      "signature": "function f(x: number): number { … }",
      "badge": "caveats",
      "description": "Named top-level functions with typed params and return. A leading this parameter (function f(this: T, …)) is parsed and erased, exactly as real TS does. The synthesized arguments object supports .length, indexing, and for…of when all declared params share one type.",
      "differences": [
        "Cannot reference a sibling top-level binding whose value is a connection handle (`Worker`/`WebSocket`/`EventSource`, `BroadcastChannel`/`MessageChannel`, `XMLHttpRequest`), a `Promise`, or a generic class instance with **inferred** rather than explicit type arguments (`new Box(5)`) — such a binding stays a `main()` local and fails with `undefined variable`. Scalars, strings, arrays, `TypedArray`s, objects, `Map`/`Set`, class instances (including explicit `new Box<number>()`), the value/event handles, and the streams + `EventEmitter` and `http.createServer` handles are promoted to module globals; arrow functions/closures capture everything regardless.",
        "A genuinely circular pair of mutually-recursive unannotated functions can't converge on a return type and keeps the scalar-default fallback.",
        "The `arguments` object reflects the declared parameters rather than growing with extra untyped arguments; mixed-type parameters, a rest/destructured parameter, and an arrow function (which has no own `arguments` in JS) are clean rejections."
      ],
      "seeAlso": ["lang.nestedfn", "lang.arrow"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/function" }
    },
    {
      "id": "lang.arrow",
      "name": "Arrow functions / lambdas",
      "statusRow": "Arrow functions / lambdas",
      "signature": "(x: number) => x * 2",
      "badge": "exact",
      "description": "Full closures; captures via a heap-allocated env struct.",
      "snippet": "function makeCounter(): () => number {\n  let count = 0\n  return (): number => {\n    count = count + 1\n    return count\n  }\n}",
      "seeAlso": ["lang.fnexpr", "lang.functiondecl"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions" }
    },
    {
      "id": "lang.fnexpr",
      "name": "Function expressions",
      "statusRow": "Function expressions (var f = function(x) {...}, callback arguments, IIFE bodies)",
      "signature": "const f = function(x) { … }",
      "badge": "caveats",
      "description": "Anonymous and named function expressions, including as callback arguments. A named function expression binds its own name inside its own body for recursion without leaking. async function(...) {...} expressions are supported. Reuses arrow closure machinery.",
      "snippet": "const add = function (x: number, y: number): number {\n  return x + y;\n};",
      "differences": [
        "A named function expression whose name shadows a *top-level function* of the same name is a clean compile error — rename one of them.",
        "An IIFE parses cleanly but hits the separate \"only simple function calls are supported\" rejection when actually called inline.",
        "A generic function used by value is out of scope (no single monomorphized symbol to point at)."
      ],
      "seeAlso": ["lang.arrow", "lang.callapplybind"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/function" }
    },
    {
      "id": "lang.defaultparams",
      "name": "Default parameter values",
      "statusRow": "Default parameter values",
      "signature": "function f(a: number, b = 2) { … }",
      "badge": "caveats",
      "description": "Default parameter values.",
      "differences": [
        "A default expression can't reference an earlier parameter — `function f(a: number, b: number = a * 2)` fails with `undefined variable 'a'` when evaluating `b`'s default, even though real TS allows this."
      ],
      "seeAlso": ["lang.optparams"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters" }
    },
    {
      "id": "lang.optparams",
      "name": "Optional parameters (param?)",
      "statusRow": "Optional parameters (param?)",
      "signature": "function f(x?: number) { … }",
      "badge": "caveats",
      "description": "Optional parameters.",
      "differences": [
        "An omitted `?` argument gets the parameter type's zero value — the same `undefined` stand-in default parameter values/destructuring defaults use, not real JS's `undefined`."
      ],
      "seeAlso": ["lang.defaultparams"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions#optional_parameters" }
    },
    {
      "id": "lang.restparams",
      "name": "Rest parameters (...args: number[])",
      "statusRow": "Rest parameters (...args: number[])",
      "signature": "function f(...args: number[]) { … }",
      "badge": "exact",
      "description": "Works on named function declarations, class methods, arrow functions/closures, and function type annotations ((...xs: number[]) => R and the same as a type alias) alike.",
      "seeAlso": ["lang.spread", "lang.destructparams"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/rest_parameters" }
    },
    {
      "id": "lang.destructparams",
      "name": "Destructured function parameters",
      "statusRow": "Destructured function parameters (function f({ x, y }: T) {} / function f([a, b]: T[]) {})",
      "signature": "function f({ x, y }: T) { … }",
      "badge": "caveats",
      "description": "Object- and array-destructured parameters. Each element/field may have its own default; nested patterns work; supported on named functions, class methods/constructors, and arrow functions.",
      "differences": [
        "Requires an explicit type annotation on the pattern (no sensible unannotated default the way a scalar param has); no combination with `...` or a whole-parameter default value.",
        "A `= default` on a nested position is a clean rejection; per-element defaults follow the same rules as the statement-level destructuring rows."
      ],
      "seeAlso": ["lang.arraydestructure", "lang.objdestructure"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Destructuring_assignment" }
    },
    {
      "id": "lang.nestedfn",
      "name": "Nested function declarations",
      "statusRow": "Nested function declarations (function outer() { function inner() {...}; return inner(); })",
      "signature": "function outer() { function inner() { … } }",
      "badge": "caveats",
      "description": "A helper function declared inside another function's body — hoisted, forward-referenceable, self-recursive, and visible to its own further-nested descendants.",
      "snippet": "function outer(x: number): number {\n  function double(y: number): number {\n    return y * 2;\n  }\n  return double(x) + 1;\n}",
      "differences": [
        "V1-scoped: only directly in the enclosing function/arrow body's own immediate statement list — one block deeper (inside a further `if`/`for`/`while`/`switch`) is a clean compile error.",
        "Captures enclosing locals/parameters by reference, but only for use **at or after** the declaration point — a capturing nested function called before its declaration is a clean error (a non-capturing one keeps full hoisting/forward-reference). Capturing an array variable, and a capturing nested *generator*, stay clean rejections.",
        "Generic (`<T>`) nested declarations and same-scope duplicate names are clean compile errors."
      ],
      "seeAlso": ["lang.functiondecl", "lang.arrow"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/function" }
    },
    {
      "id": "lang.callapplybind",
      "name": "Function.prototype.call / .apply / .bind",
      "statusRow": "Function.prototype.call / .apply / .bind",
      "signature": "f.call(thisArg, a, b); f.apply(null, arr); f.bind(null, a)",
      "badge": "caveats",
      "description": "f.call/f.apply lower to a direct call; f.apply(thisArg, arr) spreads into a rest slot; f.bind builds a new partially-applied closure. All type-checked against f's parameters exactly as a normal call.",
      "snippet": "const add = (a: number, b: number): number => a + b;\nconsole.log(add.call(null, 3, 4));\nconsole.log(add.apply(null, [5, 6]));",
      "differences": [
        "`thisArg` is evaluated then **ignored** — a closure has no rebindable `this`, so method-borrowing is unsupported; only the arguments are forwarded.",
        "Only on a first-class function **value** (a closure, function parameter, arrow/function expression, or named function) — call/apply/bind on a built-in isn't supported.",
        "`.apply` takes either a literal array or a runtime array spread into a **rest** parameter; a runtime array into a fixed-arity function is the usual spread-into-fixed-arity rejection.",
        "`.bind` is V1-scoped to functions whose parameters are plain scalar/string/pointer types with no rest slot — an array/nullable-scalar/rest parameter is a clean compile error."
      ],
      "seeAlso": ["lang.fnexpr", "lang.arrow"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/call" }
    },
    {
      "id": "lang.overloads",
      "name": "Function overload signatures",
      "statusRow": "Function overload signatures (function f(x: number): number; + impl)",
      "signature": "// not supported",
      "badge": "missing",
      "description": "Standalone overload signatures are not supported. Write a single implementation signature and union the parameter/return types instead.",
      "differences": [
        "A standalone overload signature (a `function` head terminated by `;` with no body, followed by the implementation) is a parse error (`expected {, got ;`). Write a single implementation signature — union the parameter/return types instead."
      ],
      "seeAlso": ["lang.functiondecl"],
      "spec": { "mdn": "https://www.typescriptlang.org/docs/handbook/2/functions.html#function-overloads" }
    },
    {
      "id": "lang.generators",
      "name": "Generator functions",
      "statusRow": "Generator functions (function* f(): T { yield x; }, .next(value), for...of)",
      "signature": "function* f(): T { yield x; }",
      "badge": "caveats",
      "description": "Real suspend/resume via this compiler's ucontext-based fiber primitive. The full iterator protocol: .throw(e) injects at the suspension point, .return(v) completes running enclosing finally blocks, and yield* delegates to an inner generator.",
      "snippet": "function* basic(): number {\n  yield 1;\n  yield 2;\n  return 3;\n}\nconst g = basic();\nconsole.log(g.next().value, g.next().done);",
      "differences": [
        "A free `function*` may be top-level **or nested** (a nested one captures enclosing state by reference; an *array* capture stays a clean rejection); a generator *expression* is supported only as a top-level `const/let/var G = function* ...` binding — an argument/nested/IIFE use is a clean rejection. `static`/`abstract` generator methods stay clean rejections.",
        "The return-type annotation is optional (inferred from the body's yields); still requires a plain non-destructured parameter list and a non-array element/parameter type.",
        "`break`ing out of a `for...of` over a generator leaves its fiber simply abandoned — the loop doesn't auto-call `.return()`, so a pending `finally` doesn't run on that path (an explicit `.return()` does run it).",
        "Calling `.next()` again after completion returns `{value: <T's zero value>, done: true}` — this compiler's usual stand-in for real JS's `undefined`."
      ],
      "seeAlso": ["lang.asyncgen", "lang.forof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/function*" }
    },
    {
      "id": "lang.asyncfn",
      "name": "async functions",
      "statusRow": "async functions",
      "signature": "async function f(): Promise<T> { … }",
      "badge": "exact",
      "description": "Every async fn returns a real task-shaped Promise<T>: a fn containing an await runs on a ucontext fiber; a no-await one settles inline. Named declarations, async arrows, and async function expressions are all supported. No libcurl linked unless fetch is used.",
      "snippet": "async function add(a: number, b: number): Promise<number> {\n  return a + b\n}\nconst sum = await add(10, 32)",
      "seeAlso": ["lang.await", "lang.asyncgen"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function" }
    },
    {
      "id": "lang.await",
      "name": "await expressions",
      "statusRow": "await expressions",
      "signature": "const x = await p",
      "badge": "exact",
      "description": "A Promise is a reusable value — await p twice returns the same result. Every await yields a microtask tick, of an already-settled promise and of a non-promise value, FIFO-ordered with .then/queueMicrotask, matching JS. Registers a resume reaction and parks.",
      "seeAlso": ["lang.asyncfn", "lang.promiseexecutor"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/await" }
    },
    {
      "id": "lang.promisecombinators",
      "name": "Promise.all / .race / .allSettled",
      "statusRow": "Promise.all / .race / .allSettled",
      "signature": "await Promise.all([p1, p2])",
      "badge": "exact",
      "description": "Real concurrency over Array<Promise<Response>> (fetch) and over Array<Promise<T>> from may-suspend async fns. Each member is a reusable value. .race/.any return the shortest-delay member via an event-driven waiter list. A rejecting member rejects .all/.race and reports rejected in .allSettled.",
      "seeAlso": ["lang.promiseany", "lang.await"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/all" }
    },
    {
      "id": "lang.promiseany",
      "name": "Promise.any",
      "statusRow": "Promise.any",
      "signature": "await Promise.any([p1, p2])",
      "badge": "exact",
      "description": "Over task promises it skips rejected members and resolves to the first fulfilled one; when every member rejects it throws a real AggregateError carrying each reason on .errors. Over raw fetches, an HTTP 404/500 is a fulfilled fetch, per WHATWG.",
      "seeAlso": ["lang.promisecombinators"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/any" }
    },
    {
      "id": "lang.promiseresolvereject",
      "name": "Promise.resolve / Promise.reject",
      "statusRow": "Promise.resolve / Promise.reject",
      "signature": "Promise.resolve(v); Promise.reject(e)",
      "badge": "exact",
      "description": "Both build a settled task promise, so await/.then/.catch/.finally and the combinators all work. Promise.resolve(v) fulfils with v (an already-promise arg flattens through); Promise.reject(e) is typed Promise<never>, so a rejected value assigns to any binding. No fiber/libcurl linked.",
      "seeAlso": ["lang.promisethen", "lang.promiseexecutor"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/resolve" }
    },
    {
      "id": "lang.promiseexecutor",
      "name": "new Promise((resolve, reject) => …)",
      "statusRow": "new Promise((resolve, reject) => …) (executor constructor)",
      "signature": "new Promise((resolve, reject) => { … })",
      "badge": "exact",
      "description": "The executor constructor with thenable adoption (resolve(anotherPromise) settles the outer promise with the inner's value, reaction-based), synchronous resolve/reject, first-settle-wins, and deferred resolution from a setTimeout callback. The executor may be an arrow, function expression, closure variable, or bare top-level-function reference.",
      "differences": [
        "Only a typed `Promise` argument adopts; a custom duck-typed `{ then }` object isn't adopted (this compiler has no `PromiseLike<T>` type — it's a clean type mismatch, not a silent miss)."
      ],
      "seeAlso": ["lang.promisethen", "lang.await"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/Promise" }
    },
    {
      "id": "lang.promisethen",
      "name": "Promise.prototype.then / .catch / .finally + queueMicrotask",
      "statusRow": "Promise.prototype.then / .catch / .finally + queueMicrotask",
      "signature": "p.then(f).catch(g).finally(h)",
      "badge": "exact",
      "description": ".then/.catch/.finally directly on a raw fetch Promise<Response> never block the synchronous script — the wait is deferred to a queued microtask. The callback runs as a microtask when the promise settles and its result chains into the returned promise. A missing callback slot is a pass-through. queueMicrotask is a FIFO drained after the sync script and before timers.",
      "seeAlso": ["lang.promiseexecutor", "lang.await"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/then" }
    },
    {
      "id": "lang.asyncgen",
      "name": "Async generators (async function*, async *m())",
      "statusRow": "Async generators (async function*, async *m())",
      "signature": "async function* f() { yield await x; }",
      "badge": "exact",
      "description": "V8/spec step timing: .next() starts the body synchronously up to the first await/yield; an await inside the body parks the step. Top-level or nested async function* and a class async *m() method. .next() returns a real Promise<{value,done}>. Full protocol: .throw(e)/.return(v) and yield* over an inner async generator or a Symbol.asyncIterator iterable.",
      "seeAlso": ["lang.generators", "lang.forawaitof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function*" }
    },
    {
      "id": "lang.forawaitof",
      "name": "for await...of (async iteration)",
      "statusRow": "for await...of (async iteration)",
      "signature": "for await (const x of asyncIterable) { … }",
      "badge": "exact",
      "description": "Consumes an async-generator instance, a user class or object literal implementing the async-iteration protocol, or any sync iterable the sync for...of takes (awaiting each element). Awaits each .next() promise before reading .done/.value; the loop is itself a suspension point. A destructuring loop variable unpacks each element.",
      "seeAlso": ["lang.asyncgen", "lang.forof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for-await...of" }
    },
    {
      "id": "lang.enumsnum",
      "name": "Enums (numeric)",
      "statusRow": "Enums (numeric)",
      "signature": "enum E { A, B = 5 }",
      "badge": "exact",
      "description": "Numeric enums with auto-increment and explicit values.",
      "snippet": "enum Direction {\n  North,\n  East,\n  South,\n  West,\n}\nconsole.log(Direction.East)  // 1",
      "seeAlso": ["lang.enumsstr", "lang.switch"],
      "spec": { "mdn": "https://www.typescriptlang.org/docs/handbook/enums.html" }
    },
    {
      "id": "lang.enumsstr",
      "name": "Enums (string)",
      "statusRow": "Enums (string)",
      "signature": "enum E { A = 'a', B = 'b' }",
      "badge": "exact",
      "description": "String enums; members are interned string constants. The enum name (and its [] array form) as a type annotation resolves to the enum's ptr backing type, so a typed variable/param/field/array, comparison, and switch all work.",
      "seeAlso": ["lang.enumsnum"],
      "spec": { "mdn": "https://www.typescriptlang.org/docs/handbook/enums.html#string-enums" }
    },
    {
      "id": "lang.namespaces",
      "name": "Namespaces (namespace X {}, function merging)",
      "statusRow": "Namespaces (namespace X {}, function merging)",
      "signature": "namespace X { export function f() {} }",
      "badge": "caveats",
      "description": "Merges with a same-name top-level function X(...) — TS's callable-object duality idiom (assert(x) + assert.sameValue(...)); members desugar to flat top-level declarations.",
      "differences": [
        "V1 scope: top-level only, members must be `export function` / `export const`/`let` (single binding); no nesting, no `declare namespace`, no cross-module `export namespace`; the same namespace member declared in two files is a link-time duplicate-symbol error, not file-private."
      ],
      "seeAlso": ["lang.functiondecl"],
      "spec": { "mdn": "https://www.typescriptlang.org/docs/handbook/namespaces.html" }
    },
    {
      "id": "lang.interfaces",
      "name": "Interfaces (structural)",
      "statusRow": "Interfaces (structural)",
      "signature": "interface T { x: number }",
      "badge": "exact",
      "description": "Structural interfaces backed by heap-allocated objects.",
      "seeAlso": ["lang.typealias", "lang.objliteral"],
      "spec": { "mdn": "https://www.typescriptlang.org/docs/handbook/2/objects.html" }
    },
    {
      "id": "lang.typealias",
      "name": "Type aliases",
      "statusRow": "Type aliases",
      "signature": "type T = { x: number }",
      "badge": "exact",
      "description": "Type alias declarations.",
      "seeAlso": ["lang.interfaces"],
      "spec": { "mdn": "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#type-aliases" }
    },
    {
      "id": "lang.objliteral",
      "name": "Object literals { key: value }",
      "statusRow": "Object literals { key: value }",
      "signature": "{ key: value, \"foo\": 1, 0: \"a\" }",
      "badge": "caveats",
      "description": "A key may be an identifier, or a string/numeric literal.",
      "differences": [
        "A numeric-literal key isn't dot/bracket-readable back through existing access syntax and can't be declared in an `interface`/type annotation (quoted names are an object-literal-only extension so far) — still a real field, visible to `JSON.stringify`.",
        "No duplicate-`__proto__`-key detection (Annex B legacy `SyntaxError` rule)."
      ],
      "seeAlso": ["lang.objdestructure", "lang.interfaces"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Object_initializer" }
    },
    {
      "id": "lang.getset",
      "name": "Getters / setters on classes",
      "statusRow": "Getters / setters (get x() {} / set x(v) {}) on classes",
      "signature": "get x() { … } / set x(v) { … }",
      "badge": "caveats",
      "description": "get/set are contextual (not reserved). No new storage: an accessor is registered as an ordinary method under an internal dispatch key. obj.x / obj.x = v / compound assignment all route through the accessor, including from inside the class's own methods and through optional chaining.",
      "differences": [
        "Object-literal getters/setters, static accessors, and logical assignment (`&&=`/`||=`/`??=`) on an accessor are deliberately out of scope for V1."
      ],
      "seeAlso": ["lang.class"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/get" }
    },
    {
      "id": "lang.newerror",
      "name": "new Error(msg)",
      "statusRow": "new Error(msg)",
      "signature": "new Error(msg)",
      "badge": "exact",
      "description": "The base Error constructor.",
      "seeAlso": ["lang.errorsubtypes", "lang.throw"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error" }
    },
    {
      "id": "lang.errorsubtypes",
      "name": "Built-in Error subtypes",
      "statusRow": "Built-in Error subtypes (new TypeError(msg), RangeError, SyntaxError, EvalError, URIError, ReferenceError, DOMException) and instanceof against them",
      "signature": "new TypeError(msg); e instanceof RangeError",
      "badge": "caveats",
      "description": "A small fixed kind enum (a hidden runtime tag on the shared Error object shape, not a real class hierarchy), plus a real .name field. instanceof Error matches any kind; instanceof TypeError matches only that one. new DOMException(message?, name?) takes its .name from the 2nd argument.",
      "differences": [
        "No user-definable `class X extends Error` — built-in types aren't valid `extends` targets; Error subtyping stays on its own independent mechanism.",
        "`DOMException` carries no legacy numeric `.code`."
      ],
      "seeAlso": ["lang.newerror", "lang.instanceof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/TypeError" }
    },
    {
      "id": "lang.newarray",
      "name": "new Array<T>(n)",
      "statusRow": "new Array<T>(n)",
      "signature": "new Array<T>(n)",
      "badge": "exact",
      "description": "Constructs an array of length n.",
      "seeAlso": ["lang.newmap", "lang.newset"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/Array" }
    },
    {
      "id": "lang.newmap",
      "name": "new Map<K,V>()",
      "statusRow": "new Map<K,V>()",
      "signature": "new Map<K, V>()",
      "badge": "exact",
      "description": "Constructs a Map.",
      "seeAlso": ["lang.newset", "lang.newarray"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map/Map" }
    },
    {
      "id": "lang.newset",
      "name": "new Set<T>()",
      "statusRow": "new Set<T>()",
      "signature": "new Set<T>()",
      "badge": "exact",
      "description": "Constructs a Set.",
      "seeAlso": ["lang.newmap", "lang.newarray"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Set/Set" }
    },
    {
      "id": "lang.class",
      "name": "class (fields, constructor, methods, this, new)",
      "statusRow": "class (fields, constructor, methods, this, new ClassName(args))",
      "signature": "class C { x: number; constructor(x) { this.x = x } m() {} }",
      "badge": "caveats",
      "description": "Instances reuse the same heap-object/GEP machinery interfaces use; methods compile to plain static calls (this as an implicit first arg). Instance-field initializers work, and a class with fields needs no explicit constructor. Static field initializers run in declaration order. Qualified constructors/bases parse.",
      "snippet": "class Point {\n  x: number;\n  y: number;\n  constructor(x: number, y: number) {\n    this.x = x;\n    this.y = y;\n  }\n}\nconst p = new Point(3, 4);",
      "differences": [
        "A **derived** class adding fields when its base has a rest-parameter constructor requires an explicit `super(...)`. An instance-field initializer lowered into the constructor can currently see the constructor's own parameters (which real JS's separate initializer scope forbids), and an unannotated initializer of an unrecognized expression shape falls back to `i64`.",
        "A computed/dynamic member name is a clean rejection; the recognized well-known-symbol keys are `[Symbol.asyncIterator]` and `[Symbol.iterator]`.",
        "A class expression used as a runtime value or a named self-reference (`class D {...}` whose body references `D`) is a clean rejection; `extends <expression>` is out of scope.",
        "Class early-error gaps (deferred): a field initializer containing `arguments` or a `super()` call, a `super()` in a method parameter default, field-definition ASI on the same line, and the `#constructor` private-name ban all compile instead of raising the spec's `SyntaxError`.",
        "**Parameter properties are not supported** — a modifier on a constructor parameter (`constructor(public x: number)` / `private` / `readonly` / `protected`) is a parse error; declare the field explicitly and assign it in the constructor body.",
        "The **`readonly` field modifier** is not parsed (`readonly x: number` reads `readonly` as the field name); drop it — there's no mutation checking anyway."
      ],
      "seeAlso": ["lang.classstatic", "lang.classinherit", "lang.getset"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes" }
    },
    {
      "id": "lang.classstatic",
      "name": "class static members / visibility / abstract / implements",
      "statusRow": "class static members/static {} blocks, private/protected visibility, abstract classes/methods, implements",
      "signature": "class C { static x = 1; static {} private y; abstract m(); }",
      "badge": "exact",
      "description": "static members are inherited through extends (a non-redeclared static field shares its base's storage). private/protected are compile-time-only (zero runtime check, matching TS erasure) and reject super.privateMethod(). abstract reuses override-detection; implements is a compile-time structural self-check.",
      "seeAlso": ["lang.class", "lang.privatefield"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/static" }
    },
    {
      "id": "lang.privatefield",
      "name": "Real JS/TS #x runtime-private field syntax",
      "statusRow": "Real JS/TS #x runtime-private field syntax",
      "signature": "class C { #x; #m() {} }",
      "badge": "caveats",
      "description": "Fields, methods, static fields/methods, and get/set accessors — exact-class-only access (the same rule private enforces), plus real reflection-hiding (Object.keys/.values/.entries/JSON.stringify/spread never see a #-named field, unlike private's type-level-only erasure).",
      "differences": [
        "No class-field-initializer syntax (`#x = 1;` — a pre-existing gap for every field, not `#`-specific).",
        "No early-error check for a `#m`/`static #m` name collision in the same class, or for the `#constructor`-is-always-banned rule."
      ],
      "seeAlso": ["lang.classstatic", "lang.class"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/Private_properties" }
    },
    {
      "id": "lang.classinherit",
      "name": "class inheritance (extends/super)",
      "statusRow": "class inheritance (extends/super)",
      "signature": "class D extends B { constructor() { super(); } }",
      "badge": "exact",
      "description": "Single inheritance, base-first field layout, super(args)/super.method(args), and an implicit pass-through constructor when a derived class adds no fields. Dispatch is static except for a method provably overridden somewhere, which goes through a per-tree vtable (decided at compile time via whole-program analysis). extends also accepts EventEmitter<T>.",
      "seeAlso": ["lang.class", "lang.instanceof"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes/extends" }
    },
    {
      "id": "lang.instanceof",
      "name": "instanceof (against user-defined classes)",
      "statusRow": "instanceof (against user-defined classes)",
      "signature": "x instanceof C",
      "badge": "caveats",
      "description": "Every instance carries a hidden runtime type tag. A class-typed variable whose static class is T or an ancestor of T folds to a compile-time constant; one whose static class is an ancestor of the right-hand class needs a real runtime tag read. instanceof against Array/Map/Set/Date/RegExp is a compile-time constant.",
      "differences": [
        "`instanceof Object` isn't supported — real JS's \"true for anything non-primitive\" semantics would need an exhaustive, easy-to-drift enumeration of every object-shaped type this compiler has."
      ],
      "seeAlso": ["lang.classinherit", "lang.errorsubtypes"],
      "spec": { "mdn": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/instanceof" }
    }
  ]
}
