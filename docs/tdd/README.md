# Technical Design Documents (TDDs)

This folder tracks scoping/design work done *before* a feature is implemented — the problem, the design options considered, tradeoffs, and prerequisites. `STATUS.md` was growing a "Design Notes"/"Scoping" section directly inline for every not-yet-built feature, which made it harder to scan for actual implementation status; those sections now live here instead, with `STATUS.md` linking to them.

## Numbering

- Files are named `TDD-NNNNN.md`, zero-padded to 5 digits, starting at [TDD-00001](TDD-00001.md).
- Numbers are assigned sequentially and never reused, the same convention `docs/adr/` uses.
- Before creating a new one, check the Index below for the last number used.

## Cross-referencing

Every mention of another TDD or an ADR — in prose, in the `Status` line, in `STATUS.md`, anywhere — is a real markdown link to that file (`[TDD-00006](TDD-00006.md)` from within `docs/tdd/`, `[ADR-00048](../adr/ADR-00048.md)` from outside it), not a bare `TDD-00006` or a backtick-quoted path. See `docs/adr/README.md`'s own Cross-referencing section for the one exception (a document referring to itself).

## Relationship to ADRs

A TDD **does** carry a status field, unlike an ADR: `Not Started | In Progress | Partially Implemented | Implemented | Superseded`, kept current as work actually happens — this is the quick-reference layer for "what's actually done" that `STATUS.md`'s own summary and this folder's Index both draw from. That's the opposite of an ADR, which has no status field at all, because an ADR is only ever written *after* something is finished (see `docs/adr/README.md`) — there's nothing to track.

The **design content itself** (Context, Design, Prerequisites, Open questions) still isn't edited to match what actually shipped, even as the status field moves. Once a TDD's feature is actually implemented (fully or in part):

- Write an ADR documenting what was actually built — this is the existing standing rule for every feature/bugfix, unchanged.
- Cross-reference the TDD from the ADR's `Relations` field (`Implements TDD-NNNNN`), and update the TDD's own `Status` line to point back at the ADR.
- If the real implementation diverged from the original design, that divergence belongs in the ADR ("here's what was planned, here's what was actually built and why"), not retrofitted into the TDD's Design section — the TDD stays the honest historical record of the thinking at the time. Only the `Status` line (and, for a genuinely abandoned/replaced design, a note that it was superseded) is ever touched after the fact.

The Index below keeps each of these as its own column rather than folding them together: **Status** is the bare status word only (matching the set above); **Related ADRs** lists every ADR that implements/touches this TDD, each a real link; **Notes** is for anything else worth a scanner seeing at a glance (a real caveat, an in-progress split like [TDD-00010](TDD-00010.md)'s V1/V2, an unverified platform claim) — prose, not another status value. Keeping these separate is what keeps the table scannable; when adding or updating a row, put each piece of information in its own column rather than appending it to Status as trailing text.

## Format

Copy [`TEMPLATE.md`](TEMPLATE.md) as a starting point. At minimum, a TDD should cover:

- **Context** — the problem or need, and why it's being scoped now.
- **Design** — the approach(es) considered, in enough detail that implementation could start directly from it. Lay out tradeoffs if multiple options were weighed; note a recommended direction if one exists, and why.
- **Prerequisites** — what's already built and reusable vs. what's still missing, so the work can be picked off incrementally rather than discovered mid-implementation.

## Index

| # | Title | Status | Related ADRs | Notes |
|---|---|---|---|---|
| [00001](TDD-00001.md) | Memory Management: three mutually-exclusive compilation modes (`manual`/`gc`/`auto`) | Partially Implemented | [ADR-00030](../adr/ADR-00030.md), [ADR-00071](../adr/ADR-00071.md) | |
| [00002](TDD-00002.md) | Timers (setTimeout/setInterval) | Implemented | [ADR-00031](../adr/ADR-00031.md) | |
| [00003](TDD-00003.md) | Alternative fetch Backend: a Go helper instead of libcurl | Not Started | | |
| [00004](TDD-00004.md) | HTTP Server | Implemented | [ADR-00048](../adr/ADR-00048.md), [ADR-00049](../adr/ADR-00049.md), [ADR-00072](../adr/ADR-00072.md) | |
| [00005](TDD-00005.md) | Unannotated parameter typing | Superseded | [ADR-00042](../adr/ADR-00042.md) | Superseded by [TDD-00076](TDD-00076.md). Option 1 (call-site rejection) shipped as [ADR-00042](../adr/ADR-00042.md) and remains current behavior; options 2–3 (call-site inference / real `any` semantics) re-scoped by [TDD-00076](TDD-00076.md) — nothing left pending here |
| [00006](TDD-00006.md) | Event Loop | Implemented | [ADR-00048](../adr/ADR-00048.md), [ADR-00049](../adr/ADR-00049.md), [ADR-00050](../adr/ADR-00050.md), [ADR-00051](../adr/ADR-00051.md), [ADR-00052](../adr/ADR-00052.md) | |
| [00007](TDD-00007.md) | Coerce object literal fields against their declared type | Implemented | [ADR-00077](../adr/ADR-00077.md) | |
| [00008](TDD-00008.md) | External conformance suites (TypeScript + Test262) as a test-coverage benchmark | Superseded | [ADR-00047](../adr/ADR-00047.md), [ADR-00153](../adr/ADR-00153.md) | Superseded by [TDD-00082](TDD-00082.md), which widens the external-benchmark story from one language suite to all three status buckets. The Test262 track (V2 full-corpus harness, ~9.8%, `docs/testing/CONFORMANCE-RESULTS.md`) and the six hand-ported shift tests carry forward live and unchanged; the TypeScript-suite side stays checklist-only (no runtime oracle to automate). Nothing retracted — only rehomed |
| [00009](TDD-00009.md) | Classes / OOP (methods, constructors, inheritance) | Implemented | [ADR-00062](../adr/ADR-00062.md), [ADR-00063](../adr/ADR-00063.md), [ADR-00064](../adr/ADR-00064.md), [ADR-00067](../adr/ADR-00067.md), [ADR-00083](../adr/ADR-00083.md), [ADR-00084](../adr/ADR-00084.md) | |
| [00010](TDD-00010.md) | Generics on user-defined functions and interfaces | Implemented | [ADR-00103](../adr/ADR-00103.md), [ADR-00121](../adr/ADR-00121.md) | V1 (default: monomorphized, functions/interfaces/classes) vs. V2 (`@erased` opt-in, functions only, bare `T` positions only) |
| [00011](TDD-00011.md) | IndexedDB-Compatible Storage API (pluggable embedded/proxy backends) | Not Started | | |
| [00012](TDD-00012.md) | Computed property keys (`{ [expr]: value }`) | Implemented | [ADR-00066](../adr/ADR-00066.md) | |
| [00013](TDD-00013.md) | Error subtypes / tagged errors | Implemented | [ADR-00082](../adr/ADR-00082.md) | |
| [00014](TDD-00014.md) | Full-pipeline (codegen-through-binary) fuzz testing | Implemented | [ADR-00070](../adr/ADR-00070.md) | |
| [00015](TDD-00015.md) | `JSON.parse` into nested object fields | Superseded | [ADR-00189](../adr/ADR-00189.md) | Superseded by [TDD-00077](TDD-00077.md), which reverses this doc's additive span-finding design for a full dynamic-tree rewrite once the goal widened to the whole JSON surface (`SyntaxError` validation + `any`-typed parse, neither reachable by span-finding). The array-typed-field clean-rejection drive-by this TDD found still shipped independently as [ADR-00189](../adr/ADR-00189.md) |
| [00016](TDD-00016.md) | `Promise.all` / `.race` / `.allSettled` | Implemented | [ADR-00073](../adr/ADR-00073.md) | |
| [00017](TDD-00017.md) | `fetch()` client parity — custom method, headers, request body | Implemented | [ADR-00074](../adr/ADR-00074.md) | |
| [00018](TDD-00018.md) | `ArrayBuffer` / TypedArrays | Implemented | [ADR-00078](../adr/ADR-00078.md) | |
| [00019](TDD-00019.md) | POSIX signal handling (`process.on('SIGINT'/'SIGTERM', handler)`) | Implemented | [ADR-00079](../adr/ADR-00079.md) | |
| [00020](TDD-00020.md) | Windows support | Not Started | | |
| [00021](TDD-00021.md) | `#x` real private fields | Implemented | [ADR-00155](../adr/ADR-00155.md) | Field-initializer syntax (`#x = v`, and public `x = v`) now done — [TDD-00063](TDD-00063.md) Stage 1 / [ADR-00180](../adr/ADR-00180.md); no early-error check for `#m`/`static #m` name collision or `#constructor`-is-banned |
| [00022](TDD-00022.md) | Best-effort vanilla JavaScript compatibility (opt-in) | Not Started | | |
| [00023](TDD-00023.md) | `EventEmitter<T>` (`events` module) | Implemented | [ADR-00089](../adr/ADR-00089.md) | |
| [00024](TDD-00024.md) | `os` module | Implemented | [ADR-00090](../adr/ADR-00090.md) | Darwin paths verified on Apple Silicon M4 Pro (`speed` 0 on M-series, no `irq` bucket — both documented-permanent) |
| [00025](TDD-00025.md) | Multi-process clustering for `http.listen()` | Implemented | [ADR-00097](../adr/ADR-00097.md), [ADR-00098](../adr/ADR-00098.md), [ADR-00099](../adr/ADR-00099.md), [ADR-00101](../adr/ADR-00101.md) | Both `manual` and `-mm=gc` modes production-quality; [ADR-00101](../adr/ADR-00101.md) root-caused and fixed the `-mm=gc` intermittent hang |
| [00026](TDD-00026.md) | Binary-safe `http.listen()` request/response bodies | Implemented | [ADR-00106](../adr/ADR-00106.md) | |
| [00027](TDD-00027.md) | `http.close()` (graceful listener teardown) | Implemented | [ADR-00102](../adr/ADR-00102.md) | |
| [00028](TDD-00028.md) | Array/Map/Set/EventEmitter literals as general expressions (not just var-decl initializers) | Implemented | [ADR-00104](../adr/ADR-00104.md) | |
| [00029](TDD-00029.md) | Array-of-arrays (nested array) storage representation | Implemented | [ADR-00105](../adr/ADR-00105.md), [ADR-00107](../adr/ADR-00107.md) | `.flat()`/`.flatMap()` followed as a direct follow-on |
| [00030](TDD-00030.md) | Getters / setters (`get x() {}` / `set x(v) {}`) on classes | Implemented | [ADR-00110](../adr/ADR-00110.md) | Object-literal getters/setters out of scope |
| [00031](TDD-00031.md) | Terminal UI primitives (raw mode, tty size, key reads) | Not Started | | |
| [00032](TDD-00032.md) | Native library bindings / GUI (placeholder — general FFI is the real prerequisite) | Not Started | | Bootstrapping placeholder, not a committed design; see the TDD's own scope note |
| [00033](TDD-00033.md) | Direct hardware/framebuffer access (placeholder) | Not Started | | Bootstrapping placeholder; Linux-only by nature, a first for this project |
| [00034](TDD-00034.md) | `TextDecoder` non-UTF-8 encoding support (staged) | Not Started | | Low priority throughout; Stage 4 (most WHATWG labels) not expected to ever be picked up |
| [00035](TDD-00035.md) | RegExp support (staged, PCRE2-backed) | Implemented | [ADR-00114](../adr/ADR-00114.md), [ADR-00115](../adr/ADR-00115.md), [ADR-00116](../adr/ADR-00116.md), [ADR-00117](../adr/ADR-00117.md), [ADR-00118](../adr/ADR-00118.md), [ADR-00119](../adr/ADR-00119.md), [ADR-00120](../adr/ADR-00120.md) | All 7 stages (0-6) done — full method surface (construction/literal/fields, `.test()`, `.exec()`, `str.match()`/`str.matchAll()`, `str.replace()`/`str.replaceAll()`, `str.split()`/`str.search()`) plus `--static` linking verified on bare Linux and in a real Docker `scratch` container |
| [00036](TDD-00036.md) | Freestanding microcontroller target (Raspberry Pi Pico) — minimal core | Not Started | | Deliberately low priority; minimal-core scope only, full networking/storage/peripheral parity deferred to later TDDs |
| [00037](TDD-00037.md) | Multiple type parameters for user-defined generics (`<K, V>`) | Implemented | [ADR-00132](../adr/ADR-00132.md) | Extends [TDD-00010](TDD-00010.md)'s single-type-parameter scope; unconstrained only, no explicit call-site type arguments |
| [00038](TDD-00038.md) | `EventSource` (Server-Sent Events, staged) | Implemented | [ADR-00122](../adr/ADR-00122.md), [ADR-00123](../adr/ADR-00123.md), [ADR-00124](../adr/ADR-00124.md), [ADR-00129](../adr/ADR-00129.md) | All 4 stages done: connection plumbing, SSE parsing/`onmessage`, named events/`addEventListener`/`onopen`/`onerror`, CRLF-tolerant boundaries + terminal-failure detection + auto-reconnect |
| [00039](TDD-00039.md) | `WebSocket` (server upgrade + client, staged) | Implemented | [ADR-00125](../adr/ADR-00125.md), [ADR-00126](../adr/ADR-00126.md), [ADR-00127](../adr/ADR-00127.md), [ADR-00128](../adr/ADR-00128.md) | All 4 stages done: SHA-1 + shared frame codec; `http.listen(..., { ws })` handshake + echo; automatic ping/pong + close handshake; client-side `new WebSocket(url)` (`ws://` only, synchronous connect). `wss://`/TLS and message fragmentation deliberately out of scope, see the doc's Open Questions |
| [00040](TDD-00040.md) | Real `Request`/`Headers` classes and `XMLHttpRequest` (staged) | Implemented | [ADR-00130](../adr/ADR-00130.md), [ADR-00131](../adr/ADR-00131.md) | Both stages done. Also renamed `http.listen`'s server-side `Request` annotation to `HttpRequest`, freeing the name for the client-side class — not scoped by the TDD's own naming note, found only during implementation (see ADR-00130) |
| [00041](TDD-00041.md) | True per-file module scope (mangled internal names) | Implemented | [ADR-00134](../adr/ADR-00134.md) | Also unblocked import aliasing as a direct consequence — see `docs/status/MODULES.md` |
| [00042](TDD-00042.md) | `export default`, default imports, and namespace imports | Implemented | [ADR-00135](../adr/ADR-00135.md) | Namespace imports are compile-time-only, never a runtime value; re-exports deliberately left for a future TDD |
| [00043](TDD-00043.md) | General union types (beyond `T \| null`) | Implemented | [ADR-00136](../adr/ADR-00136.md) | V1 scoped to scalar members only (`number`/`string`/`boolean`/`null`/`undefined`), not yet supported nested inside an array/object; object/array members and flow-based narrowing deliberately deferred |
| [00044](TDD-00044.md) | `symbol` V1 (opaque unique values) | Implemented | [ADR-00138](../adr/ADR-00138.md) | Scoped to `Symbol()`/`===`/`typeof`/`.description`/`.toString()` only — no dynamic property keys, no well-known symbols, no `Symbol.for` registry |
| [00045](TDD-00045.md) | Raspberry Pi (aarch64 SBC) target — minimal boot image + GPIO | Not Started | | Distinct from [TDD-00036](TDD-00036.md)'s freestanding Pico target — this is a hosted Linux target; low priority relative to core-language work |
| [00046](TDD-00046.md) | Opt-in embedded JS engine for `eval`/`vm` (pluggable backend, quickjs-ng first) | Not Started | | Deliberately low priority; gated behind a compile-time `-eval-engine` flag so it's never linked in by accident |
| [00047](TDD-00047.md) | `Worker` (background-thread concurrency) | Not Started | | High effort; found concrete process-wide singleton state (exception jump-buffer globals, GC stack-bottom tracking) that needs fixing before real OS threads are safe; staged Node `worker_threads` first, browser `Worker` global later; depends on [TDD-00049](TDD-00049.md) |
| [00048](TDD-00048.md) | WebAssembly target (`wasm32-unknown-wasi`) | Not Started | | Not a queued priority — reference/feasibility document. Shares its root blocker (no build-host/compile-target distinction anywhere in this compiler) with [TDD-00020](TDD-00020.md); `-mm=gc` and the `ucontext`-based fiber scheduler have no real wasm equivalent at all |
| [00049](TDD-00049.md) | Import-gated built-in bindings (collision-safe `fs`/`path`/`os`/…/`Memory`) | Implemented | [ADR-00141](../adr/ADR-00141.md), [ADR-00142](../adr/ADR-00142.md) | Stage 1 (default/namespace-form imports) closed the empirically-reproduced shadowing bug for all Category B built-ins; Stage 2 (named per-member imports, e.g. `import { readFileSync } from 'fs'`) needed zero codegen changes — resolved entirely by synthesizing the same member-expression Stage 1 already dispatches on. `process` deliberately stays ambient (matches real Node's `typeof process` environment-detection idiom); a future `window` global would need the same treatment, kept mutually consistent — see the TDD's Design section |
| [00050](TDD-00050.md) | Reserved ambient-global names — reject by default, `-globals=permissive` opt-in for real-JS shadowing | Implemented | [ADR-00143](../adr/ADR-00143.md) | Splits into two tiers found by directly reading the parser: plain-identifier globals (`Math`/`process`/`fetch`/…, cheap to make correctly shadowable) vs. `new`-form built-ins (`Map`/`Date`/`RegExp`/…, parser-level special forms — decided to stay reserved even under the permissive flag) |
| [00051](TDD-00051.md) | Re-exports (`export { a } from './x'`, `export * from './x'`) | Implemented | [ADR-00144](../adr/ADR-00144.md) | Namespace re-export (`export * as ns from`), local export lists without `from`, and re-exporting from a built-in module are explicit scope cuts, not oversights — see the TDD's Design section |
| [00052](TDD-00052.md) | Top-level side-effecting code in imported files (run once, dependency order, cycle-guarded) | Implemented | [ADR-00145](../adr/ADR-00145.md) | Pre-implementation investigation found a live, pre-existing UB-shaped gap (a circular pair of files reading each other's uninitialized top-level bindings via `VarDeclaration.Init`) — this TDD's cycle-detection work closes it as a natural consequence rather than as a separate fix, see its Context section |
| [00053](TDD-00053.md) | npm/`node_modules` interop (best-effort, opt-in) | Not Started | | Bottlenecked on [TDD-00022](TDD-00022.md) — npm packages ship compiled JS + signature-only `.d.ts`, neither usable by this compiler; realistic scope is a package that happens to publish its own `.ts` source with ~0 dependencies of its own. Lower priority than [TDD-00054](TDD-00054.md) |
| [00054](TDD-00054.md) | klmpm — a klainmain-native package manager | Partially Implemented | [ADR-00146](../adr/ADR-00146.md), [ADR-00147](../adr/ADR-00147.md) | Stages 1-2 done (compiler-side `klain_modules` resolution; `klain.json`/`klain.lock` parsing in a standalone `klmpm` Go package) — a package directory must still be hand-constructed, no klmpm tool exists yet (Stages 3-4: MVS resolution + git-backed fetch, the CLI itself) |
| [00055](TDD-00055.md) | Dynamic `import(...)` and `import.meta` | Partially Implemented | [ADR-00148](../adr/ADR-00148.md) | Stage 1 (`import.meta.url`) done. Whole-program AOT compile means a "dynamic" import can only ever be a literal-specifier, eagerly-run static import wrapped in a resolved Promise — real lazy-loading/runtime-conditional loading isn't implementable here at all; see the TDD's Context for why the real-world value is narrower than the feature name suggests. Named-function exports need real new codegen (thunks, Stage 3) to be usable through the result object |
| [00056](TDD-00056.md) | Dynamic `import(...)` V2 — real laziness via runtime-loaded shared libraries | Not Started | | Shares its whole parser/resolver frontend with [TDD-00055](TDD-00055.md); only the codegen backend differs (a `.so`/`.dylib` island per dynamic-import target, `dlopen`'d and run-once on first actual use — genuinely closer to real ES module semantics than V1's eager merge). Reopens the linker-adjacent boundary [ADR-00022](../adr/ADR-00022.md) deliberately avoided; explicitly incompatible with `--static` (clean compile-time rejection, same posture as `-mm`'s existing mutual-exclusivity pattern), and `-mm=gc` across the `dlopen` boundary is flagged unverified, not assumed |
| [00057](TDD-00057.md) | Nested function declarations | Implemented | [ADR-00149](../adr/ADR-00149.md) | V1 scoped to a declaration written directly in its enclosing function/arrow body's own immediate statement list (hoisted, forward-referenceable, self-recursive) — not one block deeper inside an `if`/`for`/etc.; deliberately no closure capture of the enclosing scope's locals (separate, larger follow-up) |
| [00058](TDD-00058.md) | Fixed-point unannotated-return-type inference (closes ADR-00041's forward-reference boundary) | Implemented | [ADR-00150](../adr/ADR-00150.md), [ADR-00152](../adr/ADR-00152.md) | Bounded re-inference sweep over same-scope unannotated function declarations (top-level and TDD-00057 nested), not a general multi-pass type system; a genuinely circular unannotated pair still can't converge and keeps the old default-fallback behavior; `@erased` generics were folded into the same sweep by ADR-00152 after a real repro confirmed the gap |
| [00059](TDD-00059.md) | Tagged template literals | Implemented | [ADR-00151](../adr/ADR-00151.md), [ADR-00152](../adr/ADR-00152.md) | Desugars to a plain call (array of cooked quasis + interpolated expressions as trailing args) reusing existing array-literal and rest-param-packing codegen unchanged; no `.raw` property on the `strings` array (real `String.raw` stays out of scope per [ADR-00028](../adr/ADR-00028.md)). Any callable — named function, class method, or arrow function/closure — works as a tag; ADR-00151 originally left the arrow-function case unsupported, closed by ADR-00152 |
| [00060](TDD-00060.md) | Function expressions (`var f = function() {}`, IIFEs, callbacks) | Implemented | [ADR-00168](../adr/ADR-00168.md) | V1: anonymous-only (named self-referencing deferred). The single largest conformance gap (14,881 failures, 28% of all files). All the runtime machinery already exists in arrow-function closures; this is a parser + AST addition, not new codegen. Shipping this exposed 19 pre-existing early-error gaps (duplicate params, strict-mode edge cases) as Test262 regressions — 12 fixed same-commit, 7 deferred, see ADR-00168 |
| [00061](TDD-00061.md) | Generator functions (`function* f() { yield x; }`, V1: sync, function-declaration only) | Implemented | [ADR-00171](../adr/ADR-00171.md), [ADR-00172](../adr/ADR-00172.md), [ADR-00173](../adr/ADR-00173.md) | A top-level generator function declaration, `yield`/bare `yield`, `for...of` over a generator, and real fiber-based `.next(value)` suspend/resume all work — reusing this compiler's existing `ucontext.h`-based fiber primitive ([TDD-00006](TDD-00006.md) Part 2) exactly as this document predicted. `yield*`, `.throw()`/`.return()`, generator methods, async generators, and nested generator declarations are this document's own already-scoped deferred items. `make conformance` moved by exactly zero files across both delivery passes — the corpus's own generator tests probe real JS's dynamic object model (`.prototype`, `Object.getPrototypeOf`, `GeneratorFunction`) at a level this compiler's static generator representation has no equivalent for at all, a deeper mismatch than just the deferred items above, not a sign anything is broken |
| [00062](TDD-00062.md) | Bare `any`/`unknown` as a function parameter/return type (Staged V2) | Implemented | [ADR-00176](../adr/ADR-00176.md), [ADR-00177](../adr/ADR-00177.md) | Works across every function shape (declarations, class/static methods, arrows, function expressions); arrays box by reference. Nested-dynamic positions (`any[]`, `{ x: any }`) and a boxed array's length/element access + content-accurate printing stay out of scope, rejected cleanly |
| [00063](TDD-00063.md) | Class-body expansion (expressions, field initializers, async/generator/computed members) | Implemented | [ADR-00180](../adr/ADR-00180.md), [ADR-00181](../adr/ADR-00181.md), [ADR-00182](../adr/ADR-00182.md), [ADR-00184](../adr/ADR-00184.md), [ADR-00185](../adr/ADR-00185.md) | All four stages done — field initializers (`x = v`/`#x = v`), async methods + `async`/`*`/`async *` parsing, instance generator methods (`*m()` with `this`), computed member names (constant string/number `['foo']()`), and class expressions (`const C = class {...}`, nominal binding). Deferred as clean rejections: static/async generator methods, well-known-symbol keys, `extends <expr>`, class-expression runtime values/named self-reference. Scoped the single largest in-scope conformance gap — 7,166 failing class files, 37% of all `language/` failures — into four staged sub-features (field initializers, async/generator methods, computed member names, class expressions) sharing the existing class machinery. Explicitly caps its own flip target: per [TDD-00061](TDD-00061.md)'s lesson, the corpus's dynamic-object-model assertions (property descriptors, `.prototype`, well-known symbols) sit above this static model's ceiling, so the first-error counts are a parse-unblock bound, not a goal |
| [00064](TDD-00064.md) | Runtime representation for a nullable non-pointer type (`number \| null`) | Implemented | [ADR-00199](../adr/ADR-00199.md) | Option A (presence-flagged `{ i1, T }` optional), all four stages. Fixed the shared root cause behind three tracked bugs — `??`/`??=` on a non-pointer `T \| null` never testing null, a class iterator's `next(): number \| null` terminating on a legit `0`, and `Map<K,number>.get()` returning `0` for a missing key: a non-pointer scalar has no runtime value for "absent." Recommends a presence-flagged `{ i1, T }` optional (Option A) over reusing the union box, heap-boxing, or an in-band sentinel; staged as representation+locals → flow narrowing → boundaries → the Stage 1a iterator (which can also be fixed alone by migrating it to the generator `{value, done}` result) |
| [00065](TDD-00065.md) | Generalized destructuring (for-of loop variable, nested patterns, object rest) | Partially Implemented | [ADR-00192](../adr/ADR-00192.md), [ADR-00193](../adr/ADR-00193.md), [ADR-00203](../adr/ADR-00203.md), [ADR-00204](../adr/ADR-00204.md) | Largest coherent, in-scope, non-`eval` Test262 cluster left (8,788 `dstr` files; core dirs ~14% pass). Stage 1 done ([ADR-00192](../adr/ADR-00192.md)): destructuring in the for-of loop variable. Stage 2 done ([ADR-00193](../adr/ADR-00193.md)): nested patterns across declarations, for-of, and parameters, reusing the recursive `unpack*PatternInto` core — for-of 111→130. Stage 3 split into 3a/3b/3c: **3a done** ([ADR-00203](../adr/ADR-00203.md)) — string/numeric-literal keys (`{ "k": v }`, `{ 0: v }`), a pure parser change reusing the object-literal key grammar; **3b done** ([ADR-00204](../adr/ADR-00204.md)) — `{...rest}` over a statically-known shape, synthesizing a residual `ObjectType` (visible source fields minus named keys) and shallow-copying, in the declaration/for-of/parameter positions; 3c (`{...rest}` over `any`/union/generic, and the assignment form) deferred to a future structural-types effort. Runtime iterator protocol (`Symbol.iterator`) explicitly out of scope, bounding the reachable share |
| [00066](TDD-00066.md) | Tuple types (`[T0, T1, ...]`) | Implemented | [ADR-00201](../adr/ADR-00201.md) | Positional-field struct riding the flagged-`ObjectType` machinery. V1: tuple annotations (incl. `[T0, T1][]`), tuple literals in a tuple-typed context, constant-index read, array-pattern destructuring (decl/assign/for-of, nested), rendering as an array, tuple as param/return/field, and the three `.entries()` builders. Deferred as clean rejections: element *assignment*, rest/optional/named elements, non-constant index, `.length`/array methods, tuple nested in `any`/a union, `new Map(entries)` (now a natural follow-on), spreading a tuple into a call |
| [00067](TDD-00067.md) | Aligning the PCRE2 RegExp backend with ECMAScript semantics | Implemented | [ADR-00206](../adr/ADR-00206.md), [ADR-00207](../adr/ADR-00207.md), [ADR-00208](../adr/ADR-00208.md) | All four stages shipped, behind a whole-program `-regex=<mode>` flag. A+B ([ADR-00206](../adr/ADR-00206.md)): `PCRE2_ALT_BSUX`/`MATCH_UNSET_BACKREF`/`DOLLAR_ENDONLY`/`UTF`/`NEWLINE_ANY` — retired the three correctness bugs (`$`/newline, unset backref, `\uXXXX` throw). Stage 3 ([ADR-00207](../adr/ADR-00207.md)): `es-utf16` UTF-16 code-unit offsets + a global empty-match hang fix (all modes). Stage 4 ([ADR-00208](../adr/ADR-00208.md)): `ecmascript` source-normalization pass, now the default. Deferred within Option C (disclosed in `docs/status/REGEXP.md`): `\u{…}`/`\p{…}`, Unicode `\w`/`\s`/`\b`, Annex-B octal escapes, exact `$`/`m`, `u`/`v` flags |
| [00068](TDD-00068.md) | Object-model evolution — static reach vs. the dynamic/prototype model ([TDD-00065](TDD-00065.md) Stage 3c and beyond) | Not Started | — | Direction decided. Reframes Stage 3c: object rest is never the blocker — the object model is fixed-shape nominal structs with no runtime descriptor. Splits the question onto two axes. **Now (static, Axis 1):** object type arguments in generics → generic-`T` rest falls out of monomorphization + the existing Stage 3b `bindObjectRest`, zero runtime representation. **Endpoint (dynamic, Axis 2, deferred):** a native dynamic/prototype object model (D1: per-instance property bag + prototype + descriptors), later optimized by hidden classes (D2) — this is where prototype semantics and [TDD-00022](TDD-00022.md) vanilla-JS compat land, and it must be native (delegating to the embedded engine would ship an interpreter, not native code). **Rejected:** the per-type shape descriptor — throwaway work that a per-instance D1 deletes. `any`/union rest stays a clean rejection until D1. Axis-1 work scoped in [TDD-00069](TDD-00069.md) |
| [00069](TDD-00069.md) | Object type arguments in generics (extends [TDD-00010](TDD-00010.md) monomorphization) | Not Started | — | The Axis-1 (near-term, static) move from [TDD-00068](TDD-00068.md): accept a named object/interface/class type as a generic type argument, not just scalars/arrays. Substitution machinery (`substituteGenericType`) is already type-agnostic — the one real blocker is `mangleTypeArg`, which needs an object case (mangle nominal types by `Name`/`ClassName`; anonymous inline object types stay a clean rejection, structural mangling deferred to V2). Once `T` binds to a concrete object shape, field access/destructuring/`{...rest}` (Stage 3b) all ride existing machinery — closing [TDD-00065](TDD-00065.md) Stage 3c's generic-`T` rest at zero runtime cost. Main risk: method dispatch on a `T`-typed value where `T` is a class |
| [00070](TDD-00070.md) | `let`/`const`/`var` scope semantics — hoisting, TDZ, and redeclaration early-errors | Implemented | [ADR-00210](../adr/ADR-00210.md) | Corrects an overstated `docs/status` caveat: `const` reassignment, `let`/`const` block scoping, and top-level `let x; let x` are already rejected. Four real gaps remain, staged A/B/C. **A (bug fix):** the top-level duplicate check is kind-agnostic, so legal `var x; var x` is wrongly rejected — make it kind-aware. **B (new static pass):** a resolver-side lexical early-error walk covering block-scoped `let`/`const` redeclaration and same-block TDZ (`Cannot access 'y' before initialization`). **C (design decision):** desugar `var` to function-scoped hoisting; the one open call is the hoisted-but-unassigned type — recommended hybrid: definite-assignment error for typed `var` (TS-faithful), hoist-to-`undefined` for `any`-typed (JS-faithful) |
| [00071](TDD-00071.md) | Binding-readability analysis — proper TDZ and definite-assignment errors | Implemented | [ADR-00212](../adr/ADR-00212.md), [ADR-00213](../adr/ADR-00213.md) | Definite-assignment (Stage 2) is deliberately sound-not-complete — never a false positive, misses harder flow cases | The two scoping caveats left after [TDD-00070](TDD-00070.md): a distinct TDZ error (`Cannot access 'x' before initialization`, and the `let x=1; { console.log(x); let x=2 }` shadowing-correctness bug behind it), and a definite-assignment error for a typed `var`/`let` read before assignment (replacing [ADR-00210](../adr/ADR-00210.md)'s interim zero-default). Both are one flow-sensitive resolver pass answering "is this read legal," with a per-binding TDZ→unassigned→assigned lattice. Conservatism rule: never a false positive (a missed error is a documented gap; rejecting valid code is not). Mirrors `resolver/rename.go`'s scope-aware walker; `any`-typed bindings exempt from definite-assignment |
| [00072](TDD-00072.md) | Enriched compiler diagnostics, and a strict spec-error-matching mode | Not Started | — | Two independent parts. **Part 1 (low-cost, default):** a diagnostic renderer — source snippet + `^` caret + optional hint around the existing `%d:%d:` errors; the one plumbing gap is threading each error's origin file (the resolver merges files). **Part 2 (research-gated):** classify errors under a JS error *kind* (`SyntaxError`/`TypeError`/…) and optionally match Node/V8 wording, behind a strict compatibility mode — the same axis as [TDD-00022](TDD-00022.md)'s `compatibilityMode`. Grounding: the Test262 harness scores negative tests on *phase*, not error type/text, so message-matching has little conformance value and belongs in a mode, not the default |
| [00073](TDD-00073.md) | Runtime debug symbols (DWARF via LLVM debug metadata) | Not Started | — | Emit LLVM debug metadata (`DICompileUnit`/`DISubprogram`/`!dbg`) and build with `clang -g` at `-O0`/`-Og`, behind a `--debug` flag. Positions already exist on every AST node, so it's "attach what we track", the cost being hand-numbered metadata nodes in the text IR. **MVP:** line-level `!dbg` + per-function `DISubprogram` → stack traces/line numbers/breakpoints. **Full:** `DILocalVariable` + DWARF type descriptors → variable inspection. Synergy: the ASan/UBSan builds already pass `-g`, so sanitizer reports gain `.ts` source lines for free |
| [00074](TDD-00074.md) | `bigint` — arbitrary-precision integers behind a selectable backend | Implemented | [ADR-00216](../adr/ADR-00216.md) | V1 ships the backend-agnostic `__kml_bigint_*` ABI + full operator/typeof/print/`BigInt()` surface, with two backends selected by `-bigint=libtommath\|gmp`. Default is **`libtommath`** (public domain), not the doc's `libbf` (MIT), which needs vendoring and is deferred — the ADR records why. Fixed `i128` rejected (silent overflow); hand-rolled `native` and cross-type `number` comparison deferred. Each backend is an embedded C file compiled alongside the program (the `gcshim.c` pattern) |
| [00075](TDD-00075.md) | `-compat` — a whole-program semantic-fidelity axis (stricter-than-JS default, opt-in JS faithfulness) | Partially Implemented | [ADR-00217](../adr/ADR-00217.md), [ADR-00218](../adr/ADR-00218.md), [ADR-00220](../adr/ADR-00220.md) | Generalizes the `-globals=strict\|permissive` pattern into one named axis: `-compat=strict` (default, opinionated/safer-than-JS) vs `-compat=js` (best-effort JS-faithful). A **third bucket** for divergences — deliberate "stricter default, JS on demand" choices — distinct from bugs (fix) and permanent narrowings (keep), so they stop accreting as caveats. Governs behaviors with a genuine strict-vs-JS tradeoff (`bigint`↔float comparison, `&&`/`\|\|` bool-vs-value, error-kind wording); **absorbs** `-globals` (standalone flag removed), leaves `-regex`/`-mm` independent. Subsumes [TDD-00072](TDD-00072.md) Part 2's `compatibilityMode`; distinct from [TDD-00022](TDD-00022.md) (vanilla-JS *compilation*). First inhabitant is `bigint` float-comparison at near-zero cost (strict is already the shipped behavior) |
| [00076](TDD-00076.md) | Real `any` semantics as a `-compat` axis inhabitant (implicit-`any` + operator dispatch) | Not Started | — | Settles the one decision deferred since [TDD-00005](TDD-00005.md) (2026-07-13) that a family of scattered `any` caveats have been paying down one-by-one. Splits the accreted `any` work into **Bucket A** (a genuine strict-vs-JS *policy* → an [TDD-00075](TDD-00075.md) inhabitant) vs **Bucket B** (boxed-`any` representation holes, orthogonal, stays [TDD-00062](TDD-00062.md)'s deferred set). Bucket A = two coupled sub-decisions: **A1** implicit-`any` on unannotated params (`strict`: `noImplicitAny` error / `js`: boxed `any`) and **A2** operators on a boxed `any` (`strict`: clean reject, already shipped / `js`: runtime tag-pair dispatch mirroring `emitAnyEquals`). A1's `js` side is inert without A2, so `js` order is A2-then-A1. V1 covers only unambiguous coercion tag-combos (`number⊕number`, `string+` concat); the full `ToPrimitive`/`ToNumber` ladder is a later stage. Retires [TDD-00005](TDD-00005.md) option 3 (= `-compat=js`) and judges option 2 (call-site inference) not worth building |
| [00077](TDD-00077.md) | JSON dynamic-tree rewrite — validating parser, `any`-typed parse, dynamic-value stringify | Partially Implemented | [ADR-00222](../adr/ADR-00222.md) | Supersedes [TDD-00015](TDD-00015.md) by reversing its span-finding design for a full parse-to-tree rewrite, once "perfect JSON" widened to the whole surface. Two tracks. **Track S (stringify, DONE — [ADR-00222](../adr/ADR-00222.md)):** `space` pretty-printing (`JSON.stringify(x, null, 2)`), generic `toJSON()` (`Date` kept as a builtin special-case rather than unified, a recorded divergence). `replacer` arg + runtime `space` cleanly rejected. **Track P (parse rewrite):** **P1 DONE ([ADR-00223](../adr/ADR-00223.md))** — a validating recursive-descent parser in an embedded C file (`__kml_json_*` opaque-node ABI, `bigint`/`gcshim` pattern) producing a tagged value tree + catchable `SyntaxError` ([TDD-00013](TDD-00013.md) tagged errors) with a runtime depth-guard (512) on untrusted input; `JSON.parse`/`Response.json()` now reject malformed input instead of silently defaulting (P1 parses-and-validates, projection still on the old path until P3). **P3 DONE ([ADR-00224](../adr/ADR-00224.md))** — type-directed projection tree→target retired the whole parse-caveat cluster at once (nested objects, array/object-array fields, top-level `T[]`, nested arrays, the `float64`-variable compile bug [ADR-00166](../adr/ADR-00166.md)) and deleted the old strstr extractor path. P4 `any`-typed parse + dynamic-value/mixed-array stringify, the one stage coupled to [TDD-00068](TDD-00068.md)'s dynamic object model (P1–P3 are independent of it). `replacer` arg deferred |
| [00078](TDD-00078.md) | Intersection types (`A & B`) | Implemented | [ADR-00225](../adr/ADR-00225.md) | Object-type intersection reduces to the existing fixed-shape struct model — merge member fields into one `ObjectType`, no runtime tag (simpler than [TDD-00043](TDD-00043.md) unions, which needed a runtime box). New `TypeAnnotation.IntersectionMembers` mirroring `UnionMembers`, merged in `resolveType` (named members resolve through the interface registry, which the parser can't reach — so parse-time flattening is rejected); a new `&`-loop parse level binding tighter than `\|`. V1: object members only (named/inline/alias), all object-type positions; non-object members, generic-with-args and `any` members are clean rejections. Conflicting field types are `-compat`-governed ([TDD-00075](TDD-00075.md)) — `strict` errors, `js` keeps a TS-faithful `never` field. Builds the field-merge core the discarded `interface extends` clause will reuse, and fixes the latent `rewriteType` gap that skips `UnionMembers`/`TupleElems` descent |
| [00079](TDD-00079.md) | Mapped and conditional types | Implemented | [ADR-00228](../adr/ADR-00228.md)–[ADR-00231](../adr/ADR-00231.md) | Full scope: user-defined mapped types `{ [K in keyof T]: … }` + built-in utility types (Partial/Pick/Omit/Record/…) **and** conditional types `T extends U ? X : Y` with `infer`. Monomorphization is the enabler — all types are concrete at codegen, so these **evaluate at `resolveType` to a concrete `ObjectType`**, extending the existing generic-instantiation seam (`substituteGenericType`/`instantiateGenericInterface`); no new runtime representation. Three genuinely-missing prerequisites do the real work: the type-level grammar/AST (`keyof`, indexed access, string-literal types, `infer`), field-level optional/readonly state (the `?` is discarded today), and a general structural assignability predicate (only nominal `implements` exists) for the conditional `extends`. Staged: Stage 0 clean-rejection + string-literal keys (fixes the silent `Partial<User>→User[]` bug) → 1 utility types → 2 general mapped → 3 conditional + `infer` |
| [00080](TDD-00080.md) | JS-faithful float-to-string (shortest round-trip) | Implemented | [ADR-00233](../adr/ADR-00233.md) | Floats print via bare `%g` (6-sig-digit default), so `1.1 + 2.2` prints `3.3` not `3.3000000000000003` — the only remaining single-caveat primitive row, so fixing it earns a Strict row. Recommended: an embedded-C `__kml_dtoa` that finds the shortest round-tripping digit count (`%.*e` retry + `strtod` check) then reassembles per ECMAScript `Number::toString` (fixed for `-6 < n ≤ 21`, else exponential); handles `NaN`/`Infinity`/`-0`. Compiled+linked only when a program prints a float (bigint/JSON embedded-C pattern), swapped into the three `%g` print sites |
| [00081](TDD-00081.md) | EventTarget / Event / CustomEvent (+ AbortController) | Implemented | [ADR-00234](../adr/ADR-00234.md)–[ADR-00240](../adr/ADR-00240.md) | The WHATWG event trio (distinct from Node's `EventEmitter`) — a generic `EventTarget` bus (`addEventListener`/`removeEventListener`/`dispatchEvent`) plus `Event`/`CustomEvent`, and the `AbortController`/`AbortSignal` it enables. Mostly a *generalization*: EventSource already reuses EventEmitter's `Map<string, listener-list>` registry + snapshot dispatch, so this reuses `__kml_ee_list_*`/`__kml_map_str_*`/`emitCBCall` and adds `Event`/`CustomEvent` object types, an `IsEventTarget` tag, construction nodes, and dispatch-by-`event.type`. Staged: Event/CustomEvent → EventTarget → AbortController (signal fires `"abort"`, wired into `fetch`/timers). No `class X extends EventTarget` (built-in `extends` unsupported) |
| [00082](TDD-00082.md) | External conformance suites V3 — Web Platform (WPT) + Node core | Partially Implemented | [ADR-00153](../adr/ADR-00153.md) | Supersedes [TDD-00008](TDD-00008.md), widening the external-benchmark story from the language layer (Test262, carried over live and unchanged) to the Web Platform and Node buckets. Direct measurement: `node-api-cts` rejected (native-addon C ABI, wrong layer); **WPT** is the right Web-Platform oracle — of a `dom/events`+`dom/abort`+`url` slice only the headless `.any.js`/`.window.js` files run (35 DOM-free of 47; the 174 `.html` DOM tests need a document tree), behind a ~15-function `testharness.js` shim, and `dom/abort` is a near-exact oracle for the new DOMException/`TimeoutError` work — **gated on the async/await + cancellation work first**; **Node's `test/`** is the only corpus for `fs`/`path`/`os`/`http` but 4,229/4,234 files couple to the internal `common` harness, so pure-modules-only or hand-mined, lower priority. TS-suite stays checklist-only (no runtime oracle). TS version-coverage noted, deprioritized |
| [00083](TDD-00083.md) | Promise concurrency — microtasks/`.then`, `Promise.any`, true-suspension question | Partially Implemented | [ADR-00244](../adr/ADR-00244.md) | Async/Promise is 3/6; the fetch fan-out (`Promise.all([fetch,fetch])`) already has real concurrency (TDD-00016/[ADR-00073](../adr/ADR-00073.md)). Confirmed directly: user `async` fns are inlined synchronous code (no suspendable frame); ucontext fibers exist but are HTTP-connection-scoped (spawned only on accept, park state in the connection slot); no microtask/`.then`/`queueMicrotask`. Three committed stages (nothing deferred — driven to a Strict close): **1** `Promise.any` (+`AggregateError`, reuses the group `mode`) — small, works in the current model; **2 (foundational)** true async-fn suspension — generalize fibers to a connection-independent spawn + per-call park + scheduler-as-exit-loop + a suspendable frame in `emit_async.go`, which folds away the `IsResponse` special-casing so combinators get real concurrency over *any* `Array<Promise<T>>` and `Promise.all([fetchUser(a),fetchUser(b)])` overlaps; **3** microtask FIFO + `queueMicrotask` + `Promise.prototype.then`/`.catch`/`.finally`, designed against the post-suspension model. `for await...of` becomes reachable on top (tracked separately). Stage 2 pays a real cost (a fiber stack per in-flight async call, rewriting the inlined-async model) — taken, not deferred |

| [00084](TDD-00084.md) | Unifying the async model — every `async` function a promise, one scheduler | Implemented | [ADR-00249](../adr/ADR-00249.md), [ADR-00250](../adr/ADR-00250.md), [ADR-00251](../adr/ADR-00251.md) | The architectural remainder of [TDD-00083](TDD-00083.md), fully delivered. **A** ([ADR-00249](../adr/ADR-00249.md)): every async fn returns a real task-shaped promise (no fiber/libcurl for pure-async). **B** ([ADR-00250](../adr/ADR-00250.md)): one task-aware event loop drives tasks + timers/SSE/WS/`http.listen`. **C** ([ADR-00251](../adr/ADR-00251.md)): `.any` over raw fetches + destructured/nullable params in a suspending fn |

| [00085](TDD-00085.md) | Async generators (`async function*`) and `for await...of` | Implemented | [ADR-00253](../adr/ADR-00253.md), [ADR-00254](../adr/ADR-00254.md) | All 4 stages done: `for await...of` parsing; the fused `async function*` fiber (`yield` + `await` in body + `.next(): Promise<{value,done}>` + throw→reject); `for await...of` consumption; async generator *methods* `async *m()`. `yield*`/`Symbol.asyncIterator`/`.return()`/`.throw()` are V1 scope cuts. Fuses the sync-generator fiber ([TDD-00061](TDD-00061.md)) with the coroutine-task scheduler park ([TDD-00083](TDD-00083.md)) into one fiber that both `yield`s and `await`s, so `.next()` returns `Promise<{value,done}>`; `for await...of` desugars to `await iter.next()`. A no-await async generator stays off the scheduler (fast-path philosophy). `yield*`/`Symbol.asyncIterator`/`.return()`/`.throw()` are V1 scope cuts |
| [00086](TDD-00086.md) | Generator protocol completion — `.throw()`, `.return()`, `yield*` | Implemented | [ADR-00259](../adr/ADR-00259.md), [ADR-00260](../adr/ADR-00260.md), [ADR-00261](../adr/ADR-00261.md) | Resume-mode dispatch at the `yield` point (`__resumeMode`/`__thrown` fields): a `.throw(e)` injects at the suspension point (body `try/catch` handles it, else it propagates to the caller and finishes the generator); a `.return(v)` runs enclosing `finally`s and completes; `yield*` delegates over an inner generator, forwarding sent values and evaluating to the inner's return value. Folds in the pre-existing bug that a `return` inside a generator's `try/finally` skips the `finally`. Sync generators; async-generator forwarding is a follow-on. Four stages: `.throw()` / `.return()`+finally-fix / `yield*` delegation / `.throw()`+`.return()` forwarding through `yield*` |
| [00087](TDD-00087.md) | `new Promise((resolve, reject) => …)` — the executor constructor | Implemented | [ADR-00263](../adr/ADR-00263.md) | resolve/reject as per-site closures capturing a pending task promise; `@__kml_promise_settle` (the missing bare-promise settle+wake+drain, factored from `__kml_task_finish`) with first-settle-wins; the executor invoked immediately with param hints so `resolve(x)`/`reject(e)` type-check. Deferred resolution (a `resolve` stashed into `setTimeout`) drives the lightweight await through a microtask-drain + one-shot timer-fire loop (`@__kml_timer_fire_next`). Direct use only — returning a promise across a function boundary hits the separate promise-return-flattening gap |

| [00088](TDD-00088.md) | Microtask-accurate `await` ordering (`await` always yields a tick) | Implemented | [ADR-00268](../adr/ADR-00268.md) | Every `await` yields a microtask tick even of a settled promise; the resume is a reaction/microtask ordered FIFO with `.then`/`queueMicrotask`. Any async fn containing an `await` runs as a fiber task (no-`await` ones stay inline). New `__kml_task_resume` + a reaction-registering `__kml_task_await_ready` |
| [00089](TDD-00089.md) | `Symbol.asyncIterator` — user-defined async iterables in `for await...of` | Implemented | [ADR-00269](../adr/ADR-00269.md) | Lifts `for await...of` from "async-generator instances only" to the async-iteration protocol: a user class with a `[Symbol.asyncIterator]()` returning an iterator whose `async next(): Promise<{value,done}>` is awaited each turn. The well-known symbol is a reserved compile-time `MethodSigs` key (`@@asyncIterator`, the accessor-key trick), not a runtime value — no new machinery, reuses `emitClassCall` + `emitAwaitTaskPromise` + structural `{value,done}` reads. Two rejection sites relaxed (parser computed-key, `for await` gate). `Symbol.iterator` (sync) and object-literal iterables stay out; iterable + iterator must be class instances |
| [00090](TDD-00090.md) | A Promise is a reusable value — `await`/combinators stop consuming the slot | Implemented | [ADR-00270](../adr/ADR-00270.md) | `await` and the combinators freed the promise slot after reading, making a promise single-consumption — `await p; await p`, `await p` after `Promise.all([p])`, and the same promise twice in one combinator were all use-after-frees (SIGTRAP). This made the `await` row's zero-caveat "strict" claim untrue. Fix: stop freeing (reads are non-destructive); the small slot leaks in manual mode (collected under `-mm=gc`, ASan leak check already off) — chosen over refcounting/escape-analysis. Raw-fetch `Promise<Response>` is covered by the same fix (its pending struct is never freed and `__kml_await_fetch` short-circuits a done handle) plus a `checkloop` reorder (check done before the abort signal, so a re-await of a completed fetch skips the abort teardown). Also removes the verified-stale `.race` timer-ordering caveat. Moves Async/Promise strict 4/9 → 5/9 |
| [00091](TDD-00091.md) | Thenable adoption — `resolve(aPromise)` in a `new Promise` executor | Implemented | [ADR-00271](../adr/ADR-00271.md) | `resolve(anotherPromise)` adopts the thenable: the outer promise settles with the inner's value (or reason) when it settles, instead of the old `clang` type error coercing a promise to the value type. Reaction-based (reuses `.then`'s reaction-list + microtask FIFO + `__kml_promise_settle`), so adoption is a microtask tick like JS, not synchronous — no ordering caveat. A `Type.IsPromiseResolver` marker + an `emitClosureCallByPtr` hook fire adoption only for a genuine `Promise` argument. Closes the `new Promise` row's last caveat (the other, a top-level-function executor, was verified stale); Async/Promise strict 5/9 → 6/9. Custom duck-typed `{ then }` thenables aren't adopted (no `PromiseLike` type — a clean reject, not a silent miss) |
| [00092](TDD-00092.md) | `for await...of` over a sync array | Implemented | [ADR-00272](../adr/ADR-00272.md) | JS awaits each element of a sync iterable; the common case is an array of promises consumed sequentially (`for await (const x of [f(1), f(2)])`), with a plain-value array awaiting each as identity. Reuses the array for-of loop + `emitAwaitTaskPromise` (rejection propagates, stopping the loop) + the destructuring unpack core. Sync generator/`Map`/`Set` in `for await` stay a narrowed clean rejection. Also fixes a parser bug where a generic-type array suffix (`Promise<number>[]`, `Map<K,V>[]`) dropped its `[]` and reported the initializer missing. Surfaced (but did **not** fix) a pre-existing gap: a named `function` declaration can't read a top-level `const`/`let` (they're `main()` locals; an arrow/closure captures them fine) — now fixed in [TDD-00093](TDD-00093.md) |
| [00093](TDD-00093.md) | Module-level variables — a named `function` can read a top-level `const`/`let` | Implemented | [ADR-00273](../adr/ADR-00273.md) | Fixes the gap TDD-00092 surfaced: a named `function` declaration couldn't read a top-level `const`/`let`/`var` (`undefined variable`), because top-level statements run in the synthetic `main()` (locals), while a function has its own fresh scope (an arrow/closure captured them, masking it). Promotes a top-level binding to an LLVM module global (pre-registered before function bodies emit) whenever its type is determinable before `main()` runs — the invariant `promotableInitInPrePass` states directly: annotations, literals, earlier module globals, and named non-generic non-async functions, computed with the *same* `lookup`/`resolveFuncRef`/`inferX` helpers `emitVarDecl` uses so it can't drift. Covers scalar/string/array/object/`Map`/`Set`, an **un-annotated call** (`const cfg = loadConfig()`), and an **earlier-global reference** (`const b = a`). `lookup` falls back to it; the var-decl emitters store into it (keyed by decl-node pointer, since a function body also runs at scope depth 1); both closure-capture paths skip it so a capturing arrow/function-expression shares the one global rather than a boxed copy. Scope-dependent inits / `bigint` / scalar-returning calls stay `main()` locals. Fixing this moved Test262 conformance +23 |
| [00094](TDD-00094.md) | Async-generators row to strict — nested generators, async `yield*`, `.next()` shape | Partially Implemented | [ADR-00274](../adr/ADR-00274.md) | Drives the async-generators row to strict (async/promise 6/9 → 7/9) by closing its three caveats: (1) **nested `function*`** (sync + async) — no function-decl capture exists today, so give the generator *instance* a closure `__env` reusing the arrow-closure boxing (ADR-00001) for **by-reference** capture (JS-faithful, so an enclosing `let` mutated after creation is seen by `.next()`); (2) async **`yield*`** over a `Symbol.asyncIterator` iterable (TDD-00089), not just a generator; (3) a **`.next()`-shape audit** to reclassify the already-resolved-promise note if it's observably identical. Nested generators also lifts the same caveat on the sync-generator row. Staged: nested → async `yield*` → audit + drive to strict |

*Last updated: 2026-08-20*
